// Route segment config for all API routes
// This configuration applies to all routes under /api/
export const dynamic = 'force-dynamic';

// This file acts as a route segment config for all API routes
// It ensures that all API routes are rendered dynamically at request time
// This prevents errors with features like headers, cookies, and authentication