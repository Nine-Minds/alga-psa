/**
 * Deprecated when EXT_UI_HOST_MODE === 'rust'. Retained for legacy/compat use only.
 * Delegates to shared builder to maintain a single source of truth.
 */
export { buildExtUiSrc } from './url.shared';