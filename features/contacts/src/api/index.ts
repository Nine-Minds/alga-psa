/**
 * Contact API route handlers
 *
 * These handlers can be imported into the server's app/api/v1/contacts routes.
 * They provide a shared implementation that works with Next.js App Router.
 */

// Future exports:
// export { handleGetContacts, handleCreateContact } from './handlers.js';
