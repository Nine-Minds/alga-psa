/**
 * User API route handlers
 *
 * These handlers can be imported into the server's app/api/v1/users routes.
 * They provide a shared implementation that works with Next.js App Router.
 */

// Future exports:
// export { handleGetUsers, handleCreateUser } from './handlers.js';
// export { handleGetUserRoles, handleAssignRole } from './handlers.js';
