version: '3.8'

services:
  server:
    environment:
      NODE_ENV: production
      APP_ENV: production
    command: ["npm", "run", "build"]

  setup:
    environment:
      NODE_ENV: production
      APP_ENV: production

  hocuspocus:
    environment:
      NODE_ENV: production
      APP_ENV: production
