// Keep workflow runtime payload fields aligned with the canonical event schemas.
export { tenantIdSchema, occurredAtSchema, uuidSchema, actorTypeSchema, BaseDomainEventPayloadSchema,
  updatedFieldsSchema, changesSchema, currencySchema } from '@alga-psa/event-schemas';
