# Ensure devDependencies install even if the developer shell has NODE_ENV=production
include=dev

# Temporary install guard: this repo still has peer graph conflicts
# (e.g. eslint 8 with eslint-config-next 16). Use legacy peer resolution
# until those are reconciled in a dedicated dependency alignment pass.
legacy-peer-deps=true
