{
  "name": "@alga-psa/sample-scheduler-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rimraf dist bundle *.tar.zst",
    "build": "tsup src/handler.ts --format esm --target esnext --dts --clean && node scripts/postbuild.mjs",
    "pack": "node scripts/pack.mjs",
    "test": "vitest run"
  },
  "dependencies": {
    "rimraf": "^6.0.0"
  },
  "devDependencies": {
    "@alga-psa/extension-runtime": "*",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  }
}
