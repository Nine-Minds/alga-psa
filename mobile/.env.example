# Copy to `.env` for local development.

# Hosted environment selector (V1: hosted only; no self-hosted base URLs).
EXPO_PUBLIC_ALGA_ENV=dev

# Base URL for the hosted environment (no trailing slash), e.g.:
# EXPO_PUBLIC_ALGA_BASE_URL=https://dev.alga.example.com
EXPO_PUBLIC_ALGA_BASE_URL=

