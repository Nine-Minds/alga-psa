# Co-managed IT implementation plan

## Product outcome

A Pro MSP can purchase customer-technician licenses, create a separate customer IT workspace, and deliver a joined-up service desk from its existing Alga workspace. Customer technicians manage their own operation without access to other customers or the MSP's private commercial data. Tickets and projects remain customer-owned records that both organizations can work on. Escalation changes responsibility without creating another ticket.

The customer workspace can outlast the relationship. The customer can export its data or upgrade to a directly paid AlgaPSA subscription. The MSP retains its own commercial records and historical evidence of shared work it participated in.

## Agreed scope

| Concern | Decision |
| --- | --- |
| Deployment | Hosted and self-hosted; both tenants must be in the same installation. |
| Sponsor | A Pro-tier MSP with sufficient purchased co-managed licenses. |
| Relationship | One MSP may sponsor many customers; each customer has exactly one sponsoring MSP. No nested co-management. |
| Onboarding | Create new customer workspaces only, linked to an existing MSP client record. Do not connect existing AlgaDesk or AlgaPSA tenants. |
| Commercial model | USD 11.49 per customer-technician seat per month, charged to the sponsoring MSP. |
| Allocation | The MSP buys a pool and assigns per-customer seat limits. Provisioning requires at least one allocated seat for the customer administrator. |
| Customer administration | Customer administrators manage users, boards, settings, and sharing. MSP administration requires scoped delegation. |
| Ticket visibility | Default shared service-desk visibility within an agreed scope; an escalation-only mode is available per relationship. Private boards can remain outside scope. |
| MSP work | An oversight view includes permitted customer work. The normal working queue includes work assigned or escalated to the MSP. |
| Escalation | Immediate transfer of responsibility to the agreed MSP queue, with a recorded handoff and no acceptance gate. The MSP can return responsibility to customer IT. |
| Ticket ownership | One authoritative customer-owned ticket, one shared history, collaboration from both workspaces. No mirrored MSP ticket lifecycle. |
| Notes | Requester-facing replies, shared IT notes, and organization-private notes. Ticket access is required for every audience. |
| Projects | Customer-owned, explicitly shared per project. Both teams work on the same tasks and history. |
| Time | Each organization owns its time records. MSP time feeds MSP billing. Customer operational time logging/reporting is included; timesheet submission and approval are optional. |
| SLAs | Preserve customer end-to-end tracking. MSP response/resolution timing starts at escalation, not at ticket creation or visibility grant. |
| Integrations | Include customer-owned email and identity/directory connections. Do not assume the customer owns an RMM or expose the entire PSA integration catalog. |
| Departure | Customer exports its data or upgrades the existing workspace to directly paid AlgaPSA. Live relationship access ends. |
| Retained history | Customer keeps its data, shared history, and customer-private notes. MSP keeps its time, billing, private notes, and read-only history of shared work it participated in. |
| License lapse | Immediately block provisioning and seat increases; existing customer workspaces have 30 days to renew, export, or upgrade before becoming read-only. |
| Release flag | `release-v1-6-feature` protects UI use only. No flag checks on API access, routes, server actions, services, or other backend behavior. |

## Product and license boundaries

Use **Co-managed IT** as the working product name and `co_managed` as the proposed product code. Model this as a product capability set plus sponsorship, independent of the existing Essentials/Solo/Pro tier ordering. A new rank between Essentials and Pro would incorrectly make collaboration permissions depend on tier comparisons.

Customer workspaces use the operational Pro capabilities allowed by their product and valid sponsorship. A co-managed customer cannot sponsor another customer, even if its effective operational tier is Pro. Sponsorship requires the full PSA product, verified Pro eligibility, and a distinct co-managed capacity entitlement.

### Proposed capability matrix

The operational/commercial split below carries forward the broader customer-workspace proposal. The integration and time-entry refinements are explicit decisions above; the remaining detailed surface list is subject to plan approval.

| Surface | Co-managed customer access |
| --- | --- |
| Tickets, boards, portal, contacts, email intake | Included. Customer technicians are internal workspace users; requesters remain portal contacts. |
| KBs, documents, credentials | Included within the customer's own workspace and permissions. Relationship access does not automatically disclose these resources. |
| Projects, tasks, scheduling | Included, with explicit project sharing and organization-qualified assignments. |
| Time and effort reports | Included without requiring service contracts, billing rates, or invoice configuration. Optional timesheet approval. |
| Assets and operational reports | Included for customer IT. No automatic replication from the MSP's RMM. |
| SLAs and workflows | Included for permitted internal IT operations. Workflow execution obeys the same tenant, audience, and product boundaries as interactive actions. |
| Email, sign-in, directory connections | Existing supported customer-owned connections are available. Connection setup and stored credentials remain customer-scoped. |
| Other integrations | Denied unless explicitly included in the co-managed capability registry. Do not build new RMM connectors or a general cross-tenant integration sync in this release. |
| Sales opportunities, quotes, client contracts, billing, invoices, revenue/accounting tools | Excluded from the customer workspace. MSP commercial activity continues in the MSP tenant. |
| Existing separately metered add-ons | Keep their current entitlement and consumption rules; this SKU does not implicitly include them. |

Regular MSP seats are not charged again for visiting or working in sponsored customer workspaces. Requester contacts are unmetered. Billing is based on the purchased pool, including unallocated seats, rather than silently increasing charges as customer administrators add users.

Launch with monthly co-managed seats. Do not invent an annual discount. Use configured price identifiers and integer minor units for monetary values; do not hard-code a display string as billing logic.

## UI-only release flag

Gate the co-managed feature's user interface with the exact flag key `release-v1-6-feature`. Reuse `packages/ui/src/hooks/useFeatureFlag.tsx` through the existing UI package exports and a shared client-rendered boundary. Require an enabled, resolved flag before rendering usable feature controls; disabled, loading, unknown, or error states must not flash interactive co-managed UI.

Cover MSP and customer entry points: co-managed navigation, provisioning and seat-purchase/allocation controls, relationship acceptance/settings, delegated administration, consolidated oversight, shared ticket/project access, escalation/handback, collaboration audience controls, and feature-specific export/upgrade flows. Apply the boundary to dedicated feature content as well as navigation so a direct browser URL cannot expose usable co-managed UI. Keep ordinary PSA/AlgaDesk screens and controls working; hide or render an inactive UI fallback for co-managed-only content.

This is a presentation gate only. Routes remain registered and accessible under their normal rules. Do not add middleware, route redirects/404s, API handler checks, server-action checks, service checks, worker/workflow checks, database checks, or subscription/license checks based on this release flag. Backend functionality deploys and executes regardless of the flag. Existing authorization, product capabilities, Pro eligibility, license capacity, and lifecycle rules still apply independently.

Turning the flag off does not suspend a tenant, revoke trust, stop intake, change purchased seats, or interrupt backend billing/SLA processing. Hosted and self-hosted UI use the existing client flag evaluation/override conventions; the flag does not require backend availability to depend on PostHog. Document the key and affected UI in `docs/features/feature-flags.md` during implementation.

## Primary experience

### Provision and connect

1. An authorized MSP administrator opens a client and selects **Enable co-managed IT**.
2. Show verified eligibility, purchased/allocated/available capacity, customer workspace name, initial administrator, seat allocation, visibility mode, and MSP escalation queue.
3. Reserve capacity and a new customer tenant identity atomically before starting provisioning. Repeated submission returns the same provisioning operation.
4. Reuse the existing tenant creation orchestration with co-managed seeds and sponsorship billing. Create the customer's self-organization/contact context; never copy the MSP's full client/contact directory into the new tenant.
5. Invite the customer administrator. The proposed activation default is that the administrator reviews and accepts the relationship scope at first sign-in. Provisioning prepares an empty workspace; live customer access starts only after activation.
6. Customer administrators can configure their workspace and change grants. MSP users see only the administrative actions delegated to their own roles or teams.

Show provisioning progress and a retryable failure state. A failed or canceled creation releases its reservation after cleanup; retries cannot create an extra tenant, administrator, subscription, or seat allocation.

### MSP oversight and daily work

Add a Co-managed IT area for relationships and capacity. Extend ticket lists with a customer workspace filter and a clear distinction between **Working queue** and **Customer oversight**. The working queue combines native MSP tickets with shared tickets the MSP is responsible for. Oversight shows currently permitted customer tickets, including customer-handled work.

Opening a shared ticket remains in the MSP application shell. Display customer workspace, original ticket number, canonical status, responsible organization, and customer/MSP SLA indicators. A separate delegated-administration view may open the customer's settings with an explicit workspace banner. Routine ticket/project work must not require tenant switching or impersonation.

Use customer-qualified identifiers throughout routes, list keys, caches, searches, and selections. Two customers may have identical ticket numbers. Filtering, sorting, pagination, total counts, dashboards, and exports must operate on the authorized combined result, not independently paginated lists concatenated in the browser.

### Customer work and escalation

The customer's portal and email intake create tickets in the customer tenant. Customer technicians can triage, assign, respond, track effort, and resolve them. **Escalate to MSP** takes an explanatory shared note, immediately changes responsibility, places the same ticket in the configured MSP queue, and starts MSP SLA tracking.

Sharing and responsibility are separate. A ticket can be visible while still customer-handled. In escalation-only mode, escalation creates the necessary ticket grant. Returning work does not erase the shared history or silently revoke access; customer administrators manage access explicitly.

Keep the ticket's board, statuses, priorities, categories, and requester in its owning customer tenant. MSP queue placement is additional routing metadata. The proposed default is one canonical customer status workflow, editable by permitted MSP staff; do not move the ticket's `board_id` to an MSP-owned board or synchronize two independent status lifecycles.

Only administrators configure allowed escalation destinations. Customer pickers show the agreed destination and permitted collaborators, not the MSP's complete boards, teams, user directory, or other client names. Bulk actions validate every qualified resource and report individual failures without acting on unauthorized selections.

### Projects and time

Sharing a project grants authorized collaboration on its phases/tasks and permitted history. Customer IT can remove the grant. Assignments identify both organization and team/person. Sharing a project does not itself assign every task to the MSP or place all tasks into its working queue.

Customer time is operational effort. It may be logged without a service or contract and must never enter the MSP's invoice candidate set. MSP time uses its own users, services, contracts, billing profiles, approvals, and invoice workflow, with the source work item explicitly identified in the customer tenant.

Proposed time visibility: each organization retains detailed timesheets and private time notes; shared ticket/project views can show attributed effort totals and deliberately shared work notes, without disclosing rates, costs, contract terms, or approval details. Customer and MSP effort must not be double-counted in project totals.

## Authorization and information ownership

Introduce a shared-work authorization service rather than changing the meaning of the session tenant. An authenticated MSP actor has a home tenant; the ticket/project has an owning tenant. These are distinct values throughout command execution.

For each operation, intersect:

1. The actor's current identity, session revocation state, and home-tenant role permissions.
2. Relationship state and the actor's explicitly assigned MSP relationship role/team.
3. Customer-approved resource scope or explicit object grant.
4. The requested action and target resource's policy.
5. Product/license write eligibility and, for content, its audience.

Customer administrators can change customer-side scope. MSP administrators can assign their staff within the granted scope but cannot enlarge it or override customer-private content. Delegated user management must not let MSP actors impersonate customer administrators, reset their credentials, grant themselves new scope, or convert a private record into a shared one.

Keep `withAuth` bound to the home identity. A dedicated domain boundary resolves an authorized resource context, performs narrowly scoped owner-tenant operations, and preserves the original actor. Do not accept a caller-supplied target tenant and run ordinary actions under it as though the caller were a local administrator. Existing local actions continue using normal tenant isolation.

Use the same service for server actions, API commands, lists, detail reads, document downloads, search, bulk operations, reports, realtime delivery, notifications, email processing, and workflows. Unsupported cross-tenant entry points explicitly reject foreign targets. Native APIs and automation still enforce the customer's product capabilities. An automation or API key cannot acquire greater sharing authority than its configured principal.

Access to a ticket or project does not automatically grant access to a linked credential, KB article, document library, asset, or unrelated contact. Ticket attachments and inline images inherit the parent content's audience. Shared references expose only permitted context; resource-specific grants govern opening underlying records. Existing customer-specific KB publishing can be reused through explicit relationship-aware grants, without sharing the MSP's complete KB.

### Three content audiences

| Audience | Visibility |
| --- | --- |
| Requester-facing | Authorized requester/portal readers and authorized technicians in both organizations. |
| Shared IT | Authorized technicians in both organizations; never requester email or portal content. |
| Organization-private | Authorized staff in the authoring organization only. Sponsorship and delegated administration do not override this. |

Store shared history in the customer tenant. Store MSP-private content under MSP ownership, referring to the qualified customer resource; customer exports must not contain it. The comment reader/composer presents a unified authorized timeline across these stores. Reuse these audience semantics for shared project-task comments.

Thread replies and attachments inherit the root audience. Audience changes require authorization and explicit disclosure confirmation; editing text must not accidentally widen visibility. Existing non-co-managed internal notes retain their current behavior. Do not migrate legacy internal notes to shared IT visibility by default.

Preserve an organization-qualified actor reference and historical display attribution for foreign contributions. Existing user foreign keys and local-author assumptions must be revised deliberately. Do not create shadow login users in customer tenants to represent MSP technicians or count them as customer seats. Historical attribution survives user deactivation and separation.

## Data model and application boundaries

The names below describe proposed entities. Use existing domain conventions when implementing, while preserving the ownership and uniqueness rules.

| Entity | Ownership and purpose |
| --- | --- |
| `co_management_relationships` | Customer-scoped canonical relationship: sponsoring tenant, corresponding MSP client, lifecycle state, visibility mode, acceptance, permission revision, and lifecycle timestamps. At most one non-ended sponsor per customer. |
| `co_managed_entitlements` | Sponsor-scoped verified purchased capacity, source subscription/license, effective dates, and lapse state. Distinct from regular MSP seats. |
| `co_managed_allocations` | Sponsor-scoped per-customer allocation and provisioning reservation; sum cannot exceed effective capacity. Stable operation identity handles retries. |
| Relationship scopes and grants | Customer-owned board/project/object and delegated-action grants; reference qualified MSP principals. Default shared visibility covers an explicit board scope, not all future resources silently. |
| Shared-work state | Customer-owned qualified ticket/project/task responsibility and handoff version/history. Same authoritative resource, not another ticket. |
| MSP work references | MSP-owned qualified source reference, local queue/assignee, local client/billing profile, and permitted display/evidence metadata. Unique per source resource and relationship. No separate ticket status lifecycle. |
| Collaboration actor references | Owner-local reference to original actor tenant/user with stable attribution. Not authenticatable user accounts. |
| Organization-private contributions | Owned by the contributing organization, linked to a qualified shared resource and appropriate thread. |
| MSP SLA tracking | MSP-owned policy and timing state for a qualified customer ticket, separate from the customer's ticket SLA fields. |
| Participation archives | Organization-owned immutable snapshots of authorized historical shared work, attachments, and attribution, available after live grants end. |

Every foreign resource reference carries both tenant and record identity. Keep same-tenant foreign keys where possible. Do not put a foreign tenant's user/board/status ID into a column whose constraints or readers assume local ownership.

Register every table in tenant metadata and apply appropriate tenant isolation. Put global relationship discovery behind an explicit narrow repository API; it must not turn business tables into unrestricted shared data. Enforce the one-sponsor invariant at the customer key and pool allocation under a sponsor lock. Citus uniqueness/distribution must include the appropriate tenant key. Do not invent cross-shard foreign keys that work only on plain PostgreSQL.

Relationship activation, handoff, permission revocation, and seat allocation need transactionally consistent state. Validate necessary cross-tenant transactions on Citus. Emit notifications/workflow signals after commit using the existing after-commit pattern; durable retries use the operation/version identity. A replayed handoff must not reset clocks or send another notification.

For consolidated lists, use authorized qualified work references or batched tenant-scoped reads through the shared-work service. A projection may index permitted metadata for ordering/counts; it is not the authorization authority. Revocation takes effect at read and mutation time, including against stale projections and open browser sessions.

### Product capability enforcement

Add the new product to shared constants, session product mapping, route/API metadata, settings navigation, onboarding seeds, and upgrade handling. Extend existing product checks into explicit capability checks where operational features are currently guarded by `product_code === 'psa'`. Merely allowing new routes would leave valid customer operations blocked in server actions; relaxing all PSA-only checks would expose billing.

Keep invalid/missing entitlement handling explicit. Existing legacy tier fallbacks are not proof that an MSP holds a verified Pro license or purchased co-managed capacity. Use a common entitlement resolver for hosted subscription state and verified self-host license claims, with the same business rules.

## Licensing and provisioning implementation

Hosted subscriptions require a separate identifiable co-managed seat item/subscription. Purchase confirmation and idempotent webhook reconciliation update co-managed capacity only. They must not replace the Pro seat price, change regular `licensed_user_count`, or be misclassified by generic license subscription loaders. Existing annual Pro subscriptions can sponsor monthly co-managed capacity without an invented annual co-managed price.

For self-hosting, extend signed license issuance, verification, refresh/check-in, licensing portal purchase, and local claims with explicit co-managed capacity and sponsor binding. An older license without this claim has zero co-managed capacity, even when its ordinary `seats` claim is absent/unlimited. Offline verification uses the signed entitlement and expiry; it must not require contacting hosted billing for every operation.

Current appliance license state is installation-wide. Refactor effective license resolution to distinguish the sponsoring tenant, sponsored customer allocations, and any independently licensed tenant created by a later upgrade. Do not apply the appliance's generic Pro seat count separately to every customer tenant. Preserve the existing single-tenant appliance path for installations without co-management.

Seat guards cover creation, pending invitations, acceptance, reactivation, bulk imports, directory-driven technician creation, and all API paths. Count active customer technicians and explicit pending reservations within their allocation. Portal contact sync does not consume technician seats. Use locking/constraints to prevent two last-seat requests from both succeeding.

An allocation cannot be reduced below active technicians plus pending reservations. A pool reduction cannot take effect below committed allocations through the normal UI. If an external billing/license change reduces capacity anyway, stop growth and surface the deficit; do not arbitrarily deactivate users or choose customers to evict. Apply the 30-day transition policy and let the MSP correct allocations or renew. The lapse start is durable and is not extended by retries, restarts, or repeated failed check-ins.

After grace expires, enforce read-only access at write boundaries, including API, email, workflow, and background mutation paths. Keep sign-in, read/export, licensing recovery, and paid upgrade available. Expose paused intake and resume it through the existing suspension/intake mechanisms; do not silently discard email. Renewal restores eligible workspaces without duplicate intake or renewed seat charges.

## Billing and effort accounting

Extend the work-item reference/resolver to carry source tenant separately from time-entry owner tenant. MSP time entries remain MSP-owned. Their billing client and profile come from the relationship's MSP client mapping, not from the customer's self-organization IDs.

Provide a common normalized billing-work context for both native and shared work: source reference, display snapshot, local client/profile, applicable MSP contract/contract line and service, and approval state. Adapt existing billing candidate loaders to consume it. Keep contract calculation and invoice generation in the existing billing engine. Do not create hidden tickets or duplicate project trees to satisfy a same-tenant join.

Cover contract-backed, catalog-priced/uncontracted, and non-billable MSP time through existing semantics. Customer project collaboration does not require creating a separate MSP project. Avoid adding a new fixed-price shared-project billing product in this release; ordinary MSP contract billing remains available.

Invoice evidence and posted financial records must continue to resolve after unsharing, departure, customer export, and eventual customer deletion. Snapshot the permitted work description/identity under MSP ownership at the appropriate billing/participation boundary. Never depend on continuing live trust to render a historical invoice.

Customer time logging must bypass invoice/service requirements because of an explicit operational-time capability, not fabricated zero-value contracts. Adapt project actual-hour calculations to report local and authorized shared contributions accurately, including edit/delete/reassignment. Organizational timesheet approval remains separate.

## SLA behavior

Retain the customer's existing ticket lifecycle and SLA policy. Add a separate MSP tracking record resolving policy, priority targets, and business calendar from the MSP's local client/queue configuration. Priority mapping is explicit between customer priority values and MSP SLA targets; missing required routing/policy mappings produce a setup error rather than silently choosing an unrelated priority.

The first actual escalation starts the MSP clock. Oversight reads and scope grants do not. A qualifying MSP response visible to customer IT or the requester can satisfy MSP first response; customer-authored and MSP-private notes cannot. Ordinary resolution closes the applicable timelines with their own outcomes.

Proposed handback default: pause MSP timing while customer IT is responsible, and resume the same unresolved MSP obligation on re-escalation. Preserve prior elapsed time and breaches; repeated handoffs cannot manufacture a fresh deadline. Track each handoff separately for history. A genuine closed/reopened ticket follows the existing reopen policy with distinct tracking identity where required.

Generalize SLA backend inputs and locks to identify which organization's obligation they affect. Reuse existing business-hours, pause, notification, scheduled-job, and Temporal behavior; do not overwrite customer SLA columns with MSP deadlines. Notifications and workflow actions execute with the policy-owning organization and authorized audience.

## Separation, export, and upgrade

Relationship termination and license lapse are distinct. Explicit termination revokes live cross-organization access immediately; the 30-day licensing grace does not preserve a terminated trust. Customer read/export/upgrade remains available through the agreed transition policy.

Before closing live access, finalize permitted historical evidence using a consistent cutoff and operation identity. Retention is limited to shared work the MSP participated in, such as a handoff it received, an assigned shared task, or a contribution it made. Merely appearing in oversight is insufficient. Keep evidence captured while authorized; later unsharing must not let a snapshot worker read newly private or never-shared content.

The MSP archive includes permitted shared history, attachments, its own private notes, and billing/time references. It excludes customer-private notes, unrelated customer resources, connection secrets, and private credential data. Customer export includes customer-owned data, customer-private notes, shared history and attribution, and customer-owned files. It excludes MSP-private notes, commercial records, and integration credentials owned by the MSP.

Build a customer-authorized portable export on top of existing export/provisioning/migration infrastructure. A database dump from the internal exporter is not sufficient. Include schema/version metadata, record relationships, workspace configuration needed for reconstruction, referenced file blobs, and stable author attribution. Use explicit export manifests and audience-aware selection. Do not include platform secrets, session tokens, or signing keys. Handle customer-owned credential-vault content with an encrypted portable representation and the existing secret-management conventions; never export unusable ciphertext tied only to the old installation's key.

Validate a restore into an isolated installation. External OAuth connections must be reauthorized; imported users must not retain active sessions. Preserve ticket/project histories and usable documents/credentials without copying live trust to the old MSP.

Generalize the current AlgaDesk-to-PSA upgrade workflow for a sponsored-to-independent transition. The customer secures its own paid PSA entitlement; seed only missing commercial capabilities and permissions; preserve operational data; detach sponsorship and release its allocation exactly once. Do not swap the MSP's entire subscription or charge the customer for the MSP's other clients.

On self-hosted installations, independent upgrade requires tenant-bound license resolution. An in-place product upgrade does not move hosting or remove the installation operator's infrastructure control. The export/restore path lets a customer leave that installation. Automatic hosted migration or a hosting contract transfer is outside this release.

Read-only status does not delete data. Use existing explicit deletion/retention workflows after export or other authorized deletion; do not introduce automatic purge as part of the 30-day policy. Preserve MSP-owned archives and invoice evidence when deleting customer data.

## Code entry points and current constraints

| Area | Existing entry points and required change |
| --- | --- |
| Product/tier | `packages/types/src/constants/productCodes.ts`, `tenantTiers.ts`, `tierFeatures.ts`; product and tier are already separate. Add an explicit customer product and sponsorship checks. |
| Product enforcement | `server/src/lib/productSurfaceRegistry.ts`, `server/src/lib/settingsProductTabs.ts`, `shared/services/productAccessGuard.ts`, `packages/auth/src/lib/nextAuthOptions.ts`; replace applicable binary PSA-only checks with capabilities and preserve commercial denial. |
| UI release flag | `packages/ui/src/hooks/useFeatureFlag.tsx`, `packages/ui/src/components/feature-flags/FeatureFlagPageWrapper.tsx`, `docs/features/feature-flags.md`; use client-rendered gates for `release-v1-6-feature`, without adding route or backend flag enforcement. |
| Identity/policy | `packages/auth/src/lib/withAuth.ts`, `rbac.ts`, `policy/PolicyEngine.ts`, `packages/types/src/interfaces/authorization.interface.ts`; session/home-tenant permissions must remain distinct from foreign-resource scope. |
| Tenant database | `packages/db/src/lib/tenantDb.ts`, `tenantTableMetadata.ts`, `tenantScopedQuery.ts`; add narrow collaboration repositories without weakening ordinary tenant joins. |
| Ticket actions/UI | `packages/tickets/src/actions/ticketActions.ts`, `packages/tickets/src/models/comment.ts`, `packages/tickets/src/components/ticket/CommentItem.tsx`, `server/src/lib/api/services/TicketService.ts`; current board/user joins and two-state internal notes assume local identity. |
| Projects | `packages/projects/src/actions/projectActions.ts`, `projectTaskActions.ts`, `projectTaskCommentActions.ts`; add qualified collaborators and audience filtering. |
| Time/billing | `packages/scheduling/src/actions/timeEntryCrudActions.ts`, `packages/db/src/services/projectTaskActualHours.ts`, `packages/billing/src/lib/billing/billingEngine.ts`; existing work-item resolution and invoice candidate queries use same-tenant tickets/projects. |
| SLA | `packages/sla/src/services/slaService.ts`, `slaPauseService.ts`, `slaBackendActions.ts`, `ee/temporal-workflows/src/workflows/sla-ticket-workflow.ts`; current ticket fields and workflow input describe one obligation. |
| Seats | `packages/users/src/lib/internalUserLicenseGuard.ts`, `packages/licensing/src/lib/license-types.ts`, `license-state.ts`, `ee/server/src/lib/license/userSeatGuard.ts`; generic appliance claims/guards need separate customer capacity and tenant-aware resolution. |
| Subscription | `ee/server/src/lib/stripe/StripeService.ts`, existing license webhook/lifecycle handlers, `ee/temporal-workflows/src/activities/appliance-license-activities.ts`; ordinary seat subscription selection must distinguish co-managed purchases. |
| Provisioning | `ee/temporal-workflows/src/workflows/shared/tenant-creation-steps.ts`, `tenant-creation-workflow.ts`, `ee/temporal-workflows/src/db/tenant-operations.ts`, onboarding seed sets; reuse orchestration and add sponsored provisioning. |
| Upgrade | `ee/temporal-workflows/src/workflows/tenant-product-upgrade-workflow.ts`, `activities/product-upgrade-activities.ts`, `db/product-upgrade-operations.ts`; currently designed around AlgaDesk and a tenant's own Stripe subscription. |
| Export/lifecycle | `ee/server/src/lib/tenant-management/tenant-export.ts`, `ee/temporal-workflows/src/workflows/tenant-export-workflow.ts`, `tenant-deletion-workflow.ts`, `packages/db/src/lib/tenantSuspension.ts`; internal export needs customer authorization, portable files/configuration, and audience-aware retention. |
| Integrations/files | `packages/integrations/src/actions/integrations/`, `packages/documents/src/actions/documentActions.ts`; allow only selected customer connections and enforce resource/audience scope on downloads and linked data. |

## Implementation sequence

Complete the following in dependency order. These are implementation checkpoints within the release, not permission to ship an incomplete tenant-switching or ticket-copying substitute.

1. **Capability and identity contracts.** Define product code/capabilities, qualified work/actor references, lifecycle enums, permission scopes, and common hosted/self-host entitlement results. Enumerate affected binary product checks and entry points. Preserve the native PSA/AlgaDesk paths.
2. **Schema and authorization.** Add relationships, allocations, grants, actor references, private contributions, work references, obligation tracking, and archives. Register metadata; establish transaction/locking boundaries. Implement shared-work policy/query/command services and foreign-actor attribution before exposing cross-tenant reads.
3. **Commercial capacity.** Implement hosted purchase/reconciliation and self-host signed capacity issuance/refresh. Make seat guards and license resolution tenant-aware. Add allocation controls, race-safe reservations, and lapse/read-only enforcement.
4. **Provisioning and administration.** Extend creation seeds/orchestration, client-linked creation UI, administrator invitation/activation, relationship grants, delegated settings, and customer product navigation. Establish the shared client-side `release-v1-6-feature` boundary and apply it to this and every later co-managed UI surface; leave routes and backend operations independent of the flag. Test both deployment modes without granting arbitrary tenant provisioning to ordinary users.
5. **Shared tickets and content.** Add canonical ticket access in both shells, MSP queue references, immediate handoffs/handbacks, qualified assignment pickers, three audiences, attachments, and routing-aware notifications/email. Preserve local board/status semantics and handle denied bulk selections.
6. **Oversight and projects.** Deliver combined authorized ticket lists/dashboard counts, working/oversight filters, shared project/task collaboration, and scoped search/export. Reuse current UI components and project behavior under the shared-work context.
7. **Effort, billing, and SLAs.** Add operational customer time, MSP qualified work billing resolution, accurate effort totals, independent MSP SLA tracking, priority mapping, and pause/resume behavior across all existing SLA backends.
8. **Departure and continuity.** Implement immediate trust revocation, authorized historical snapshots, portable customer backup/restore, independent PSA upgrade for hosted and self-hosted tenants, allocation release, and archive-safe deletion behavior.
9. **Validation and product finish.** Complete the behavioral suite below, permission/locale/error copy, dark/light UI checks, and CE/EE build checks. Verify UI flag coverage and document `release-v1-6-feature`. Resolve all price/license configuration dependencies before enabling purchase or provisioning.

All new user-facing controls need stable unique IDs, shared UI primitives, translations, and existing theme tokens. Errors distinguish insufficient capacity, missing Pro entitlement, expired grace, provisioning failure, missing mapping, and revoked access without revealing unauthorized record contents.

## Behavioral coverage

Use real migrated PostgreSQL/Citus queries for tenancy, concurrency, and billing. Source-string tests alone cannot prove these behaviors. Reuse existing integration/Temporal/UI harnesses; this plan does not require running the future feature tests before implementation.

| Test | Required evidence |
| --- | --- |
| T01 Product matrix | Customer can use agreed operational features through UI/API/actions; billing, sales, unsupported integrations, and nested sponsorship are denied. PSA/AlgaDesk behavior remains intact. |
| T02 Purchase separation | Confirmed monthly purchase increases only co-managed capacity. Duplicate/out-of-order webhook reconciliation, regular Pro seat changes, and annual Pro sponsorship preserve the correct quantities/prices. |
| T03 Self-host claims | Pro with explicit capacity works offline within signed validity; old/unbound-to-sponsor/missing-capacity/invalid licenses cannot provision. Customer allocations do not consume regular MSP seats or inherit another tenant's independent license. |
| T04 Allocation concurrency | Concurrent last-seat allocation, invitation, acceptance, import, and reactivation cannot oversubscribe. Failed/retried provisioning releases or reuses reservations without duplicate tenants/users. |
| T05 Topology/provisioning | New customer is linked to the intended local client, has one sponsor and correct seeds, and requires configured activation. Existing-tenant attachment, second sponsor, nested relationship, and different-installation targets fail. |
| T06 Isolation matrix | Two MSPs, multiple customers, requester, customer technician/admin, and differently scoped MSP roles exercise list/detail/mutation/API/search/count/report/download/realtime paths. Denied siblings, private boards, foreign IDs, and stale grants expose no contents or counts. |
| T07 Delegated administration | MSP delegates can perform only granted settings/user operations; they cannot expand scope, impersonate customer admins, reset their credentials, reveal private notes, or turn ordinary users into privileged proxies. |
| T08 Shared ticket lifecycle | Customer creates one ticket; oversight sees it without working-queue placement or MSP SLA. Escalation adds immediate MSP responsibility, queue placement, one handoff, and one SLA start. Retry, handback, re-escalation, close, reopen, and bulk operations preserve one authoritative ticket. |
| T09 Content audiences | Both organizations and portal users see exactly their permitted notes/threads/attachments. Foreign authors retain attribution. Email previews, notifications, search, exports, and shared project comments never disclose organization-private content. |
| T10 Canonical routing | Different customer/MSP board/status/priority sets and identical ticket numbers remain unambiguous. MSP edits use customer-valid statuses; local queue placement cannot corrupt owner-tenant FKs or silently remap priority targets. |
| T11 Consolidated views | Mixed native/shared tickets sort, paginate, count, filter, and export correctly; unauthorized/stale projection rows cannot leak or create false totals. Routine shared ticket/project actions do not switch sessions. |
| T12 Project collaboration | Explicit sharing permits both teams to assign and work on the same tasks; unshared projects remain private. Shared grants do not assign all work automatically. Revocation closes live access. |
| T13 Time and billing | Customer can log time without service/contract setup. MSP logs time on the same customer work and invoices only its own approved effort using its local client/profile/contract. Include contract-backed and catalog-priced time, edits, approvals, and double-invoice prevention. Native billing remains correct. |
| T14 Effort totals | Customer/MSP task contributions, time edits/deletes/moves, private timesheet fields, and optional approvals produce accurate organization-separated and permitted combined effort views. |
| T15 SLA independence | Customer work before escalation never consumes MSP time. Different calendars/priorities, qualifying response authors/audiences, handback pauses, re-escalation resumes, breaches, retries, closure, and both backend implementations maintain independent obligations. |
| T16 Revocation race | An open page, cached search result, queued email/notification, workflow, or concurrent command cannot continue using revoked grants. Only already-authorized historical evidence remains available. |
| T17 Lapse/recovery | Durable 30-day deadline blocks growth immediately and writes after expiry across interactive/API/background intake. Renewal recovers without extending an old lapse improperly, losing email, or duplicating processing. Reduced external capacity does not arbitrarily delete users. |
| T18 Archive/export/restore | MSP archive contains only participated-in shared work and its own private/commercial data; customer export excludes it. Restore customer records, attribution, files, configuration, and encrypted business credentials in isolation. OAuth/session secrets and old live trust do not carry over. |
| T19 Independent upgrade | Hosted customer buys its own PSA subscription or self-hosted customer obtains its own license; data remains, commercial capabilities initialize once, sponsorship ends, capacity releases once, and MSP billing/other customers stay unchanged. Failed payment/seed retry leaves recoverable state. |
| T20 Deletion and migration | Customer deletion after departure leaves MSP invoices/archive usable. Fresh install and upgrade migrations pass PostgreSQL and Citus constraints; local actor/audience backfills do not widen existing access. |
| T21 UI release boundary | On both deployment modes, disabled/loading/unknown/error flag states expose no usable co-managed entry points or content, including direct browser navigation. Enabled `release-v1-6-feature` reveals the permitted feature UI. Ordinary PSA/AlgaDesk UI remains usable. |
| T22 Backend flag independence | With `release-v1-6-feature` off, authorized direct API/server-action operations and route access still work under normal product/license rules. Existing provisioning, email intake, workflows, billing, and SLA processing remain functional. Unauthorized operations remain denied for their ordinary authorization reasons, independent of flag state. |

Run focused package tests and affected integration/Temporal suites, typechecks for changed packages, and both CE/EE builds. Use a browser smoke journey with one MSP and two customer workspaces to validate provisioning, isolation, escalation, collaboration, invoicing, and departure. Test light/dark layouts and localized labels in the affected screens. Do not add a large set of duplicated source-contract tests as a substitute for these journeys.

## Dependencies and explicit non-goals

- Production purchase requires a configured USD 11.49 monthly price and self-host signed-license issuer/portal support for co-managed capacity. Add fixture contracts in this repository and identify any external service changes; do not silently ship self-host provisioning with an unsigned local seat counter.
- Hosted SKU contract: configure `STRIPE_CO_MANAGED_USER_PRICE_ID` as USD 1149 minor units, monthly interval count 1, licensed usage. Dedicated subscriptions carry `metadata.subscription_kind=co_managed` and `metadata.tenant_id=<sponsor>`. Capacity requires a paid invoice and a current configured Pro subscription belonging to the same Stripe customer. This subscription is excluded from ordinary MSP seat import, update, and deletion handling. The dedicated purchase actions and UI create this contract; production price configuration remains external.
- Offline issuer contract: sign `aud=<sponsor tenant UUID>` and `co_managed_seats=<purchased pool>` independently from `seats`, with integer, monotonically advancing `iat` when capacity or validity changes. Older signed licenses cannot override newer reductions. Existing unbound licenses provide no customer capacity. Issuer/portal changes live outside this repository and need deployment verification.
- The first release has no nested relationships, multiple sponsors, connections between installations, or attachment of existing tenants.
- There is no separate mirrored ticket lifecycle, automatic RMM replication, new directory connector, fixed-price shared-project billing product, or general integration synchronization framework.
- The customer backup is portable and restorable; automated transfer between hosting providers is not included.
- The working name and product code are sufficient for implementation. Marketing naming, annual pricing, and partner discounts are separate decisions.

## Defaults presented for plan approval

The interview settled the main scope and commercial model. These concrete defaults complete the design and are included in approval of this plan:

1. The detailed operational capability matrix above, with monthly-only launch pricing and existing separately metered add-ons retaining their rules.
2. Initial customer-admin acceptance of the prepared relationship scope; explicit MSP role/team assignment within that scope.
3. One customer-owned board/status workflow, with separate MSP queue metadata and explicit SLA priority mapping.
4. Shared effort totals with organization-private timesheet details, and the same three note audiences on project tasks.
5. Handback pauses and re-escalation resumes the unresolved MSP SLA obligation without resetting elapsed time or breaches.
6. Normal capacity reductions cannot strand current allocations; external deficits use the same 30-day transition policy. Explicit trust termination still revokes live access immediately.
7. Self-host independence can be licensed in place, while export/restore provides an exit from MSP-operated infrastructure. No automatic hosting transfer is promised.

## Competitive evidence

These sources informed the separation of visibility, responsibility, licensing, and workspace ownership. They do not establish unverified current competitor seat prices.

- [StreamlineIT feature sheet](https://university.connectwise.com/content/documents/StreamlineIT-FeatureSheet.pdf): historical internal-first support, escalation, and MSP resale/bundling model.
- [Autotask co-managed associations](https://autotask.net/help/developerhelp/Content/APIs/REST/Entities/ComanagedAssociationsEntity.htm) and [ticket fields](https://ww1.autotask.net/help/developerhelp/content/apis/REST/Entities/TicketsEntity.htm): permitted company associations and separate co-managed visibility/assignment.
- [Autotask Taskfire](https://www.autotask.net/help/content/4_Admin/5ExtensionsIntegrations/CPPortalTaskfire/CPTASKFIRELANDING.htm): a customer internal help desk integrated with the provider's operation.
- [Halo co-managed setup](https://usehalo.com/halopsa/guides/1948/): roles, customer restrictions, teams, and escalation actions.
- [Halo instance synchronization](https://www.usehalo.com/guides/2455?Use-Case=CSM): separate-instance collaboration requires explicit ticket/action/field mappings.
- [Dataverse security concepts](https://learn.microsoft.com/en-us/power-platform/admin/wp-security-cds): business-unit scope participates in record authorization, beyond view configuration.
- [Published Alga pricing](https://www.nineminds.com/plans): the USD 7.99 AlgaDesk and USD 14.99 AlgaPSA monthly prices used to choose the approved USD 11.49 midpoint, checked during design on 2026-09-06.

## Completion criteria

### Implementation progress

- 2026-09-06: Added signed, sponsor-bound `co_managed_seats` capacity independent of MSP seats. Corrected signature verification to preserve the existing tenant audience claim, with malformed-claim rejection and cached-expiry checks for capacity. Licensing tests pass (39 tests across four files), as does the licensing package typecheck. Provisioning and capacity allocation are not connected yet.
- 2026-09-06: Added the `co_managed` product/capability registry, session preservation, operational route/API metadata, commercial and unknown-surface denial, and restricted settings. Both customer UI shells use the client-only `release-v1-6-feature` boundary. Focused product/UI regressions pass (135 tests across 20 files, including targeted reruns); types, auth, and server typechecks pass (server needs a 12 GiB Node heap). This is boundary preparation: database product support, provisioning, operational server-action conversion, restricted integration configuration, and sponsorship enforcement are still pending. No customer workspace can be provisioned through this checkpoint.
- 2026-09-06: Qualified authorization-kernel RBAC cache entries by the full principal context, including tenant. A shared request cache can no longer reuse the same user ID's grant/denial across workspaces or changed user/API-key/agent contexts. Authorization package tests pass (43 tests across six files) and its typecheck passes. Cross-tenant collaboration authorization is still to be implemented separately.
- 2026-09-06: Added product/schema migration and tenant metadata for customer-owned relationships, sponsor entitlements, and sponsor allocations. Added an internal, transactionally locked reservation service with new customer identities, immutable request fingerprints, no nested sponsorship, and signed self-host capacity checks. Twenty-one PostgreSQL integration tests pass against disposable databases, covering contention, retries, scope isolation, lapse/expiry growth denial, acceptance constraints, and safe migration replay/rollback. Licensing and database typechecks pass. Test databases are cleaned up; the running app database was not migrated. Citus can be exercised with `CO_MANAGED_TEST_CITUS=1` but is not installed in this local PostgreSQL environment, so distributed migration/transaction validation remains outstanding.

- 2026-09-06: Added the common entitlement lifecycle: serialized current-provider observations, signed-license version ordering, durable 30-day expiry/deficit/revocation deadlines, and renewal recovery. Stripe checkout/import/update/delete processing recognizes the dedicated co-managed SKU and refreshes capacity independently of MSP seats; base subscription events also refresh eligibility. Manual/connected appliance license writers update offline capacity atomically. Online revocation starts the co-managed grace period immediately and matches the observed token so delayed responses cannot revoke newer licenses. PostgreSQL reservation/entitlement coverage is now 32 tests; Stripe adapter and lifecycle suites pass 36 tests; licensing tests pass 39 tests. Licensing, server, EE server, and Temporal worker typechecks pass. Mutation enforcement across customer workflows is still pending: persisting a read-only deadline alone does not complete T17.

- 2026-09-06: Added dedicated hosted co-managed seat purchasing with charge previews, embedded checkout, quantity updates/cancellation, billing-update authorization, and a client-only flagged Pro navigation/screen. Durable purchase intents serialize charges and allocations; retries recover existing checkouts, applied quantity changes are not charged again, and pending purchases block reservation growth until reconciled. Verified checkout webhooks can finalize replacement subscriptions without a browser return. Self-hosted users manage capacity through their signed-license screen. PostgreSQL coverage is now 37 tests, Stripe adapter/lifecycle/purchase coverage is 49 tests plus four billing-action authorization tests, and focused billing UI/navigation/route coverage is 18 tests. Licensing, server, and EE server typechecks pass. No live payment was made; real Stripe price/webhook verification and browser smoke remain pending.

- 2026-09-06: Added the co-managed onboarding seed set and product bootstrap resolution. The unified catalog explicitly includes 142 operational permission identities for this product, with six default technician/requester roles and no finance, sales, billing, accounting, RMM, or extension grants. Shared project and asset seeds are reused; operational document folders omit commercial defaults and preserve customer changes on replay. The PostgreSQL suite now passes 40 tests, including actual role/permission/folder seed execution, replay, and product isolation; 44 existing permission-catalog tests and seven Temporal bootstrap-selection tests pass. This prepares the worker seed set; sponsor-authorized tenant creation, administrator invitation, and acceptance are not connected yet.

- 2026-09-06: Added `@alga-psa/co-managed` as the domain boundary, initially with durable provisioning requests and transactional worker steps. A request atomically reserves capacity and stable workspace, self-organization, board, and administrator-invitation identities; retries reject changed administrator/name/destination details. Database steps serialize duplicate deliveries, skip committed work, require predecessor steps, and stop on lapse or cancellation. Failure retains capacity until cleanup; final cleanup checks that the tenant and registered customer-owned rows are gone before releasing seats, and cannot operate on accepted relationships. PostgreSQL coverage is now 52 tests. The domain package builds and typechecks. These are internal primitives: the tenant creation adapter, invitation delivery, activation, public actions, and provisioning UI still need wiring.

- 2026-09-06: Connected durable steps to the existing tenant, onboarding-seed, tenant-settings, and board-status engines through an explicit shared transaction context. Added a registered Temporal provisioning workflow and invitation-delivery activity. Customer bootstrap creates one self-organization/contact, operational seeds, service desk/statuses/priorities, and the initial administrator invitation; it reaches pending acceptance with an explicit initial board scope and sponsor escalation destination. No customer user/password or sponsor subscription is copied. System-created priorities can have a null author, matching statuses. Invitation delivery retains its token in the customer tenant, records delivery status, renews only an unused invitation on delivery retry, and skips terminated relationships or completed delivery. Five integration tests execute this against a disposable copy of the complete installed schema, with a mocked email provider; 52 persistence tests and 16 board regressions pass. Domain, server, and Temporal typechecks pass. The full-schema test needs `pg_dump` and `psql`; it copies schema and public reference defaults only, and never mutates the source database. It is not Citus or full historical-migration validation. No live workflow, migration, or invitation was started.

- 2026-09-06: Added customer-local acceptance actions and a first-sign-in scope review inside the UI-only release flag. Acceptance derives the actor from the authenticated home workspace, checks live internal-user permission rows, rejects stale revisions or changed displayed terms, and serializes with provisioning cancellation. Relationship activation, reserved-seat activation, and a customer-owned receipt of the approving actor and exact scope commit together; duplicate submissions produce one receipt. Initial scope explicitly has no shared projects or delegated administration. Added catalog read/manage capabilities for PSA and co-managed administrators and an additive reconciliation migration. Requester portals retain the release boundary without administrator acceptance. Twelve complete-schema PostgreSQL tests now pass, alongside 52 reservation/lifecycle tests and 63 permission/action/UI tests; the domain package builds and server/EE typechecks pass. No running database was migrated or live invitation sent. The acceptance UI does not itself enforce pending activation on operational APIs/intake; that remains part of the lifecycle mutation guard work.

- 2026-09-06: Connected sponsor-authorized provisioning and retries to the registered Temporal worker. The public adapter overrides caller-supplied sponsor/actor fields with the authenticated home identity, reserves before scheduling, returns a recoverable scheduling status, and uses a stable sponsor/operation workflow ID. Added a client-detail header action, client-linked creation form, capacity-limited allocation, administrator details, initial visibility mode, escalation destination, and a paginated sponsor progress/retry list. The form preserves its operation ID and freezes uncertain submissions; an acknowledged domain rejection permits corrections without changing that identity. Client/board options return only authorized home-workspace labels, and progress returns selected lifecycle fields without customer tokens or content. Relationship readers can see their co-managed capacity without gaining billing mutation authority. All entry points remain inside the UI-only release flag. Thirty-two server UI/authorization tests and fourteen EE scheduling/action tests pass; server and EE typechecks pass. These checks use mocked scheduling and email; no live workflow or customer workspace was created. Live browser validation, pending-activation intake enforcement, cleanup/resend controls, and full lifecycle behavior remain outstanding.

- 2026-09-06: Added transactionally locked customer seat admission against the sponsor allocation, with active technicians and distinct unexpired invitations counted together. Customer admission bypasses ordinary MSP/appliance seat limits, requires the original administrator token before activation, blocks growth on license lapse, and serializes direct creation, invitation reservations/acceptance, reactivation, and allocation resizing. Wired the shared check into add-user actions, UserService API/bulk creation and updates, the invitation service, SCIM reactivation, MCP backing-user creation, and the ordinary Temporal admin factory. Team setup now creates the account, role, preferences, and token consumption in one transaction, and its co-managed form uses the UI-only flag based on the verified invitation product. Added sponsor allocation resizing with ownership checks, stale-value rejection, purchased-capacity checks, pending-purchase protection, and no eviction or invitation cancellation to satisfy a reduction. Twenty-four full-schema PostgreSQL tests pass (including real UserService writes and SCIM reactivation), alongside 52 reservation/lifecycle tests, 22 user-service regressions, 18 management/setup/allocation UI tests, and nine sensitive-field checks. Licensing, server, EE, and worker typechecks pass. Fixed the licensing package's missing standalone build entry configuration; its build now passes with separate runtime and Next.js action entries. These checks use disposable databases and mocked external delivery; the running application has not been migrated. Lifecycle-wide read-only enforcement, restoration/deletion paths, explicit invitation resend and cleanup controls, and Citus validation remain outstanding.

- 2026-09-06: Added a runtime-independent lifecycle admission engine and an operational transaction wrapper that preserves the existing after-commit behavior. It checks activation, live allocation, sponsor eligibility, signed/hosted entitlement, and the fixed grace deadline under the established sponsor-to-customer lock order. A state probe is explicitly advisory; mutations must retain the admission transaction through their writes. Ended relationships and missing allocations fail closed, while reads and separately authorized recovery operations remain available. Sponsor Pro loss now materializes durable grace even if the separate seat subscription is current. Nine new full-schema PostgreSQL tests exercise pending/active/grace/read-only states, renewal, sponsor downgrade, termination, lock wait rechecks, rollback, and ordinary PSA behavior; all 85 bootstrap/reservation tests and the licensing typecheck pass. This commit adds the engine; operational callers, durable intake pause/resume, and Citus runtime verification remain to be wired and tested.

- 2026-09-06: Wired operational lifecycle admission into 15 TicketService API transactions: ticket creation/editing, creation from assets, agent/team assignment changes, comment creation/editing, and bundle mutations. Admission runs before operational row access in the same transaction. API error mapping returns 409 before acceptance and 423 for read-only workspaces. Two additional full-schema PostgreSQL tests exercise all 15 direct service entry points in pending/read-only states and execute real ticket creation/editing through active/grace states, preserving reads after expiry and suppressing publication on denial. All 35 full-schema tests and ten ticket API regression checks pass; the server typecheck and standalone licensing build pass. These checks mock outbound analytics/events only; database admission and ticket writes are real. Remaining ticket entry points include deletion, asset links, files, materials, server actions, and intake/worker adapters; this is not yet complete lifecycle enforcement.

- 2026-09-06: Added lifecycle deferral to the V2 durable inbox processor. Pending/read-only/terminated workspaces retain staged source and defer without claiming or spending attempts; pause state and the next check persist in PostgreSQL. Lifecycle admission runs again before inbox and operational locks, and a pause during source fetch releases only the matching lease and refunds only a newly spent attempt. Reclaimed leases retain prior attempt counts; earlier error provenance survives. Due-row scans and claims honor the persisted wakeup, and completed inboxes replay their stored outcome during a later lapse. Three new full-schema PostgreSQL tests cover repeated pauses, the source-fetch race, lease fencing, renewal, and exactly-once ticket/comment/outbox effects using real shared ticket models with sender/routing and source transport mocked. All 38 full-schema tests, 17 queue regressions, the server typecheck, the email-service build, and a plain-Node import of its compiled processor pass. Added the worker licensing dependency, compilation aliases, and Docker manifests. No running database or mailbox was changed. This guards V2 core processing only: legacy intake, ensuring co-managed producers/consumers use durable processing when the installation default is off, artifact/outbox workers, other operational mutations, and Citus/live validation remain outstanding.

- 2026-09-06: Made durable intake selection tenant-aware across pointer producers, direct IMAP staging, ingress-to-inbox handoff, recovery, Microsoft reconciliation, V2 dispatch, and outbox consumer deduplication. Co-managed products and customer workspaces with co-management history require enforce mode regardless of the installation rollout setting; retained intake therefore stays on the same pipeline after an independent upgrade. V1 deliveries for those workspaces persist an idempotent durable ingress before acknowledgement, preserving provider pointer details and surviving Redis enqueue failure without executing legacy effects. V2 consumers remain available after runtime provisioning; the dispatcher defers all ordinary-tenant V2 job types while their rollout is off. Seven added PostgreSQL tests verify all three provider pointers, off/shadow overrides, upgrade history, old V1 deliveries, direct source staging, recovery sweeps, and outbox consumer deduplication. All 45 full-schema PostgreSQL tests and 43 legacy-fetch/maintenance/consumer regressions pass, as do the server typecheck, email-service build, and plain-Node imports of both compiled dispatchers. Source storage and Redis delivery are mocked; no live mailbox or running database was changed. Remaining lifecycle work includes operational attachment/document persistence and background event consumers, other API/action mutations, paused-intake UI, and live/Citus validation. This does not complete T17 or the overall plan.

- 2026-09-06: Added lifecycle deferral for durable email attachments, including original MIME and embedded-image persistence. Pending and previously failed artifacts retain their scheduled wakeup and failure history without spending attempts; only the exact lease may release claimed work, and reclaims never refund an earlier attempt. Document, file metadata, folder, and ticket-association writes now check lifecycle inside their transaction. Expiry during upload rolls those writes back, removes the uniquely owned uncommitted object on a best-effort basis, and propagates a typed pause through the concurrent artifact batch after outstanding attempts finish. Renewal resumes the same artifact and successful replays do not upload again. Two additional full-schema PostgreSQL cases cover repeated pauses, due scans, stale/reclaimed lease fencing, expiry during upload, rollback, renewal, and duplicate replay. All 47 full-schema tests and eight threading regressions pass; the server typecheck, email-service build, and plain-Node import of the compiled artifact worker pass. Storage transport is mocked, and no running database or live mailbox was changed. General storage-error orphan recovery and legacy attachment-claim fencing are pre-existing limitations; broader operational mutation coverage, paused-intake UI, shared work, and the remaining plan are still outstanding.

- 2026-09-06: Extended transactional lifecycle admission to ticket deletion, asset linking/unlinking, and document removal APIs. Admission runs before deletion dependency checks and resource reads, and the sponsor/relationship locks remain held through cleanup and association writes; ticket deletion publishes only after the outer transaction commits. Expanded the pending/expired API denial matrix and added a full-schema preservation/renewal case using actual assets, documents, associations, ticket cleanup, and tag cleanup. All 48 PostgreSQL tests and seven existing document/tenant-scoping contracts pass, as does the server typecheck. Upload admission still needs the storage metadata boundary, and ticket materials must be guarded in the canonical inventory service so its events remain after commit. Other operational actions/workers and the remaining plan are outstanding.

- 2026-09-06: Added lifecycle admission inside the canonical ticket/project material add, delete, and project billing-update transactions. This covers API and action delegates without wrapping post-transaction inventory effects in a new outer transaction. Two full-schema PostgreSQL cases cover both parent types, direct/API denial before acceptance and after expiry, retained material reads, real tracked-stock consumption/reversal, renewal, and caller rollback. All 50 full-schema tests and three existing material API contracts pass; the inventory package builds and the server typecheck passes. These checks establish lifecycle admission, not additional product capabilities or commercial permissions. Storage/file mutation admission, other operational actions/workers, and the rest of the plan remain outstanding.

- 2026-09-06: Added transactional lifecycle admission to file creation, metadata updates, soft deletion, and document-system entries. Buffer/stream uploads probe before transport, recheck before file persistence, and remove the uniquely owned uncommitted object on a lifecycle rejection without publishing workflow events. Physical deletion retains admission through the provider call, rolls back its metadata change on provider failure, and publishes after commit; provider and database failures remain separate transport concerns. Ticket uploads now persist their file, document, association, and activity in the same guarded transaction through an internal related-record callback. Added the missing nullable external-file metadata column with idempotent migration and a data-preserving rollback refusal; supplied upload metadata is persisted. Four added PostgreSQL cases verify pending/read-only denial, retained downloads, both transport-expiry races, renewal, metadata migration/persistence, deletion lock serialization, and atomic ticket attachment visibility before events. All 54 full-schema tests, six API contracts, and six storage unit tests pass; targeted metadata reruns, server typecheck, storage/email-service builds, and compiled-worker import pass. Transport is mocked; no live objects or running database were modified. The inherited BaseService bulk mutation methods still need lifecycle admission, as do other operational actions/models/workers. The full plan remains incomplete.

- 2026-09-06: Added an injected BaseService mutation-admission callback that runs before row access in all six inherited single/bulk transactions, avoiding a database-to-licensing dependency cycle. Ticket, board, status, and priority APIs supply the lifecycle guard; custom overrides retain their existing explicit guards. Bulk audit fields now use the existing per-table filtering, matching single-row operations and allowing configuration tables without generic audit columns. Expanded the ticket denial matrix to inherited bulk calls and added real board/status/priority single/bulk denial, retained reads, and renewal tests. All 56 PostgreSQL cases and six BaseService regressions pass; the database package builds and the server typecheck passes. Other API services must opt into the callback and guard custom writes; operational server actions/models/workers remain outstanding. Before guarding ProjectService bulk updates, make its per-project updates reuse the caller transaction: the current outer wrapper invokes update through a separate connection.

- 2026-09-06: Added transactional lifecycle admission to all ProjectService custom mutations and inherited bulk methods, covering projects, phases, tasks, checklist items, and ticket links. Nested helpers and per-project bulk updates reuse the caller transaction; project/task events run only after its commit and are discarded on rollback. Added missing project-status defaults to new co-managed workspace seeds, preserving customer changes on replay. Three new PostgreSQL cases exercise 17 denied mutation paths, successful project/phase/task creation, bulk rollback, event visibility after commit, renewal, and retained reads during expiry; bootstrap also verifies seeded defaults and replay preservation. All 59 full-schema PostgreSQL tests and two tenant-scoping contracts pass, as does the server typecheck. External event publication is mocked; the running database was not changed. Project server actions/models and other operational entry points still need lifecycle coverage; this does not complete the lifecycle milestone or the full plan.

- 2026-09-06: Added lifecycle admission to the canonical project model's 12 mutation methods, including project/phase creation and edits, cascading deletion, status configuration, structure updates, and phase status cloning/remapping. Each uses the shared operational transaction wrapper and retains an existing caller transaction; reads remain unguarded. Replaced manual transaction ownership in six methods with the shared wrapper. Two additional PostgreSQL tests exercise every denied entry point, real project/phase/status writes, clone/task-remap rollback, renewal, retained reads, and cascading cleanup. All 61 full-schema tests and 14 status/tenant-scoping regressions pass; the server typecheck and projects package build pass. Model admission protects callers that delegate their writes, but direct action writes and caller lock ordering still require an action audit. The project tree read currently initializes missing mappings and needs to become read-only; task models and the remaining plan are still outstanding.

- 2026-09-06: Added lifecycle admission before operational reads/locks in all 13 mutating entry points in projectActions, including direct phase completion/reopening, reordering, project close handling, and deletion validation/cleanup. Project creation registers its event on the caller transaction and suppresses it on rollback. Project-tree reads no longer create missing status mappings; legacy phases remain visible with empty status children. Project task CSV export now checks the projects capability, allowing co-managed reads during lapse while retaining product and RBAC checks. Two new full-schema cases run the action functions with the test session's customer identity and real role permissions, covering pending/expired denial, actual project/phase operations, external-transaction rollback, retained legacy-tree reads, and CSV content after expiry. All 63 PostgreSQL tests and 18 product/project regression checks pass; server typecheck and project package build pass. Authentication session transport, Next.js cache invalidation, and event delivery are mocked. Task, status, template/import, comment, and dependency action files and their models still need lifecycle coverage; this does not complete T17 or the overall plan.

- 2026-09-06: Added lifecycle admission to all 21 mutations across ProjectTaskModel, TaskTypeModel, and TaskDependencyModel. Tasks, status changes/reordering, checklists, additional assignees, ticket links, custom types, and dependency changes now retain admission through their shared or caller-owned transaction; task deletion/reordering no longer manage transaction ownership manually. Three new full-schema cases cover every pending/expired entry point, real task and related-record writes, caller rollback, retained reads, renewal, cascading task cleanup, custom-type deactivation, dependency edits, and circular-dependency rejection. All 66 PostgreSQL tests and 16 model regressions pass; server typecheck and project package build pass. All shared project-model mutations now have admission, but task/status/template/import/comment action files and services with direct writes still need their own checks before operational locks. The remaining lifecycle and co-management plan are not complete.

- 2026-09-06: Extracted task/phase ordering repair and regeneration into a lifecycle-aware service that preserves caller transactions. Standalone repair actions retain their permission checks; task cleanup and task/phase reorder recovery now invoke the service on their existing connection. Task recovery retries once without recursively invoking another action, and rejects neighbors from another phase/status or the task itself. Validation checks every fractional key, including singleton/final rows, and repair retains deterministic ordering. Two new full-schema cases verify all four service admission paths, actual task/phase action recovery with exactly one connection, caller rollback, invalid-neighbor rejection, lapse preservation, renewal, and no-op valid groups. All 68 PostgreSQL tests and 80 action regressions pass; server typecheck and project package build pass. This resolves a self-locking prerequisite for wider task action admission; most task actions still need explicit guards and after-commit event handling, and the remaining plan stays open.

- 2026-09-06: Added lifecycle admission to the remaining projectTaskActions mutation transactions, including direct status/team updates, task move/duplicate, checklist/resource/ticket-link actions, dependency changes, and bulk tags. Together with ordering guards and the custom-type model delegate, all 25 mutating entry points in this file now check admission. Events previously published inside a transaction capture their payloads there and run only after the outer commit; existing publication after completed transactions remains after commit. Two new full-schema cases exercise 23 throwing admission paths plus bulk-tag denial, real task/checklist writes, failed checklist rollback with no events, committed-row visibility at publication, retained reads during lapse, and renewal. All 70 PostgreSQL tests and 80 action regressions pass; server typecheck and project package build pass. Status-library, template/import, comment/reaction actions, other operational domains, and the remaining co-management plan are still outstanding.

- 2026-09-06: Guarded all ten project task status library/mapping mutations inside their write transactions. Library reorder now swaps positions atomically under the existing creation lock and rejects invalid/occupied positions; replacement mappings must remain in the same project and phase scope. Removing phase overrides preserves the underlying status identity despite display-name changes. Two additional full-schema tests cover pending/expired denial, retained reads, renewal, real status swaps, invalid reorder preservation, scope rejection, and task remapping. All 72 PostgreSQL tests and 96 project action/model regressions pass; server typecheck and project package build pass. Template/import, comment/reaction and other operational lifecycle paths, and the remaining co-management plan remain outstanding.

- 2026-09-06: Added transactional lifecycle admission to task comment creation/edit/deletion and reaction toggles. Comment events now wait for the owning transaction to commit, including threaded replies and soft/hard deletion. Two full-schema tests verify pending/expired denial, caller rollback without rows/events, committed-row visibility at publication, retained comment/reaction reads during lapse, renewal, reply counts, and reaction/thread cleanup. All 74 PostgreSQL tests and 96 project action/model regressions pass; server typecheck and project package build pass. Template/import, outbound project update actions and other operational lifecycle paths, plus shared-work collaboration and the remaining plan, are still outstanding.

- 2026-09-06: Guarded all 30 standard template mutations, three wizard/editor mutations, both direct template application/status-repair services, and phase/task CSV imports before operational writes. Template creation and CSV workflow events now wait for the outer transaction commit. Two full-schema cases exercise every entrypoint during pending/expired states, actual wizard templates with tasks/checklists, application/import rollback without events or usage-count changes, committed-row publication, retained template/import-reference reads during lapse, and renewal. All 76 PostgreSQL integration tests, 48 template unit tests, and 96 project action/model regressions pass; server typecheck and project package build pass. The audit found older direct ordering actions/services, outbound project status updates, and client-portal project document writes for the next lifecycle pass; shared-work collaboration and the remaining plan are still outstanding.

- 2026-09-06: Client-portal project task uploads now persist the file, document, and task association in the storage service's admitted transaction. A real portal-requester test verifies expiry during transport removes the uncommitted object and leaves no metadata/events, renewal commits the full attachment before publication, and lapse retains document reads. Removed four unused direct ordering writers after a repository-wide caller audit; existing key generation and guarded ordering actions/services remain. All 77 PostgreSQL tests, three portal project contracts, and server typecheck pass. Outbound project status email admission and other operational lifecycle paths remain outstanding alongside shared-work collaboration and the remaining plan.

- 2026-09-06: Project status update email now acquires lifecycle admission at transmission time and holds it through the provider call. Previews remain readable. A full-schema test exercises pending acceptance, normal send, expiry during provider preparation, retained lapse previews, and renewal, using a mocked outbound provider and the real template/recipient queries. All 78 PostgreSQL tests, 96 project action/model regressions, server typecheck, and project package build pass. Other operational API/action/worker lifecycle paths, shared-work authorization and collaboration, and the remaining plan remain outstanding.

- 2026-09-06: Added KB API lifecycle admission to all seven custom mutations and inherited bulk mutations. Article, document, and block content now share one transaction; nested ticket conversion and return reads reuse it, and failed cleanup no longer silently commits partial deletion. Draft/internal article documents stay client-hidden; audience/status/publication changes synchronize document visibility atomically. Ticket conversion reads the actual attributes.description field. Two full-schema cases cover pending/expired denial, retained reads, invalid content/database rollback, caller rollback, conflicting-slug update rollback, renewal, publication visibility, ticket conversion, and complete deletion. All 80 PostgreSQL integration tests, five KB regressions, and server typecheck pass. Shared KB creation, UI/review/feedback actions and import workers remain to be guarded alongside other operational lifecycle paths and the remaining co-management plan.

- 2026-09-06: Shared KB creation now owns or reuses a lifecycle-admitted transaction for article/document/content writes, with draft documents explicitly client-hidden. UI creation and ticket conversion register creation events after the owning transaction commits. The import worker acquires admission before locking staged files and propagates lifecycle denial without marking files failed or erasing source content; retrying the same staged IDs after renewal imports once. Added a compiled licensing lifecycle subpath and worker image build/copy/import checks for package-loaded handlers. All 82 PostgreSQL tests, 18 source/built-handler tests, 15 document contracts, 39 licensing tests, server/Temporal typechecks, and licensing/shared/jobs builds pass; the compiled KB handler imports successfully in plain Node. The full worker Docker image was not rebuilt here. Remaining KB mutations/reviews/feedback/import staging and retry UI, other operational lifecycle paths, and the rest of the co-management plan remain outstanding.

- 2026-09-06: KB UI editing, publication, archival, deletion, review submission/completion, and feedback now retain lifecycle admission through their writes. Article locks precede reviewer locks; review assignment replacement and completion are atomic, deduplicate requested reviewers, and preserve assignment identities across re-review. Document visibility follows article audience/status, and search events wait for the owning transaction to commit. View counters become a no-op during a lifecycle pause so article reads remain available. All 84 PostgreSQL tests and 17 document contracts pass, as does the server typecheck. Import staging/retention and renewal orchestration, other operational lifecycle paths, and the remaining co-management plan are still outstanding.

- 2026-09-06: KB import staging and terminal-row cleanup now share an admitted transaction; pending source survives the old one-day sweep, and terminal retention starts at the last update. Job scheduling runs after the owning commit. A nullable, indexed batch identity keeps polling stable across delayed scheduling acknowledgements and job-ID assignment while retaining legacy job-ID polling. Scheduling failures preserve committed source and return its batch identity. All 85 PostgreSQL tests, 18 KB action/contracts, and the server typecheck pass; migration replay and rollback refusal with retained identities are covered in the disposable schema. The running database was not migrated. Automatic/manual resumption and pending-import discovery, explicit abandonment/retention controls, lifecycle-aware import status UI, and the rest of the co-management plan remain outstanding.

- 2026-09-06: Retained KB imports are discoverable through paginated, tenant-scoped metadata reads and resumable through an admitted action that reuses pending file identities, upgrades legacy batch identities atomically, and schedules only after commit. Status distinguishes license pauses and recoverable job failures from per-file failures. The import dialog exposes saved batches and resume controls, preserves identity across response loss, and cancels stale polling across close/reopen. Recovery copy is present in eight supported languages and both generated pseudo-locales. All 87 PostgreSQL tests, 19 KB checks, four UI tests, locale interpolation checks, and server typecheck pass. Overlapping worker recovery creates one article; caller rollback schedules nothing and cross-tenant recovery is denied. Scheduling/email transports remain mocked and live browser/Citus validation is pending. Existing worker retry budgets still govern automatic retries; this checkpoint supplies explicit recovery, not a new automatic renewal scheduler. Other operational lifecycle paths and the remaining co-management plan are outstanding.

- 2026-09-06: Added customer-owned project grants and MSP-owned, customer-qualified user/team assignments, with tenant metadata and cleanup registration. Project grants have colocated same-tenant foreign keys; cross-tenant/polymorphic identities remain qualified soft references validated by the domain. Policy reads and replacements retain the lifecycle lock order and live home administrator permission rows. Only customers can change board/project scope; only the sponsoring MSP can assign its own staff. Revisioned audit receipts support exact retry without duplicate history, reject competing edits, and preserve caller rollback. License pauses deny expansion but permit strictly checked reductions in existing access. All 90 PostgreSQL tests, domain build/typecheck, and server typecheck pass, including ownership, foreign/inactive principals, permission revocation, replay, contention, and rollback. These are internal policy primitives; authenticated adapters, management UI, the shared-resource authorization resolver, content audiences, and collaboration are not connected yet. Citus execution and full departure cleanup remain outstanding.

- 2026-09-06: Added a transaction-retaining shared-work resolver for session-backed MSP ticket/project/task reads and updates. It snapshots qualified identities, validates live home sessions/users/roles, intersects explicit staff/team assignments with customer board/project grants, and preserves the home tenant context. Home authorization bundles evaluate MSP client identity rather than customer UUIDs; unsupported home-field constraints fail closed and redaction requirements accompany the authorized command context. Bundle resolution can now retain membership/assignment/bundle/rule locks, and the common locked RBAC lookup is shared with acceptance/policy administration. Session expiry and write lifecycle are rechecked before command execution. All 95 PostgreSQL tests, 43 authorization tests, authorization/domain builds, and server typecheck pass. Coverage includes caller rollback, deactivation/revocation, license pause, team membership, actual task parentage, customer policy changes during a lock wait, UUID-scope separation, and bundle lock retention. This internal primitive does not yet expose commands or UI: authenticated session/API/agent adapters, escalation-only work grants, mapped MSP work references, DTO redaction application, content audiences, and collaboration remain pending. Non-session principals and unrelated linked resource types currently fail closed. Citus and live browser validation remain outstanding.

- 2026-09-06: Connected collaboration policy to authenticated server actions and separate customer/MSP access editors. Customer targets derive from the authenticated home workspace; MSP targets derive from MSP-owned provisioning operations. Option searches are paginated and home-scoped, while MSP customer labels include only approved resources. Customers edit board/project scope; MSP administrators assign their own staff/teams. Saves preserve revision receipts across uncertain responses, freeze unresolved submissions, and ignore stale responses after changing customer. Read-only workspaces permit access reductions and explicit reload, with growth disabled. Added customer navigation and active MSP workspace links with permission checks, all inside the UI-only release flag; the actions do not check the flag. New copy covers eight supported languages and both generated pseudo-locales. All 97 full-schema PostgreSQL tests, 70 UI/product/navigation tests, locale interpolation checks, and the server typecheck pass. Tests verify forged actor/target fields, foreign operations, unselected resource isolation, live permission revocation, pagination, inactive staff, replay, and license pause. Database checks use disposable schemas and mocked authentication transport; the running database was not migrated. Live browser validation, shared-resource commands and queues, collaboration audiences, and the remaining plan are still outstanding.

- 2026-09-06: Added a shared ticket/project/task metadata reader through the retained authorization transaction and an authenticated browser-session action adapter. Responses use an explicit field allowlist and customer-qualified board/status/priority/project/phase references. Home-policy redactions remove derived display references along with their source fields, including nested display redactions; request extras cannot enter the response. Task status labels require a mapping for the actual project and phase. The adapter compares the verified session with the authenticated home user and rejects API-key identity overrides instead of borrowing a browser cookie. All 100 full-schema PostgreSQL tests, eight adapter tests, domain build/typecheck, and server typecheck pass. Reads retain home context, remain available during license lapse, and fail after scope revocation. Rich text, people, comments, attachments, effort, commercial fields, and unrelated linked records are deliberately absent from this metadata response and need their own audience/grant commands. This adds a read command and adapter, not the shared-work screen, consolidated queues, editing, or API/agent support. The running database remains unchanged; full builds, Citus, and live browser validation remain pending.

- 2026-09-06: Added customer-owned ticket responsibility/grant state and an immutable handoff journal, plus MSP-owned qualified routing references with no copied ticket/status lifecycle. Customer escalation commits responsibility, the explicit ticket grant, the approved MSP destination, and a plain-text shared IT note together. Handback retains the grant and work identity; re-escalation preserves the first escalation timestamp and reference identity. Operation receipts reject changed payloads and competing revisions and remain stable after later transitions. Customer administrators can revoke the explicit grant and return responsibility during a license pause; independent board grants remain effective, and a stale MSP reference cannot prevent revocation. Verified browser-session actions expose escalation, handback, and revocation without release-flag checks. Shared metadata now includes work revision/responsibility and supports authorized customer-local ticket reads during lapse. A common locked session/RBAC/bundle boundary serves both organizations, and MSP queue/assignment policy fields come only from a verified local work reference. All 107 full-schema PostgreSQL tests, 11 adapter tests, domain build/typecheck, and server/Temporal typechecks pass. Coverage includes canonical-ticket preservation, no shadow MSP users, concurrent requests, caller rollback, shared actor attribution, retained access, grant restoration, portal-permission rejection, destination validation, and session/license expiry while waiting for routing locks. Fixed an ambiguous fixture selection between MSP and portal permission rows. Migration replay and refusal to discard retained handoffs are covered in disposable PostgreSQL. Handoff timestamps/journal are ready for SLA consumers, but no MSP SLA obligation/consumer, consolidated queue, shared screen, handoff composer, or grant-revocation UI is connected yet. The running database was not migrated; Citus, full builds, live browser validation, and the remaining plan remain outstanding.

- 2026-09-06: Connected native customer ticket pages and a qualified shared ticket route to responsibility, escalation, handback, and customer revocation controls. A shared composer requires a plain-text IT note, freezes uncertain submissions for exact retry, and offers explicit state reload; navigation discards late responses from previous customer resources. A customer-owned, keyset-paginated grant inventory permits revocation independently of ticket-read access, returning ticket labels only after the actual read/redaction checks. The handoff reader exposes only the qualified shared IT journal, with historical actor attribution, cursor pagination, and source-aware redactions; hidden work/history fields cannot leak through journal state. Capability hints probe live command authority, while commands retain their own authorization and lifecycle checks. All new UI is inside release-v1-6-feature; authenticated read/write actions remain flag-independent. Copy covers eight supported languages and both generated pseudo-locales. All 111 full-schema PostgreSQL tests and 29 UI/action tests pass, as do domain build/typecheck, server typecheck, and locale key/interpolation checks. Tests cover live viewer/technician rights, lapse revocation, unreadable ticket labels, sibling/revoked grant exclusion, both pagination paths, immutable retries, and stale responses on navigation. The shared route currently provides metadata and handoff history; canonical editing, other content audiences, attachments, and consolidated queue links are still pending. The running database was not migrated; Citus, full builds, and live browser/theme validation remain outstanding.

- 2026-09-06: Guarded both canonical ticket update actions and the reusable transaction core with lifecycle admission before ticket locks and a deadline recheck before writes. Ticket rows are locked before reading the mutation baseline. Legacy update events/analytics and the optimized path's remaining transition events now run only after the owning commit; optimized cache invalidation also waits for commit. The auto-close closing transaction acquires lifecycle locks before inserting its resolution comment, preserving lock order when it delegates to the core. Added the tickets package's direct licensing dependency. All 116 full-schema PostgreSQL tests and 39 ticket regression tests pass, together with server and Temporal typechecks. Coverage includes pending/lapsed denial for all three entry points, retained ticket reads, renewal, caller rollback with no publication, atomic close/audit state, committed-state visibility in transition subscribers, grace expiry while waiting for the ticket lock, and actual system auto-close. These mutation paths still assume a tenant-local actor; qualified foreign attribution and shared editing are not yet connected. Auto-close state synchronization/warning dispatch and other operational entry points still need lifecycle coverage. The standalone tickets package build script currently invokes tsup without inputs/configuration; its Nx build target is a no-op and server source typechecking succeeds. Full CE/EE builds and runtime/Citus checks remain required.

- 2026-09-06: Added customer-owned collaboration actor references with qualified source tenant/user identity and no login or cross-tenant user FK. The admitted shared-work helper creates/reuses references and refreshes display metadata from the verified home user; the ticket audit writer snapshots that name and organization, rejects mixed local-user/contact identities, and requires an owner-local reference in the active transaction. Local audit records retain their existing user fields. Audit names now use text so valid long user names are preserved. Both ticket timeline presentations display foreign names with their saved organization, without looking up a customer-local user. Registered tenant metadata and deletion ordering. All 119 full-schema PostgreSQL tests and 19 timeline/update regressions pass, along with server, domain, and Temporal typechecks and database/domain/shared builds. Tests cover caller rollback, cross-owner references, source-user deletion, identical user UUIDs in different tenants, source rename/deactivation/separation, historical snapshots, long names, legacy rows, migration replay, and refusal to discard retained attribution. Compiled shared audit exports import successfully in plain Node. Direct import of the standalone co-managed dist barrel still reaches the licensing source export and fails extension resolution; Temporal compiles these sources into its own alias-rewritten tree, whose full runtime build remains to be checked. These are attribution primitives and native timeline presentation; the canonical mutation core, closure/checklist events, live updates, and shared editor are not yet connected to foreign actors. No live database migration or Citus/browser validation was performed.

- 2026-09-06: Connected owner-local actor references to the canonical ticket transaction core. A separate internal collaboration context validates the source tenant/user against the saved reference and rechecks session authority after mutation locks. Foreign edits clear local updated_by/closed_by fields and carry qualified identity through closure, reopening, checklist-template audit, workflow events, and live updates. Existing local event payloads remain compatible. Event publication validates mixed/local attribution before legacy schemas can strip fields; shared runtime and event-bus helpers now reuse the canonical contract. The client-safe actor schema and live parser retain the MSP organization and reject mixed identities or references outside the connected ticket tenant. Native live conflict handling accepts foreign actors without confusing colliding user UUIDs. Shared routing/assignment, notification suppression/close overrides, and propagating bundle workflow edits fail closed until their own authority is connected; master title edits cannot change children. All 125 full-schema PostgreSQL tests, 37 ticket/live-update regressions, and 46 event-schema tests pass, along with server, Temporal, and event-schema typechecks and event-schema/event-bus/shared builds. Tests cover actual foreign close/reopen, checklist attribution, caller rollback, closure gates, reference/source mismatch, missing collaboration context, bundle isolation, and session expiry during an actor-reference lock wait. Compiled event contracts and the shared publisher helper import successfully in plain Node. No running database migration or live browser/Citus validation was performed. Shared editor/action wiring and qualified notification-consumer presentation remain pending.

- 2026-09-06: Connected qualified ticket-update and close actors to immediate email, queued/accumulated email, and in-app notification consumers. A shared notification context preserves the event's MSP name/organization snapshot and batches only customer-local directory lookups; foreign identities cannot enter local actor fields or suppress a customer recipient with a colliding UUID. Accumulated events retain the reference through serialization and render both local and foreign actors. Notification change formatting accepts old/new and previous/new, including the in-app status/priority/assignment label lookups. Shared due-date and response-state-only edits now enter the event change map without updated_by/updated_at bookkeeping. All 128 full-schema PostgreSQL tests, 23 notification regressions, and 15 canonical ticket-update regressions pass; server typechecking passes. Real-handler tests inspect immediate, queued, mixed accumulated, and close output with mail/notification delivery mocked, and test source snapshots, colliding UUIDs, cross-owner/mixed actor rejection, and no source directory requirement. No messages were sent and the running database was not migrated. Assignment/comment/project consumers, audience-aware content, shared editor/command wiring, and remaining plan work still need implementation.

- 2026-09-06: Connected the MSP shared ticket route to a canonical editor for title, reference URL, customer status/priority, due date, and response state. Read and update policy redactions jointly constrain editing; customer-owned, ticket-scoped picklists retain current labels and paginate/search without directory access. Read-only access retains permitted values. Commands snapshot their qualified actor/resource and field baselines, lock authority before mutation, reject stale changed fields without overwriting unrelated customer edits, and commit a customer-owned operation receipt with the canonical mutation. Exact retries preserve the original receipt and emit no duplicate effects; operation reuse, caller rollback, revocation, and owner boundaries are enforced. Browser adapters verify the actual home session and cannot accept API overrides or client users. The UI freezes uncertain requests for exact retry, requires reload after conflicts, clears values after revoked access, and discards late responses after navigation. All UI remains inside release-v1-6-feature, while commands and actions remain flag-independent. Ticket interface priority and due-date nullability now match the database and validator. All 139 full-schema PostgreSQL tests and 44 UI/action tests pass, together with server/domain/Temporal typechecks and database/domain/types builds. All 23 editor strings validate across eight languages and both generated pseudo-locales. Tests include pagination, literal search, viewer/lapse behavior, field restrictions, concurrent screens/edits, operation replay, migration replay, and rollback refusal for retained receipts. Bundle child workflow edits and master workflow propagation remain guarded pending authority for every affected child. No running database migration or live browser/Citus validation was performed; consolidated queues and the remaining plan are still outstanding.

- 2026-09-06: Added combined working and customer-oversight ticket queues, accessible from the MSP ticket list and co-management overview. A single SQL relation applies current staff assignments, customer grants, home RBAC, and authorization-bundle narrowing before search, sorting, workspace filtering, pagination, and counts. Native policy fields remain local; shared policy fields come from qualified MSP routing references. Field redactions also constrain search, ordering, status counts, and working-queue membership. Workspace choices come from authorized rows; one statement snapshot returns counts and page rows together. Handback removes shared working responsibility while retaining oversight; revocation removes access, and license lapse retains permitted reads. The browser action verifies the tracked home session, and only the UI uses release-v1-6-feature. The queue screen links same-number/same-UUID tickets through qualified routes, clears stale/failed results, and has 33 translated strings in eight languages and both pseudo-locales. All 147 full-schema PostgreSQL tests and nine queue UI/action tests pass; server/domain/Temporal typechecks and core/domain builds pass. Coverage includes two customers under one MSP, another MSP, private/unshared work, global pagination/counts, literal search, canonical statuses, field/scope restrictions, unknown constraint fields, lapse, revocation, and session expiry while waiting for lifecycle locks. No live database migration or browser/Citus validation was performed. Queue export, bulk commands, assignment routing, richer ordinary-ticket filters, and SLA indicators remain to be connected alongside the remaining plan.

- 2026-09-06: Added nullable thread audience metadata, owner-local qualified comment actor references with immutable display snapshots, and separate MSP-owned private thread/comment tables with qualified soft source references. Legacy internal or indeterminate notes remain organization-private; no existing note is promoted to shared IT. Database constraints keep explicit thread audiences consistent with internal/public visibility, prevent mixed local/foreign actors, and require private replies to belong to their parent's owner and thread. Migration replay preserves data and rollback refuses retained collaboration content. Registered tenant scoping and deletion ordering. A shared audience helper applies the additional visibility restriction and locks thread/parent context for native model, API, and shared-model replies. Native comment model writes now own/reuse an operational transaction and recheck lifecycle after audience locks; they reject thread/owner reparenting, foreign-actor forgery, ordinary changes to co-managed thread visibility, and native repair/deletion of qualified foreign comments. Nullable attribution fields still work in ordinary local edits. API operator repair cannot claim a foreign-authored comment. Shared-model comment creation rejects a missing transaction before writing. All 151 full-schema PostgreSQL tests, two audience unit tests, and ten shared-model regressions pass, along with server/Temporal typechecks, database/shared builds, and a plain-Node import of the compiled audience module. No live database migration or Citus validation was performed. These are storage and native-path foundations: qualified content creation/editing, disclosure-confirmed audience changes, the unified reader/composer, attachment audience enforcement, and qualified comment notification rendering remain to be implemented.

- 2026-09-06: Added the authorized published conversation reader and verified browser action. Customer-owned public/shared IT history and permitted MSP-owned private notes merge with stable owner-qualified keyset pagination; exact PostgreSQL microseconds prevent skipped same-millisecond comments. Customer readers never select MSP-private storage. Legacy inconsistent visibility fails closed, unpublished roots/replies remain excluded, hidden parents lose their references, and tombstones never return retained bodies. Foreign authors retain saved name/organization snapshots without customer-directory lookups; responses omit metadata and email headers. Source-aware redactions independently suppress each store and remove author references together. Each page rechecks live resource/session authority while retaining permitted reads during license lapse. All 157 full-schema PostgreSQL tests and five browser-action tests pass, along with server/domain/Temporal typechecks and shared/domain builds. No running database migration was performed. The reader is not yet wired to a composer; qualified create/edit/delete/disclosure commands, attachment audience enforcement, and notification consumers remain required before enabling content controls.

- 2026-09-06: Added MSP-private ticket note creation, inherited-private replies, author-only text edits, and soft deletion with revision conflicts and immutable retry receipts. Both content and command receipts are MSP-owned; qualified source references are soft historical IDs. These operations do not change customer comments, ticket timestamps, audit history, or customer-owned command receipts. Plain-text input becomes literal text nodes and escaped Markdown, with no caller-supplied audience, attribution, attachment references, or block payload. Every command retains live shared ticket read/update authority and both actions’ field restrictions; customer sessions, viewers, foreign stores, unrelated ticket/thread parents, another author, and stale/deleted notes are rejected. Receipt replay retains its original result after subsequent changes while still requiring current trust and license eligibility. The reader exposes private revisions for conflict handling. Registered the additive receipt migration, tenant metadata, and deletion ordering. All 164 full-schema PostgreSQL tests and 11 conversation/private-action tests pass, together with domain/server/Temporal typechecks and database/domain builds. Tests cover concurrent retry/edit races, caller rollback, migration replay/retention, deletion tombstones, immutable attribution, lapse and revocation, and actual session expiry while waiting for a private thread lock. No live database migration or notification delivery was performed. This command currently accepts plain text; rich content, shared/customer-owned comment commands, disclosure changes, attachments, unified UI, archive access after separation, and audience-aware notifications remain outstanding.

- 2026-09-06: Added retry-safe customer-owned ticket comment creation around the native Comment.insert model. Customer technicians can start requester/shared IT/customer-private threads; admitted MSP technicians can start requester/shared IT threads. Replies carry qualified parent references and inherit the locked root audience. Unpublished/deleted roots or parents and inconsistent legacy visibility fail closed; neither implicit audience changes nor replies into the other organization’s private store are accepted. Native insertion has a separate internal collaboration context bound to the ticket and verified owner-local actor reference, retains foreign display snapshots with null local user/contact IDs, and rechecks write authority after thread locks. Ordinary native callers retain their attribution requirements. Customer-owned receipts, comments, thread counts, and attribution commit or roll back together; immutable requests and exact retries use stable comment/thread identities. Extracted literal plain-text encoding for both storage paths. All 170 full-schema PostgreSQL tests and nine comment regressions pass, together with server/domain/Temporal typechecks and the domain build. Tests include both organizations, nested replies, source/store and actor forgery, scope redactions, lapse/revocation, concurrent retry, caller rollback, and session expiry while waiting on a root lock. Fixed the existing shared-work fixture to explicitly start in an open status and updated stale native transaction-source assertions. The creation command accepts an application-supplied native mutation adapter; the production browser action, response-state effects, qualified audience-aware notifications/workflow events, native foreign-author presentation, and shared composer are not yet connected. Shared text edits/deletion/disclosure, rich content, and attachment enforcement remain open. No running database migration or external delivery was performed.

- 2026-09-06: Added a dedicated comment-added event contract preserving explicit audience, thread references, and qualified foreign attribution without a local user ID. It rejects foreign private audiences, mismatched internal/public visibility, inconsistent identities, and mixed actor attribution while retaining ordinary local producers. Email and in-app comment consumers use saved MSP name/organization references and never suppress a customer recipient with the same user UUID. Explicit collaboration events recheck the current published comment, inherited audience, owner-local reference/source identity, and saved attribution before delivery; deleted, unpublished, narrowed, or forged content produces no notification. Delivery uses the current stored body. Shared IT notes notify customer assignees without notifying requester contacts; internal-comment mention creation and updates exclude portal users. Existing native event shapes remain supported. All 172 full-schema PostgreSQL tests, 57 event-schema tests, and 26 email/notification regressions pass, together with server/Temporal typechecks and the event-schema build. Real-consumer tests mock delivery and cover a colliding local user ID, requester versus shared IT mail, private portal mentions, delayed audience changes, deleted content, and historical source deactivation. Updated a stale email-test directory mock for the existing batched actor-name reader. The production comment action/event publisher, response-state effects, workflow message contracts/consumers, separately authorized MSP recipient delivery, and full composer are still pending; this checkpoint does not enable the new content UI or claim full recipient authorization coverage. No messages were sent and the running database was unchanged.

- 2026-09-06: Connected customer-owned comment creation to a verified browser action and production adapter. The native comment, response-state transition, qualified activity row, and retry receipt commit together; comment/response/workflow events validate before commit and publish through existing after-commit hooks with distinct stable event IDs. Exact receipt retries do not rerun effects. Public technician replies honor effective response tracking and cannot change a response-state field restricted by either read or update policy; internal audiences leave response state unchanged. Workflow message/note contracts preserve explicit audience and qualified message authors, reject flattened local IDs and private foreign attribution, and are reused by the workflow runtime. Collaboration comment email stays on the authorized canonical ticket instead of automatically sending to bundle child requesters; ordinary local bundle delivery remains covered by regression tests. All 176 full-schema PostgreSQL tests, six action tests, 65 event-schema tests, and four workflow-builder/runtime tests pass, together with server/domain/Temporal typechecks and event-schema/shared/domain builds. Tests verify committed-state visibility to subscribers, rollback, replay, disabled tracking, qualified activity and workflow identity, field restrictions, customer-private versus shared IT events, and child-recipient isolation. The action remains independent of release-v1-6-feature. Publication currently follows the existing best-effort after-commit infrastructure; durable recovery after publication failure remains to be audited with the broader event/intake work. Native foreign-author presentation, scoped MSP recipient delivery, content live updates, shared edits/deletion/disclosure, rich text, attachments, and the unified composer remain pending. No live database migration or external notification delivery was performed.

- 2026-09-06: Native ticket conversation and Bento author resolution now render saved foreign technician name/organization snapshots with person initials and no owner-local user/contact/avatar identity. Partial or mixed attribution fails closed instead of falling back to local directory or inbound-email metadata. Native edit, delete, schedule, and forced-editor paths exclude foreign comments; qualified commands remain required. Comment DTOs type the existing nullable attribution fields, and the model lookup types its source identity and saved labels without changing SQL. All 30 targeted author/avatar/copy/map regression tests, the types package build, and server/Temporal typechecks pass. This passive historical display adds no new feature entrypoint. No running database was changed. Portal thread-root visibility and author enumeration need alignment with the stricter shared reader; separately authorized MSP delivery, live updates, qualified editing/disclosure, attachments, and the unified composer remain outstanding.

- 2026-09-06: Portal ticket details now require published comment and root rows with matching requester audience, ticket, thread, and tenant. Body and commenter enumeration clone the same predicate; shared IT, either organization’s private notes, unpublished roots, and inconsistent legacy replies remain hidden. Foreign display snapshots reach the native author UI without owner-directory lookup. Explicit comment projection omits search vectors and scheduling internals; tombstones remove retained body/Markdown/metadata, and replies omit hidden intermediate parent IDs. Two full-schema tests invoke the real portal action with actual portal RBAC and visibility queries, mocked session transport/avatar lookup, and disposable customer/MSP data. They cover inherited audience, author enumeration, later root narrowing, tombstones, hidden parents, board denial, and role removal. Corrected existing query-builder test doubles for the shared query clones. All 178 PostgreSQL tests, 18 portal regressions, and server typecheck pass. No live browser validation, running database mutation, or external delivery was performed. Attachment access and other content surfaces still need their separate audience audit; this checkpoint does not complete the overall plan.

- 2026-09-06: Separated shared resource authorization from browser-session admission and added a server-side read-only notification-recipient adapter. Background recipients use their own active internal home identity, locked roles/teams, explicit staff assignment, current customer grant, and home RBAC/bundle projection; they neither require nor borrow a browser session. Session commands retain their session checks and reject recipient principals for reads and writes. Recipient callbacks receive field restrictions and retain trust/identity/grant locks through delivery; permitted reads remain available during license lapse. Three full-schema tests cover missing sessions, viewers, identity/type and role denial, home-versus-customer policy IDs, redactions, unknown constraints, lapse, grant/staff revocation, and concurrent revocation lock retention. All 181 PostgreSQL tests, server/domain/Temporal typechecks, and the domain build pass. This is recipient authority only: content audience/redaction validation, recipient selection/preferences, notification persistence/realtime visibility, and email/in-app delivery adapters still need connection. No browser/API entrypoint accepts recipient identities, no release flag is applied to the backend, and no external messages or running database changes occurred.

- 2026-09-06: Added a recipient-authorized current comment delivery boundary for customer-owned requester/shared IT content. It reads current published thread/root/comment state, excludes private/unpublished/deleted content, and holds thread/body locks in native command order through the delivery callback. Interactive conversation and notification reads share field-source definitions. Recipient content restrictions suppress delivery; author/title/number restrictions omit those containers, and messages omit email metadata. Saved foreign author snapshots survive source rename/deactivation; self-suppression compares tenant-qualified identity so colliding customer UUIDs still notify the MSP recipient. Three full-schema tests cover independent viewer recipients without sessions, source-history attribution, current-body retries, hidden content, redactions, and concurrent body/audience lock retention. All 184 PostgreSQL tests, server/domain/Temporal typechecks, and the domain build pass. The boundary supplies an application delivery callback; email/in-app recipient selection, preferences, idempotent persistence, and qualified notification read/broadcast enforcement still need wiring. No external delivery or running database migration was performed.

- 2026-09-06: Added routing-aware comment fanout to the MSP’s qualified work reference. Direct assignees and current team members are deduplicated candidates; customer assignee/mention IDs and unrelated relationship staff never become MSP recipients. Each candidate goes through independent home authority and current content/audience checks, then routing is rechecked under retained locks before the channel callback. Stable delivery keys qualify source tenant/ticket/comment, event, channel, and recipient, including canonical UUID case. Expected access loss skips a recipient; unexpected channel failures are surfaced after other eligible recipients are attempted. Three full-schema tests cover direct/team overlap, viewers, RBAC denial, unassigned staff, stale work references, reassignment between discovery and delivery, revoked grants, retry key isolation, and channel failures. All 187 PostgreSQL tests, server/domain/Temporal typechecks, and the domain build pass. Channel preferences, idempotent persistence, email/in-app subscribers, and qualified notification read/broadcast enforcement remain to be connected; no external delivery was attempted.

- 2026-09-06: Qualified production comment/response/workflow event UUID generation by the owning tenant as well as comment identity and event type. Two customer tenants can reuse a command UUID without producing identical event IDs; exact command retries still publish nothing additional. A full-schema production-adapter regression creates the same operation UUID in two owners and verifies six distinct comment/message/internal-note event IDs. All 188 PostgreSQL tests and server typecheck pass. Existing event-bus processing sets already partition by tenant; this change also makes emitted IDs distinct for consumers that identify events globally. No running database mutation or external publication was performed.

- 2026-09-06: Added a transactional MSP in-app comment storage adapter around routing-aware fanout and the existing template core. It preserves recipient locale, notification enablement and priority, renders current authorized content, and stores only qualified source/author metadata with the shared-ticket route. MSP-owned per-delivery receipts atomically record created or disabled outcomes; concurrent retries create one row, disabled decisions remain terminal after preferences change, and deleted notifications are not resurrected. Template failures and caller rollback leave no receipts or notification rows. The additive receipt migration is tenant-distributed, validates outcome/notification consistency, retains soft historical source references, refuses destructive rollback with retained rows, and is registered for tenant metadata/deletion. Three full-schema tests cover actual French template selection, user priority/enablement, stale event body exclusion, qualified metadata, parallel retry, outer rollback, failure recovery, suppression, revocation, audience narrowing, migration replay and constraints. All 191 PostgreSQL tests, server/Temporal typechecks and the database build pass. This adapter performs storage only and is not subscribed yet: qualified inbox/count/detail visibility, realtime/push/Teams delivery authorization, channel recovery, and subscriber integration remain required before enabling it. No live migration, external delivery or broadcast occurred.

- 2026-09-06: Added the shared stored-notification read verifier and a distinct session-backed comment-content reader. Interactive reads now can validate an MSP-owned created receipt against the actual recipient, source tenant/ticket/comment, event, thread and audience, then reload current authorized content. The verifier returns fresh rendering inputs without cached title/body/link/metadata, preserves permitted reads during lapse, and hides missing receipts, tampered/deleted notifications, newly private/deleted source content, revoked grants, and restricted fields. Receipt/notification locks follow resource/content locks; session expiry is rechecked after the last lock wait. Background recipient and interactive session entrypoints retain distinct principal validation. Three full-schema tests cover fresh body reads, field redactions, lapse/revocation, source and recipient tampering, deletion, missing receipts, and actual session expiry while waiting on a notification lock. All 194 PostgreSQL tests, server/domain/Temporal typechecks, and the domain build pass. This verifier is not yet wired into native inbox actions: pagination/count/detail/read-return and broadcast consumers still require integration before the storage adapter is subscribed. No live database or external delivery was changed.

- 2026-09-06: Connected current shared-notification authority to native inbox lists, total/unread/high/category counts, detail reads, mark-read responses and mark-all counts. Shared candidates are classified by metadata or an MSP receipt, so removing metadata cannot make a stored shared notification ordinary. Verified home browser sessions qualify the shared subset; API overrides do not acquire this access. SQL excludes hidden candidates before pagination/counting, and visible shared messages are rerendered from fresh authorized content using the existing language template. Shared presentation formatting is reused by persistence and reads, preserving qualified metadata and routes. Mark-read paths acquire update locks initially to avoid concurrent share-lock upgrades, and final session checks cover later query waits. Three full-schema action tests cover mixed ordinary/shared/malformed rows, pagination, all counts, current detail/read text, concurrent mark-read, mark-all isolation, revocation, redactions, API overrides and removed metadata. All 197 PostgreSQL tests and 20 native notification regressions pass, alongside server/Temporal typechecks and notification/domain builds. Native unit fixtures stub only the shared-access boundary; real SQL/session coverage lives in the PostgreSQL tests. Added declared notification dependencies and preserved unrelated lockfile changes. Shared candidate authorization currently runs per notification; group it by resource before production-scale inbox validation. Realtime/push/Teams access, subscriber integration, email delivery/recovery and remaining collaboration work are still pending. No running database migration or external delivery occurred.

Next implementation checkpoint: apply qualified authority to realtime/push/Teams delivery, connect in-app storage to subscribers, and batch shared inbox authorization by resource; then implement email delivery/recovery and content live updates; implement shared editing/deletion/disclosure, then the unified reader/composer and attachment audience enforcement. Production creation actions, response-state effects, qualified activity, and after-commit comment/workflow events are connected. MSP-private plain-text commands and the unified published reader are implemented, but content UI remains to be connected. Preserve requester/shared-IT/private distinctions in notification and email consumers before enabling new content controls. Consolidated working/oversight queues now connect native and shared ticket routes; integrate scoped queue export, bulk operations, routing assignments, richer ticket filters, and SLA indicators as their commands become available. The canonical mutation core, ticket-update/close notifications, and production shared-work editor/actions now support qualified attribution and retry-safe field edits. Assignment/comment/project notification consumers must be connected when their qualified commands are introduced. Authorize every affected bundle child before propagating shared edits. Consume the durable handoff history for independent provider SLA obligations and wire audience-aware collaboration. Customer-owned grants, policy administration, browser-session metadata/history reads, ticket handoff commands and UI, and explicit grant-revocation management are implemented; API/agent principals, shared content/attachments, project assignments, time/billing, provider SLAs, and exit/export/upgrade remain open. Continue lifecycle admission across remaining operational APIs/actions, intake, and workers for pending activation and license lapse. Provisioning cleanup has guarded acknowledgements but still needs the real cleanup worker and cancellation UI; explicit administrator invitation resend remains to be completed. Audit remaining special user restoration/deletion paths. Verify external price/issuer configuration, Citus execution, full builds, and live end-to-end behavior before release.

- 2026-09-06: Added receipt-backed delivery authorization to the actual Redis broadcaster, the CE-safe Teams delegator, and post-creation hooks used by push. Queue payloads identify the stored notification; delivery reloads current rows, recognizes shared items by either metadata or their independent receipt, and renders current authorized comment content rather than queued/cached text. Background delivery reuses the same recipient/resource/audience/field-redaction checks as storage and also rechecks current MSP routing assignment. Source, routing, receipt and notification locks remain held through awaited channel callbacks. Async post-creation hooks now return/await their promises, including the registered push dispatcher. Interactive inbox reads keep their tracked-session requirement and do not require a still-current routing assignment, provided current ticket authority remains valid. Missing/deleted notifications, damaged receipts or markers, changed audiences, wrong recipients, and revoked grants suppress delivery. Ordinary notifications reload their current stored content. Validation: 203 full-schema PostgreSQL tests and 26 targeted native notification/Teams/push regressions pass (229 total); tests exercise actual Redis and hook entry points, prove retained source locks with PostgreSQL NOWAIT, verify fresh qualified output, and cover assignment removal versus retained inbox read authority. Server and Temporal typechecks and co-managed/notifications package builds pass. Existing native outbox test hooks were updated to return their async work and wait for callback completion after authorization reads. The general outbox PostgreSQL suite was not run against the live stack; actual shared authority was tested only in the disposable database, with external transports mocked. Subscriber connection, durable delivery recovery, email fanout, qualified mobile/Teams navigation, and inbox authorization batching remain pending. No live database migration or external notification was performed.

- 2026-09-06: Connected qualified MSP comment notification storage to the production internal-notification event dispatcher. Customer-local assignee/contact delivery remains independent, so MSP fanout works even when the customer has neither recipient. The dispatcher preserves the inbound outbox's supplied transaction; a failed completion rolls back MSP receipts and rows together and is recorded for retry. Ordinary event failures propagate after independent recipients have been attempted, and completed per-recipient receipts prevent duplicate rows or creation effects when the event retries. Extracted one notification-created effect registrar used by native internal creation, authenticated creation, and the new shared fanout; workflow publication, realtime broadcast and push hooks attach to the owning commit, with the existing authenticated action's hook behavior preserved. Validation: 206 full-schema PostgreSQL tests plus 23 native notification action/after-commit regressions pass. New actual-dispatch tests prove concurrent ordinary replay, independent MSP delivery without local recipients, suppression, inbound completion failure/rollback/recovery, post-commit row visibility, and partial-recipient retry. Server and Temporal typechecks and the notifications package build pass. All 15 selected native subscriber tests also pass after correcting their stubs to return arrays for batched actor lookups (244 passing tests total); the general outbox PostgreSQL suite was excluded, with new real shared-outbox cases covered in the disposable database. These immediate transport effects remain best effort; crash-safe channel recovery, email fanout, qualified mobile/Teams navigation and inbox batching still require implementation. No live database migration or real external notification was performed.

- 2026-09-07: Added durable independent `in_app`, `teams`, and `push` delivery records in migration 19. Queue rows contain only MSP notification/recipient identity and attempt state, commit atomically with the notification and receipt, and backfill older created receipts without changing completed attempts on replay. Immediate after-commit delivery now processes these records; CE registers a global minute-by-minute recovery job and EE schedules the same bounded tenant fanout through Temporal. The global selector discovers active MSP tenants with due work, including MSPs created after server startup. Every attempt reacquires current recipient/resource/audience/routing authority and current notification preferences, renders fresh text, then takes the channel row lock through provider acceptance and acknowledgement. Concurrent workers skip locked/completed work; channels retry independently with capped exponential backoff and retain terminal failures after ten attempts. Database/authorization-loading outages remain retryable rather than being treated as grant revocation. Revoked, deleted, malformed or newly disabled deliveries skip without removing authorized inbox history. Redis and Teams expose transport-only adapters used under that guard; the server registers its push adapter for both immediate and maintenance execution. Push now reports provider acceptance, retryable errors, invalid devices, permanent oversized-message errors and incomplete responses, and includes the stable notification ID in payload metadata. External delivery is explicitly at-least-once: provider acceptance followed by loss before database acknowledgement can repeat that channel's notification ID, while completed channels are not resent. Validation: 213 full-schema PostgreSQL tests and 45 targeted notification, push and maintenance regressions pass (258 total); server and Temporal typechecks and database, notifications and jobs package builds pass. Real tests cover rollback, missed immediate effects, independent retries, current-content refresh, overlapping workers, retained resource locks, revocation, damaged receipt/metadata classification, failed acknowledgement after external acceptance, retry caps, preference changes, migration replay/backfill/checks, and recovery through the scheduled server handler. External transports were mocked and only disposable databases were migrated. Cached realtime/notification activity readers, qualified mobile/Teams navigation, email fanout and inbox batching still require work; full Citus, deployment and browser validation remain pending.

- 2026-09-07: Replaced notification Yjs payload rooms with authenticated, read-only `notification-signals` rooms that carry only a constant refresh hint. Browser sessions obtain a purpose-bound token capped at 60 seconds and their tracked session expiry; issuance locks and checks the active home user, tenant and session and rejects API overrides. Legacy unauthenticated notification rooms are rejected. The browser reads message text and counts only through the current authorized inbox action, continues periodic revalidation while connected, clears content on failed reads, ignores old-identity responses, and releases its owned socket timer on renewal/unmount. This backend authentication is independent of the UI-only release flag. Validation: 216 full-schema PostgreSQL tests plus 19 notification hook/room tests pass (235 total); server and Temporal typechecks pass. An isolated actual Hocuspocus websocket check confirmed rejected client document writes, constant-only Redis hints and legacy-room rejection; its teardown completed successfully after the owned socket cleanup fix. No live schema or outbound notification changed. Activity-feed readers, email fanout, qualified navigation, inbox batching and remaining collaboration features still require implementation.

- 2026-09-07: Applied the shared inbox authority boundary to notification activity feeds: the MSP aggregate and paged readers now qualify rows before SQL totals/offset/limit and render current source text under retained locks; client portal activities exclude shared receipts and malformed shared metadata. Caller-supplied activity targets and API overrides cannot borrow another browser session. Notification-inclusive activity responses bypass both server and browser presentation caches. The notification feed refreshes on every authorized inbox snapshot even if counts stay unchanged, clears failed reads, and rejects delayed responses from a previous identity or superseded request. The legacy server broadcaster now reexports the guarded package implementation. Validation: 218 full-schema PostgreSQL tests, six notification browser hook/component tests and four native activity regressions pass (228 total); server and Temporal typechecks and the notifications build pass. The user-activities package has no standalone build script; its changed source was checked through the server typecheck. Real database cases cover current content, qualified links/metadata, hidden-row pagination/counts, revocation, alternate recipients, API overrides, removed metadata and portal isolation. Browser cases cover cache bypass, unchanged-count refresh, failed reads and old-identity races. The prior isolated websocket smoke also completed with exit code zero after socket cleanup. No live schema or real outbound delivery changed. Activity detail drawer refresh/back-navigation, notification email and qualified mobile/Teams navigation, authorization batching, and the remaining collaboration features still need completion; full deployment, Citus and live browser acceptance remain outstanding.

- 2026-09-07: Added production browser commands for customer-owned comment text edits and deletion by the original qualified technician. Both customer-local and admitted MSP callers retain current read/update scope, tracked-session and lifecycle checks; other authors, foreign stores, stale versions and implicit audience fields are rejected. The exact microsecond timestamp exposed by the conversation reader serves as the optimistic version. The existing canonical command receipt records the committed timestamp, so concurrent exact retries return the same result and do not repeat activity or publication; conflicting reuse fails. Extended the native comment model with an internal qualified mutation adapter that preserves saved author/thread/audience fields and retains deletion tombstones and replies. Mutation, metadata-only activity and receipt share the owning transaction, with a final authority check after locks. Owner-qualified edit/delete events carry source identity and audience without old/new bodies; ordinary update producers retain their existing local identity requirement. The mention subscriber ignores these content-free invalidations, and the search indexer/live result verifier exclude deleted and unpublished comment bodies so delayed invalidation and reconciliation cannot restore deleted content. Validation: 222 full-schema PostgreSQL tests, 14 action/audience tests, 38 collaboration event-schema tests and five native comment regressions pass (279 total). Server and Temporal typechecks and co-managed/event-schema builds pass. Real cases cover both organizations, exact retries, stale edits, retained replies/attribution, private audience preservation, revocation, owner rollback, publication/receipt atomicity, deleted search results, and session expiry during a thread-lock wait. Backend actions remain independent of release-v1-6-feature. No live schema migration or real external publication occurred. Disclosure changes, rich text and the unified reader/composer/edit controls remain to be connected; full content live invalidation and durable mutation event publication recovery remain open, alongside attachments, projects, time, SLA and departure work.

- 2026-09-07: Connected a unified text conversation panel to the existing shared ticket page and customer co-management panel under their UI-only release boundary. The reader displays qualified authors, organization labels, explicit audiences, inherited replies and tombstones; qualified keyset pages refresh independently instead of accumulating previously authorized pages. Server-derived write-audience hints check current read/update content policy and lifecycle; commands still repeat authorization. New MSP-private messages/replies/edits/deletes use the home-private commands, while customer-private and requester/shared IT messages use canonical customer commands. Own-author controls compare both tenant and user identity; edits retain exact timestamps or private revisions. Delete uses an explicit confirmation step. Uncertain submissions freeze the draft and retry the identical operation, with no fresh operation on transport failure. Identity changes and failed access reads clear old content; periodic refresh removes an editor whose audience is no longer permitted, and StrictMode cleanup cannot strand an in-flight read. Text rendering does not resolve embedded foreign images, documents or mentions in the home tenant; formatted/attached messages are not offered destructive plain-text edits. This remains the text stage of the planned conversation UI: full rich-text rendering/editing, attachments, explicit disclosure commands, and event-driven live updates are still required. Validation: 223 full-schema PostgreSQL tests and 32 focused conversation/action/panel/text tests pass (255 total), along with server and Temporal typechecks and the co-managed build. New database coverage verifies content-specific write hints and retained reads during lapse. New keys are present across eight real locales plus both pseudo-locales. The whole-repository translation validator still reports 774 missing keys, all verified missing at the preceding commit; none belongs to the added conversation keys, and those earlier translation gaps remain part of final acceptance work. Live product browser/light-dark/pseudo-locale validation has not yet been performed. No running database migration or real outbound delivery occurred.

- 2026-09-07: Extended canonical and MSP-private conversation creation/editing with a shared, browser-safe rich document boundary. Commands accept either legacy text or validated BlockNote blocks, snapshot content before awaiting authority, derive Markdown from the admitted document, and retain the existing text request hashes and exact retry receipts. Rich blocks preserve styles, safe absolute links, nested lists, code, quotes and merged tables; validation bounds size/depth and rejects unsafe links, malformed spans, hidden properties, media and mentions until qualified resource resolvers exist. The unified composer and reader now use a restricted BlockNote schema, retain formatted own-author edits, preserve literal HTML/Markdown text, and freeze the editor for uncertain submissions. Unsupported attached messages retain the safe text fallback and cannot be flattened by editing. Editor labels use the supported locale dictionaries and both pseudo-locales; light/dark themes follow the app preference. The existing release boundary remains UI-only. Validation: the full 225-test PostgreSQL suite passed, with both new rich-text database cases passing again after the final domain changes; 48 focused document, composer, helper and action tests pass. Server and Temporal typechecks and the co-managed package build pass. Actual BlockNote component tests cover formatting, nested lists/tables, literal text, read-only transitions, light/dark attributes and French/pseudo-locale placeholder rules. No live product browser smoke, running database migration or external delivery occurred. Qualified attachments and mentions, explicit disclosure commands, event-driven conversation updates and the remaining plan scope still require implementation.

- 2026-09-07: Added durable comment-scoped attachment reservations and qualified upload/list/download commands for internal technicians in either organization. Canonical files belong to the customer; MSP-private files stay in the MSP store. The owning published comment and root determine audience, with current resource grants, read/update field policy, own-author upload checks and tracked-session/lifecycle locks retained through transport. Deleting a root preserves access to files on surviving replies; deleting/unpublishing the file's own comment hides it. A committed pending reservation binds the operation to a detached byte snapshot, content digest, name, MIME type, actor and qualified parent before upload; incomplete transport or lost acknowledgements remain unreadable and exact retries reuse the same path. Uploads require a root database connection so reservation evidence survives transport failures. Files use the existing provider in a separate namespace without a generic document/external-files entry, because generic file actions do not enforce comment audiences. Downloads verify size/digest and return opaque errors, private/no-store headers and non-executable attachment disposition. Browser actions and the qualified download route have no release-flag dependency. Registered the tenant-owned table, Citus distribution declaration and deletion ordering; migration replay preserves rows and rollback refuses retained objects. Validation: the full 230-test PostgreSQL suite passed, followed by all ten targeted attachment regressions including two added cases; eleven action/route tests passed. Server and Temporal typechecks and database/co-managed builds pass. Real database tests cover concurrent exact retries, immutable bytes, incomplete production-provider acknowledgements, private stores, forged references, redacted metadata, revoked grants, deleted/scheduled comments, root tombstones, session expiry during upload/download, lapse reads, renewal, corrupted paths/content and outer-transaction rejection. Storage transport was mocked; no live schema or external object changed. This completes the internal transfer backend checkpoint; attachment controls, staged new-message composition, inline images, portal-requester access, deletion/disclosure workflows, abandoned-upload cleanup and archive/export integration still need implementation, alongside the remaining plan work.

- 2026-09-07: Connected qualified attachment lists, downloads and own-message file uploads to both co-managed conversation surfaces under the existing UI-only release boundary. Server-derived upload hints check actual qualified authorship, current read/update field policy and lifecycle; each transfer still repeats authorization. The compact picker shows the effective server-action size limit, reserves multipart overhead, and keeps the same file/operation for uncertain retries. Periodic reads clear old filenames and drafts after access loss; identity generations and draft generations prevent delayed reads or upload outcomes from restoring cleared state. Read-only users retain permitted downloads. All controls have stable IDs, and the shared Input label now associates with its input for keyboard/screen-reader access. Awaited the storage service's asynchronous file-policy validator so disallowed files cannot pass an unawaited check. Added attachment copy in eight real locales and both pseudo-locales, with matching interpolation placeholders. Validation: all 233 full-schema PostgreSQL tests and 31 focused component, action, upload-limit and storage regressions pass; server and Temporal typechecks and storage/co-managed builds pass. Cases cover qualified links, flag-off UI, actual authorship and redactions, retained lapse reads, size rejection before submission, exact retry, read/identity races, revocation during upload, StrictMode effects and deferred validation rejection. No live browser/theme screenshot, running schema migration or real outbound file transfer occurred. New-message attachment composition, inline rendering, portal-requester access, deletion/disclosure, pending-object cleanup and archive/export wiring remain open with the rest of the plan.

- 2026-09-07: Added private conversation draft reservations with immutable message content, destination/audience and a bounded file manifest. Begin/upload/publish browser actions retain actual home sessions and existing content/attachment policy without a release-flag dependency. Draft transfers reuse the verified reservation/transport engine and remain linked to the draft through an owner-local FK. Attachment readers now require that link's draft to be published, so even an unrelated request creating the future comment ID cannot expose staged files. Final publication verifies every prepared file against the manifest, rechecks the current parent audience and scope, and commits the native customer comment or MSP-private note with draft publication in one transaction. Canonical response-state/activity/events remain on the production adapter; after-commit observers see the complete message and files, and concurrent publication/retries emit one message event. Published receipts are validated before replay. Tenant metadata, deletion ordering and Citus declarations include drafts; migration rollback refuses retained data. Validation: the full 238-test PostgreSQL suite passed, followed by the added audience-change/damaged-receipt case (239 distinct PostgreSQL cases); 18 action/route tests pass. Server and Temporal typechecks and database/co-managed builds pass. Tests cover incomplete uploads, detached content/bytes, manifest conflicts, other actors, private stores, inherited replies, message-ID collisions, caller rollback, lapse/revocation, migration replay, concurrent publication and after-commit attachment visibility. Storage and publication transports were mocked; no live schema or external delivery changed. The composer still needs to orchestrate these draft actions, and inline images, portal attachments, abandoned-draft cleanup, disclosure/deletion and archive/export integration remain open with the rest of the plan.

- 2026-09-07: Connected new-message and reply file selection to the private draft protocol under the existing UI-only release boundary. The composer captures rich content, qualified destination, immutable Files and stable operation/file IDs before asynchronous hashing; retries ask which files are already ready and publish only after every upload succeeds. Lost begin/upload/publication acknowledgements keep the same prepared message and freeze edits. Preparation failures remain editable, incomplete publication can retry or cancel, and canceled local selections cause no server writes. Server-derived attachment audience hints honor file/draft redactions and effective upload limits; count/size validation keeps invalid selections removable. Identity changes, periodic permission loss and direct forbidden/read-only responses clear selected filenames and stop subsequent transfer/publication steps. Canonical text replies and attachment drafts now accept an optional expected audience from the displayed parent, rejecting a stale pre-submission audience instead of silently widening disclosure; callers omitting the constraint retain their previous request hashes. Added accessible picker/removal controls and progress/error copy across eight real locales and both pseudo-locales. Validation: all 240 full-schema PostgreSQL tests and 41 focused composer/orchestration/action tests pass; server and Temporal typechecks and the co-managed build pass. Cases cover SHA-256 manifests, detached intent, partial retries, lost acknowledgements, both private stores, stale displayed audiences, exact constrained replay, size/count limits, unreadable files and permission loss during preparation/upload. New translation keys/placeholders match in all ten locales; the repository validator still reports the previously recorded 774 missing keys outside this addition. No live schema migration, product browser smoke or real outbound upload occurred. Inline images, portal attachments, abandoned-draft cleanup, disclosure/deletion and archive/export remain open alongside the rest of the plan.

- 2026-09-07: Connected requester attachment lists/downloads to native portal conversation cards through an application composition provider under the UI-only release boundary. The requester store comes exclusively from the verified browser session; API overrides and technician identities cannot use this entry point. Portal admission retains active user/contact, tracked session, actual portal RBAC, contact/client/board visibility, ticket and published root/comment audience locks through storage transport. It rechecks session deadlines after lock waits and after transfer. Only requester-facing, ready files are returned; staged drafts, deleted parent content, shared IT, organization-private and forged references remain inaccessible. Surviving replies retain files after a root tombstone. Revised the common attachment reader beneath technician/requester admission so both use the same publication, path and checksum checks and non-executable download headers. Customer-local requester reads do not depend on a live MSP relationship, preserving permitted files after lapse, termination and a product upgrade. The portal UI renders literal filenames, polls current access, clears failed/stale reads, and remounts on requester or tracked-session changes; native conversations without the provider retain their existing behavior. Validation: the full 245-test PostgreSQL suite passed, followed by seven requester regressions including an added lock-wait/session-expiry case (246 distinct PostgreSQL cases); 41 action/route/composition tests and 30 native conversation/portal-visibility regressions pass. Server and Temporal typechecks and the co-managed build pass. Concurrent tests verify retained permission, board, contact, session and audience locks during download. Provider transports were mocked; no live schema or external object changed. Live product/browser/theme acceptance remains outstanding. Requester upload composition, inline media, abandoned-draft/object cleanup, disclosure/deletion and archive/export wiring remain open alongside the remaining plan scope.

- 2026-09-07: Added durable cancellation and cleanup for unpublished conversation uploads. Active authors can discard their own draft after scope loss or license lapse without gaining access to live work; a publication that already won the owning lock is reported as published. Cancellation marks the draft and its files unusable before storage deletion, and retained operation hashes/file reservations prevent late begin/upload/publish retries from resurrecting them. Idle drafts and standalone pending files expire after seven days since their last admitted activity; successful retries refresh activity. A bounded maintenance sweep runs every 15 minutes in CE and EE, uses the existing storage provider, and includes suspended tenants while ordinary maintenance retains its suspension gate. Published files are excluded. Deletion failures and lost acknowledgements retain retry state with bounded backoff; corrupt paths cannot reach another object, and independent files continue. After every draft object is confirmed deleted, cleanup removes draft bodies and filenames while retaining minimal operation history. The composer retries an uncertain cancellation as cancellation, reports a publication that won the race, and preserves local content after expiration for an explicit new submission. Added migration checks/indexes and a direct jobs dependency on the co-managed domain. Validation: all 252 full-schema PostgreSQL tests passed, followed by seven cleanup regressions including the added draft-lock/session-expiry case (253 distinct PostgreSQL cases); 63 focused composer/action/fanout/suspension tests pass. Server and Temporal typechecks and co-managed/jobs builds pass. Real database cases cover current author isolation, both owning stores, idle versus fresh/published files, cancellation during upload, concurrent cleanup, acknowledgment loss, path corruption, lapse/departure, migration replay/rollback refusal and the production scheduled handler. New translation keys/placeholders match in all ten locales. Storage deletion was mocked; no live migration or external object deletion occurred. Live browser and deployment/Citus validation remain required; inline media, requester upload composition, explicit disclosure/published-attachment deletion, archive/export and the rest of the plan are still open.

- 2026-09-07: Added explicit removal of published conversation attachments under the existing UI-only release boundary. The original internal author confirms a qualified file; the backend independently rechecks current comment read/update scope, authorship, tracked session and operational lifecycle, including after attachment lock waits. Removal commits an actor-qualified marker before any provider deletion, immediately excluding the file from technician and requester lists/downloads while preserving the message and its audience. The immutable file identity provides safe concurrent/exact retries, including after object cleanup. Staged files cannot be removed through a colliding future comment ID. The existing bounded cleanup worker accepts published objects only with explicit author-removal proof, retains durable retries and scrubs the removed filename after deletion. Published draft receipts/manifests remain intact; removed file IDs are excluded from progress and cannot be re-uploaded or resurrected. The confirmation dialog freezes uncertain requests for an exact retry, validates receipts, and clears file details on current-access failure or identity/session changes. Removal remains available when upload transport limits disable new files. Validation: all 259 full-schema PostgreSQL tests and 60 focused action/attachment/portal/draft UI tests pass; server and Temporal typechecks and co-managed/jobs builds pass. Cases cover both private stores, requester access revocation, concurrent receipts, caller rollback, staged-file collision, lifecycle/grant loss, expired lock waits, migration replay/rollback refusal and post-cleanup retry. Six new keys and placeholders match in all ten locales. Storage transports were mocked; no live schema or external object deletion occurred. Explicit audience disclosure, inline media, requester upload composition, archive/export integration, live browser/deployment acceptance and the remaining plan scope are still open.

- 2026-09-07: Connected explicit whole-thread audience changes for customer-owned ticket conversations. The root author must retain current ticket read/update authority, unredacted conversation/author/file scope, a live tracked session and operational write eligibility. Preview returns qualified counts and a fingerprint of the thread, replies, tombstones, files and drafts. Confirmation binds that snapshot, destination audience and operation ID; new replies/files, changed content or pending standalone uploads require another review. The owning transaction changes root/reply visibility together, preserves content/authorship/file ownership, advances each comment version monotonically, and records metadata-only activity plus exact retry history. Requester/MSP file readers immediately obey the new inherited audience. Published draft files stay attached; unfinished replies prepared for the old audience cannot silently publish into the new one. After-commit, content-free audience mutation events invalidate each comment's search representation without replaying its body or manufacturing a new reply. The dialog explains whole-thread scope, requires an explicit audience selection, freezes uncertain requests for exact retry, and clears on access/session loss. Added eight keys across all ten locales and retained the UI-only release flag. Validation: all 264 full-schema PostgreSQL cases passed, followed by five disclosure regressions including monotonic future-timestamp coverage; 87 focused conversation/attachment/action/event-schema tests pass. Server and Temporal typechecks and co-managed/event-schema builds pass. Database tests cover customer/private-to-requester transitions, MSP-qualified attribution, inherited downloads, reverse restriction, stale confirmation, author isolation, caller rollback, pending uploads, draft publication, lapse/grant loss and session expiry after thread lock waits. No live schema, browser or external transport was changed. MSP-private thread disclosure still requires a durable transfer into customer-owned storage, and the reverse MSP-private transition needs corresponding ownership handling; these transitions are not represented as in-place relabeling. Inline media, requester upload composition, durable event delivery, archive/export, live browser/deployment acceptance and the remaining plan scope stay open.

- 2026-09-07: Connected MSP-private thread disclosure to the existing confirmation UI and authenticated action. Shared disclosure admission now retains the same actor, ticket, lifecycle and field-redaction checks for both storage paths. A source-owned transfer ledger binds the confirmed snapshot, destination audience, qualified source, immutable comment mapping and purpose-separated file paths before transport. Each completed copy commits independently; retries verify source checksums and reuse only its reserved path. Customer comment/file rows appear together in the final transaction, with original timestamp precision, parent topology and historical author snapshots preserved. Deleted source messages become empty customer tombstones, and unshared/deleted/staged files are not copied. Publication atomically marks the source thread as retained history, removes it from the live private timeline, prevents further source mutations, records metadata-only activity and queues per-comment invalidation after commit. No foreign user accounts are created; customer-owned file reads/removal no longer depend on the source ledger. Exact and concurrent retries return a receipt qualified by both source and destination. Destination collisions return a known conflict without overwriting existing content. The existing CE/EE cleanup job now discovers source owners of abandoned transfers, including suspended tenants, expires idle preparations after seven days and removes only reserved unpublished destination objects with durable backoff. Source objects and published customer files remain outside that sweep. Added tenant metadata, a colocated source-history FK, deletion ordering and migration rollback refusal; publication paths remain qualified soft references in customer file records. The dialog explains the ownership transfer and validates both ends of its receipt, with copy in all ten locales and the UI-only feature flag retained. Validation: the full 270-case PostgreSQL suite passed, followed by nine transfer cases including three added cleanup/redaction/collision cases (273 distinct cases); five canonical disclosure regressions also pass after the shared admission extraction. All 85 focused conversation/action/download/draft/fanout tests, server and Temporal typechecks, and co-managed/database/jobs builds pass. Tests cover invisible staging, partial/lost copy acknowledgements, concurrent retry, final transaction rollback, source changes, historical/inactive authors, empty tombstones, microsecond timestamps, checksums, session expiry before upload, lapse/redaction denial, source isolation, cleanup acknowledgment loss/path corruption, copied-file removal and migration replay. File transports were mocked; no live schema or external object transfer/deletion occurred. The reverse transition from shared customer history to MSP-private ownership remains open, along with inline media, requester composition, durable event delivery, archive/export/restore, live browser/deployment/Citus acceptance and the remaining plan scope.

- 2026-09-07: Persisted conversation event intent in the same customer-owned transaction as comment creation, edit/deletion, draft publication and thread-disclosure invalidations. The outbox retains stable event/workflow identities and metadata; created-comment bodies are loaded from current published source rows at dispatch, with audience and deletion checks held through transport. Restricted or deleted creation events are cancelled, while metadata-only historical invalidations remain deliverable. An explicit root-connection after-commit hook supports immediate dispatch without changing existing zero-argument hooks; committed intent also survives a missing hook or process loss. The existing minute-cadence CE/EE recovery job discovers both customer event owners and MSP notification owners, retries strict transport failures with bounded exponential backoff, and isolates concurrent claims and damaged rows. Added tenant metadata, deletion ordering, replayable migration and rollback refusal for retained history. Validation: 279 of 280 full-schema PostgreSQL cases passed on the first full run; the remaining legacy job-result assertion was updated for the added event counts, and all eight recovery/handler cases then passed (280 distinct cases verified). The 26 hook/fanout and 34 authenticated-action tests pass, as do server and Temporal typechecks and database/co-managed/jobs builds. Tests cover rollback, omitted hook flushing, fresh content, changed audiences/deletion, lost acknowledgement, stable workflow keys, concurrent claims, retained visibility locks, tenant separation, event identity conflicts, damaged-row isolation and migration replay. Event transports were mocked and all database writes used a disposable clone. Backend delivery remains independent of the UI-only release flag. Publication is at-least-once; durable consumer completion/recovery, attachment-change events, event-driven conversation refresh, email and the remaining plan scope are still open. Shared-history withdrawal into MSP-private ownership remains an unresolved design question; this checkpoint does not add withdrawal.

- 2026-09-07: Added durable consumer obligations for co-managed comment search indexing and in-app notification creation. Obligations are seeded with the original event transaction, and the migration backfills retained events; a lost first Redis message is therefore recoverable without a prior consumer attempt. Each consumer reloads retained intent and current published source, runs its database effects and completion mark in one transaction, and retains pending work after rollback. Event identity comes from the tenant-qualified outbox row even when a transport audience marker is absent. Search indexing accepts the owning transaction for upsert/deletion; in-app creation retains qualified MSP receipts, channel queues and post-commit effects, with concurrent replay producing one committed result. The existing CE/EE recovery job discovers unfinished consumer owners and replays only due obligations, including after successful initial publication; replay acceptance never substitutes for consumer completion. Pending publication is excluded before the recovery batch limit so it cannot starve published work. Current audience/deletion checks cancel obsolete message delivery, while completed siblings remain untouched. The event bus now supports explicitly targeted force replay, which bypasses channel fanout and workflow triggers and cannot be acknowledged by an unrelated subscriber. Processed event/handler markers include their channel so one channel cannot suppress another using the same stable event ID. Added tenant metadata, qualified FK/deletion ordering, backfill replay and rollback refusal for retained consumer history. Validation: all 286 full-schema PostgreSQL cases pass, including actual search/in-app consumers, completion-trigger rollback, missing first delivery, lost replay acknowledgements, concurrent consumption, caller rollback, tenant/event mismatch, corrupted transport body/marker, source restriction and targeted scheduled-job routing. All 65 focused event-bus/fanout/search/native-subscriber regressions pass, together with server/Temporal typechecks and database/domain/jobs/event-bus builds. Tests use disposable database state and mocked external transports; no live migration or notification was sent. The release flag remains UI-only. Durable email and other external consumer completion, workflow-consumer recovery, attachment-change events, event-driven conversation refresh and the broader remaining plan scope are still open.

- 2026-09-07: Prepared the email transport boundary for authorization-aware retries. TenantEmailService now accepts an explicit caller-owned retry policy: rate limiting still applies, but returns retryable metadata instead of caching the rendered message in DelayedEmailQueue. The policy is also preserved by the legacy static service entry point. Added sendEventEmailWithOutcome to distinguish actual acceptance, generic queue acceptance and intentional disabled-service skips, while the existing sendEventEmail keeps its void contract. Generic queued messages retain their existing reply-token persistence; rejected caller-owned attempts do not create reply tokens. Validation: all 47 focused real-service/notification-template/sender-settings regressions pass, along with the server typecheck and email package build. Tests mock providers and queues; no email was sent. This is the transport prerequisite for the co-managed recipient queue, not completed co-managed email delivery. Next: persist qualified recipient obligations, recheck current source/routing/grants/preferences for each attempt, render only authorized content, use caller-owned retries and stable delivery identities, and connect independent recovery. Customer-native email completion, qualified reply processing, workflow-consumer recovery and the broader remaining plan scope remain open.

- 2026-09-07: Connected durable MSP email notification discovery and recipient delivery. A dedicated `co-managed-email` subscriber/consumer persists MSP-owned, qualified source/recipient obligations without cached bodies, recipient addresses or provider credentials. Existing and newly committed comment events receive that consumer obligation; targeted replay does not resend customer-native mail. Each recipient attempt reloads current comment/thread audience, customer grant, MSP relationship role/RBAC, routing assignment, active user/address and email preferences. Those authority/source locks remain held through awaited transport. Independent recipients retain delivered/skipped/failed history and bounded retries; a lost provider acknowledgement reuses the same RFC Message-ID without claiming exactly-once SMTP. The shared CE/EE job discovers MSPs with pending email recipients and invokes a worker-safe transport using current tenant settings, recipient locale and caller-owned retry policy. Rendering escapes source text, includes only allowed author/ticket fields and links the customer-qualified ticket; it does not misidentify the source as an MSP-local ticket or mint a local ticket reply token. Labels cover the shipped languages and pseudo locales, with regional English fallback. Added the queue migration, consumer backfill/constraint extension, tenant metadata/deletion ordering, jobs email dependency and rollback refusal for retained history. Validation: all 294 full-schema PostgreSQL tests pass, followed by two focused queue/discovery checks after request snapshotting. All 60 focused email-rendering, native email/reply-token and fanout regressions pass; server/Temporal typechecks and database/domain/jobs builds pass. Tests cover duplicate/concurrent discovery and delivery, partial failures, fresh address/body, private/revoked/unassigned/inactive/opted-out suppression, held authority locks, lost acknowledgements, attempt exhaustion, actual subscriber/job recovery, qualified URLs, HTML escaping, supported locales and provider outcomes. No live schema or actual mail transport was used. The release flag remains UI-only. Qualified email reply processing, durable customer-native email completion, workflow-consumer recovery, inline email media, attachment-change events/live refresh, archive/export and the broader remaining plan scope are still open.

- 2026-09-07: Added customer-local technician notification admission as the next prerequisite for durable customer email. The boundary uses the recipient's active internal identity, current ticket RBAC and bundle scope/redactions, with tenant/user/role/ticket/source locks retained through the awaited adapter. It accepts customer-owned requester, shared-IT and organization-private content without consulting MSP grants, sponsor state or a borrowed interactive session; it never reads MSP-private storage. Retained reads continue after capacity lapse, relationship termination and PSA upgrade, while customer suspension, removed roles, inactive/portal identities and foreign-tenant recipients are rejected. Extracted the existing current comment-content reader so customer and MSP adapters share publication, root-audience, author identity, self-suppression and field-redaction checks; the MSP adapter retains its original active-trust admission and two-audience restriction. Validation: all 304 full-schema PostgreSQL tests pass, including 10 new customer cases, plus all 16 email-rendering regressions. Server and Temporal typechecks and the co-managed package build pass. Tests use disposable database state and mocked delivery; no live migration or mail was sent. This checkpoint adds the tested authority boundary, not completed customer email delivery. Customer requester/contact authority, recipient routing/discovery and durable send completion, qualified reply handling, workflow recovery and the broader remaining plan scope remain open. The release flag remains UI-only.

- 2026-09-07: Connected durable customer-technician email discovery, delivery and scheduled recovery. Customer-owned recipient obligations retain event/comment/thread/user identities and independent bounded completion; current direct assignments, additional ticket resources, team membership, local RBAC/bundle policy, active address, tenant/user preferences, publication/audience and close-paired suppression are rechecked before transport. Source and authority locks remain held through the awaited send, with fresh rendering and stable RFC Message-IDs across acknowledgement loss. Delivery survives sponsorship termination and PSA upgrade because customer authority remains local. A distinct `customer-internal-email` consumer records discovery atomically and targeted replay does not resend native requester mail. The native subscriber delegates technician completion to this consumer while preserving its requester email/reply-token path. Connection-aware after-commit delivery respects outer transaction rollback. Customer email uses localized native-ticket navigation and the same escaped, caller-owned transport engine as MSP email; it does not create an unqualified reply token. Added the owner-local queue migration, metadata/deletion ordering, recovery fanout and consumer catalog extension. Rollout marks previously published events as remaining with the legacy native sender, including events still waiting for first consumption; only unpublished historical work enters the new durable consumer. Earlier migration replay preserves the expanded catalog. Validation: all 316 full-schema PostgreSQL tests pass, followed by three focused production/rollout/transaction cases covering the final after-commit change and one additional rollback test (317 distinct database cases). All 80 focused native mail, requester-token preservation, rendering/locales, watcher-helper and fanout regressions pass; server/Temporal typechecks and database/domain/jobs builds pass. No live migration or real mail transport ran. This completes the customer-technician queue for co-managed conversation outbox events, not every customer email path: requester/contact and watcher durability, native events outside that outbox, qualified email reply handling, inline media, workflow recovery and the wider remaining plan scope still require work. The release flag remains UI-only.

- 2026-09-07: Added requester-email authority for durable discovery and later delivery. The selected recipient is a customer-qualified contact/client identity or an explicitly selected default client location, never a synthesized technician or portal session. Every read reloads the current ticket contact, active client/contact, contact board visibility, current address/default location, published root audience and agent authorship. Contact/client/location replacement cancels an old obligation instead of redirecting it, even if the new recipient reuses an email address; address updates on the same identity remain valid. Fallback mail cannot bypass inactive/mismatched contacts, malformed nonempty contact addresses or contact board restrictions. Only requester-facing agent content is admitted; self-notifications, close-paired messages, drafts and deleted comments are suppressed. Owner/source/contact/visibility/location locks remain held through the callback, and customer ownership survives departure and PSA upgrade. Moved the two existing contact-visibility policy modules unchanged into shared runtime entry points, retaining ticket-package facades and the browser-safe filter export so portal reads and background workers reuse one policy. The current comment-content reader now accepts an explicit absent interactive actor after separate requester admission; technician/MSP self-suppression remains unchanged. Validation: all 328 full-schema PostgreSQL cases pass, including 11 new requester cases; all 11 native visibility/form/onboarding regressions pass after repairing their preexisting query mocks for the established lock-aware API. Server/Temporal typechecks, shared/domain builds and a plain Node runtime import of both visibility entry points pass. No running schema or external email changed. This is the requester authority prerequisite: durable requester/watcher recipient storage and completion, preferences/transport integration, qualified reply processing and the wider remaining plan scope remain open. The UI-only release flag boundary is unchanged.

- 2026-09-07: Added the customer-owned requester reply-token store and transactional reply admission. Opaque `cm1:` tokens bind the original delivery key, normalized sent address, requester contact/location/client and canonical ticket/thread/comment. Exact delivery/address retries reuse a token; address changes receive a distinct token and cannot redirect old mail. Issuance reuses current requester authority and refuses conflicting identity reuse. Reply admission requires trusted receiving-MTA sender authentication, the original sent address, current requester ownership/public agent source and current operational write eligibility. It holds ticket/thread, source, recipient and token locks through the callback; all callback writes share the transaction. Expiry and lifecycle checks repeat after lock waits and after the writer returns, rolling back expired work. Revoked, expired, private, reassigned, foreign-owner, malformed and unauthenticated tokens are rejected without invoking the writer. Optional expiry retains existing indefinite-token behavior unless explicitly set; current visibility/lifecycle still applies on every use. Added tenant metadata/deletion ordering, retained-history rollback refusal and an explicit shared runtime export for the existing sender-authentication policy. Validation: all 345 full-schema PostgreSQL tests pass, including 17 new token cases, together with 19 parser/sender-authentication regressions, server/Temporal typechecks, database/shared/domain builds and a plain Node runtime import check. No live migration, outgoing mail or inbound product flow was changed. This checkpoint is the reply authorization core, not completed reply processing: durable requester send completion and committed token issuance, inbound adapter/quarantine integration, watcher and technician/MSP reply handling, canonical reply events/artifacts and the broader remaining plan still require implementation. Recognized co-managed token rejection must not fall through to native token/header/subject matching when that adapter is connected. The release flag remains UI-only.

- 2026-09-07: Added the customer-owned durable requester-email queue and registered its metadata/deletion ordering. Discovery fixes the initial contact or default-location identity per event; replay cannot redirect that event to a replacement requester. The queue persists routing/source identities and completion metadata without rendered content or addresses. Processing commits address-bound reply-token issuance before SMTP can expose the token, then reacquires current requester, public-source and tenant notification settings under locks through transport. Address changes between phases defer for fresh preparation; changed visibility, disabled settings and revoked/expired tokens suppress delivery. Retries reload current content/address, preserve a stable Message-ID, reuse tokens for the same delivery/address, back off and stop after ten failed attempts. Source/recipient ownership survives sponsor departure and a PSA upgrade. The shared preference reader retains internal users' separate opt-outs. All 363 full-schema PostgreSQL tests pass, including 18 new cases for concurrent discovery/workers, committed-token visibility from another connection, retries, recipient replacement, default-location delivery, tenant/subtype/category settings, source changes, retained locks/history, transaction rollback, migration replay/refusal, and actual authority changes between preparation and send. Database/domain builds and server/Temporal typechecks pass. Tests use a disposable database and mocked transport; no live migration or mail was sent. This is a domain delivery checkpoint: production requester consumers, transport rendering/markers and qualified incoming-reply routing remain unconnected, so native requester sending stays in place. Watchers, bundle-child requester authority and the other open acceptance work remain outstanding.

- 2026-09-07: Connected qualified requester reply admission to both production durable-inbox worker entry points through a typed composition callback, without creating a shared-to-co-managed dependency cycle or consulting the UI release flag. Recognized `cm1:` tokens, including malformed/case-variant and HTML markers, never fall through to native token/header/default routing. Valid replies use the canonical thread and current contact/default-location identity, with no internal-user resolution or CC watch-list expansion. Comment writes, response state, reopen activity, events, effects and inbox completion use the owning transaction. A savepoint rolls back writes before an authorization/expiry rejection records quarantine; the staged source is retained with a specific reason and no reply effects/artifacts. Database failures retry; lifecycle pauses defer and refund a new claim, including errors crossing independently compiled package constructors. Normal reopen, automated-message suppression, the Redis backstop and cutoff-to-new-ticket policy remain in place. Cutoff creation fixes client/contact identity and checks current destination-board visibility; qualified operations use durable inbox/effect identity rather than substituting a flat legacy Message-ID match. HTML-only replies are no longer rejected merely for lacking a text part, while marker/markup/non-breaking-space-only mail creates no reply. Added the worker runtime subpath, dependency and Docker build manifests. All 387 full-schema PostgreSQL tests pass, including 24 new actual-intake cases; 64 native parser/intake/reopen/automated-mail regressions pass. Server and Temporal typechecks, authorization/licensing/domain/shared/email-service builds and plain-Node imports pass; the runtime check also proves lifecycle deferral recognizes an error from the separately compiled package. Tests use disposable PostgreSQL and mocked MIME transport/Redis policy, with real canonical writes/outbox/effects; no live mailbox, migration or Docker image was exercised. Production outgoing requester rendering/markers, durable requester consumer discovery and scheduled delivery remain next, so native requester sending remains active. Watchers, bundle-child authority, other native email/threading paths, external-consumer completion and the other open plan acceptance work remain outstanding.


- 2026-09-07: Activated durable primary-requester email for co-managed conversation outbox events. A dedicated `requester-email` consumer records recipient discovery and completion atomically, then starts token preparation and delivery only after the owning transaction commits. The shared CE/EE recovery job and tenant fanout recover pending requester work independently of Redis receipt and completed sibling consumers. The native subscriber delegates primary requester completion while preserving the existing co-managed watcher/bundle-child authority exclusions and ordinary native event behavior. Migration replay preserves the expanded five-consumer catalog; already-published rollout history remains owned by the native sender, so rollout does not resend historical primary mail. The worker renders current authorized content in the recipient's customer locale, uses configured ticketing sender/originating active intake mailbox and active customer portal domain or tenant-qualified fallback link, and uses caller-owned retries with the stable Message-ID and previously committed `cm1:` reply token. It neither impersonates a portal user nor creates a native unqualified reply token. Reply boundaries precede the quoted notification; the shared HTML parser now trims the whole boundary opening tag rather than retaining a malformed tag fragment. Validation: all 391 full-schema PostgreSQL cases and 112 focused rendering/routing/recovery/native mail/parser cases pass. The new integration cases exercise concurrent production discovery, actual scheduled retry, a real canonical incoming reply using the committed outgoing token, outer rollback, rollout ownership/replay and native fanout exclusion. Server/Temporal typechecks, shared/domain/jobs builds and the tenant-slug plain-Node runtime import pass. Transports are mocked and the database is disposable; no live migration or email ran. The release flag remains UI-only. This activates primary-requester delivery for retained co-managed conversation intent, not all email paths: native events outside the outbox, watchers and bundle-child recipients, customer-technician/MSP replies, inline media, external consumer completion and the wider project/time/SLA/export/departure and acceptance scope remain outstanding.


- 2026-09-07: Added customer-technician reply-token persistence and transactional reply authorization as the next email integration prerequisite. Customer-owned opaque `cm2:` tokens bind the active internal user, original sent address, delivery and canonical ticket/thread/comment/audience. Retries reuse the same delivery/address token; address changes cannot redirect old mail, and conflicting source reuse fails. Reply admission requires receiving-MTA sender authentication, current internal identity, current local read and update RBAC/bundle policy, visible published source, unchanged original thread audience and operational write eligibility. A later disclosure cannot silently turn an answer to an IT-only email into a public requester reply. Admission does not borrow an interactive session or require MSP resource grants; retained customer tokens remain usable after an independent PSA upgrade, while license lapse/departure block writes until recovery. Source/user/token locks remain held through the writer, and database-time expiry and lifecycle checks run after lock waits and again after writes. Explicit savepoint handling rolls back rejected writer mutations even when the owning inbox catches the rejection. Added a database savepoint helper that promotes successful child after-commit hooks to the owning transaction and discards rolled-back hooks without early dispatch. Registered the token table in tenant metadata/deletion ordering with Citus distribution and retained-history rollback refusal; portable export must exclude these tokens. Validation: all 422 full-schema PostgreSQL cases pass, including 31 new technician cases for actual canonical comments in all three audiences, current authority/address/source changes, read versus update bundle narrowing/redaction, transaction rollback, license lapse/departure/upgrade, real lock-wait and in-writer expiry, migration replay, token identity conflict, and root/outer after-commit behavior. All nine existing database hook regressions, server/Temporal typechecks, database/domain builds and the plain-Node savepoint export check pass. No live migration, mailbox or transport ran. This is the customer-technician reply authority core; durable incoming mail routing/quarantine, closed-ticket/cutoff behavior, two-phase outgoing token preparation and transport markers remain to be connected before production technician tokens are issued. Requester delivery remains active, and the other open watcher/MSP reply/project/time/SLA/export/departure and acceptance requirements are unchanged. No backend release-flag checks were introduced.


- 2026-09-07: Connected customer-technician reply admission to both durable mail worker roots through a shared typed qualified-reply callback. Recognized `cm1:` and `cm2:` markers, including malformed/case-variant and HTML forms, are reserved before legacy dedupe/token/header matching. Requester behavior keeps its separate identity and quarantine reason. Technician replies use the exact current internal user in canonical comments, activity/events and follow-up creation; they cannot add CC watchers or borrow a contact identity. Tightened technician authentication to the existing internal-sender policy: aligned DMARC or both aligned SPF and DKIM, rather than the weaker contact-sender rule. Current read/update permissions, source audience and lifecycle remain enforced by the committed token boundary. Actual comment/reopen/outbox/effect writes share the inbox transaction, with rejected late writes rolled back before quarantine retains the staged source. Internal notes leave requester response state unchanged. Closed-ticket cutoff retains the fixed customer/client, checks create/read/update permissions and destination bundle scope, preserves the source thread audience in a new root, and uses the admitted technician as creator. IT-only follow-ups use the authorized original public ticket title and keep the private email subject/body out of public ticket description and ticket-created events. Extended the existing comment model/helper with explicit audience validation; mismatched visibility or a different parent audience rejects without persisting content. Validation: the full 452-case PostgreSQL suite passes, followed by eight final authority/artifact cases including an additional explicit-conflict test (453 registered cases). All 64 native parser/routing/reopen/automated-mail regressions pass. Server/Temporal typechecks and shared/domain/email-service builds pass; plain Node loads the compiled combined adapter and confirms invalid requester/technician tokens reject without writer invocation. The final artifact test deletes editable comment metadata and proves immutable staged MIME still selects protected handling. No live migration, mailbox, transport or Docker image ran. Artifact handling remains deliberately unfinished: the native artifact worker forces portal-visible folders, so technician-token MIME retains its original source and pending manifest with fenced, attempt-preserving deferral until the conversation attachment adapter is implemented. Outgoing customer-technician token preparation/markers have not been activated. These are the next requirements, followed by native-event convergence, MSP replies, watchers/bundle-child authority and the wider remaining plan/acceptance work. The release flag remains UI-only.


- 2026-09-07: Added durable customer-technician reply receipts as the prerequisite for conversation-safe email artifacts. Accepted canonical comments, their inbox effects and a receipt commit together; rejected late authorization or mismatched MIME identity rolls everything back. The receipt records the owner-qualified author, admitted audience, actual destination ticket/thread/comment, original reply source, retained relationship identity, source SHA-256 and a token fingerprint. It contains no raw token, address or message body. Cutoff-created tickets record their actual destination separately from the original source. Replay preserves one receipt, and editable comment metadata or subsequent token deletion cannot erase accepted-mail provenance. The migration supports tenant distribution and refuses destructive rollback over retained receipts; metadata and tenant cleanup are registered. Validation: all 455 disposable PostgreSQL integration tests, 64 native email unit tests, server/Temporal type checks and domain/database builds. Actual artifact materialization and outgoing technician reply-token activation remain open; the current worker continues to retain protected artifacts without native portal-visible processing.


- 2026-09-07: Connected protected customer-technician MIME artifacts to both durable worker roots. The worker selects exactly one manifest attachment or embedded image from the digest-verified retained MIME, validates encoded bytes and metadata, and supplies an explicit worker adapter instead of invoking native folder/document processing. The adapter admits the committed reply receipt and inbox/comment effect, current owner identity, read/update RBAC and bundle redactions, current published source/author, unchanged original audience, lifecycle and exact live lease. Locks remain held through transport. The shared conversation transfer engine now accepts retained write authority independently of sessions while its interactive wrappers preserve existing session checks. Immutable pending reservations, checksum/identity conflicts and stable object paths remain unchanged; ready publication and fenced, unexpired artifact completion commit together. Lost object acknowledgements retry at the same path; failed completion or expiry leaves uploaded bytes unpublished. Original MIME is retained at its staged private object path as customer audit evidence, with no generic file/document or conversation attachment, because quoted history and reply credentials must not inherit public comment visibility. Owner authority can resume after an independent PSA upgrade without reusing a deleted token. Missing/currently denied authority defers with retained source and no new attempt charge. Added the nullable conversation-artifact link migration and a compiled worker entry; lifecycle capability catches now recognize errors across compiled package boundaries. Validation: all 472 disposable PostgreSQL tests (17 new artifact cases), 64 native email unit tests, server/Temporal type checks, shared/domain/email-service builds and a plain Node runtime import. No live mail or object storage was used. Outgoing technician token activation, attachment change notifications/inline rendering, audit export/viewing and full plan acceptance remain open.


- 2026-09-07: Activated reply tokens in outgoing customer-technician comment email. The durable customer queue now commits address/source/audience-bound `cm2:` issuance before transport, then reacquires current recipient assignment, user identity, read authority, published source/audience, preferences, queue row and live token locks for sending. Changes between phases skip delivery or retry a changed address with fresh preparation. Retry and lost delivery commits retain the original Message-ID and same-address token; a new email address receives a distinct token. The renderer now discriminates requester identity from token presence, preserving technician locale/user preferences and native ticket navigation. Technician mail uses the existing configured/originating intake mailbox, renders both text and HTML reply boundaries and rejects invalid tokens before routing or SMTP. Requester portal routing remains intact. Actual queue-produced tokens round-trip through the durable inbox in all three audiences with qualified technician authorship. Validation: 484 tests in the full disposable PostgreSQL run plus two focused final commit-loss/token-lock checks (485 registered total), 50 transport/routing unit tests, server/Temporal type checks and domain/jobs builds. No live SMTP was used. Native email/outbox convergence, watcher/bundle-child routing and qualified MSP replies remain open, along with the rest of the plan.


- 2026-09-07: Converged co-managed inbound comment publication onto the conversation outbox. Both mail worker roots supply a compiled retention adapter through the native publisher/core composition. The adapter derives the canonical owner/thread/audience in the writer transaction and retains metadata-only intent plus consumer obligations; the native inbox outbox no longer independently publishes the same comment. Missing composition fails the co-managed write for retry, while ordinary PSA comments keep native outbox behavior. After commit, publication reconstructs current content and uses one retained event ID; a lost acknowledgement remains recoverable without recreating the inbox/comment. Deletion or changed audience cancels undelivered content and its pending consumers. Initial comments retain an explicit internal-notification-only channel. Excluded email/search consumers are cancelled at creation and checked again during consumption/recovery, so stale backfill cannot revive an initial comment email. Existing scheduled recovery publication now preserves that channel. Validation: all 494 disposable PostgreSQL tests, including nine new publication/recovery/channel cases and late reply rollback checks; 64 native email unit tests; server/Temporal type checks; domain/shared/jobs/email-service builds; and a plain Node import of the compiled adapter. No live Redis/mail was used. Native UI/API/scheduled comment writers, non-comment workflow delivery, watcher/bundle-child mail and qualified MSP replies remain open; this checkpoint completes only the inbox-comment part of event convergence.

- 2026-09-07: Connected native comment creation in the generic UI action, optimized ticket detail action, simple ticket action, and ticket API to the same canonical conversation event outbox as inbound mail. Generic UI response-state and message/note workflow events now retain intent in the comment transaction; native workflow intent carries the source audience. Generic and API edits retain content-free invalidations, and generic deletion captures canonical thread metadata before deleting the source, so recovery remains possible afterward. Event construction/retention failures roll back the write rather than escaping through legacy best-effort catches. Ordinary PSA transport remains best effort after commit. The simple action now also applies the operational lifecycle gate before its direct inserts. No backend release-flag checks were introduced. Validation: 514 tests in the full disposable PostgreSQL suite, including 20 new native creation/edit/delete/rollback cases; 26 final focused PostgreSQL checks also verified workflow audience labels, real event schemas and inbound compatibility; 16 native UI unit/contract tests; server/Temporal type checks, domain build and plain-Node imports of both retention entry points. No live transport or browser smoke was used. T09/T16 remain partial: scheduled publication, model/workflow-created comments, watcher and bundle recipients, workflow consumer authority/completion and live-update delivery still need convergence. The remaining project/time/lifecycle/export and acceptance work is unchanged.

- 2026-09-07: Connected scheduled co-managed comment publication to retained current author/RBAC/bundle authority and the durable conversation event outbox. Publication locks the current internal author, roles, ticket, thread and source; withheld messages remain unavailable after user deactivation, permission loss, body redaction, audience restriction, deletion or operational license lapse. Response-state permission is required when publication actually changes that field. The visibility transition, response state, activity and comment/message/response event intent commit together; duplicate workers reuse the committed identities. Existing published schedules transfer their pending dispatch markers into the outbox without requiring a new operational write grant. Their original requester audience remains fixed, so current-source dispatch cancels deleted/restricted content instead of retargeting it. Migration `20260907135115_add_scheduled_comment_recovery.cjs` retains per-source retry time; maintenance discovers schedule-only tenants and retries deferred sources after recovery without starving later due comments. Native reschedule/cancel clears stale retry metadata. Boot reconciliation isolates failed sources. Ordinary PSA schedules preserve native stable-ID retries. Shared response settings and after-commit event transport now have worker-accessible composition paths. No API/backend release flag was added. Validation: all 533 disposable PostgreSQL tests, including 19 new schedule authority/recovery/compatibility cases; 22 maintenance-fanout and 16 native UI unit/contract tests; server/Temporal type checks; domain/shared/jobs builds and plain-Node imports of compiled authority/settings entry points. No migration was applied to a live tenant database, and no live transport, Citus cluster or browser smoke was used. T09/T16/T17 remain partial overall: model/workflow-created comments, watcher/bundle recipients, workflow consumer authority/completion, native actor/command admission and live-update delivery still need work; deferred-publication UI/browser smoke and the remaining project/time/lifecycle/export acceptance work remain open.
- 2026-09-07: Connected native ticket-model and workflow comment producers to transaction-owned conversation retention. Workflow business actions now own after-commit delivery; their actor lookup joins the exact executing definition version. A shared runtime registry injects the co-managed provider from server and workflow-worker startup without a shared-to-domain dependency cycle or release-flag backend gate. The provider locks the current run/version, active actor, role/bundle authority, ticket and source; rejects cancelled/expired runs, mismatched internal authors, inactive/unrelated contact authors, lifecycle denial and hidden conversation/response fields; and retains a body-free event that reloads current content on retry. Email workflow ticket-plus-initial-comment writes now share one transaction, preserve contact authorship and the initial in-app-only channel. A typed publisher contract makes retention failures abort model writes. Ordinary native model publication preserves its workflow event shape. Validation: 554 disposable PostgreSQL integration tests passed (21 new); 27 focused shared email/publisher tests, three native adapter tests and two worker startup tests passed; types, shared, co-managed and workflow-worker builds, server and Temporal typechecks passed. Worker build includes runtime import validation; Docker images, Citus and browser flows were not exercised. This checkpoint covers comment producers and their current runtime actor admission; general workflow mutation/attachment authority, consumer completion/causal behavior, webhook/Teams/import/auto-close event contracts, broader native command admission and the remaining plan acceptance criteria stay open. No live migration or transport was performed.
- 2026-09-07: Added current browser-session admission for creating, rescheduling and cancelling customer-owned scheduled comments, including active local identity, current RBAC/bundle scope, conversation/schedule field policy, lifecycle and final session-expiry checks. Commands lock canonical ticket/thread/source rows; creating binds the stored author to the session, while authorized technicians can cancel departed authors' schedules. Rescheduling a reply requires a published, undeleted root; cancellation remains available for withheld replies beneath unavailable roots. Queue cancellation/arming now happens only after the schedule mutation and activity commit. Initial and changed schedules share an after-commit helper that checks the current scheduled instant, attaches a returned job conditionally and cancels stale returned jobs; independent queue hooks let arming proceed if old-job cancellation fails. The existing maintenance worker recovers due co-managed sources after queue failures. Ordinary PSA queue operations also use this transaction boundary. General native comment/API command admission, live update audience delivery, workflow consumers and the other remaining acceptance work are still open. No UI release-flag condition was added to backend commands and no live database migration or queue transport was performed. Validation: 574 disposable PostgreSQL tests passed (20 new schedule-command cases), including real queue-response races, failed activity insertion, parent-thread validity, current bundle scope and expired/revoked sessions. Server and Temporal typechecks, domain build and compiled-entry import check passed; the native comment contract test also passed. Docker, Citus and browser validation remain outstanding.

- 2026-09-07: Added customer/MSP access to canonical project task lists and working-field edits (name, effective phase status and due date). Qualified routes and project-policy links are protected by the UI-only release flag. Each read/write retains current home identity, project grant, role/bundle scope and lifecycle authority; project-before-task locks serialize edits, field masks shape returned values and paginated choices, and operation receipts make retries safe while stale baselines conflict. The native task model writes the customer record without copied users, tasks or assignments. Audit records retain qualified authorship plus immutable display/organization snapshots. Migration `20260907150600_preserve_explicit_audit_tenant.cjs` preserves explicit audit ownership and keeps legacy connection-context fallback; replay and guarded rollback are covered. Added task-specific copy in all nine locale files. Validation: all 589 disposable PostgreSQL tests passed (15 new), followed by three focused attribution/ownership tests after the final snapshot adjustment; 22 UI and 43 action/product tests passed; server and Temporal typechecks and the domain build passed. Root package plain-Node import remains unsupported because its existing exports resolve source dependencies; these UI/server consumers use the application compiler. No live migration, Citus, Docker or browser/theme smoke was performed. T12 remains partial: qualified assignments, shared task conversation/attachments/history, consolidated task queues and operational effort still need implementation. Remaining lifecycle, time, consumer and other acceptance work is unchanged.

- 2026-09-07: Added the canonical task change-history reader and UI. Only co-management working-field audit entries for the current qualified task and relationship enter this history; legacy/private or unrelated audit records are excluded. Current project authority and field masks apply on every page, including derived author/time values. Pagination compares timestamps and IDs in PostgreSQL to preserve microsecond precision, skips hidden-only changes and exposes no total count. Read-only license state retains authorized history access; revoked grants and expired sessions deny reads. Audit writes now snapshot the selected status name alongside the existing immutable author/organization names. The editor reloads history after successful saves, clears task/history on a failed history read, and ignores late results from previously viewed customers. Copy is present in all nine locales, and the existing UI boundary prevents history requests when the release flag is unavailable; no backend flag gate or new migration was added. T12 remains partial: task comments/attachments, qualified assignments, consolidated task queues and operational effort are still open. Native task changes outside the co-managed editor are not yet included in this history. Validation: all 595 disposable PostgreSQL tests passed (six new history cases), 25 UI tests passed, and the server typecheck and domain build passed. No live database migration, transport, Docker, Citus or browser/theme smoke was performed.

- 2026-09-07: Added explicit MSP task assignments alongside native customer assignments. Migration `20260907154500_create_co_managed_task_references.cjs` stores MSP-owned qualified participation references, current person/team assignment, revision and permitted display snapshots; it creates no shadow tasks/users and preserves withdrawn participation. Tenant metadata and deletion ordering are registered, with no cross-tenant source/user foreign keys. Commands require current editor read/write authority, active project collaboration scope for assignment, eligible MSP staff/team membership and current candidate home policy. Candidate checks evaluate the proposed assignment, enabling assigned-work-only policies without lending a session or enlarging the customer grant. Shared task admission now resolves the active MSP reference for local assignment policy. Concurrent changes use expected revisions and atomic operation receipts; assignment/reference/history/actor writes roll back together. Customer withdrawal remains possible after project unsharing, and MSP access still closes immediately. The editor provides person/team choices and assign/remove controls, exact retries after unknown outcomes, conflict reloads and history refresh without discarding unsaved task fields. Field masks hide assignment values, derived history and directory choices. Copy is present in all nine locales and the existing release flag remains UI-only. A common task audit writer preserves qualified immutable attribution for both field and assignment changes; active non-session identity evaluation now has an explicit helper. T12 remains partial: consolidated task working queues/navigation, customer native-assignment integration in the shared editor, shared comments/attachments and operational effort remain open. Explicit assignment currently retains the working reference; consolidated queue consumption follows in the next checkpoint. Full export/archive/deletion lifecycle validation remains outstanding. Validation: all 612 disposable PostgreSQL tests passed (17 new assignment cases), 28 UI tests passed, server and Temporal typechecks passed, and domain/database package builds passed. No migration was applied to a live tenant database, and no Citus, Docker or browser/theme smoke was performed.

- 2026-09-07: Added consolidated project task queues at `/msp/co-management/tasks`, linked from the projects workspace and MSP co-management overview. The MSP working view combines native tasks and customer tasks with explicit active MSP assignment references; oversight includes currently granted customer projects without creating assignments. Customers receive their own canonical task queue with qualified links to the shared editor. Native MSP tasks open through the existing project/task route. One authorized SQL relation drives workspace/search/status/assignment filters, sorting, counts and pagination. Current home RBAC/bundle rules, staff/team membership, lifecycle and project grants apply before results; reference locks retain assignment policy, source/derived field redactions prevent hidden search/count/filter signals, and final session-expiry checks cover lock waits. Personal filters include native additional task resources; team filters use current membership. Effective phase status overrides replace project fallback. A shared policy compiler adapter now serves both ticket and task queues without changing ticket projections. Added queue copy across all nine locales and retained UI-only release gating. A broader UI run also found and fixed duplicate React keys between task assignment/history panels, with a regression assertion. Two fully provisioned customer tenants with identical task IDs/names now verify combined pagination and independent unsharing. T11/T12 remain partial overall: bulk/export/dashboard integration, live updates, native assignment editing within the shared screen, shared task conversations/attachments and effort still need work. Validation: all 620 disposable PostgreSQL tests passed (eight new task queue cases), 55 UI/product tests passed, and the server typecheck and domain build passed. No live migration, Citus, Docker or browser/theme smoke was performed.

- 2026-09-07: Added the task-conversation domain foundation. Migration `20260907163000_add_project_task_collaboration_comments.cjs` preserves native task comments while adding qualified foreign attribution, immutable display snapshots and positive revisions; repeat application is safe and rollback refuses retained collaboration data. Customer/shared notes remain canonical, MSP-private notes and receipts remain in the MSP store, and no users or tasks are copied. Native task threads were internal-only despite their false `is_internal` default, so only explicit consistent audience metadata permits sharing. Commands require current project/session/lifecycle authority, explicit root audiences, inherited reply audiences, own-author edits/deletes, expected revisions and atomic operation receipts. Readers combine authorized canonical/private content with exact composite cursors and source/derived field masks; deleted roots retain existing replies but reject new ones. Tests caught and fixed an unnecessary timezone conversion: both native and private timestamps are `timestamptz`, and non-UTC connections now preserve instants and microseconds across pages and writes. The private note writer is shared internally between resource-specific ticket/task entry points, preserving existing ticket validation and retry behavior. Validation: all 643 disposable PostgreSQL tests passed (23 new task-conversation cases), including revision races, revocation on retries, receipt failure and session-expiry rollback; server typecheck and domain build passed. This is backend preparation only: no task-conversation server action or UI was exposed, and task event/outbox delivery, notifications, requester portal authority, attachments, disclosure and native comment/reaction/read/count integration remain unfinished. Before exposing this writer, native paths must enforce current project/session authority, preserve authorship/revisions and avoid implicit sharing through legacy reply/edit interfaces. T09/T12 and the remaining plan criteria remain open. No live migration, external delivery, Citus, Docker or browser/theme smoke was performed. The release flag remains UI-only.

- 2026-09-07: Connected native task-comment bodies, counts, edits/deletes, replies and reaction batches to a common retained local-owner admission boundary. Co-managed and retained formerly sponsored ownership requires an actual tracked browser session, current internal identity, project RBAC/bundle scope and current lifecycle; API overrides cannot borrow browser authority. Batch locators are snapshotted, every actual parent project is authorized, project/task/comment locks retain scope, and final session/lifecycle checks cover callbacks and lock waits. Customer-owned history remains readable after live trust terminates, while cross-organization reads and prohibited writes stop. Native private roots now carry explicit organization-private metadata and immutable author/organization snapshots. Edits require the displayed revision and original author; deletion retains history and advances the revision. Replies cannot revive deleted roots or implicitly write into shared/requester threads. Native reads display shared contributions with saved foreign names and no fabricated local user, and suppress masked author/reaction details. The legacy screen treats shared contributions as read-only until the qualified shared conversation surface is connected; this does not complete shared task composing. The UI sends revisions, shows audience/organization labels, clears obsolete content/reactions on refresh or failed access, ignores late task responses, hides failed-read composers, and supports a read-only thread drawer. Existing ticket drawer behavior remains intact. Added translated copy in all ten locale files. Validation: all 658 disposable PostgreSQL tests passed (15 new native cases), including scope denial across all eight entry points, portal/API isolation, mixed batches, own authorship, post-termination local reads, input mutation and session-expiry rollback; 18 task/ticket UI tests passed; server typecheck and co-managed/projects package builds passed. No new migration was needed. T09/T12 remain partial: qualified shared task UI, task event/outbox and per-recipient delivery authority, attachments, disclosure, requester portal policy, search/export/live-update coverage and the rest of the plan remain open. No live database migration, transport, Citus, Docker or browser/theme smoke was performed. No backend feature-flag check was added.

- 2026-09-07: Extended the existing conversation outbox and consumer engine to canonical task comments. Migration `20260907171500_qualify_co_managed_conversation_events.cjs` adds resource type/ID with source-family consistency checks, preserves ticket IDs and hashes, backfills existing ticket identities and supports older ticket writers during rolling deployment. Rollback refuses retained task event history. Canonical task create/edit/delete writes now retain strict metadata-only `PROJECT_TASK_COMMENT_*` publications and search-consumer obligations in their mutation transaction; successful command retries create no duplicate event, and MSP-private notes create no customer event. Native co-managed project publications use the same outbox and after-commit dispatcher; maintenance recovery reuses stable IDs after transport failure. Existing native `TASK_COMMENT_ADDED/UPDATED` notification behavior remains separate and still needs the task-specific delivery/recipient-authority work. Task event preparation rechecks current source/audience for creates and preserves body-free invalidations after removal; consumer delivery reconstructs retained intent rather than trusting transport content. Event schemas accept qualified metadata without fabricated local users and reject hybrids containing cached bodies. The actual search subscriber commits indexing with its consumer receipt, and task-comment indexing/backfills exclude soft-deleted rows so delayed edit events cannot resurrect deleted content. Task audience rules now live in a pure shared helper. Validation: all 667 disposable PostgreSQL tests passed (nine new event cases), 70 search/indexer/subscriber tests passed, server and Temporal typechecks passed, and co-managed/event-schema/projects package builds passed; compiled event schemas were also imported in Node and checked against valid and mixed-body payloads. No live migration, mail, Citus, Docker or browser smoke was performed. T06/T09/T12 remain incomplete: inspection confirmed global task-comment search visibility currently uses client scope but lacks current project bundle and field-mask admission before results/counts; that must be completed before exposing shared task conversations. Shared task composing, per-recipient notifications/email, qualified workflow consumption, requester policy, attachments, disclosure, and the broader plan remain open. No backend release-flag check was introduced.

- 2026-09-07: Added current owner-local project policy to global search before matching, ranking, snippets, pagination and counts. Full search, typeahead and REST search share one retained transaction and admitted relation for projects, phases, tasks and task comments. The relation uses actual parent projects and the existing queue policy compiler; current field masks remove source text and its derived search vector, including task value/display aliases. Current names, parent links and client scope replace cached projections, and cached metadata cannot restore hidden terms. Task-comment entries require matching current revision/timestamp evidence and an undeleted source; rolling entries without revision metadata remain omitted until the existing indexer/backfill rebuilds them. Browser adapters bind actual tracked sessions, while REST supplies the authenticated key ID; active home identity, session/key expiry and API-key-specific bundle assignments are checked without borrowing browser authority. Customer-owned history remains searchable after separation and paid PSA upgrade; this does not provide cross-tenant MSP search or portal project access. Added real PostgreSQL cases for masks, scope changes without reindexing, stale/deleted cache entries, browser overrides, expired/revoked credentials, key-specific policy, pagination/counts, post-read expiry and retained history. Stabilized the existing task-status fixture by explicitly ordering Ready/Verified mappings, whose native defaults previously tied at zero. No backend feature-flag gate or new migration was introduced. T06/T09/T12 remain partial: other search families, qualified shared task composing, task recipient notifications/email/workflow authority, requester project policy, attachments, disclosure, export and live updates—and the broader time/lifecycle plan—remain open. Validation: all 682 disposable PostgreSQL tests passed (15 new search cases), 90 search/query/indexer/controller tests passed, server and Temporal typechecks passed, and co-managed/search package builds passed. The compiled project-search policy also loaded successfully in Node. No live migration, mail, Docker, Citus or browser smoke was performed.

- 2026-09-07: Connected qualified task conversations to the shared task editor. Browser actions bind the tracked home session, retain read/write hints and the conversation in one transaction, recheck expiry before returning, and route mutations through the canonical task/private-store command engine. Canonical publications begin only after commit through the existing outbox dispatcher, preserving event IDs and backoff; MSP-private notes create no customer publication. The reader now returns structural reply eligibility from the actual root in each store, preventing replies beneath deleted roots while retaining child history. The panel supports validated rich text, explicit root audiences, inherited reply audiences, own-author revision-checked edits/deletes, confirmation before deletion, exact retries after unknown outcomes, and microsecond cursor paging. It displays saved foreign author/organization names without copied users, clears content/composers after failed access checks, ignores late responses from another task/session, and periodically revalidates access. It inherits the task route's UI-only release flag; the backend has no flag gate. Reused existing translated conversation/task copy and filled the missing Portuguese project/task labels; panel copy resolves in all ten locale files. Validation: all 693 disposable PostgreSQL tests passed (11 new action cases), 54 task/ticket UI tests passed (12 new panel cases), and the co-managed package build passed. Server and Temporal typechecks also passed. No live migration, mail, Docker, Citus or browser/theme smoke was performed. T09/T12 remain partial: task-specific per-recipient notifications/email/workflow authority, requester portal policy, attachments, disclosure, push updates, native assignment integration and operational effort remain open, along with the broader plan. The existing native task screen still treats shared contributions as read-only; shared composing now uses the qualified task screen.

- 2026-09-07: Connected canonical task-comment creation to the retained internal-notification consumer and existing inbox/channel delivery engine. Current direct assignees, team members and additional customer task resources supply candidates; each recipient must independently pass current parent-project RBAC/bundle policy, identity, field masks and (for MSP recipients) live relationship/staff scope. Qualified author identity suppresses self-notifications. Customer-private comments remain local. Migration `20260907183000_qualify_co_managed_notification_receipts.cjs` adds resource type/ID to existing receipts, preserves rolling ticket producers and refuses destructive rollback of task receipts. Both tenants' receipts, preference outcomes, channel recovery rows and consumer completion commit atomically; concurrent event consumers reconstruct retained intent and cannot use supplied resource IDs or cached text. Every inbox read and queued in-app/Teams/push delivery reloads current content; recipient routing and preferences are rechecked for delivery, while authorized inbox history survives assignment changes. Customer-owned task history and native task links remain valid after separation/PSA upgrade. One source-classification predicate also prevents identified legacy customer task/mention notifications from falling back to cached content in inbox counts/pages or transport; native cached task handlers are suppressed for retained customer ownership. Pending pre-rollout task creation events join recovery, while already published native alerts are not replayed. No backend feature-flag gate was added. Validation: all 713 disposable PostgreSQL tests passed (20 new notification cases), 173 server notification/push tests passed with four existing skips, all 15 notification package tests passed, server and Temporal typechecks passed, and the co-managed package build passed. Transport adapters were mocked; no live migration, external delivery, Docker, Citus or browser/theme smoke was performed. T09/T12 remain partial: task email/workflow recipient authority, MSP-private notification events, qualified mentions, requester project access, attachments, disclosure, live conversation updates, mobile/Teams navigation, native assignment integration and operational effort remain unfinished, along with the broader plan.

- 2026-09-07: Connected canonical task-comment email to the existing qualified email discovery, retry and maintenance engine. Migration `20260907190000_qualify_co_managed_email_deliveries.cjs` extends existing rows with resource type/ID, preserves rolling ticket writers, permits customer-private task mail only under customer ownership, and refuses rollback that would discard task delivery history. Pending pre-rollout creation events enroll the email consumer; published native alerts are not replayed. Both organizations' admitted assignees/team members/additional customer task resources are discovered atomically with consumer completion. Queued rows retain only source/recipient identities; each attempt reloads current project authority, live MSP trust where applicable, assignment, address, task-specific tenant/category/subtype/user preferences, content and field masks. Locks remain held through transport. Current customer delivery survives separation/PSA upgrade, while MSP delivery loses access. Shared receipt insertion, retry limits, stable Message-ID, independent recipient completion and caller-owned provider retries reuse the ticket engine; processed counts now include recipients terminally skipped after authorization rejection. Task mail shares escaping/locale handling and covers all ten locales, with qualified MSP navigation and admitted native customer task paths. Native cached task-email handlers and event-email retries are suppressed for retained customer ownership. Task email does not issue ticket reply tokens or borrow ticket routing. Validation: all 732 disposable PostgreSQL tests passed (19 new task email cases), followed by 27 focused ticket/task email regressions after consolidating receipt insertion; all 181 email unit tests passed (12 new task renderer cases), server and Temporal typechecks passed, and co-managed/jobs package builds passed. No live migration, SMTP, Docker, Citus or browser smoke was performed. T09/T12/T16 remain partial overall: qualified task email replies, MSP-private task events, workflow recipient authority, qualified mentions, requester project access, attachments, disclosure, live updates, native assignment integration and operational effort remain unfinished, along with the broader plan. The release flag remains UI-only.

- 2026-09-07: Added the operational-time storage and billing-mode foundation. Co-managed products now explicitly expose `operational_time`; the product resolver distinguishes operational customer effort from commercial PSA time and denies unavailable/misconfigured products. Migration `20260907192000_preserve_operational_time_entries.cjs` records `billing_mode` on native time entries, classifies customer-owned writes as operational, excludes service/contract/tax fields and billable minutes from operational rows, prevents operational history from becoming commercial after separation/paid upgrade, and rejects operational or foreign-owner invoice source links. Existing customer billing evidence causes an explicit migration failure for reconciliation; repeat migration preserves valid history, and rollback refuses to discard operational entries. An internal billing-mode helper retains lifecycle/product/entry locks and preserves the mode of historical entries; its field normalization retains actual effort through start/end instants. It is not a substitute for actor, work-item or timesheet authorization. Fixed a race in the existing shared task-hours recalculation service by locking affected tasks in sorted order before taking the elapsed-minute aggregate snapshot. Root callers now receive an owned transaction; native write callers retain their transaction. Service-free operational entries contribute actual minutes correctly across edits, deletes, moves and concurrent saves. Validation: all 746 disposable PostgreSQL tests passed (14 new operational-time cases), all 25 product capability/mode tests passed, server and Temporal typechecks passed, and types/DB/co-managed package builds passed. No live migration, invoice, Docker, Citus or browser smoke was performed. T13/T14 remain incomplete: native form/API service requirements still need to consume the new billing-mode admission under current principal/source/timesheet authority; optional approval behavior, MSP-qualified work references and normalized billing context, invoice evidence, shared effort projections and the broader plan remain open. This checkpoint prepares and protects storage; it does not yet expose service-free time logging in the native UI or API. The release flag remains UI-only.

- 2026-09-07: Connected operational time to the native browser save and time-entry dialog. The save owns one transaction through current product/lifecycle admission, tracked home session, active user, retained local RBAC/bundles, actual work-item source, entry ownership, and old/new editable timesheets. Customer effort supports tickets, tasks, general time, assigned ad hoc appointments, and local interactions without service/contract/tax selections; actual task totals remain elapsed minutes and logging does not implicitly change assignments. Moves confirm both old and new sources and sheets, reject submitted/approved work and invalid periods, and recalculate both affected task totals. Final session expiry rolls back the entry and totals; search events publish only after commit and omit customer note payloads. Returned work labels respect current source masks. Saved operational history remains service-free after paid PSA upgrade, while new commercial PSA entries retain the service requirement. The existing dialog loads the current product mode, skips billing catalog/client defaults for operational entries, hides billing controls, and discards stale initialization/default responses; the existing customer workspace boundary continues to gate UI use with `release-v1-6-feature`, with no backend flag checks. Legacy customer time search rows are excluded before matching/counting until their current source/private-note search policy is connected. Validation: 763 full disposable-PostgreSQL cases pass (17 new native time cases), 22 targeted form/provider/validation tests pass, server and Temporal typechecks pass, and co-managed/search package builds pass. No live data, migration or browser smoke was used. Still open for T13/T14: native API/timer writes, read/list/export/source authorization, approval/deletion integration, current-authority time search, organizational effort visibility, and MSP-owned qualified billing context/invoice evidence.

- 2026-09-07: Connected REST time-entry create/edit and bulk dispatch to operational mode. Product-dependent service requirements now run inside the retained service admission, so customer requests omit services while new commercial PSA requests still require them. Current API-key ownership, active home identity, key-specific bundles, local work/entry/sheet authority and lifecycle remain locked through save and final expiry checks. Browser credentials cannot substitute for the actual key. Shared credential admission also backs project search without changing its policy. Each API mutation uses a fresh transaction-bound service instance, keeping automatic timesheet creation, native helpers, source projections, task totals and after-commit events together; rejected billing selections, lost authority and final key expiry leave no sheet or effort artifacts. Same-user API creates serialize overlap/sheet checks. Responses use actual elapsed duration and admitted work labels; customer note payloads stay out of update events. All manual time API handlers forward their verified context instead of dropping the key ID. Validation: 774 full disposable-PostgreSQL cases pass, plus the new controller-level bulk create/update/revoked-key case (775 unique cases now); 19 targeted schema/event/UI-boundary unit cases pass, including a final seven-case schema/event rerun. Server and Temporal typechecks, DB/co-managed builds and a compiled project-policy import pass. No live migration or data was changed. API read/list/export, approval/deletion, current-authority time search, timers and qualified MSP billing remain open.

- 2026-09-07: Timer discovery/implementation direction: the existing REST timer methods encode a running timer as a time entry with `end_time = NULL`, but the native `time_entries` schema requires `end_time` and consumers assume completed intervals. No separate timer store exists. Keep running timers separate from completed effort records; the next timer implementation must preserve one active timer per home user, its original billing mode, current credential/work authority, and an atomic stop-to-entry transition. Do not weaken completed-time invariants or expose unfinished clocks to native timesheets, task totals or invoice candidates. The existing timer methods are not yet converted.


- 2026-09-07: Replaced REST timers backed by unfinished time entries with a tenant-owned active-clock store (`20260907210000_create_native_time_tracking_sessions.cjs`). One active clock per home user is serialized with current identity; clocks preserve their source, starting timezone/work date and original commercial/operational mode. Start creates no completed effort, sheet, task total or invoice candidate. Stop retains the actual API credential, current work/time policies and editable sheet, creates one completed native entry with the clock ID, recalculates task effort, records an immutable request receipt and publishes only after the owning transaction commits. Exact retries read the current completed entry under current source/field authority; a changed stop conflicts and deleted effort is never recreated. Credential expiry rolls back entry, sheet, effort and receipt together. Operational clocks stay unpriced after separation/paid upgrade; ordinary PSA clocks require an owned service. Current read masks also constrain native/API save admission so partial edits cannot return hidden stored notes. Timer responses project optional masks on both first completion and replay. Timer records are registered in tenant metadata, export and deletion ordering. Invalid dates/end ordering and submitted sheets leave the active clock intact; same-minute stops retain zero elapsed minutes without fabricating effort. Validation: 786 full schema-only PostgreSQL integration cases, followed by 12 focused timer cases (787 unique suite cases) after the final response projection; 6 schema/workflow-event unit cases; server and Temporal TypeScript plus DB/co-managed package builds. No live migrations, browser, Citus or export/restore execution. Remaining timer follow-up: explicit cancellation/recovery when source access is withdrawn or a source disappears, browser timer integration if a native timer surface is added, and end-to-end HTTP/product smoke. Native time read/list/export/approval/delete and MSP-qualified billing remain open; this checkpoint does not complete the time workstream or the overall plan. The release flag remains UI-only.


- 2026-09-07: Added `POST /api/v1/time-entries/cancel-tracking/{sessionId}` and domain cancellation for a specifically identified, authenticated user's active clock. Abandonment creates no completed effort and discloses no work data, so it remains available after source scope or the writing entitlement is lost. Actual home credential/product/user admission still applies. Unknown or foreign-owned IDs receive the same harmless canceled response without affecting another user; a completed clock conflicts and retains its receipt. Named-clock retries cannot cancel a newer timer. Cancel and stop serialize under the same user/clock locks. The controller uses the existing API authentication, rate-limit, product and user-context boundary; no release flag gates the API. Validation: all 16 timer cases passed with cancellation, then all 5 cancellation cases passed after adding the actual controller test (792 unique suite cases); server TypeScript and domain build passed. The controller test mocks transport validation/rate limiting/product middleware but executes the real time service and migrated database credential check, including rejection after the transport validator becomes stale. No live HTTP/browser validation. This closes cancellation after lost source access; native time reads/approval/delete, shared MSP billing and the broader plan remain open.


- 2026-09-07: Connected native browser `getTimeEntryById` and REST `getById`/`getWithDetails` to one retained customer-owned time-detail reader. Current product and durable co-management ownership select the boundary, including operational history after a paid upgrade; ordinary PSA detail behavior remains available. Reads retain lifecycle locks in the established order, the actual home session/API credential, active reader, current local read permission, work-item and time-entry bundle policies, sheet ownership, and the actual entry/source identities through final expiry checks. Approved entries and license-read-only history remain readable under current resource authority. A currently authorized manager can still read a deactivated technician’s history; the requesting identity must remain active. Browser DTOs receive ISO dates and admitted work-item labels; API DTOs use actual elapsed duration (90 minutes = 1.5 hours even when billable minutes are zero) and do not invoke the old unconstrained billing/work-item enrichment for customer-owned entries. Hidden notes and optional financial metadata are projected before return; hidden required interval identity/timing rejects the detail instead of inventing values. Current key-specific source masks remain independent from the same user's browser permissions. Validation: 796 full schema-only PostgreSQL integration cases and seven focused time-detail cases after final expiry/financial-mask/history additions (799 unique suite cases); four operational time-entry provider unit cases; server/Temporal TypeScript and co-managed build. The expiry test observes the reader waiting on the actual task lock, lets its API credential expire, then confirms no result escapes after release. No live browser/HTTP smoke. Sheet collections, API list/search/stats/export, native approval/deletion, current time search and MSP-qualified billing remain open; this checkpoint covers individual detail readers, not the whole time workstream.


- 2026-09-07: Connected native timesheet entry collections and the separate change-request-history reader to the retained time projection. Each sheet requires current home identity, local read permission, ownership/delegation and its own sheet bundle policy; each returned entry independently requires current source/time-entry scope and field visibility. Out-of-scope entries and their review comments are omitted together. Expiry during a source lock wait rejects the entire response rather than returning a partial or empty collection. Current-sheet review comments are loaded under retained entry/request/author locks, and hidden notes or review fields suppress comment history, latest-comment and state/count evidence. General time retains the native fallback work ID and a label derived only from visible notes; interval ordering and standalone review-history ordering remain stable. Approved/read-only/post-upgrade history and permitted deactivated-technician history remain readable. Read operations now use shared sheet locks, avoiding a shared-to-exclusive upgrade deadlock on simultaneous post-upgrade reads. Ownership/delegation is a shared primitive; bundle checks run on actual sheets and work records instead of prematurely evaluating a client-scoped entry policy without a client. Native save/detail/sheet/review adapters share the tracked-browser credential resolver; API identity remains separate. Validation: all 806 schema-only PostgreSQL integration cases, seven provider/change-request unit cases, server and Temporal TypeScript, and co-managed package build passed. No live browser/HTTP smoke. API collection/search/stats/export, broader sheet summaries, approval/deletion, time search and MSP-qualified billing remain open; the release flag still protects UI use only.


- 2026-09-07: Connected native browser and REST time deletion to a shared customer-owned transaction boundary. Current product/lifecycle, actual session/API credential, owner/delegation, work-item read scope, entry read/delete policies and editable entry/sheet state remain retained through deletion and the final expiry check. Approval-state conflicts are reported only after scope passes, avoiding an out-of-scope state disclosure. Authorized deletion may remove an entry whose notes are hidden; no notes or review text enter its response or event. A current manager may delete permitted draft history from a deactivated technician. Deletion removes attached review requests, recalculates actual task minutes and schedules identity-only events after commit; completed timer receipts remain intact and cannot recreate deleted effort. Events now preserve both the entry owner and `deletedBy` acting user. The shared billing-reversal adapter reuses native bucket/hour-block engines for commercial entries after PSA upgrade; failures preserve the entry, allocations and balances. Operational time with unexpected hour-block evidence and time with invoice evidence is rejected for reconciliation. Native commercial deletion also now propagates hour-block reversal failures rather than committing a deletion after a failed reversal. Validation: 816 full schema-only PostgreSQL cases, then ten focused deletion cases after the final state-privacy ordering correction; six UI/workflow-event unit cases; server/Temporal TypeScript, co-managed/event-schema builds and a compiled event-schema attribution check. No live deletion, migration or HTTP/browser smoke. Remaining: approval commands, API collections/search/stats/export, broader sheet summaries, time search, qualified MSP billing, and shared serialization with nightly allocation reconciliation. This checkpoint does not claim concurrent reconciliation coverage or completion of the time workstream. The release flag remains UI-only.


- 2026-09-07: Validation workflow updated at the user's request: prioritize implementation throughput using the running Node development stack and small, focused source-mode checks. Stop routine package builds, whole-server/worker type checks and repeated full regression runs between checkpoints. Keep targeted checks for consequential permission, financial and data-integrity changes; defer broad builds/regressions and most remaining validation to the later review pass. Record deferred coverage explicitly rather than treating it as verified.

- 2026-09-07: Shared hour-block allocation, reversal and nightly reconciliation now serialize on the persisted time entry. Allocation ignores stale caller duration/mode/client data and refuses a second burn until existing allocations are reversed. Reconciliation locks all candidate entries before client block balances, rereads after lock waits and skips deleted or newly invoiced entries; operational time cannot acquire a new burn. A shared reversal engine retains per-client scope, and moved entries with another client's remaining allocation wait for that client's pass before receiving another burn. Validation followed the faster workflow: five focused source-mode PostgreSQL checks passed in 9.27 seconds, covering concurrent reversal, persisted allocation inputs/retry, both deletion/reconciliation orders and invoicing during a wait. No package builds or broad regression/type checks were run. Broader billing/Citus, source-parent changes and invoice lock interactions remain for later review. No release flag was added to backend access.

- 2026-09-07: REST time lists, both search methods, statistics and CSV/JSON exports now share the retained customer-owned detail reader and actual API-key authority, including after PSA upgrade and entitlement expiry. Source/entry scope and field masks are applied before search/filter predicates, paging, counts, grouping and totals; source client IDs and user/service labels are projected under their applicable field masks. Collections retain all candidate owners/sheets before reading source parents and reject the whole result if credentials expire. Operational duration filters, average duration and top-work totals use elapsed minutes. Masked billing produces null financial totals while visible total effort remains available; API response schemas accept these disclosures and the absent legacy tax-percentage field. CSV uses visible fields, stable dates and formula escaping. Five focused source-mode PostgreSQL cases pass (10.86 seconds final run), including schema parsing; no package builds, broad regression or type-check runs. Remaining: approval commands, native broader sheet summaries, time global search and qualified MSP billing; collection scalability/lock audit and live HTTP/browser checks are deferred to review. Existing export grouping/field-selection options and true XLSX generation remain unimplemented; co-managed XLSX now reports unsupported instead of returning JSON as if it were a workbook. Release gating remains UI-only.

- 2026-09-07: Native per-entry approval status changes and REST approve/request-changes now share a retained customer-owned review command. Actual session/API key, home approval permissions, owner/delegation, source/entry read and approval scope, sheet approval scope and field masks are checked before state conflicts or writes. Approval invokes actual mutation guards (including configured not-self-approver restrictions); current managers can review authorized former-technician submissions. The shared source admission now exposes its retained policy subject/record for command-specific guards. Transitions require submitted/returned work before approval; reopening requires sheet reversal authority, and invoiced entries or invoice evidence prevent changes. Feedback and detailed API context are stored as private entry review history together with the entry/sheet state; failed final credential checks roll everything back. Identity-only events publish after commit; repeated approval produces one state change/event, while REST batches retain per-entry results. Six focused source-mode PostgreSQL cases passed in 9.41 seconds; no package builds, broad regression or type checks. Remaining: whole-sheet submission/approval/reversal, bulk-sheet commands, native sheet summary readers, optional API approval-note storage, global time search and qualified MSP billing; broader concurrency/HTTP/browser verification remains deferred. Release flag remains UI-only.

- 2026-09-07: Native whole-sheet submission, approval, bulk approval, change requests and explicit reversal now share a customer-owned transaction engine. It retains actual browser identity, current lifecycle, all owners/sheets, source/entry scope, sheet read/write policy and approval mutation guards before any writes; one excluded entry or invalid bulk member prevents the whole operation. Technician submission requires submit/update permissions without requiring approval permission. Returning or resubmitting a partially reviewed sheet preserves approved entries, while explicit reversal reopens those too; any invoiced entry or invoice evidence rejects the sheet transition. Audit notes remain stored on the sheet and identity-only entry events run after commit, with final session/lifecycle checks rolling back sheet, entry and audit changes together. Submitted sheet responses honor optional field masks. Six focused source-mode PostgreSQL scenarios validated; a two-case follow-up (8.70 seconds) checked the localized missing-sheet result and corrected the expiry trigger to verify an actual authentication rejection. No package builds, broad regressions or whole-project type checks. Remaining: sheet comments/read summaries and related comment/deletion commands, API sheet commands, optional approval notes, global time search and qualified MSP billing; full concurrent lock-order and HTTP/browser coverage remains deferred. The release flag remains UI-only.

- 2026-09-07: Native single-sheet details, the alternate sheet-entry reader, sheet comment reads and comment creation now use the retained customer time boundary. Detail counts and total minutes/hours derive only from independently admitted intervals, with optional period/summary fields honoring masks. The private projection records whether all source/time content is visible; sheet-wide comment streams are withheld when any entry is excluded or content is masked, and blind comment writes are rejected. Comment creation checks actual browser identity, ownership/delegation, current sheet read and submit/approve policy, field masks and lifecycle; caller-supplied author IDs cannot impersonate another user, and reviewer attribution is derived from actor-versus-owner identity. Comments remain readable after write entitlement expiry, while new comments are blocked. Final session expiry rolls back insertion. Five focused source-mode PostgreSQL cases pass in 9.10 seconds, including alternate/native/API detail contracts and native schema parsing; no builds or broad regression/type checks. Remaining: sheet lists and approval dashboard readers, sheet creation/deletion and API sheet commands, optional approval-note storage, global time search, qualified MSP billing and the broader deferred browser/concurrency validation. Release gating remains UI-only.

- 2026-09-07: Native own/all timesheet lists, approval dashboard queries and period-status views now share retained sheet detail projections. Current identity, home permissions and delegation apply to every sheet, with all candidate owners/sheets retained before source reads and final credential expiry rejecting the whole collection. Approval rows additionally honor sheet approval scope; employee names/emails and comments respect field/content visibility. Period effort, days and last dates derive only from visible entries inside the period. Incomplete entry or global sheet visibility leaves removal counts unknown so filtered zeros cannot enable destructive UI actions. Unopened periods stay unbacked and use the selected user's sheet-read scope. Masked hours/days remain null and render as an em dash. Six focused source-mode PostgreSQL cases pass in 11.35 seconds; no package builds, broad regressions or whole-project type checks. Remaining: sheet creation/deletion, API sheet and period adapters, optional approval-note storage, global time search and qualified MSP billing, plus deferred scale/concurrency/browser checks. Release flag remains UI-only.

- 2026-09-07: Native lazy sheet opening and empty-draft removal now use retained customer lifecycle commands; automatic sheet creation from time-entry API saves uses the same open command. Actor/target-user locks serialize user-period creation, existing duplicate sheets report a reconciliation error, periods are retained against deletion, and read/create/delete policy plus owner/delegation and field masks govern each branch. Existing sheets remain readable after write expiry or creation-permission removal; absent sheets require active owners, create permission and current write entitlement. Removal retains the actual sheet against entry insertion, checks empty draft state, and deletes private sheet comments/orphan review children only in the same successful transaction. Final session expiry rolls creation/deletion and children back. New co-managed role defaults grant sheet creation to technicians/project managers and entry approval to project managers, matching their existing time capabilities; existing custom role assignments are not overwritten. Seven focused source-mode scenarios verified (initial six-case run 8.60 seconds, two-case API follow-up 8.91 seconds), including concurrent opens/insertion and actual API permission revocation. No builds, broad regressions, type checks or live migrations. Remaining: standalone sheet and period API adapters, optional approval-note storage, global time search and qualified MSP billing; wider concurrency/scale/browser checks remain deferred. Release gating remains UI-only.

- 2026-09-07: Standalone timesheet API detail/list/search, private comments, submit/approve/request-changes/reversal and empty-draft removal now reuse the retained customer time domains. Manual controller routes preserve the verified API key ID in their service context. Lists filter, sort, count and paginate admitted projections; summaries use actual visible effort, mask financial metrics and withhold approval readiness for partially visible sheets. API DTOs accept real calendar periods, optional stored metadata and masked totals; general-time entries retain nullable API source identity. Submission/review feedback is persisted in private sheet comments, with command, feedback and response admission in one transaction and identity-only entry events after commit. Comment text/role API aliases honor native field masks. Five focused source-mode PostgreSQL scenarios verified: initial four-case run (9.91 seconds) passed workflows, scope and expiry rollback but found a calendar-date response-schema mismatch; corrected DTOs and a masked-history/empty-deletion scenario passed in a three-case follow-up (8.87 seconds). No package builds, whole-project type checks, broad regressions, live migrations or browser testing. Remaining: standalone sheet create/update/notes and statistics, export contract fidelity (including filtering/full pagination/formats), period/schedule API adapters, optional per-entry approval notes, global time search and qualified MSP billing; collection scale and wider concurrency/HTTP/browser validation remain deferred. Release gating remains UI-only.

- 2026-09-07: Standalone timesheet API creation and generic editing now use shared lifecycle commands. Explicit creation and lazy opening serialize on the same target user; explicit duplicates return conflict while lazy opens stay idempotent. A nullable persistent `time_sheets.notes` column stores sheet notes separately from review comments; migration rollback refuses to discard retained notes. Creation honors read/create masks and actual API authority. Editing retains actor/owner/sheet, enforces read/update scope and current write entitlement, and permits note replacement/clearing only for draft or returned sheets with complete content visibility. Generic status changes invoke existing submit/approve/request-changes commands and cannot assign arbitrary states or bypass explicit reversal. Partial source/content visibility withholds persistent notes from native/API detail/list/search projections, and command receipts omit this free text. API mutation response admission stays inside the same outer transaction. Five focused source-mode PostgreSQL scenarios pass in 9.40 seconds, covering concurrent duplicate creation, private note persistence/clearing, actual review permission revocation, masked/partial-source writes, entitlement expiry and key-expiry rollback. No builds, broad regression/type checks or live migrations. Remaining: sheet statistics and export fidelity, period/schedule API adapters, optional per-entry approval notes, global time search and qualified MSP billing, plus wider deferred concurrency/scale/browser validation. Release flag remains UI-only.

- 2026-09-07: Customer timesheet statistics now replace the API placeholder with counts, status/user groupings, actual effort averages, current-period totals, approval/submission rates and top-user summaries derived solely from admitted sheet projections. Masked metrics and unknown periods remain null. Exports consume the complete admitted collection rather than a default list page, honor user/period/status/date filters, selected fields and optional entries/comments, and group by user/period/status. CSV quotes multiline fields and neutralizes formula prefixes; XLSX uses the existing ExcelJS dependency to produce real workbooks with stable group worksheets and string-valued free text; JSON preserves optional grouping. The controller sends matching content types, parses repeated/comma-separated collection parameters for search/export, and passes validated statistics filters. Calendar filters accept calendar dates or existing timestamps. Three focused source-mode PostgreSQL scenarios pass in 10.22 seconds, including a 28-sheet export beyond the list page, workbook read-back, and masked private content across all formats. Additional direct Node source checks pass for search/date filters and exclusive period ends; four changed API files pass a syntax-only transpilation check. No builds, full type checks, broad regressions or browser testing. Remaining: period/schedule API adapters, time-entry export parity/optional approval notes, global time search and qualified MSP billing, plus deferred HTTP, scale, concurrency and browser coverage. The release flag remains UI-only.

- 2026-09-07: Customer period API list/detail/current/create/update/delete/generate and native latest/all/create/update/single/bulk removal now retain current home credentials, time-period permissions/bundle scope and lifecycle write admission. Native current-period time picking stays under the existing own-sheet boundary so technicians do not gain calendar administration. A shared database calendar engine serializes empty-calendar overlap checks on a tenant-scoped lock row, locks actual periods before checking sheet references, rejects invalid/overlapping ranges and in-use changes, and supports atomic period batches. The existing TimePeriod model uses this engine, so background model writes share its overlap/emptiness locks. API generation uses contiguous half-open full periods with anchored month/year boundaries; native settings-driven generation now acquires calendar/credential authority before reading retained settings and accepts a period ending exactly at the requested boundary. Native settings projection normalizes unused nullable calendar components to absent optional values. Seven focused scenarios verified: six core source-mode PostgreSQL cases pass in 9.43 seconds; the added generator case passed in an 8.80-second follow-up after correcting fixture timestamps and the real null/optional projection mismatch. Seven changed source files also pass syntax-only transpilation. No builds, full type checks, broad regressions, live migrations or browser checks. Remaining: period settings administration/API contract alignment, explicit background period-job authorization/lifecycle, schedule API adapters, time-entry export parity/optional approval notes, global time search and qualified MSP billing; wider calendar/Citus/concurrency/browser checks stay deferred. The release flag remains UI-only.

- 2026-09-07: Period settings API read/create/update and both native settings readers plus native create/update/delete now share retained customer calendar authority. Current read/manage permission, record bundle scope, field visibility, write entitlement and final credential checks govern settings; settings mutations serialize on the same calendar lock as generation. The API accepts legacy label/count pairs and canonical numeric-count/unit inputs, including legacy count-only partial updates; responses expose the actual canonical model plus the documented settings-ID alias. Monthly day ranges and year components now match the native contract, explicit end-date clears are preserved, inactive settings remain available through API history, and compatible active schedules coexist while conflicting activation/creation is rejected without implicit deactivation. Native settings-driven generation reads fully admitted configuration after acquiring calendar/actor authority. Five new focused source-mode PostgreSQL scenarios plus the affected native generator pass in 9.46 seconds; no builds, broad regression/type checks or live migrations. Remaining: explicit background period-job authorization/lifecycle and broader calendar-generation parity (including annual profile behavior), schedule API adapters, time-entry export parity/optional approval notes, global time search and qualified MSP billing, plus deferred HTTP/scale/Citus/browser coverage. Release gating remains UI-only.

- 2026-09-07: Background time-period creation now lives in a worker module outside server actions and requires its actual current processing job, scheduler trigger and matching queue identity. The boundary retains current tenant/product/suspension and co-management write entitlement before calendar locking, loads persisted active configuration under that lock, and rechecks job/lifecycle authority before commit. Read-only, suspended and unconfigured tenants skip safely while the existing daily scheduling chain continues. Duplicate execution serializes against the same calendar, and job invalidation or entitlement lapse during generation rolls back all dates. Browser generation and worker suggestions share exclusive end-date calculations, including adjacent semi-monthly and seasonal annual profiles; profile generation advances to each configured start. Six focused source-mode PostgreSQL scenarios verified: five passed in the initial 9.64-second run; the concurrent duplicate case passed in an 8.09-second follow-up after correcting its PostgreSQL Date assertion. Existing infrastructure worker tests were adapted to seed real system jobs but were not run. No builds, full type checks, broad regressions or live migrations. Remaining: wider calendar edge/parity checks, schedule API adapters, time-entry export parity/optional approval notes, global time search and qualified MSP billing, plus deferred HTTP/scale/Citus/browser coverage. Release gating remains UI-only.

- 2026-09-07: Customer schedule API list/detail and native detail now share current home credential, schedule read/assignment and linked work-root admission. Actual project/phase/task, ticket, interaction or appointment-request linkage is retained before the schedule and assignees; schedule bundle scope receives the actual source client/board and schedule ownership. Private unassigned entries expose Busy projections without notes, source identity or recurrence content; source field restrictions suppress copied schedule text, and schedule masks govern projected fields and derived metrics. Lists omit excluded rows and apply date/assignee filters to admitted projections; final credential expiry rejects the entire collection. API work-item titles now come from admitted records, assignee DTOs expose IDs without unadmitted PII, optional stored metadata and JSON recurrence shapes match response validation, and native detail retains Date objects. Five focused source-mode PostgreSQL scenarios verified: three passed in the initial 9.25-second run; a corrected bundle-fixture case passed in 8.65 seconds; actual expiry during a source lock wait passed in 10.39 seconds. Four source files passed syntax-only transpilation. No builds, full type checks, broad regressions or live migrations. Remaining: native schedule collection/recurrence admission and schedule create/update/delete adapters (including atomic response admission, private mutations, assignment effects and events), wider appointment/interaction/source-lock and calendar edge coverage, time-entry export parity/approval notes, global time search and qualified MSP billing, plus deferred HTTP/scale/Citus/browser checks. UI-only release gating is unchanged.

- 2026-09-07: Native schedule calendar reads now use retained schedule/source/assignment authority before recurring expansion. Occurrences inherit their actual master scope and receive the same private Busy projection, field masks and final credential checks as detail; explicit recurrence restrictions also withhold derived occurrences. Technician filtering preserves own-only reads and the dispatcher unassigned-request behavior. Calendar queries include spanning allocations and apply half-open timestamp overlap after recurrence expansion, including occurrences that begin before the visible range. Global holidays and exceptions govern every occurrence. Shared recurrence generation now offers explicit master inclusion, used by both the admitted calendar and existing model expansion, fixing omission of the first event while preserving the subsequent-only default used by other callers. Native calendar/detail adapters normalize dates and pattern dates to Date objects. Three focused source-mode PostgreSQL calendar scenarios pass in 8.91 seconds, 18 pure recurrence cases pass in 173 ms and four existing model recurrence cases pass in 816 ms. No builds, full type checks, broad regressions or live migrations. Remaining: schedule create/update/delete adapters and recurrence command authority, atomic response admission and assignment/event effects, broader calendar timezone/workday/count/scale and source-lock coverage, time-entry export parity/approval notes, global time search and qualified MSP billing, plus deferred HTTP/Citus/browser checks. Release gating remains UI-only.

- 2026-09-07: Customer schedule API create/update/delete now run through retained command admission: actual home key/session identity, current write entitlement, old/proposed work sources before the schedule lock, locked assignments, active internal assignee validation and action-specific bundle rules. The established own-calendar read permission still permits creation/editing/deletion of solely assigned entries; operations on other assignments require dispatch update/delete permission. Private existing entries remain editable/removable only by their sole assignee. Read and command masks prevent blind field replacement, sole assignment defines actual calendar ownership for own-scope rules, and response admission runs inside the mutation transaction. Inputs whitelist actual model fields, validate partial date edits and recurrence configuration, preserve explicit source/note/recurrence clears, deduplicate assignees and normalize API aliases. Model-backed updates avoid writing assignment arrays as schedule columns and now refresh updated_at even for empty patches. Recorded ad-hoc time and active clocks prevent schedule deletion or source-type changes that would orphan effort. Events publish identity-only payloads after commit; final key or lifecycle rejection rolls back rows and assignments. Six focused source-mode PostgreSQL scenarios verified: an initial run found an unexported scope helper; after fixing that wiring, four cases passed in 9.44 seconds and one dependency fixture collided with preseeded effort; the corrected dependency case and an added active-clock/lapse case pass in 8.94 seconds. Eight existing model-update cases pass in 865 ms; syntax-only checks pass for four command/API sources. No builds, full type checks, broad regressions or live migrations. Remaining: native schedule mutation adapters and single/future/all recurrence command admission, appointment/meeting cleanup and workflow consequences, wider assignment/inactive-user/concurrency/source-lock coverage, calendar timezone/workday/count/scale checks, time-entry export parity/approval notes, global time search and qualified MSP billing, plus deferred HTTP/Citus/browser checks. Release gating remains UI-only.

- 2026-09-07: Native schedule create/update/delete now use retained command admission and tracked browser credentials, preserving native Date-shaped responses and own-assignment defaults. Commands parse master/occurrence locators independently of body-supplied original IDs, verify exact stored recurrence membership after scope admission (including counts, exceptions and holidays), and honor explicit recurrence field restrictions. Single edits retain siblings and create admitted standalone rows; future splits preserve occurrence duration and remaining count; all-scope virtual edits retain the original anchor date while changing clock/duration. Explicit text/source clears and recurrence removal are preserved by model branches, private-state edits apply to all-scope changes, and configuration replacement retains existing exceptions. Cancelling the first/single occurrence records an exception on its existing master rather than replacing/deleting that identity; retained time survives, while destructive master removal remains blocked by recorded effort/active clocks. After-commit events describe actual row changes: master update plus child creation for a split, update for an excluded occurrence, deletion only when the master is removed. Eight focused source-mode PostgreSQL scenarios pass across the six-case core run (9.52 seconds) and two-case final-session-expiry/history follow-up (8.96 seconds); all 22 existing recurrence-model cases pass in 891 ms and three source files pass syntax-only checks. No builds, full type checks, broad regressions or live migrations. The new co-managed native branches currently publish identity-only schedule events; appointment/Teams synchronization and cancellation cleanup, derived workflow/dispatch/capacity consequences and their retained consumers remain to be connected. Also remaining: broader recurrence replacement/count/holiday/timezone/workday/concurrency coverage, time-entry export parity/approval notes, global time search and qualified MSP billing, plus deferred HTTP/scale/Citus/browser checks. Release gating remains UI-only.

- 2026-09-07: Calendar commands now update local appointment requests and online meetings in the same retained transaction as schedule changes. Requests retain their actual client scope and binding; calendar edits cannot approve pending requests, adopt another request's slot or split a bound appointment/meeting into recurring masters. Rescheduling updates the request's local date/time, duration and sole preferred assignee while preserving previously disclosed meeting subject text. Cancellation clears live request join fields, cancels local meeting records and removes schedule conflicts before physical deletion. Legacy request-only Teams IDs are retained in an actual meeting row before clearing the request. Migration 46 adds durable pending update/delete synchronization intent fields to online_meetings; rollback refuses to discard pending work. The provider synchronization worker is still pending: this checkpoint does not call Graph or synchronize external Teams meetings. Final credential rejection rolls back schedule, request, meeting and conflict changes together. Six focused source-mode PostgreSQL scenarios pass in 9.45 seconds, including local rescheduling/cancellation, legacy provider binding, conflicting/pending request rejection and key-expiry rollback; six source files and the migration pass syntax-only checks. No builds, full type checks, broad regression runs or live migrations. Continue implementation using focused Node dev-mode checks; defer broad verification to XO/review. Remaining: the durable provider-intent consumer, explicit appointment approval/cancellation and online-meeting action admission, derived schedule workflow/dispatch/capacity consequences, wider recurrence and source concurrency coverage, and the previously recorded outstanding plan work. Release gating remains UI-only.

- 2026-09-07: The existing recurring Teams meeting sweep now discovers pending co-managed schedule meeting operations and invokes a retained domain consumer. The committed operation is narrow system authority for an existing provider meeting's clock update or cancellation; it cannot create meetings or disclose new calendar text/attendees. The consumer rechecks current workspace/lifecycle after acquiring the actual operation lock, holds that lock through the provider call, rejects stale/foreign/inconsistent operations, and clears only a confirmed matching action. Failures, skipped configuration and transport exceptions retain the operation with bounded exponential retry pacing and sanitized diagnostic codes. Suspended/read-only tenants retain pending work for recovery. A bounded sweep selects 25 due identities; existing ten-minute Teams maintenance provides crash recovery on its configured runner. Reconciliation requires a persisted event ID and original organizer; incomplete legacy bindings remain pending instead of interpreting a 404 on a guessed resource as successful deletion. Existing callers retain their configured-organizer fallback. The Graph update/delete fetch is bounded to 30 seconds. Six focused source-mode PostgreSQL scenarios pass across a five-case run (9.70 seconds) and an actual-lock/subsequent-edit case (8.94 seconds); the strengthened incomplete-binding case passes in a 9.06-second follow-up. Six mocked Graph adapter cases pass in 167 ms and six sources pass syntax-only checks. No builds, broad type checks/regressions, live Graph calls or live migrations. Remaining: legacy provider-binding repair, assignment/invite-recipient reconciliation, explicit appointment approval/cancellation and online-meeting action authority, derived workflow/dispatch/capacity consequences, wider lifecycle/provider-config concurrency and live maintenance scheduling validation, plus the previously recorded full-plan work. UI-only release gating is unchanged.

- 2026-09-07: Native customer appointment list/detail/ticket-linked readers now share current home credential, linked-ticket/client scope, technician visibility and bundle admission. Visibility uses actual preferred assignment, retained management/team relationships and current company/per-technician approver configuration (including legacy defaults); dispatch update authority permits oversight. Projections whitelist request fields, apply masks before filters/search, and authorize related client/contact/user labels independently. Explicit decline and reschedule commands retain the same request authority plus actual linked schedule/assignment bundle scope, current write lifecycle and final credentials. Decline handles pending/approved requests, clears live request bindings, removes schedule/conflict rows atomically and retains meeting deletion intents; history/active clocks and inconsistent/private bindings prevent destructive changes. Rescheduling preserves pending status, validates local dates/times including DST gaps, updates requested and calendar/provider clock state in one transaction and queues meeting synchronization. Events contain identities and publish after commit. Eight new appointment scenarios pass across two focused Node source-mode PostgreSQL runs (10.09 seconds and 10.12 seconds); the latter also verifies the existing calendar implicit-approval guard. Five source files pass syntax-only checks. No builds, broad type checks/regressions, live email/Graph calls or migrations. These co-managed branches do not yet send requester decline/reschedule notifications or expose meeting recording artifacts; those need retained delivery/artifact admission. Remaining: explicit approval and Teams creation/compensation, ticket association, requester-facing cancellation/intake, attendee reconciliation, incomplete legacy provider/schedule binding repair, derived workflow/dispatch/capacity consumers, and the previously recorded full-plan work. UI release gating remains unchanged and UI-only.

- 2026-09-07: Customer appointment approval without Teams creation now uses the retained local command boundary. It accepts only pending requests, checks current configured/global approver authority, actual active internal assignees, old/proposed ticket roots and request/schedule bundle fields, and admits its final response before commit. Existing pending calendar identity is retained; unlinked requests receive a model-created allocation under a stable reserved identity. The schedule model accepts an optional validated command-reserved ID while ordinary callers retain generated IDs. No-override approval interprets requester-local dates; explicit form overrides retain the existing UTC contract. The request's wall-clock date/time is synchronized with the approved schedule, and internal approval notes remain in calendar notes rather than the request projection. Approval serializes duplicate attempts and emits only committed schedule identities. Ticket association now retains old/proposed ticket roots before request locks, rejects cross-client links, can establish an unspecified client from the admitted ticket, preserves the appointment as the calendar work item, and repairs the narrowly matching legacy ticket-backed allocation. Subsequent rescheduling still works. Meeting rows with no provider meeting ID no longer receive futile update/delete intents. Six focused Node source-mode PostgreSQL scenarios verified: five pass in the initial 10.31-second run; the cross-client/legacy case passes in a 9.55-second follow-up after removing a generated database column from its fixture, also checking revoked ticket-read permission. Six sources pass syntax-only checks; no builds, broad type checks/regressions or live external effects. Approval with generate_teams_meeting enabled still uses the legacy path and is not covered by this new coordinator; retained Teams creation, idempotency/compensation and Generate Teams Meeting remain required next work. Also remaining: requester notifications, artifact access, recipient reconciliation, other legacy binding repair, qualified/derived consumers and previously recorded plan work. The release flag remains UI-only.

- 2026-09-07: Added the preparation and provider-recovery substrate for Teams-enabled approval. Migration 47 creates tenant-distributed co_managed_meeting_creation_operations with immutable actor/credential evidence, frozen approval/provider details, target identity and source fingerprint; a partial unique index permits one unfinished creation per request, state constraints retain recovery evidence, and rollback refuses to discard journal rows. There are deliberately no request/user foreign keys so cleanup can survive source or author deletion. The shared approval planner supplies current request/ticket/assignee/schedule authority. Preparation separately admits technician identity fields and requester contact projection, excludes internal approval notes from provider disclosure, serializes identical retries, rejects changed details and rechecks credentials/lifecycle before commit. It returns only operation identity/status and does not approve or contact Graph. The EE provider exposes nonsecret creation-target metadata, an optional durable creation identity and read-only recovery. New identified creates use a Graph transactionId plus an exact searchable extended-property marker; retries first find the existing event, ambiguous/mismatched results fail closed, and recovery never POSTs or follows arbitrary continuation URLs. Creation stops if the original Microsoft directory/organizer/invite policy changed; recovery can query the original organizer with current credentials for the same Microsoft directory. Partial creation outcomes retain the actual event receipt even if the join URL or online-meeting ID is unavailable; Graph create/lookup fetches have 30-second bounds. Existing callers without a creation identity keep their ordinary path, and CE adapters return unavailable outcomes. Microsoft documents [event transactionId retry semantics](https://learn.microsoft.com/en-us/graph/api/resources/event?view=graph-rest-1.0) and [exact extended-property event filtering](https://learn.microsoft.com/en-us/graph/api/singlevaluelegacyextendedproperty-get?view=graph-rest-1.0). Seventeen mocked provider cases pass in 169 ms; three journal cases and the affected local approval case pass in a focused source-mode PostgreSQL run of 9.84 seconds. Eight sources and the migration pass syntax-only checks. No builds, broad regressions/type checks, live migrations or Graph calls. This checkpoint does not connect Teams-enabled approval to the journal yet: create-attempt admission, durable uncertain/receipt transitions, atomic local attachment, compensation/recovery worker and Generate Teams Meeting integration remain next, followed by the previously recorded full-plan work. UI-only release gating is unchanged.

Implementation checkpoint — Teams-enabled appointment approval and creation recovery (2026-09-07): The actual approval action now enters the retained co-managed domain when Generate Teams Meeting is selected. It reserves the frozen disclosure, commits an uncertain attempt before contacting the provider, re-admits current sources and the actual credential, then holds the operation through the provider call. The actual event receipt is committed independently of local approval so credential expiry during creation cannot erase compensation evidence. Attachment rechecks the frozen request, actual assignment, source permissions, credential and lifecycle, then atomically approves the canonical allocation, creates the bound online_meetings row, stores the live join fields, and completes the journal; public results use the admitted appointment projection. Internal approval notes never enter the provider payload. Known failures retain cleanup intent and do not later auto-approve; the explicit approve-without-meeting choice remains available. The existing Teams sweep now discovers creation journals even after integration removal and includes suspended owners for compensation only. Recovery performs GET lookup using the original directory/organizer and attaches only under the still-current captured authority; changed sources or revoked authority move the recovered event to cleanup. Deletion requires the original directory and actual event identity, works without a currently configured organizer, and serializes duplicate workers. Unattempted abandoned reservations can expire; uncertain attempts with no event found remain pending without another POST, requiring a later explicit resolution path rather than guessing that creation never happened. Nine new focused PostgreSQL cases pass across source-mode runs of 10.53 and 9.79 seconds, including the actual approval action, receipt retention on expiry, lost-response cleanup, interrupted recovery, changed disclosure, revoked credential/deleted source/suspended tenant, ambiguous absence and duplicate workers. Nineteen provider cases pass in 168 ms, including two new directory-bound compensation cases. Eleven source files pass syntax-only checks; a first dynamic file-list syntax command hit sandbox child-process restrictions and was replaced with the successful explicit file list. No build, broad regression/type check, browser sweep, live migration or live provider call. Generate Teams Meeting for already-approved requests, requester notifications and artifacts, attendee reconciliation, legacy binding repair, ambiguous-operation resolution and the previously recorded full-plan work remain open. The release-v1-6-feature boundary remains UI-only.

Implementation checkpoint — Generate Teams Meeting for approved appointments (2026-09-07): The actual Generate Teams Meeting action now uses the same durable creation, receipt, attachment and recovery engine as Teams-enabled approval. A separate retained generation planner requires the current approved request, actual canonical or narrowly proven legacy allocation, current calendar permissions and unmasked fields. It takes times and all technician recipients from the actual schedule and preserves the original requested wall-clock, approval actor/time, schedule times, assignments and local notes. Generation intent is journaled as purpose=generate with only the request identity as input; the shared preparation engine freezes its admitted provider payload and relevant failed-row evidence. Existing approval fingerprints retain their prior encoding. Attachment reuses a failed Teams row only when it has no external event/meeting/link, interaction, artifact or pending synchronization evidence, and no unrelated request/allocation binding; otherwise it requires recovery. The same retained slot check now also prevents approval from creating over an unrelated meeting already tied to the allocation. A proven legacy ticket allocation is repaired to the canonical appointment work item during generation attachment. A current recovered generation attaches without approving again; a changed allocation or lost authority sends the original event to cleanup. Unavailable Teams returns an explicit failure without altering the approved request, and unavailable-provider handling still requires current actor/source admission. The action returns the admitted appointment projection and publishes an ID-only schedule update after commit. Nine new generation cases plus three affected approval/journal cases pass in focused source-mode PostgreSQL runs of 11.53 and 9.67 seconds (one affected generation-admission case was rerun after tightening its field check). Six changed sources pass syntax-only checks. No builds, broad type/regression checks, browser sweep, migration, or live provider effects. Requester notifications, artifact admission/delivery, attendee reconciliation after later edits, broader legacy provider-binding repair, ambiguous absent-event resolution and the previously recorded full-plan requirements remain open. UI-only release gating is unchanged.

Implementation checkpoint — Native appointment artifact access and transcript delivery (2026-09-07): Appointment projections now include admitted artifact metadata and an artifact-specific download URL, omitting raw provider URLs, storage identifiers and transcript document identifiers. The shared meeting reader retains every actual appointment/interaction owner, validates the actual calendar binding and private assignees, and requires full source fields because binary media cannot be selectively redacted. Transcript reads additionally retain the document's own read permission and field scope. The new artifact download route supports local transcript text, tenant-scoped local files and original-directory Graph streaming; Graph endpoints are constructed from retained creation/meeting/artifact identities instead of stored content URLs, redirects are rejected, header acquisition is bounded, and responses use private/no-store and nosniff. Content lookup runs under current source and credential locks; a prepared stream is cancelled if the final credential check expires before return. The existing recording proxy delegates co-managed requests to this admission path. Appointment UI links use the artifact route, including block-backed transcripts that the generic file-only document route could not serve. Generic document authorization also checks every meeting binding on a transcript and the document's full content scope, preventing broad document permission from substituting for current meeting-owner access. Seven focused source-mode PostgreSQL cases pass in 11.31 and 10.11 seconds: metadata projection, private/calendar binding, source and document masks, final credential expiry/discard, actual transcript HTTP delivery, actual generic block-document action, and retained reads during product lapse. Eleven changed sources pass syntax-only checks. No builds, broad type/regression checks, browser sweep, migration or live provider calls; provider-stream transport has not been exercised against Graph. This checkpoint covers native internal readers and trusted domain API-key callers; HTTP API-key/portal principal adapters, capture and refresh workers, interaction DTO/UI migration, legacy meetings without retained provider-directory evidence, broader stored-file/generic-download delivery boundaries, requester notifications and the previously recorded full-plan work remain open. UI-only release gating is unchanged.

Implementation checkpoint — Artifact HTTP API-key principal (2026-09-07): Both artifact download URLs now support an actual validated API key as well as a tracked browser session. An explicit x-api-key header selects key authentication, including when cookies are present; blank, invalid, expired or exhausted keys return unauthorized without a cookie fallback. Tenant, user and credential identity come from the validated hashed-key record, and the retained artifact consumer independently rechecks the actual key and its current narrowing rules before content delivery. User/tenant/key-id headers cannot substitute another principal or select a customer's data with an MSP key. Requests without an API key retain the browser-session path. Five focused source-mode PostgreSQL/HTTP cases pass in 9.99 seconds using the real hashed-key validator and disposable database: both URLs, explicit-key precedence/failure, API-key document content masks, revocation after initial validation, and sponsoring-MSP tenant isolation. Four changed sources pass syntax-only checks. No builds, broad regressions/type checks, live data/provider calls or migration. The artifact HTTP API-key adapter gap recorded in the prior checkpoint is closed; portal principals, capture/refresh workers, interaction projections, generic document API credential propagation and wider stored-file/download delivery boundaries, legacy provider-binding evidence, requester notifications and the previously recorded full-plan requirements remain open. UI-only release gating is unchanged.

Implementation checkpoint — Interaction meeting read projections (2026-09-07): The actual interaction page, recent and entity list actions, plus the dedicated meeting action, now use retained co-managed readers. The interaction reader admits current rows and separately admits client/contact/user labels; it applies field masks before search, filtering, sorting, totals and pagination, and checks an entity parent even when no child is returned. Meeting enrichment uses the same appointment/interaction/calendar/artifact boundary as downloads, so an interaction alone cannot expose a private or inaccessible appointment meeting. The shared artifact projector supplies the same artifact-specific URLs to both screens. Explicit public meeting/artifact view types replace the assumption that an interaction's embedded meeting contains the full storage/provider record; the interaction UI now follows admitted artifact URLs for transcripts and recordings. Current API-key scope is retained by trusted domain callers, while native actions require the actual tracked browser session. Six new focused PostgreSQL cases and the affected appointment artifact case pass in 11.90 seconds, covering all four native read paths, masked-text search/totals, multiple owners/private allocations, related labels and empty parent admission, owner filtering before pagination, and key expiry during a source lock wait. Four ordinary meeting-action tests pass in 136 ms. Eleven changed sources pass syntax-only checks. No build, broad type/regression run, browser sweep, migration or live provider call. The canonical reader currently projects admitted rows before pagination; large-list query pushdown remains an optimization to audit. Interaction mutation authorization/returned enrichment, refresh and capture paths, other direct model/API consumers, portal principals, generic document API credential propagation and broader stored-file delivery boundaries, requester notifications and the previously recorded full-plan work remain open. UI-only release gating is unchanged.

Implementation checkpoint — Retained interaction edits (2026-09-07): The actual updateInteraction action now uses an admitted co-managed command rather than the legacy model's separate connection and raw meeting-enriched return. The command accepts only writable interaction fields, retains the actual credential and operational lifecycle, locks old/proposed ticket/client/contact/user relationships before the interaction, validates current type/status/time values and applies read/update scope to both old and proposed records. Only changed fields are written; unrelated masked fields stay private in the returned projection. Standalone interactions can change ownership and local timing when both record scopes admit the change. Calendar, meeting, recorded-effort or active-clock dependencies prevent ordinary interaction edits from relabeling their ownership or changing dependent timing; linked-work operations must handle those changes explicitly. Title/notes edits preserve provider data and return the same admitted interaction/meeting projection as reads, closing the edit-response artifact bypass. The shared reader supports an exact retained identity for mutation responses. Credential expiry or a read-only lifecycle rolls back the edit, and an ID/changed-field-only event is registered after commit; no-op edits publish nothing. Six new focused PostgreSQL cases plus the affected native read case pass in 10.40 seconds, covering the actual action, forged/extra fields, private meeting response, key field scope/revocation, linked ownership/timing and input validation, old/proposed reparenting scope, final credential expiry and product lapse. Five changed sources pass syntax-only checks. No build, broad type/regression checks, browser sweep, migration or provider call. Creation/deletion, direct model/API mutation consumers, recording refresh/capture, portal principals, generic document API credential propagation and broader file-delivery boundaries, requester notifications and the previously recorded full-plan requirements remain open. UI-only release gating is unchanged.

### Native interaction creation and deletion (2026-09-07)

Implemented current-credential creation/deletion in the co-managed domain and routed the native interaction actions through it. Creation accepts operational fields only, generates the record identity, resolves a contact's client under retained parent validation, validates the owner/type/status, intersects read/create field scope, and returns the existing admitted projection. Deletion requires full read/delete scope and refuses linked meetings, calendar allocations, effort, or tracking receipts; it no longer reaches the legacy transcript/document/blob cascade for managed records. Both commands retain lifecycle and credential checks through commit and publish identifier-only events afterward. Shared classification/link checks also serve interaction edits.

Fast validation: seven focused source-mode PostgreSQL tests passed in 11.97 seconds (five new lifecycle tests plus two affected edit tests); three syntax checks and `git diff --check` passed. No build or broad regression run. Tests cover native round-trip creation/deletion, ignored forged identities/commercial/provider fields, contact/client consistency, inactive owners, linked media preservation, API field restrictions and revocation, and rollback on credential expiry for both commands. Broader regression remains deferred per the implementation-speed preference.

Still open: direct model/API/workflow mutation consumers, interaction creation workflow consequences under current authority, refresh/capture and portal meeting principals, generic document/API credential propagation and delivery leases, and the remaining original plan tasks. This checkpoint completes the native interaction form commands, not the full plan.

### Consolidated ticket queue export (2026-09-08)

Added CSV export to the consolidated working/oversight queue, closing the missing export path in implementation step 6 and part of T11. Export and pagination now share the same authorized SQL relation; export evaluates all filtered rows in one statement snapshot without a page limit or concatenated page queries. It preserves workspace/relationship/ticket identities, canonical visible fields, global ordering, literal search, current staff/grant/bundle restrictions, and read access during licensing lapse. Masked fields remain empty in CSV. The browser action uses the tracked home session and has no release-flag dependency.

The queue export control sits inside the existing UI boundary, sends applied filters without pagination, disables duplicate clicks, discards pending exports after navigation/refresh/unmount, clears stale rows after failed export, and releases the browser blob URL after download. Added three strings in eight languages and both pseudo-locales. CSV uses the existing serializer with formula protection and a UTF-8 BOM; fixed carriage-return quoting in both existing serializer copies.

Validation: five focused source-mode PostgreSQL cases passed in 8.36s, including 107-row export, filters, current redaction/revocation, lapse/session expiry, and affected mixed-queue pagination/colliding identifiers. Twelve UI/action tests passed in 1.10s and 25 CSV utility cases in 107ms. Nine syntax checks, ten-locale key checks, and whitespace validation passed. No build or broad regression run. Live browser/theme and large-export memory/load validation remain deferred. This does not complete T11: queue bulk/routing commands, additional normal-ticket filters and the remaining release requirements are still open.

### Bulk ticket handback (2026-09-08)

Added a consolidated-queue handback action for selected shared tickets on the current page. The server freezes the batch before awaiting work and invokes the existing canonical handback command independently for each qualified ticket. Each item retains current read/update permission, field restrictions, lifecycle and session through commit, with its own operation identity and expected work revision. Results distinguish successful handback, forbidden access, changed work, invalid selection and read-only lifecycle. Unexpected failures remain uncertain and can retry the exact batch against existing per-ticket receipts. Native MSP tickets and duplicate/malformed selections cannot become customer-targeted commands. No ticket copying, tenant switching or separate status lifecycle is introduced.

The UI offers only shared MSP-responsible rows with a visible work revision, identifies each by workspace/relationship/ticket, discloses the shared-note audience, freezes selections and note after uncertain submissions, and reports every returned result. Reload discards selection state and refreshes the queue. All controls remain inside the existing UI release boundary; the browser action is flag-independent. Six strings were added in eight languages and both pseudo-locales. Queue projection now exposes the admitted work revision for these commands.

Validation: six focused source-mode PostgreSQL cases passed in 8.76s (four new command cases and two affected queue cases). A field-alias refinement passed its targeted database case in 8.10s. Sixteen distinct UI/action tests passed across focused runs (15 initially in 1.11s, then the expanded seven-case action file in 397ms). Changed TypeScript parsed, ten-locale keys and whitespace checks passed. No build, full regression or live browser run. User/team assignment, configured routing destinations, other bulk operations and remaining original release requirements are still open.

### Shared ticket assignment commands (2026-09-08)

Added assignment state, paginated MSP user/team choices, and assign/clear commands for customer-owned tickets with an existing qualified MSP work reference. Both organizations use their current read/update authority. Candidate admission requires current active internal identity, technician participation and candidate read/update policy evaluated against the proposed MSP assignment and actual MSP board. Teams require an explicit technician assignment and an eligible current member. Current ticket or board collaboration must admit participation; an independent board grant remains effective after explicit ticket-grant revocation.

Commands update the MSP reference, advance the shared work revision for optimistic concurrency, and retain a customer-owned qualified audit record and operation receipt atomically. Customer ticket fields and handoff/SLA timestamps stay intact. Exact retries reuse the receipt; stale revisions and changed operation payloads fail distinctly. Ineligible current assignees do not disclose labels through state reads, while a permitted administrator can still clear the assignment. Read field restrictions also hide assignment state and revision. Final credential/lifecycle checks roll back routing, revision, attribution and receipts on expiry. Browser actions preserve the tracked home session and do not check the release flag.

Extracted the existing project-task candidate eligibility and qualified audit writers into internal shared helpers used by both domains. The task-specific public audit guard remains. Ticket and task lifecycle, storage and pagination remain separate.

Fast validation: eight source-mode PostgreSQL cases passed in 9.08s (five new ticket cases and three affected project-task cases); the additional board-scope case passed in 7.91s. Ten browser-action cases passed in 397ms. Syntax and whitespace checks passed. No build or broad regression run. The ticket assignment UI, assignment before the first handoff, assigned-work queue membership semantics, configurable destination routing and broader assignment notifications are not completed by this checkpoint; remaining original plan requirements also remain open.

### Ticket assignment UI (2026-09-08)

Connected scoped MSP assignment controls to both customer and MSP ticket panels. The component reads the current assignment and revision, loads paginated eligible technician/team choices, and supports assigning or clearing the MSP work reference. An unavailable collaborator can be cleared without exposing its stale label. Read-only views retain permitted values without loading pickers; masked state remains restricted. Until first-handoff assignment is implemented, tickets without a work reference explain that escalation is required.

Successful saves refresh the parent ticket revision. Conflicts require reload, unknown outcomes freeze the exact request for retry, and known access loss clears the ticket panel. Component and picker lifetimes discard late responses after qualified navigation or unmount. Callback identity changes do not restart reads. Stable control IDs, shared primitives and theme tokens follow the existing assignment UI. Generic assignment labels and ticket error copy are reused; two additional strings cover unavailable collaborators and the current first-handoff requirement in eight languages plus both pseudo-locales.

Validation: 23 focused source-mode UI tests passed in 2.67s, covering flag-disabled/loading/unknown/error states, qualified requests, pagination/team selection, both uncertain outcomes, unavailable-assignee clearing, read-only/masked state, conflicts/revocation, late picker responses and both parent panels. Four changed UI/test files parsed; all referenced copy keys exist in ten locales; whitespace checks passed. No builds, database rerun or live browser/theme smoke. Assignment before the first handoff, assigned-work queue behavior, configurable destinations, assignment notifications and remaining original plan work stay open.

### Assignment before the first escalation (2026-09-08)

Shared tickets on customer-approved collaborative boards can now receive an MSP user/team assignment before their first handoff. The existing assignment UI accepts revision zero and uses the same current candidate and record admission. Assignment atomically creates a qualified MSP work reference and customer work identity without changing ticket responsibility, creating a handoff, granting permanent explicit access, or setting escalation/transition timestamps. Initial work remains dependent on the board grant. Private or read-only board scope cannot be converted into collaboration by assignment.

Migration `20260908043851_allow_co_managed_assignment_before_escalation.cjs`, generated through Knex, makes the two escalation/transition timestamps nullable and constrains that state to customer responsibility with no active explicit grant. Replay is safe; rollback refuses retained work that has never been escalated. The first actual escalation fills its timestamp while preserving the assignment's work identity. Later handoffs retain that original escalation time. Handback and explicit revocation clear active MSP assignee fields, preserving routing/history references.

Working-queue membership now includes either MSP responsibility or a visible MSP assignment. Assignment-only customer work appears in the queue without implying MSP responsibility or an SLA start. Clearing assignment removes that work from the working queue; oversight remains governed by grants. Assignment field restrictions apply to membership/counts and therefore exports. Removing board sharing removes live access even when an assigned reference still exists.

Validation: eleven focused source-mode PostgreSQL cases passed in 9.85s (eight new pre-handoff cases plus affected canonical handoff, assignment and queue cases). Thirteen UI tests passed in 2.48s, including revision-zero assignment. Five source/test syntax checks, migration syntax and whitespace checks passed. The disposable harness now applies 48 feature migrations; no migration was applied to the running development database. No build or broad regression run. The independent MSP SLA consumer/engine, configurable routing, assignment notification handling, Citus/live validation and other original release requirements remain open.

Implementation checkpoint — independent SLA engine (2026-09-08): added policy-owner/source-qualified obligation identity, separate transaction-scoped locking, and durable organization SLA state with per-event receipts. The clock preserves fractional minutes, breach timestamps, calendar snapshots, and independent pause reasons across handbacks; only MSP requester/shared-IT responses satisfy first response. Resolution stops timing without fabricating a response, and reopening has a distinct obligation identity. Calendar calculations reuse existing business-hours segmentation. Migration 49 adds owner-scoped obligations/history with no foreign customer-ticket FK, replay-safe setup, and history-protecting rollback; metadata/deletion ordering include both tables. This is an internal engine, not an authorization entry point: current-source/credential admission, explicit priority mapping and MSP policy resolution, handoff/comment/resolution adapters, reopen admission, backend scheduling, and indicators/notifications remain unfinished. Validation uses nine Node source-mode clock cases and four focused disposable-PostgreSQL store cases; no builds or broad regression suite.

Implementation checkpoint — SLA handoff integration and setup (2026-09-08): first actual escalation now creates the MSP obligation atomically with customer responsibility, the explicit grant, queue reference, and handoff receipt. Handback/revocation pauses the retained obligation; re-escalation resumes its original target/calendar snapshot even if mappings or policy targets later change. Missing mapping, policy, or target fails setup without partially escalating; historic work lacking an obligation requires reconciliation rather than receiving a fabricated fresh deadline. Current tracked session and lifecycle are rechecked after the handoff receipt. Migration 50 adds MSP-owned priority mappings, and the tracked sponsor-administrator configuration API retains current relationship revision, permissions, session, workspace suspension, and licensing checks. The MSP access editor links to a localized mapping screen, and customer handoffs distinguish setup failure from uncertain outcomes. The new page/link/controls use `release-v1-6-feature` only through the UI boundary; backend SLA/configuration has no flag checks. Policy precedence and calendar loading were extracted from native SLA without changing its behavior. Validation: 19 distinct focused PostgreSQL cases across two runs, 38 native SLA source-mode cases, and 41 UI/action cases passed; no builds or broad regression run. MSP response/resolution adapters, genuine reopen lifecycle, historical reconciliation, priority/policy changes to active obligations, job/Temporal dispatch, notifications, and SLA indicators remain unfinished.

Dependency correction for this checkpoint: the clock, store, organization lock, and policy resolver live in `shared/lib/sla`; the SLA package retains compatible re-exports. This avoids introducing a dependency cycle through SLA notifications and co-managed code. Shared build entry points/exports include the primitives. After relocation, 47 native/organization source-mode cases and two targeted PostgreSQL handoff/configuration cases passed again. Production builds and live theme/browser/Citus checks remain deferred.

Implementation checkpoint — MSP SLA replies, closure, and reopening (2026-09-08): admitted shared-comment creation now records first response from the actual persisted MSP actor reference and effective root/reply audience in the same transaction. Customer messages and MSP-private notes do not satisfy it; retries and subsequent replies preserve the original response. Canonical optimized and legacy customer ticket close paths finish still-shared MSP obligations, including paused handbacks, without rewriting customer SLA columns. Fully revoked ticket visibility freezes the MSP record; explicit re-sharing of an already closed ticket resolves at that new visibility boundary without reading private-period closure time. Genuine reopen transitions create a new obligation only while MSP responsibility continues; customer-responsible reopening waits for escalation. Missing new-period setup rolls the reopen back and has localized feedback in the shared editor. Comment and shared-edit commands recheck the initiating session after their final receipts. Validation: 17 distinct targeted PG cases across focused runs and 24 UI/action cases passed; no build, full suite, or live browser run. Remaining SLA work includes other direct model/API/workflow writer adapters, active priority/policy and response-state pause changes, historical reconciliation, backend jobs/Temporal, indicators, and notifications.

Implementation checkpoint — scheduled MSP SLA observation (2026-09-08): a minute-based organization SLA maintenance job now runs through CE pg-boss and EE Temporal's existing shared maintenance fanout. Tenant discovery selects policy owners with unfinished obligations. The worker scans due targets in keyset pages and revalidates lifecycle, current relationship, canonical ticket/work, and visibility in each transaction before taking the organization lock and choosing database time. Completed, paused, handed-back, revoked, detached, suspended, wrong-owner, obsolete-generation, and read-only work cannot advance. Concurrent workers preserve one breach event; revoked/deleted historical candidates do not starve later candidates. Breaches preserve their original due time and do not change customer SLA columns. Validation: nine distinct focused disposable-PostgreSQL cases passed across two runs (8.58s and 7.81s), 23 maintenance fanout unit cases passed (2.33s), and nine changed source files passed syntax checks. A stale Teams fanout expectation was corrected for already-implemented retained meeting recovery. No production build, full regression suite, live scheduler/Temporal worker, browser, or Citus run. Notification thresholds/delivery, SLA indicators, other direct writer adapters, active policy/priority and pause changes, historical reconciliation, and remaining plan requirements are still outstanding.

Implementation checkpoint — customer/MSP SLA indicators (2026-09-08): the admitted co-managed ticket screen now returns separate, minimized SLA displays for the canonical customer's timing and the MSP's latest qualified obligation. Customer ticket fields retain their original meaning; MSP reads project the existing shared clock without writing observation events. Outcomes distinguish pre-escalation, missing configuration/history, running, paused, breached, completed, and missing response. Current ticket RBAC, source/derived field redactions, relationship/grant checks, final session expiry, and read-only behavior apply. MSP policy identifiers/names, mappings, calendar details, and private pause reasons are omitted. Fully revoked foreign readers are denied; customer readers retain the paused shared timing without advancing it. The existing shared/local co-management panel presents organization names, per-target outcomes, and localized completion/due dates using theme-aware primitives inside the existing UI flag boundary. Fifteen new labels exist in eight languages and both generated pseudo-locales, without changing the existing SLA setup copy. Validation: seven distinct focused PostgreSQL display cases passed across runs (six initial passes and the corrected revoked-error expectation recheck), 22 UI/setup-panel cases passed in 1.03s, five source files passed syntax checks, and locale keys/interpolations passed validation. No production build, full regression suite, or live browser/theme/Citus run. Indicators refresh with the existing ticket screen load/reload; consolidated queue SLA presentation, scheduled/live UI refresh, warning/notification delivery, other direct writer adapters, active policy/priority and pause changes, historical reconciliation, and remaining plan requirements are still outstanding.

Implementation checkpoint — durable MSP SLA threshold crossings (2026-09-08): organization SLA events now capture pending warning/breach notices in the same transaction, using the MSP policy's existing notification thresholds and a retained configuration snapshot. Notice identity is unique per obligation/target/percentage and references the exact source SLA event. Missed thresholds are retained together; retries/concurrent observations deduplicate them; a late actual shared reply captures its breach before any timer run. Paused or completed targets cannot gain notices from later unrelated observations/configuration changes. Exact-deadline completion remains met. The existing CE/EE observer also discovers configured warning candidates before their due date, evaluating elapsed business time through the shared clock; revoked and read-only work still cannot advance. Migration `20260908062358_create_organization_sla_notification_events.cjs` was generated through Knex, adds tenant distribution/metadata/deletion ordering, replays safely, and refuses retained-history rollback. No live database migration was run. Validation: 15 distinct focused PostgreSQL cases passed across a 14-case scheduler/threshold run (9.26s) and an actual reply case (7.93s); six changed source files passed syntax checks. No build, full suite, or live scheduler/Citus run. This checkpoint captures durable notification inputs only: recipient qualification/fanout, delivery receipts, inbox rendering, channel transports, and notification recovery are still required before SLA notification delivery is complete. Queue indicators, active SLA policy/priority/pause changes, remaining writer adapters/reconciliation, and the rest of the plan remain outstanding.

Implementation checkpoint — qualified SLA recipients and in-app delivery (2026-09-08): SLA fanout now resolves MSP-local assignee/team, board-manager, and escalation-manager candidates from the qualified routing reference. Saved threshold flags/channels and current configuration must both permit background delivery, and each candidate must independently retain current trust, staff membership, ticket read policy, and source-field visibility. In-app notices use existing localized templates/preferences/priority and shared durable in-app/Teams/push channel tasks. New per-event/recipient/channel receipts deduplicate creation and retain email obligations separately as pending; fanout completion does not mean email was sent. Stored SLA notices bind recipient, durable event/obligation, qualified resource, and template metadata; inbox and channel readers share one dispatcher that rebuilds current authorized content and never falls back to cached shared text. Removed metadata remains classified through its receipt. Inbox counts/pagination respect visibility, historical inbox reads require the actual browser session, and background delivery additionally rechecks current assignment/manager/threshold eligibility. Delayed warnings are suppressed when completed, paused, or already breached; late-response breach outcomes remain deliverable. Maintenance discovers pending notices even after every clock completes, and one failed clock does not block processing previously captured notices. Migration `20260908063356_create_organization_sla_notification_recipients.cjs` was generated with Knex and includes distribution, metadata, source FK/state checks, deletion ordering, replay, and retained-history rollback refusal. Validation: 16 distinct focused PostgreSQL cases passed across runs (14 SLA cases and two existing comment/inbox regressions), 24 maintenance unit cases passed in 2.29s, and 13 changed source files passed syntax checks. Later receipt tightening was rechecked in two PostgreSQL cases; obsolete-warning suppression passed its focused case. No production build, broad suite, live transport/browser/Temporal, or Citus run. Email send/recovery, notification workflow effects and wider consumer validation, queue indicators/live refresh, other SLA writer/policy/pause/reconciliation work, and remaining plan requirements are still outstanding. The preceding threshold milestone's lane fact remained unverified after board-service timeouts; its committed plan checkpoint retains the decision.

Implementation checkpoint — SLA email delivery and retry recovery (2026-09-08): pending SLA email receipts now run through a source-qualified delivery worker that reloads current assignment/manager eligibility, trust, ticket field visibility, recipient address, tenant/category/subtype settings, and user preferences on every attempt. A stable per-event/recipient Message-ID survives retries. Database/transport failures remain retryable with capped backoff/provider retry hints; permanent failures and exhausted attempts retain terminal state, while revoked/disabled recipients are skipped. In-app receipt state is independent. Existing tenant/system SLA email templates are loaded with the recipient locale, and admitted values are rendered with HTML escaping plus plain text/subject handling before the existing tenant email service sends with caller-owned retries and automated-mail headers. Unexpected generic queueing is rejected. Warning delivery projects current elapsed time/deadline without rewriting the immutable crossing; obsolete warnings remain suppressed. CE/EE maintenance discovers pending email even after clock/fanout completion and attempts retained sends despite failures in another maintenance stage. Migration `20260908065525_add_organization_sla_email_retry_state.cjs` was generated through Knex, adds retry fields/index/state checks, replays safely, and refuses removal of retained email state. Validation: 11 distinct targeted PostgreSQL cases passed across runs, 25 maintenance unit cases passed in 2.31s, five transport cases passed in 0.40s, and seven changed source files passed syntax checks. The tenant-settings fixture was corrected to respect its existing non-unique tenant column. No production build, full suite, live mail/provider/Temporal/browser, or Citus test was performed. Notification workflow effects, wider channel/consumer validation and terminal-failure operator recovery, queue indicators/live refresh, active policy/priority/pause handling, remaining writer adapters/reconciliation, and the rest of the plan remain outstanding. The earlier threshold lane fact was subsequently verified as persisted (`e27f3605-6a31-42df-88ae-28c4ee5071e5`).

Implementation checkpoint — MSP awaiting-client SLA pause (2026-09-08): the independent MSP obligation now uses the MSP's existing `pause_on_awaiting_client` setting and requires both tenants' response tracking to be enabled. It reads the actual canonical response state inside the source transaction, retains source and organization locks before its timestamp, and does not publish new clock facts during revoked/private, suspended, terminated, or read-only periods. The separate `awaiting_client` reason composes with `customer_responsible`; clearing one preserves the other, elapsed time, and sticky breaches. First escalation and genuine reopening can start paused at zero elapsed; re-escalation establishes the response pause before removing handback's pause. Shared comment creation records an actual MSP first response before pausing. Native legacy/optimized response-state edits, optimized comment creation, and scheduled customer publication are connected. Customer SLA columns remain independent. SLA display and notification readers now hide derived timing when response state is redacted. Validation: 18 distinct focused PostgreSQL cases passed across two runs (the initial event-column assertion was corrected), including actual production shared comment rollback, native edits, scheduled publication, re-escalation/reopen, settings, access boundaries, and timing redaction. Runs took 10.58s and 11.36s; eight source files passed syntax checks and `git diff --check` passed. No production build, broad suite, live worker/browser, or Citus run. Remaining SLA work includes requester portal/inbound email and other direct writer adapters, explicit MSP-owned configuration for customer status pauses, active settings/policy/priority reconciliation, queue indicators/live refresh, notification workflow effects and recovery; the overall plan remains incomplete.

Implementation checkpoint — requester and customer reply SLA adapters (2026-09-08): generic native comment creation now reconciles the MSP awaiting-client pause after its actual response-state mutation. The shared qualified email admission adapters apply the same effect inside the retained reply transaction: requester replies clear awaiting-client, while customer-technician public replies pause without counting as an MSP first response; private/shared-IT replies preserve their existing response-state behavior. Late token rejection rolls back SLA state together with canonical comments and outbox writes, and terminal inbox replay does not create another clock event. Actual requester portal replies now clear the pause; portal close/reopen invokes the independent MSP obligation adapters. Those two portal mutation paths now retain operational lifecycle admission before writing and recheck before transaction completion. Explicit co-managed/licensing portal dependencies were added without changing unrelated lockfile edits. Validation: nine targeted source-mode PostgreSQL cases passed in 12.11s, two actual late-token rollback regressions with SLA assertions passed in 9.08s, four changed source files passed syntax checks, and `git diff --check` passed. No production build, broad suite, browser/provider transport, or Citus run. Legacy integration email actions, direct model/API/workflow/bundle writers, portal event publication and other portal mutation lifecycle paths, active SLA settings/policy/priority reconciliation, explicit MSP-owned customer status pause configuration, and the remaining plan requirements remain open. The preceding pause milestone's workflow-board fact could not be verified because both add and read requests timed out; its committed checkpoint remains the durable local record.

Implementation checkpoint — direct ticket API SLA transitions (2026-09-08): the direct TicketService update path now retains the canonical ticket and selected statuses through mutation, applies the independent MSP close/reopen and awaiting-client adapters inside its source transaction, and rechecks lifecycle before completion. Update schema validation accepts only the two supported response states or null. API close clears response state even when a conflicting value accompanies the close; returned closure fields reflect the actual persisted row. Response changes participate in existing update/event tracking. Missing reopen mapping or a later source failure rolls back both canonical fields and SLA history; private changes after full grant revocation leave MSP history frozen. Validation: four focused PostgreSQL API cases passed in 10.21s, two API source files passed syntax checks, and `git diff --check` passed. No production build, broad suite, live event-consumer/browser, or Citus run. Existing native API event transport is not yet generalized into the qualified ticket event pipeline; remaining direct model/workflow/bundle writers, active configuration reconciliation, explicit MSP-owned customer status pause settings, and the rest of the plan remain outstanding. The preceding reply-adapter milestone's workflow-board write timed out; local committed checkpoints retain both milestones.

Implementation checkpoint — bundle child-reply SLA reopening (2026-09-08): the shared child-reply helper now retains operational lifecycle admission and the child link, locks the master before reading its current status/settings, and chooses an open status only from the master's actual board. Genuine reopening invokes the independent MSP SLA adapter inside the same transaction; concurrent sibling replies create one reopening/generation. Handed-back or fully revoked master obligations remain retained without restarting; missing setup rolls back the master change and originating child comment together. Reopen audit records the actual previous closure timestamp. Validation: five distinct focused PostgreSQL cases passed; the five-case run took 11.06s, and two further checks after retaining the child link and explicitly creating a competing-board status passed in 10.21s. Syntax and `git diff --check` passed. No production build, broad suite, browser/provider transport, or Citus run. Native sync-updates bundle propagation still uses a direct child update and needs full per-child lifecycle/SLA handling; broader bundle authority/lock ordering, direct model/workflow writers, active settings/policy reconciliation, explicit MSP-owned customer status pause configuration, and the rest of the plan remain outstanding. The preceding API milestone's workflow-board write timed out; its committed checkpoint is the durable local record.

Implementation checkpoint — per-child bundle propagation lifecycle (2026-09-08): native sync-updates propagation now retains children in deterministic ticket order and updates each canonical row with its own close/reopen boundary. Each child gets aligned `is_closed`, closure attribution/timestamps, response-state clearing on close, close-rule validation, audit, and independent MSP SLA completion/reopen. A child's missing reopen mapping or unmet close gate rolls back the master and all child work in the source transaction. Revoked child obligations remain frozen. Copying a status from another board fails rather than persisting an invalid child status. Child assignment propagation uses the existing resource-reassignment engine so promotion preserves other additional agents. Master-owned close notification behavior remains intact; system propagation follows the existing primary path's live-update suppression. The update core rechecks lifecycle before transaction completion. Validation: seven distinct focused PostgreSQL cases passed (five in 10.70s and two corrected checks in 11.39s), covering the existing foreign-actor boundary, per-child clock generations, missing setup, revoked history, board status, close gates, and resource promotion. The focused checks caught a missing helper import and a test expecting a thrown error instead of the public action's structured error; both were corrected. Syntax and `git diff --check` passed. No production build, full regression suite, live browser/event consumers, or Citus run. Broader bundle authority and lock-order auditing, priority-change SLA reconciliation, direct model/workflow writers, active SLA configuration, and remaining plan requirements stay open. `ticket-close-transition` markers/ledger record the shared lifecycle work without recursively duplicating master-owned notifications.

Implementation checkpoint — workflow ticket field mutation admission and SLA (2026-09-08): `tickets.update_fields` now enters a composed co-managed transaction adapter before reading/mutating canonical work. It retains the actual running workflow/version and lease, resolves that version's current customer actor, checks source and resulting ticket read/update scope and requested/returned field visibility, and rechecks lifecycle/run validity after the write and SLA effects. Missing runtime composition rejects co-managed mutations. App and workflow-worker roots register the adapter through the shared registry; the co-managed package exposes/builds its dedicated runtime entry without adding a shared-to-co-managed cycle. Workflow field updates accept the supported response-state values. The shared ticket model now retains its source row and applies canonical close/reopen fields, including clearing response state on close; the composed workflow adapter applies independent MSP response pause and close/reopen effects and records gated workflow close-rule bypass. Missing SLA setup or late run expiry rolls back canonical rows, clock events, and run audit together. Validation: 12 targeted PostgreSQL cases passed across 11.64s and 10.34s runs, two workflow-worker startup cases passed, three existing model board-status cases passed after their row-lock mock was updated (0.374s run), and nine source files passed syntax checks. No production build, broad regression suite, live worker/browser, or Citus run. Dedicated workflow close/assignment and other operational actions, direct model callers without this admission adapter, native event/notification effects, active SLA configuration reconciliation, and the rest of the plan remain outstanding. The workflow authority duplication is recorded in the leverage ledger and inline markers.

Implementation checkpoint — workflow assignment admission (2026-09-08): `tickets.assign` now uses the composed co-managed workflow mutation adapter before source reads, including no-op returns and optional comments. Current run/version/actor, lifecycle, source and resulting record scope, assignment/comment fields, and the action's returned fields are checked. The registry now accepts explicit `readFields` per action; assignment does not require unrelated priority visibility, while field updates retain their existing output restrictions. User, team-manager, team-member, queue-member and explicit additional-user choices retain their actual rows through assignment. Existing assignment reconciliation and no-op behavior preserve resources and the independent SLA obligation; optional public comment publication remains transactional and is not duplicated by an already-assigned retry. Validation: 13 focused PostgreSQL cases passed in 12.97s (11 assignment cases plus two field-update redaction regressions). Thirteen existing workflow board/status unit cases passed across runs; their transaction fake was updated for the admitted transaction boundary, and a stale closure-comment expectation now requires the existing transactional publisher. Three changed source files passed syntax checks, and `git diff --check` passed. No production build, full regression suite, live workflow/email transport/browser, or Citus run. Dedicated workflow close and its qualified notification delivery, other operational actions, broader assignment eligibility/lock auditing, direct model callers outside admission, active SLA configuration, and remaining plan requirements are still open. The preceding field-mutation milestone's workflow-board write timed out; its committed checkpoint remains the durable local record.


Implementation checkpoint — admitted workflow closure and durable requester email (2026-09-08): `tickets.close` now retains the executing workflow version/author, lifecycle, canonical source and selected statuses, close/resolution/comment field authority, and returned-field visibility. Closure clears response state and applies the independent MSP resolution inside the same transaction; the existing gated workflow close-rule bypass is audited once. Co-managed requester email is recorded as an owner-qualified closure command before final lease/lifecycle checks, so any failure rolls back the ticket, SLA, run audit, and email intent together. The new migration was generated through Knex; its distributed tenant table has metadata and tenant-deletion coverage and refuses to discard retained history on rollback. Recovery rechecks the originally selected contact/client, current contact activity/address and board visibility, current author/version/field permissions, tenant notification gates, and the same still-closed ticket generation. A completed workflow can deliver a committed command without an execution lease; canceled runs cannot. Email uses safe canonical template substitution, stable Message-ID, bounded caller-owned retries, and completion receipts. The normal notification maintenance fanout discovers these commands; recovery stages report aggregate failures while continuing independent committed work. Ordinary PSA workflow email behavior remains compatible. Validation: 21 focused source-mode PostgreSQL cases passed in 12.58s (19 closure/email cases plus field-update/assignment compatibility), 42 workflow/transport/fanout unit cases passed in 2.43s, and two independent recovery unit cases passed. All email sends were mocked. No production build, broad regression suite, live workflow/provider/browser, or Citus run. Broader qualified workflow ticket lifecycle events, requester reply credentials for closure emails, other operational workflow/model writers, active SLA configuration reconciliation, operator recovery for terminal email failures, and the remaining plan requirements remain open.


Implementation checkpoint — MSP time work references and normalized billing context (2026-09-08): added an MSP-owned time work reference with a qualified customer tenant/relationship/source kind/source ID, the relationship's MSP client, an optional local billing profile, and retained permitted descriptive evidence. Registration requires current shared read/collaboration access, local time-entry read/create authority, source-field visibility, and final session/lifecycle checks; registration alone is not archive participation. A dedicated `co_managed` time work kind addresses the local reference rather than fabricating tickets or reusing foreign IDs as local ticket keys. The Knex-generated migration adds a tenant-local time-entry/reference FK and matching-ID/type checks, metadata and deletion ordering, and refuses to remove retained evidence. The existing contract and catalog time loaders now consume a common normalized work join keyed by owner/type/id. Shared tasks have no fabricated MSP project/phase identity, and invoicing reads retained local evidence instead of live customer tables. Time-charge snapshots and renderer grouping preserve customer tenant/relationship/source provenance, including identical IDs across native and customer work. Validation: seven distinct targeted PostgreSQL cases passed across runs (including actual catalog charge generation and already-invoiced exclusion, concurrent reference registration, current permission/redaction checks, revocation retention, task context, and native UUID collisions); twelve focused time-compute/snapshot unit cases and five invoice-grouping unit cases passed in sub-second runs. Tax and contract selection were stubbed only in the catalog loader integration case; no invoice was posted. Source checks and `git diff --check` passed. No production build, broad regression suite, full contract-backed invoice run, browser, or Citus run. This establishes the billing/reference layer: MSP shared-time create/edit/read/approval/timer/API/UI wiring, local billing profile selection and effort totals, participation archive completion, downstream invoice evidence retention validation, and the remaining plan requirements are still open.


Implementation checkpoint — native MSP shared-time commands (2026-09-08): the existing browser and API time writers now admit MSP-owned `co_managed` work references using actual home sessions or API credentials, current local source/time permissions and field visibility. New contributions additionally retain live customer collaboration/read authority; permitted descriptive evidence and the time write commit together, including final credential/lifecycle checks. Existing MSP effort is readable/editable/reviewable after customer revocation under current home permissions without fetching newly private customer content. The writer derives the local reference FK, uses its MSP client/profile for native contract selection, and resolves bucket/hour-block work through that client. Detail, collection, timesheet, approval and deletion guards recognize commercial shared work on ordinary PSA tenants. Approval retains native transitions and invoiced-evidence protection; draft deletion preserves the customer source and retained reference. The shared-source engine accepts authenticated API principals without synthetic sessions. Validation: ten distinct focused source-mode PostgreSQL cases passed across three runs (latest approval/deletion run: two cases in 8.91s), including native/API create/edit/read, timesheet projection, late credential rollback, revoked API credentials, financial field restrictions and affected native/shared-authority regressions. Twenty changed source files passed syntax checks; whitespace checks passed. No production build, broad regression suite, live browser, invoice posting or Citus run. Shared-time UI/registration actions, timers, billing profile selection, full API contract/bucket/hour-block parity, combined effort totals, downstream invoice evidence validation and the remaining plan requirements stay open.


Implementation checkpoint — shared-work native timers (2026-09-08): MSP timers now store the same owner-local qualified time-work reference as manual entries. A Knex-generated migration extends the existing timer kind constraint and adds an explicit matching reference ID plus tenant-local FK; source identity remains protected by the existing immutable clock trigger, and rollback refuses retained shared clocks or completion receipts. An actually started, owned clock admits access to retained MSP evidence after customer sharing ends, while fresh timers still require live collaboration. Stop uses the existing completion engine and immutable receipt, preserving original commercial mode/service and preventing duplicate time or changed-payload retries. Current home credentials, time/source scope and financial field visibility remain required. The shared commercial save-field check was factored for browser, API and timer callers. Existing timer metadata and deletion order already place clocks before their reference rows. Validation: five focused Node/source-mode PostgreSQL cases passed in 10.26s (three new shared-clock cases, existing operational completion and manual shared financial restriction). Cases cover concurrent/replayed stop after revocation, current API-key and field restrictions, cancel, guessed source IDs, local FK retention and migration replay/rollback refusal. Six changed source files and the migration passed syntax checks; whitespace checks passed. No production build, broad suite, live browser/provider, Citus or posted invoice run. Timer UI and shared work registration actions, full API/native billing parity, profile selection, effort totals, archive/departure and remaining original requirements stay open.


Implementation checkpoint — shared ticket/task time-entry UI (2026-09-08): shared MSP ticket and task screens now offer Log time and open the existing native service/contract/time form using an admitted MSP-local work reference. The browser registration action retains the tracked home session, rejects API overrides and applies domain authority without release-flag checks. The control and form are inside the UI-only release boundary and belong to the qualified screen; navigation, write-access loss or flag changes discard pending preparation and unmount private draft content. Task editor state now exposes actual write admission independently of which editable task fields remain visible. Native time preparation was factored from drawer launching so both callers use the same current user, period and timesheet logic. Save failures propagate to the native dialog instead of producing a false success. The form resolves the current MSP client through shared source/time admission; existing-entry lookups retain actual owner/sheet/work identity and can resolve authorized retained billing context after revocation, while new work requires live collaboration. Current client/billing field restrictions apply. Initialization errors are visible in the form. Two new copy keys exist in eight languages and both pseudo-locales. Validation: six distinct focused source-mode PostgreSQL cases passed across runs (two browser/client cases plus four affected task-authority cases); 45 distinct UI/preparation/provider cases passed across focused runs. The latest two PostgreSQL cases took 9.20s, the button/provider run 1.45s and preparation run 658ms. Sixteen source files passed syntax checks and whitespace checks passed. The full translation validator reports 774 existing missing keys elsewhere; neither new key appears in those errors and both are present in all ten locales. No production build, broad test suite or live browser/theme smoke. Native timer UI, local billing-profile selection, full API/native billing parity and invoice proof, organization effort totals, archive/departure and other original requirements stay open.


Implementation checkpoint — common native/API time billing (2026-09-08): replaced the API billing stub with the same transaction-bound contract selection used by manual time saves. Selection reads the time owner’s local work client/profile, validates local services and explicit contract lines, resolves profile ambiguity, preserves still-eligible API selections on duration-only edits, and reselects when work/service/date changes. API writes now accept an optional contract-line selection. Candidate loading was separated from browser authentication so writes keep their actual retained connection; inactive contracts/lines are excluded, and bucket membership/catch-all coverage works without a duplicate contract-service row. Extracted the native reverse-old-before-draw-new bucket sequence and best-effort hour-block savepoint logic for both writers; operational time bypasses commercial selection/allocation. Timer completion uses that same API save engine. The prepaid engine’s canonical client resolver now shares a lower-level owner-local work context with scheduling, and nightly reconciliation includes qualified shared references. Shared runtime/package entry points are declared. Validation: 14 distinct focused source-mode PostgreSQL cases passed across runs (nine affected native/API/timer/deletion cases plus five billing cases); the five new cases exercise actual manual/API/timer contract selection, explicit selection/service rejection, weighted bucket edit/non-billable/delete reversals, prepaid edit/delete and nightly reconciliation, profile narrowing and inactive-line rejection. Four existing contract-selection UI cases passed in 2.09s; their expected optional entry-identity argument was updated. Thirteen changed source files passed syntax checks and whitespace checks passed. The checks found the prepaid engine’s native-only client resolver and a fixture missing its required default profile; both were corrected. No production build, broad regression suite, posted invoice/PDF, live browser or Citus run. Full contract-backed invoice posting and retained downstream evidence, broader native cross-client move/concurrency/eligibility lock audits, billing-profile UI, timer UI, organization effort totals, archive/departure and the other original plan requirements stay open.

Implementation checkpoint — organization effort totals (2026-09-08): shared ticket and task screens and customer/shared project pages now show customer, MSP and permitted combined completed-time totals. A transaction-bound reader admits current work access, sums actual elapsed minutes directly from each owner's time entries in one SQL statement, and uses qualified MSP references. Project rollups check actual task membership and current child access; cached native actual-hours values remain untouched. Non-billable and unapproved time still contributes, while running clocks, private notes, authors, rates, contracts and approval details are absent from the response. Source field restrictions hide derived totals, and a hidden organization total also withholds the combined value. Revoked MSP work access denies the read; the customer retains its own total but cannot read ongoing MSP effort after sharing ends. The browser adapter binds the actual home session, and the UI-only release boundary prevents fetching or presenting the feature while disabled. Totals refresh manually and after successful shared-time saves; navigation and denied reloads discard previous values. Eight copy keys exist in all ten locales. Validation: six focused Node/source-mode PostgreSQL cases passed in 9.75s, covering actual native edits/deletion, non-billable/approval independence, revocation/expiry, field restrictions, task/project rollups and polymorphic ID collisions. Forty-four distinct focused UI cases passed across a 43-case run (1.41s) and the added save-callback check with its nine existing cases (887ms). Fifteen changed source files passed syntax checks, and locale interpolation and whitespace checks passed. No production build, broad regression suite, live browser/theme or Citus run. Customer native task-form composition, automatic refresh following native customer edits, broader task move/scope/concurrency checks, timer controls, invoice evidence validation and the remaining plan requirements stay open. Continue using focused Node dev/source-mode checks; defer broad builds and most regression coverage until the implementation is ready for the later review pass.

Implementation checkpoint — customer native task effort (2026-09-08): the native task form now receives the same organization effort view through a project composition slot, mounted above page content and the shared drawer outlet. Only resolved co-managed workspaces supply the slot; PSA/AlgaDesk and task creation retain their existing behavior. A local-task action resolves the relationship and source from the actual customer browser identity, ignores forged tenant/relationship extras, and rejects API overrides or mismatched sessions. Effort controls use distinct resource IDs and explicit non-submit buttons inside task forms. Aggregate state now belongs to the authenticated home user/tenant as well as the work identity, clearing on session/workspace changes even when local IDs collide. Validation: 15 distinct focused UI cases passed across the native form/provider/effort runs (latest eight effort cases: 767ms); two focused source-mode PostgreSQL cases passed across runs, including the actual browser adapter and task movement into an unshared project. The move fixture needed a destination mapping and a nonconflicting native status order; its final run passed in 9.10s. Ten source files passed syntax checks and whitespace checks passed. No production build, broad regression suite or live browser/theme run. Native task totals can be manually refreshed or reloaded on reopening the form; no separate time writer or aggregate cache was added. Timer controls, full invoice evidence validation, departure/archive/export/restore, independent upgrade and remaining original requirements are still open.

Implementation checkpoint — relationship closure transaction (2026-09-08): added an internal transaction boundary shared by explicit departure and independent-upgrade orchestration. It validates the current tracked administrator from either organization, locks in the existing entitlement/sponsor/relationship/customer order, and accepts security reduction after licensing grace expires. A mandatory trusted archive finalizer runs with a fixed operation identity and cutoff while live authority is retained. Only after it returns does closure pause existing MSP SLA obligations, terminate the relationship, release its allocation and store an immutable MSP-owned receipt. Concurrent identical retries return one receipt without re-finalizing evidence or releasing seats twice; conflicting/stale requests fail. Archive errors or late session expiry roll back all effects. Closure itself never grants PSA entitlement or seeds commercial capabilities. The Knex-generated receipt migration has tenant distribution/metadata and deletion ordering, no customer/source/user FK, replay-safe setup and retained-data rollback refusal. Validation: all six focused source-mode PostgreSQL cases passed in 9.36s, including both organizations, concurrent replay, actual retained MSP time reads, permission loss, expired license grace, finalizer/session rollback, migration replay/refusal and actual SLA pause/observer behavior. Five source files and the migration passed syntax checks; whitespace checks passed. No production build, broad suite, live migration or Citus run. This is an internal orchestration primitive, not a finished departure feature: the tests use explicit transactional finalizer stand-ins. No public termination action/UI has been exposed. Production participation capture, permitted content/file archival, archive readers, portable export/restore, independent entitlement/upgrade orchestration and the remaining original requirements are still required before departure is usable.

Implementation checkpoint — durable participation evidence (2026-09-08): actual ticket handoffs and shared work audit writers now append MSP-owned evidence in the same transaction as the canonical command. The recorder reads the exact qualified persisted event and copies an explicit allowlist of already-shared history and captured author attribution, rather than caller-supplied history or ticket attributes. Handoffs and MSP contributions establish participation; customer-only task edits and oversight do not. Customer activity can continue an already-participated resource through an actual routing/assignment reference or previous evidence. Task assignment and withdrawal retain shared assignment history. Evidence is qualified by both tenants, relationship, source kind, work kind and source identity; retries compare a content hash and never overwrite a prior capture. A Knex-generated migration adds immutable rows, tenant distribution/metadata and owner deletion ordering, with no FK to customer/source/actor/client records and a retained-data rollback guard. Validation: four focused source-mode PostgreSQL cases passed in 8.90s, using actual handoff/edit/assignment commands, replay, unsharing, real ticket/task deletion, private-field exclusion, immutable-row enforcement, migration replay/refusal, and a late session failure rolling back both work and capture. Six source files and the migration passed syntax checks; whitespace checks passed. No production build, broad regression suite, live migration or Citus run. This establishes handoff/assignment/work-edit evidence only: time and conversation participation adapters, full permitted content and file capture, capture at all grant-reduction boundaries, archive readers, the production closure finalizer, portable export/restore and independent upgrade remain required. No public departure action is exposed yet.

Implementation checkpoint — completed-time participation (2026-09-08): the native manual and API time writers now retain participation from an actual completed MSP-owned entry and its qualified local work reference. Timer completion uses the same API insertion path. Registration, opening a form, ordinary reads and running clocks create no evidence. Admitted moves capture the old source before mutation and the new source afterward, including older entries that predate capture. The first proof preserves attribution and permitted retained title/number through later edits, staff renames and deletion; it omits timesheet notes, elapsed/billable details, service/rate/contract/profile and approval fields. The adapter only reads MSP-owned records, allowing already-started timers to finish after unsharing without fetching newly private customer content. Existing native/API authority and final credential checks still enclose all evidence writes. The qualified immutable append/hash store was factored for event and time adapters; a Knex-generated migration adds the time source kind and refuses rollback once time evidence exists. Validation: six distinct focused source-mode PostgreSQL cases passed across the initial five passes and corrected API-move recheck (11.18s). These use actual manual/API/timer commands and cover no proof for registration/running clocks, one proof on repeated completion, retained attribution/deletion, private-field exclusion, both sides of an API move, post-revocation completion, late-session rollback and the existing assignment adapter. The move fixture needed its generated ticket search column omitted. Seven source files and the migration passed syntax checks; whitespace checks passed. No production build, broad regression suite, live migration or Citus run. Conversation participation/content capture, files, remaining grant-reduction capture hooks, archive readers and production closure finalization, portable export/restore, independent upgrade and the remaining original requirements remain open.

Implementation checkpoint — conversation participation capture (2026-09-08): newly retained canonical ticket/task comment events now capture MSP-owned text history inside the actual writer transaction. The adapter reads persisted comments, consistent thread/root audiences, current effective sharing and actual author references; publication bodies and author claims are ignored. A first MSP shared reply establishes participation, while oversight and customer-only activity do not. Later shared customer contributions continue existing participation. Customer-private threads, newly unshared content and fresh bodies beneath deleted roots are excluded. Deletion evidence contains identity/revision markers without deleted bodies; exact event replay never refreshes the snapshot. Outbox publications remain metadata-only. Contact authors retain their actual contact identity instead of a fabricated user ID; system/unknown authors also have explicit kinds. A Knex-generated migration extends the immutable evidence schema, preserves previous source hashes, and refuses rollback over conversation evidence. Effective source disclosure was factored from effort totals for reuse without introducing a new authorization boundary. Validation: 12 distinct focused source-mode PostgreSQL cases passed across runs (four conversation cases, metadata-only event regression and seven affected effort cases). The final ticket/task capture check passed in 9.36s, including permitted reply ancestry; earlier ticket fixtures needed author/audience values corrected to match existing schema constraints. Six source files and the migration passed syntax checks; whitespace checks passed. No production build, broad suite, live migration, browser or Citus run. Capture currently covers newly written eligible shared text, not prior conversation backfill, file preservation or MSP-private note capture. Archive read authorization, grant-reduction capture hooks, complete file/history finalization, public departure UI, portable export/restore, independent upgrade and the other original requirements remain open.

Implementation checkpoint — retained conversation files (2026-09-08): eligible published ticket attachments now acquire a separate MSP-owned byte snapshot while current sharing, participation and thread/root audience locks are held. Ordinary uploads reuse their already-confirmed bytes; draft publication and explicit disclosure read and verify the immutable source object. Unpublished drafts, customer-private content and newly unshared uploads do not enter the archive. The source checksum and size must match before the writer can commit. A dedicated tenant-distributed table temporarily stages up to the existing 25 MB file limit per attachment, retaining qualified source identity and immutable metadata without customer/source FKs. Existing upload maintenance now discovers pending archive owners, including suspended MSPs and owners without live trust, and moves staged bytes to an MSP-owned archive object path. It clears staging only after complete storage confirmation; failures retain the bytes and retry the same object identity. This prevents source attachment cleanup or customer record deletion from destroying already-retained evidence. Added the storage workspace dependency without changing unrelated lockfile edits. Explicit thread disclosure exposed an existing capture edge: updated tombstones now retain body-free identity instead of blocking the disclosure. Validation: seven distinct focused source-mode PostgreSQL cases passed across runs, with the final four archive/disclosure cases passing in 10.72s; 26 maintenance fanout unit cases passed in 2.47s. Checks cover publication rollback on corrupt bytes, private/draft/unshared exclusion, concurrent/repeated storage, failure recovery, actual customer source-record deletion, immutable metadata, migration replay/refusal, and original upload/draft/disclosure behavior. The deletion fixture was corrected to remove ticket comments and audit dependencies first. Eleven changed source files and the migration passed syntax checks; whitespace checks passed. No production build, broad suite, live migration, real object-provider or Citus run. This covers the existing ticket conversation attachment path. Archive readers/download authorization, pending-byte delivery, historical/grant-reduction sweeps, task/project files, MSP-private history/files, complete closure finalization, export/restore and independent upgrade remain open.

Implementation checkpoint — MSP archive access (2026-09-08): added a local archive admission/read boundary and the `/msp/co-managed/archive` screen. Work discovery uses retained participation and file records, never time-reference registration or live customer data. Each result applies the current MSP session/API credential, internal identity, RBAC and client/queue/assignment bundle projection from qualified local references; ended trust does not become live access. History exposes explicit retained fields and captured authors, with source/derived field restrictions including stored payload, author, title and filename aliases. Time-participation history additionally requires current local time-entry read authority. New conversation/handoff/audit captures retain the authorized work title and ticket number, so archive navigation can use historical labels without querying the customer; older data can use an existing local time reference. File listing and the browser download route enforce the same resource and file restrictions. Downloads serve pending MSP-owned bytes immediately or verify the checksum of the separate archived object, then recheck the session after transport. Responses are non-cacheable, non-executable attachments; API overrides cannot borrow a browser session. The UI release boundary covers the page, link, pagination and downloads, while backend actions/routes/storage remain flag-independent. Session changes, denied refreshes and navigation clear prior data and ignore delayed responses. Added 27 copy keys in all ten locales. Validation: six distinct focused source-mode PostgreSQL cases passed in 10.70s, with the final expanded field-policy case passing in 9.17s; six UI cases passed in 826ms. These cover customer source deletion, no revived live access, tenant/relationship/kind isolation, current client and field policy, pending/stored bytes, corruption, transport-time expiry, actual download headers/session binding and existing participation capture. Eleven source files passed syntax checks; all new locale keys/interpolation and whitespace checks passed. The global translation validator still reports 774 pre-existing errors, with none for the archive keys. No production build, broad suite or live browser/theme run. This exposes currently captured shared history and ticket files, not a finished separation feature. MSP-private history/files, other source types, historical and grant-reduction sweeps, complete closure finalization, portable export/restore, independent upgrade and the remaining original requirements stay open.

Implementation checkpoint — MSP-private archive history and files (2026-09-08): actual MSP-private ticket/task commands now capture the persisted receipt, revision, author attribution and private note in the same transaction. Private note creation is actual MSP work and establishes participation; oversight alone still does not. Captures remain entirely in the MSP store, with no customer conversation/event publication. Edits retain the original saved author identity, and deletion evidence omits deleted bodies while preserving previously captured history. A final writer credential/lifecycle check encloses capture. Private ticket attachments now use the same verified byte staging/storage protocol as shared files, with their actual MSP source tenant and organization-private audience. Private drafts are captured only after publication; ordinary uploads and edits can preserve already-published owned files. Qualified source tenant plus attachment identity keeps colliding private/shared file IDs distinct. Archive readers apply private-note restrictions separately from customer conversation restrictions, and the UI labels MSP-private notes and files explicitly. Customer actors cannot read either from the MSP archive. A Knex-generated migration adds the private evidence source and correct owner/audience combinations for archive files, refusing rollback over retained private data. Earlier evidence source migrations now preserve later source expansions when replayed. Validation: five focused source-mode PostgreSQL cases passed in 10.77s, covering actual task revisions/attribution/tombstones, customer non-publication, source deletion, private draft publication, private/shared ID collisions and independent policy, late-writer rollback, prior migration replay, and existing shared/disclosed conversation behavior. Seven archive UI cases passed in 817ms. Ten source files and three migrations passed syntax checks; the two new copy keys exist in all ten locales and whitespace checks passed. The global translation validator remains at 774 pre-existing errors, with none for these archive keys. No production build, broad suite, live provider/browser/theme or Citus run. Historical/grant-reduction sweeps, other file source types, complete closure finalization, portable export/restore, independent upgrade and the remaining original requirements stay open.

Implementation checkpoint — history at sharing reductions (2026-09-08): explicit ticket revocation and board/project scope removal now capture currently permitted conversation history before changing the grant. This includes older shared comments on work with actual MSP participation; oversight, private customer notes and unpublished comments do not qualify. The capture uses the same canonical audience, attribution and ticket-file staging boundary as new conversation events, without publishing synthetic events. Snapshot operation IDs link to the actual handoff or persisted policy event. Retries return the existing command receipt, and failed transactions roll back both snapshots and the scope change. Surviving replies under a deleted root retain their still-visible text/files; the deleted comment itself remains a body-free tombstone. Seven distinct focused Node source-mode PostgreSQL checks passed across two runs (final three reduction cases: 9.60s); five changed TypeScript files passed syntax checks. No build or broad suite. Audience changes, structural moves, complete closure finalization, other file sources, export/restore and independent upgrade remain open. Continue prioritizing fast source-mode checks and implementation; defer broad validation as requested.

Implementation checkpoint — thread audience reduction (2026-09-08): making a customer-owned shared ticket thread organization-private now snapshots that thread's permitted history and published files before the audience changes. Unrelated threads are not swept. Existing command replay and final authority checks enclose capture, and later private edits cannot refresh retained history. Three focused source-mode PostgreSQL checks passed in 10.17s, including legacy file capture, live download denial after the change, rollback and the existing disclosure flow. No build or broad suite. Structural moves and complete relationship closure finalization remain open.

Implementation checkpoint — source moves (2026-09-08): native ticket update actions, the ticket API service and admitted workflow ticket board mutations capture permitted conversation evidence before changing board membership. Native task moves, project structure updates and phase reparenting likewise capture under the original project scope. The internal source adapter retains the active relationship and reuses the existing participation/audience/file admission without creating an MSP principal; source changes and captures share the caller's transaction. Batched project structure updates capture affected sources before any phase changes. Three focused source-mode PostgreSQL checks passed in 9.38s for the actual optimized ticket writer and native task/phase models, including private exclusion, current access loss and rollback. Nine changed TypeScript files passed syntax checks. API/legacy/workflow entrypoint-specific move checks remain deferred; no build or broad suite. Source deletion, other direct writers, full closure finalization, portable export/restore and independent upgrade remain open.

Implementation checkpoint — closure archive seal (2026-09-08): the closure transaction now seals the source finalizer's retained MSP evidence and files before ending trust or releasing seats. A Knex-generated tenant-distributed manifest table records the closure operation, cutoff, ordered evidence identities/checksums and file identities/byte checksums/sizes, with an owner-only FK and immutable-update trigger. Hashing uses a fixed ordered representation that survives JSONB key reordering. Pending file staging can finish after closure without rewriting the seal or reopening customer storage. Manifest failure rolls back final capture, closure and capacity release; exact closure retries reuse the original seal. Conversation sweeps can use the closure's fixed cutoff. Tenant metadata/deletion ordering includes the new owner-scoped table. Eight focused source-mode PostgreSQL checks passed in 9.74s, covering concurrent retries, actual source deletion, deferred file storage, rollback, immutable data, migration replay/refusal and existing closure authorization/SLA behavior. Six changed TypeScript files and the migration passed syntax checks. No build or broad suite. The required production source finalizer is still an explicit callback; this seal does not make its source coverage complete or expose the departure UI. Historical private capture, remaining work/source types, portable export/restore and independent upgrade remain open.

Implementation checkpoint — private history source finalizer (2026-09-08): added a closure-specific capture adapter for existing MSP-owned private ticket/task threads. It uses actual saved author identities and published private source rows, preserves empty tombstones and surviving replies, and stages owned ticket files before the closure seal. It can retain original private sources after explicit disclosure and after customer work is unshared. Captions come only from earlier MSP evidence; newly private customer titles/content are never reopened. The normal private writer and closure adapter share the same evidence construction/file staging. Six distinct focused source-mode PostgreSQL checks passed across runs; final two closure cases passed in 9.25s. The first run exposed a fixture using the shared-comment writer for private content; corrected it to the real private-note writer. Checks cover legacy capture, saved attribution, revoked grants, private files, sealed membership, exact closure retries, rollback and existing private archive/disclosure behavior. Three changed TypeScript files passed syntax and whitespace checks. No build or broad suite. Full production finalizer composition, remaining shared work/file source coverage, departure UI, portable export/restore and independent upgrade remain open.

Implementation checkpoint — retained work metadata (2026-09-08): work snapshots now preserve the existing shared metadata allowlist, including qualified status/priority/project/phase labels and dates. The live reader and archive source share the same metadata projection and derived-field source aliases. Capturing requires a current effective grant and actual MSP participation; oversight alone does not qualify. Grant removal and source moves capture this context after conversation capture, while thread-only audience changes stay scoped to that thread. Snapshots use automatic system attribution, with no fabricated human author. Archive reads apply current MSP source/derived-field restrictions, including alternate stored title paths; hiding note bodies leaves permitted work context visible. The archive UI displays saved labels without links into customer resources. Added six copy keys in all ten locales and a Knex-generated source-type migration; prior private-source migration replay preserves the new source. Eight focused source-mode PostgreSQL checks passed in 9.82s, with the expanded policy case passing in 8.26s; eight archive UI checks passed in 871ms. Eight changed TypeScript files, both migrations, copy-key presence and whitespace passed checks. No build or broad suite. Full source finalizer composition, remaining history/file sources, departure UI, export/restore and independent upgrade remain open.

Implementation checkpoint — archive finalizer composition (2026-09-08): added `finalizeCoManagedArchive` as the production composition for the existing ticket/task archive sources. It captures owned private history and completed time first, then discovers work through retained evidence, qualified MSP references, actual foreign contributions, received escalations and historical MSP collaboration audits. Unused time-reference registration and oversight lists do not establish participation. Each customer source still requires current effective sharing. Missing handoff/audit evidence is recovered from actual saved source attribution and the existing field allowlist; previously captured evidence is never refreshed. Conversations and work metadata use the fixed closure cutoff, then the existing closure seal records the final owned set. Four focused source-mode PostgreSQL cases passed in 9.40s, covering full composed closure/retry, legacy audit-only discovery, unshared source exclusion, customer-private exclusion, original evidence preservation and completed-time versus unused-reference selection. Five changed TypeScript files passed syntax checks; whitespace checks passed. No build or broad suite. Additional file/source types, departure UI, portable export/restore and independent upgrade remain open; closure still requires an explicit source finalizer rather than silently accepting incomplete coverage.

Implementation checkpoint — independent tenant licensing foundation (2026-09-08): the existing self-host license record is installation-wide, so independent customers now have a separate tenant-distributed signed-license store. Internal activation requires a real valid Pro signature bound exactly to the target tenant, self-host mode, the actual owner row and an unexpired database-clock deadline. It stages entitlement only: product identity, sponsorship, MSP installation license and co-managed capacity remain unchanged. Tenant tier resolution prefers its own signed state, including expiry, over the installation license; failures other than an older missing schema do not revive Pro from a stale plan value. Co-managed product tiers remain operational Pro while lifecycle guards independently enforce acceptance, lapse and termination. EE seat checks receive the actual tenant and transaction, honoring independent seats before legacy installation seats; CE retains its no-op stub. Added a Knex-generated table and tenant deletion metadata. Three focused source-mode PostgreSQL checks passed in 8.37s with real ephemeral ES256 signatures, covering binding, seat isolation, malformed/foreign/unbound/expired rejection, rollback, hosted-mode rejection, migration replay/refusal and no fallback after expiry. Twenty-one focused licensing unit checks passed in 863ms. Ten changed TypeScript files and the migration passed syntax/whitespace checks. No build or broad suite. This is entitlement storage/resolution, not an executed customer upgrade. Tenant-aware license management UI/actions, paid hosted entitlement, commercial seed orchestration, atomic product conversion with closure, export/restore and the remaining plan requirements stay open.

Implementation checkpoint — retained PSA capability backfills (2026-09-08): the existing PSA seed, additive RBAC, client tax-default and SLA-parity backfills now accept a retained transaction through the existing bootstrap transaction helper. Ordinary AlgaDesk activities keep their previous independent retry behavior. The permission catalog recognizes the supported co_managed-to-PSA transition before the product flip; it does not grant entitlement or terminate sponsorship itself. One focused Node source-mode PostgreSQL scenario verifies actual commercial defaults and SLA writes, whole-transaction rollback, replay without duplicate defaults, preservation of existing customer work/grants and sponsor isolation. Final run: 8.26s, including schema setup; two TypeScript syntax checks, CJS syntax and whitespace checks passed. No production build, full type check or broad suite. Paid entitlement admission, atomic upgrade composition, license-management actions/UI, departure/export/restore and the previously recorded requirements remain open.

Implementation checkpoint — independent self-hosted upgrade transaction (2026-09-08): a customer-admin coordinator now retains current session/permission, sponsor-capacity, relationship and customer-tenant locks while the concrete self-host adapter validates an independently staged signed tenant license and runs the existing PSA seed/RBAC/tax/SLA backfills. Installation licenses and trials cannot satisfy paid upgrade admission. Signed capacity must cover active technicians and pending invitations, with at least one paid seat; the transaction checks license and session expiry before commit. Active trust uses the production archive finalizer and closure primitive, then flips the customer to PSA/Pro with its own seat count. A customer that already departed reuses the verified immutable seal and closure, without reopening trust, recapturing newly private sources or releasing capacity again. Migration 66 creates tenant-owned immutable upgrade receipts with operation fingerprints, original entitlement reference and closure identity; retries require current customer authority and return the original result. The MSP installation license and entitlement remain unchanged. Three new focused Node/source-mode PostgreSQL scenarios pass: active/concurrent retry and already-departed conversion passed in the initial run; the corrected insufficient-seat/final-write-failure fixture passed in 8.81s. Checks cover actual signed licenses, preserved ticket data, loss of live MSP access, immutable receipts, mismatch/revoked-session denial and complete rollback/recovery. Two existing matching licensing scenarios also passed in the first run. Source syntax, migration syntax and whitespace checks passed; no build or full type check. This is the internal transaction and self-host runtime adapter, not a public upgrade workflow/UI. Hosted paid-subscription admission, tenant-aware license-management controls, public commands, departure/export/restore and remaining plan requirements stay open.

Implementation checkpoint — tenant-aware license management (2026-09-08): self-host license status and key activation now route sponsored and independently upgraded customers to their own tenant store. The domain retains the real tracked browser session, co-management administrator permission and customer tenant lock through reads/writes and the final response. Installation license/token/connection/trial details never enter the tenant status. Appliance trial, refresh and connection actions refuse tenant-scoped callers before provider access. Signed renewal updates an already independent PSA tenant's admission count; staging a key while still co-managed leaves its allocation/count unchanged. Independent license inputs require a matching audience, never inherit trials, and report license_required for a missing/invalid key. Immutable upgrade history preserves independent licensing and management scope even if its key row disappears, so it cannot fall back to the MSP's valid installation token. The workspace-license panel and navigation use the UI-only release boundary, support manual renewal and portal navigation, and show safe activation errors; form state follows session identity. Appliance-only banner messaging excludes tenant licenses. Twelve copy keys exist in all ten locales. Two actual server-action PostgreSQL scenarios passed in 8.36s, covering binding, installation isolation, blocked provider controls, API override/revoked-session denial, renewal counts, missing-key resolution and late-session rollback. Seventeen focused licensing unit checks and sixteen distinct license/sidebar UI checks passed (final navigation run 1.17s); fourteen changed sources passed syntax checks, locale interpolation and whitespace checks passed. No builds, broad type checks, browser/theme inspection or Citus run. Tenant-aware edition-runtime consumers, hosted upgrade admission, public upgrade commands, broader departure/export/restore and remaining requirements are still open.

Implementation checkpoint — tenant-aware authentication tier refresh (2026-09-08): repository inspection found no active callers of eeRuntimeEnabledServer; the actual installation-wide tier consumer was NextAuth's self-host session override. Both generated and synchronous configurations now pass the actual sign-in/refresh tenant into the existing tenant licensing resolver. Hosted sessions retain their original plan/trial presentation. License read failures resolve to Essentials instead of reviving a cached paid tier; tier resolution occurs before subscription metadata refresh so a later metadata failure cannot retain the old paid value. Existing throttling and explicit update() refresh remain unchanged. Two focused Node/source-mode PostgreSQL scenarios exercise actual callback/session mapping, durable session identity, co-managed sign-in, independent upgrade, a valid own license while the installation license expires, and expired own licensing while the installation license remains valid; both passed in 8.71s. Twelve focused auth mapping/expiry unit checks passed in 241ms, including both configurations' failure behavior. Four source syntax checks and whitespace checks passed. No builds or broad checks. The unused global edition helper was not expanded. AI gateway appliance-credential ownership is a separate remaining integration concern; hosted paid-upgrade admission/public orchestration, export/restore and the previously recorded requirements remain open.

Implementation checkpoint — hosted independent upgrade admission (2026-09-08): the hosted adapter verifies the customer's own active PSA subscription and paid invoice with read-only Stripe calls outside database transactions. Authenticated preparation and final completion share existing customer-admin admission; final completion rechecks retained local subscription/customer/catalog identities and current session before the existing atomic PSA backfills, archive closure, capacity release and product conversion. Completed retries return the receipt without contacting Stripe again. Configured monthly/annual PSA seat prices are required; trials, foreign ownership, unpaid invoices and mismatched seats/prices are refused. No provider purchase or MSP subscription mutation occurs in this adapter. A Knex-generated idempotent migration supplies the subscription-item column already required by existing Stripe code but absent from fresh schemas; rollback preserves that identity. Two focused Node/source-mode PostgreSQL cases passed in 8.45s after the first run exposed this schema gap. They cover provider reads without retained owner locks, unchanged local billing and other customer allocation, exact replay, invalid provider data, changed local billing, revoked sessions and recovery. Three existing self-host upgrade cases passed in the initial focused run. Six source syntax checks, migration syntax and whitespace checks passed. Per the user's speed preference, routine validation stays in Node dev/source mode with small targeted tests; builds, full type checking and broad suites are deferred. Public hosted checkout/purchase recovery and upgrade UI/actions, independent AI gateway ownership, departure/export/restore and remaining plan requirements are still open.

Implementation checkpoint — customer upgrade UI and worker orchestration (2026-09-08): the co-management page now links customers to `/msp/co-management/upgrade`, inside the existing UI-only release boundary. The screen explains preserved work, immediate MSP access removal, retained participation history, required paid seats and unchanged self-host hosting. It offers tenant-license setup, explicit conversion confirmation, status refresh/polling, stable-command retry after a lost acknowledgement, and session refresh before opening PSA. Session changes discard stale UI responses. Twenty-two copy keys exist in all ten locales. New EE actions derive the actual browser customer/session, perform current domain admission, schedule the existing product-upgrade workflow and expose only safe progress. CE stubs retain the licensed-worker boundary. The workflow has a co-managed branch that runs one atomic upgrade activity with bounded retries; it does not execute the AlgaDesk subscription swap or separate seed/flip steps. The activity chooses the deployment's actual licensing mode and rechecks the retained customer authority and paid entitlement through the existing adapters. Customer-owned advisory readiness remains available after lapse/departure and completed status comes from the durable receipt. Expected subscription setup failures are distinguished from database failures. A focused source-mode PostgreSQL action-to-worker scenario passed in 8.43s after correcting its test user context; it covers supplied actor/tenant rejection, session revocation before worker execution, actual signed conversion, completed status, retry without rescheduling and API-override denial. The hosted readiness/upgrade scenario passed in 8.89s. Four UI checks and one direct workflow-branch check passed in 991ms. Fifteen changed sources passed syntax checks; copy/interpolation and whitespace checks passed. No production build, broad type check, live Temporal worker, Stripe purchase or browser/theme run. Hosted checkout/purchase recovery still needs implementation; a hosted customer must already have its own paid subscription to use this completion path. Portable export/restore, remaining archive source coverage, independent AI gateway ownership and the other recorded requirements remain open.

Implementation checkpoint — hosted independent checkout and payment recovery (2026-09-08): customers can now start monthly or annual PSA checkout from the upgrade screen and resume its original seat quantity and interval. The new customer-owned purchase journal persists intent before provider calls, allows one pending purchase, rejects conflicting terms/another live base subscription and checks current technicians plus invitations. Every delivery of embedded checkout credentials rechecks the actual tracked customer administrator; self-host callers remain on signed licenses. Billing customer ownership is established by tenant metadata, never email matching. Provider customer/session creation uses operation-specific idempotency keys and metadata-based recovery; unresolved creation older than 23 hours refuses another provider creation instead of assuming Stripe still retains the key. Client secrets are returned only to the admitted browser and are not stored in the journal. The existing Stripe webhook handler reconciles verified independent checkout events into customer-owned catalog/subscription rows, including when the browser has closed; replay reuses the paid receipt. The customer must still explicitly confirm product conversion after payment. While still co-managed, subscription changes and cancellation update only independent billing, without altering sponsored seats/tier, terminating trust or invoking ordinary PSA tenant deletion. Cancellation before payment reconciliation releases the pending checkout for another explicit purchase. After product conversion, ordinary PSA subscription lifecycle resumes. The paid provider validator is shared by checkout reconciliation and the worker adapter through the existing EE source alias. Added a Knex-generated purchase table and tenant metadata/deletion ordering, plus five copy keys in all ten locales. Three distinct focused source-mode PostgreSQL scenarios pass: the two payment/replay/rollback cases passed in 9.24s after the cancellation review fix; the early-cancellation recovery case passed in 9.31s. Tests include the actual Stripe webhook handler, customer ownership, response loss, duplicate events, revoked browser sessions, no early conversion, cancellation isolation, paid import rollback, migration replay/refusal and subsequent actual independent upgrade. Five upgrade UI checks passed in 1.06s, including hosted resume and separate conversion confirmation. Fifteen changed sources, migration syntax, copy/interpolation and whitespace checks passed. No live Stripe purchase, browser/theme inspection, production build or broad type check. Provider timing/async terminal failure cases, changes to paid seat quantity before conversion, and deployment verification still need wider validation; ambiguous aged provider creation remains a reconciliation condition. Portable export/restore, remaining archive source coverage, independent AI gateway ownership and the rest of the plan remain open.

Implementation checkpoint — failed-payment recovery and paid seat changes (2026-09-08): Stripe documents that an asynchronous payment failure can void the invoice while leaving the subscription active ([subscription lifecycle](https://docs.stripe.com/billing/subscriptions/overview)). Reconciliation now records a distinct pending `payment_failed` state from current checkout/invoice evidence, keeps paid upgrade readiness closed, and prevents a duplicate purchase. A Knex-generated migration extends the journal constraint and pending uniqueness index. A customer-confirmed retry verifies current tenant ownership and the still-unpaid void invoice before canceling without proration or an immediate invoice, then opens a fresh checkout. Lost cancellation responses recover by observing the canceled subscription; a payment that has since succeeded refuses cancellation and can reconcile normally. Current customer authority is checked before provider operations and before returning results. The upgrade screen also offers payment-method management and a proposed paid-seat change through Stripe's [subscription update confirmation flow](https://docs.stripe.com/customer-management/portal-deep-links), which displays pricing/proration and handles payment confirmation. Only the actual customer's subscription/item/current PSA price is supplied; local seat requirements and retained billing identity are rechecked before returning the portal URL. No direct seat mutation or MSP billing change occurs in this action. UI state follows session identity, and nine keys were added in all ten locales. The hosted worker now resolves its Stripe key through the existing secret provider with the existing environment fallback, matching checkout configuration. Three focused Node/source-mode PostgreSQL checks passed in 8.40s, covering async failure, duplicate-purchase prevention, lost cancellation recovery, payment-success races, migration replay/refusal, seat minimums, subscription ownership and final-session denial. The existing checkout ownership/rollback case passed in 8.45s after its expected earlier ownership error was updated. Seven upgrade UI checks passed in 1.04s, including explicit retry confirmation and separate paid-seat review. Source syntax, migration syntax, copy/interpolation and whitespace checks passed. Deployment must enable payment-method updates and PSA quantity updates in the Stripe billing portal configuration; no live portal/provider purchase, browser/theme inspection, build or broad type check was run. Live provider timing/deployment validation, portable export/restore, remaining archive source coverage, independent AI gateway ownership and the other plan requirements remain open.

Implementation checkpoint — portable vault encryption (2026-09-08): the existing internal tenant exporter produces business-table JSON without blobs; the current AMP producer supports a narrower migration entity set and does not yet reconstruct a full workspace. Added an internal portable native-vault primitive using the existing source Transit/AES decryption dispatcher and destination encryption conventions. The passphrase envelope uses asynchronous scrypt and authenticated AES-GCM, binds package/workspace identity, rejects altered ciphertext and mismatched manifest credential membership before destination encryption, and returns only destination ciphertext. It does not carry installation keys. The versioned format, limits, ownership of authorization/audit duties and integration boundaries are documented in `docs/features/co-managed-portable-vault.md`. Three focused Node tests passed in 2.62s using real scrypt/AES, including destruction of the source key before usable password/OTP restoration under a different installation key; destination Transit dispatch and failures are simulated. This is the transport primitive only: customer-authorized collection/audits, complete manifest/schema/attribution selection, file packaging, public export UI/action, isolated destination admission and atomic full-workspace restore remain open. No database schema change, build, broad suite or live provider call was needed.

Implementation checkpoint — customer-authorized vault collection (2026-09-08): added a reusable tracked customer export boundary, available after departure or licensing lapse, and an internal native-vault collector. The collector derives ownership from the authenticated customer actor, requires current administration and credential-read permissions, enforces retained entry ACL and bundle rules, and fails rather than silently omitting an inaccessible native credential. Customer metadata, grants and native associations are explicitly projected; MSP credentials and external Hudu secret references are not read. Value-free reveal audits commit before source decryption. Encryption/provider work runs outside transactions, and delivery re-enters current customer authority and compares the complete retained vault source to reject changes during encryption. Two focused Node/source-mode PostgreSQL checks passed in 9.16s on the disposable schema, covering departed access, actual metadata/audit selection, usable password/OTP recovery, MSP exclusion, restricted ACL denial, audit failure, provider-time session revocation and source changes. The first run caught a missing departure timestamp in the fixture; the corrected run passed. No build, broad suite, live provider call or schema migration. This completes authorized collection of the vault component, not the full workspace export: remaining record/schema/configuration/attribution manifests, referenced blob packaging, customer UI/action, isolated destination admission and atomic workspace restoration remain open alongside the other plan requirements.

Implementation checkpoint — portable identity/directory manifest component (2026-09-08): inspected local schema metadata only and added 16 explicit versioned projections for workspace identity, users, teams/memberships, local roles/permissions, clients/locations, contacts/custom contact methods and customer-owned historical collaborator references. Password hashes, MFA seeds, login bindings, directory authentication metadata, platform billing identifiers, sessions and live sponsorship are excluded by column/table allowlists. One repeatable-read transaction retains tracked customer administration and current directory/security permissions, enforces record narrowing/redactions and final session expiry, and returns package/workspace identity, snapshot time, declared restore references and a content checksum. Validation rejects unexpected columns, duplicate/malformed identities and missing internal references; document links remain declared cross-component dependencies. The vault collector now also rejects field-redacted credential decisions rather than reducing those decisions to an allowed boolean. Four focused source-mode PostgreSQL cases passed together in 9.47s; after adding explicit duplicate-ID and security-scope checks, the two core cases passed in 8.49s. No build or broad suite. This is a manifest component, not a complete backup: coordinated capture across components, full authenticated package integrity, operational configuration/history projections, file blobs, public export UI/action, isolated restore admission and atomic reconstruction remain open. See `docs/features/co-managed-portable-workspace.md` for the format and its current limits.

Implementation checkpoint — portable ticket/project records (2026-09-08): added 26 explicit projections for boards/status/priority/category configuration, tickets/projects/phases/tasks, checklists/resources/dependencies/links, canonical ticket/task conversations/reactions, ticket activity and inert handoff history. One repeatable-read snapshot retains customer ticket/project/configuration permissions and record narrowing/redaction checks. Customer-private and shared text, rich-text source, audiences and saved foreign authors are preserved; MSP-private stores are never queried. Comment/email transport metadata, provider thread references, schedule dispatch/job IDs and billing-profile pointers are excluded. Ticket audit JSON is narrowed to existing curated ticket changes and explicit operational scalar details. Historical handoffs omit live relationship identity/command fingerprints and declare a historical-activity restore policy; scheduled publication is marked for paused restoration. Identity/column/parent/conversation validation rejects incomplete records rather than silently claiming a valid backup. Two focused source-mode PostgreSQL checks passed in 9.29s for actual ticket/shared/private history selection, provider metadata exclusion, audit filtering, handoff history, task structure/rich-text/attribution and current project permission denial. The first run exposed a test expectation of plain text in the rich-text source column; the corrected assertion verifies both original rich text and markdown. No build or broad suite. Complete cross-component user/service/SLA reference validation, physically missing legacy conversation roots, project mutation audit coverage, remaining operational configuration, document/file blobs, coordinated authenticated package assembly, UI and isolated atomic restore remain open. Core/work validation now has a shared-pattern marker for a later bounded extraction as additional components establish the final shape.

Implementation checkpoint — portable documents/KB and native file staging (2026-09-08): added 15 explicit document/KB projections for versions, block/text content, associations/folders/types/templates and article/review records. The normal document association authorization resolver now lives in the shared layer, with unchanged default native behavior and optional retained association/parent locks for export. Export requires current tracked customer administration, document/system-configuration read permissions, record narrowing/redaction admission and existing co-managed meeting-artifact admission. Native file references and legacy document storage paths are streamed to a private staging lease, with exact source byte counts, per-file SHA-256, mode-0600 files and a mode-0700 directory. Only verified customer native/PDF path layouts are read. Provider reads occur outside transactions; source metadata and current authority are rechecked before the lease returns. Failures remove staging, and the archive assembler must dispose successful leases in finally. Portable metadata includes blob/document bindings and no source bucket/path or temporary filesystem location. The first focused run discovered missing facade registrations for existing tenant-owned document template tables; both were registered without a schema migration. Two focused Node/source-mode PostgreSQL checks passed in 8.65s, exercising actual native/legacy bytes on disk, KB/version content, privacy modes, MSP exclusion, short streams, session revocation, foreign storage paths and cleanup. Seven existing document authorization/content wiring checks passed in 462ms; these wiring checks supplement the real-query tests. No build, broad suite or live provider call. Conversation-specific attachment stores, other inline/media blobs, remaining operational configuration, coordinated authenticated package assembly, public UI/action, and isolated atomic restoration remain open.

Implementation checkpoint — portable published conversation files (2026-09-08): added a customer-owned canonical conversation-file component using the existing published-attachment selection and purpose-specific storage path validation. It retains actual ticket/comment/root and current customer permissions, preserves file/thread/audience/qualified author bindings, and works after departure, including surviving replies beneath soft-deleted roots. MSP-private stores, pending/unpublished uploads and discarded/purged files are not read. The component omits live relationship/storage identities and declares conversion to native ticket documents on restore. Document and conversation exports now share a private streamed staging lease; immutable conversation objects must match both stored SHA-256 and byte size. Both collectors recheck source/current authority after transport and clean staging on failure. Four focused source-mode PostgreSQL cases passed in 9.72s, covering actual exported bytes, after-departure customer/shared visibility, private/pending exclusions, same-size checksum corruption, comment deletion during transport, purpose-path rejection and existing document cleanup behavior. No build, broad suite or migration. Native restored attachment insertion/link rewriting, unpublished composer policy, other inline/media files, remaining operational configuration, coordinated authenticated archive assembly, UI/action and complete isolated restoration remain open.

Implementation checkpoint — parallel inventory, operational export and departure surfaces (2026-09-08): user authorized parallel implementation. Three agents implemented bounded modules while the parent integrated real-query tests and extracted the repeated portable record validator. Asset export adds 19 explicitly projected tables with native asset authority and inactive maintenance/integration restore policies. Operational export adds 17 time, scheduling, business-hour and SLA configuration tables under native owner/source/privacy rules; elapsed co-managed effort remains reconstructable from timestamps without commercial billing state. The existing core/work/document collectors now share strict record/identity/reference validation, with their domain checks retained. Explicit departure has a flagged review/confirmation page and actual tracked-admin actions for both customer and MSP, using the existing production archive finalizer and atomic closure. The review selector derives its target from the customer's home or an MSP-owned provisioning operation. No backend flag, subscription cancellation or customer deletion was added. Five shared-validator unit checks passed in 409ms and six existing collector PostgreSQL regressions passed in 9.71s. Two operational PostgreSQL cases passed in 8.59s; two asset cases and three departure domain/rollback/retry cases passed together in 8.93s. Five departure UI cases passed in 802ms. Cross-module review identified and corrected missing canonical time/calendar locks in operational export. The retained native source now precedes locking and confirming canonical time/schedule/assignment records. Concurrent privacy and assignment changes abort collection with PostgreSQL serialization errors. Those two race cases, the two operational regressions and an actual departure browser-action case passed together in 9.17s. The browser fixture first failed because its restored session omitted the required internal user type; corrected fixture checks API overrides, mismatched sessions and ignored forged actor/tenant fields. A separate concurrent time-source reassignment case passed in 8.53s. Across this wave, 17 distinct focused PostgreSQL cases and 10 unit/UI cases passed; syntax and whitespace checks passed. No production build, broad type check, live migration or browser/theme run. Public archive/export assembly, remaining operational domains, isolated restore and the other original requirements remain open; these components are not a complete portable backup.

Implementation checkpoint — coordinated export selection, workflow/engagement configuration and AI ownership (2026-09-08): continued parallel work in three bounded modules while the parent supplied a shared MVCC snapshot capability and integrated real-query tests. Core, work, assets, operational time, documents, conversation files and native-vault collectors can now share one database cutoff. Capabilities are process-local, tied to the exact database object and expire with the awaited capture. Snapshot retention holds no customer-row/authentication locks; each collector retains normal source/admission locks, and document/conversation/vault post-provider checks still use fresh transactions. A frozen snapshot is not final archive authorization: newly inserted restrictions require the future coordinator's fresh complete delivery check. Whole-section final fingerprints can conservatively require a retry after unrelated post-cutoff changes. PostgreSQL snapshot support is verified; Citus coordination remains unverified.

Workflow export adds five explicit definition/version/form/task configuration tables, reuses native workflow administration/read permissions and existing dependency analysis, preserves secret names without resolving them, and requires encrypted transport of authored literals. Restore must leave drafts/forms paused, remap embedded identities and reauthorize connections before activation. Engagement export adds twelve tables for interactions, appointments, availability, meeting/artifact metadata and referenced service/type descriptors, including global standard service types. Native privacy/redaction and canonical-source retention are enforced; provider bindings, join credentials and prices are omitted. Cross-module review caught task-only service dependencies and missing custom-to-standard service mapping; both are now included under retained project admission, with the task service dependency declared by the work component. Operational sheet notes omitted from the cached pre-feature schema are now included. Artifact-only and provider-only bytes still require separate capture.

The AI gateway's EE and CE clients now use tenant-aware self-host credential admission. Active/departed co-managed customers and independently upgraded customers cannot borrow appliance AI credits, even after their own license row is removed; the immutable upgrade receipt retains their ownership boundary. Hosted own-tenant JWTs and ordinary appliance behavior remain intact. Database errors fail closed. A tenant-owned self-host AI connection flow and direct-provider ownership checks remain unfinished.

Validation: 22 distinct focused PostgreSQL cases passed across this wave, including shared-cutoff exclusion, expired/forged snapshot rejection, post-provider session revocation, actual departure/paid-upgrade AI isolation, workflow secret-reference preservation, private meeting denial/concurrent change, sheet notes and task-only service reconstruction. The first snapshot fixture attempted to insert a generated search column; removing it corrected the fixture. Latest six engagement/work cases passed in 9.72s; snapshot/AI cases passed in 8.83s; workflow/post-provider snapshot cases passed in 8.94s. Thirty-five focused AI client/helper/resolver tests passed in 569ms. Syntax/schema-column and whitespace checks passed. No production build, broad type check, live migration/provider call or browser/theme run. Authenticated full archive assembly, fresh complete delivery authority, public export UI/action, isolated restore, remaining record/file coverage and all other previously recorded plan gaps remain open.

Implementation checkpoint — authenticated archive transport, record graph and restore identity foundation (2026-09-08): three parallel modules added passphrase-encrypted streaming archive transport, explicit restore identity remapping, and missing native meeting-artifact file staging. Archive framing binds header/index/length/final marker and authenticates all content before manifest parsing; strict JSON preflight and byte/file/depth limits bound processing. Private quarantine/extraction uses fixed generated names and explicit cleanup. Four real-crypto/disk tests passed in 4.55s, covering frame boundaries, byte preservation, wrong passphrase/context, tampering/substitution/truncation/trailing data, unsafe paths and cleanup. Independent review found no demonstrated framing/path/cleanup defect. Temporary-disk admission remains outside this internal primitive.

The pure restore mapper uses explicitly declared identity domains and references, including extension aliases, composite keys, global destination catalog mappings and text IDs. Local authors map only when an imported user exists; foreign and deleted-local qualified pairs remain inert history. Nine focused tests passed in 424ms. No users, sessions, live trust or destination records are created by this transformation. Supplemental native artifact files use the existing artifact admission, shared safe native path validation and staged byte/hash checks; document-covered IDs are deduplicated against actual primary/thumbnail/preview references, and fresh post-provider authority/coverage checks reject changes. Three supplemental cases plus work/document regressions and the initial assembled graph case passed together (eight cases, 10.30s).

The parent assembled seven strict record sections into a trusted scalar/conditional/polymorphic reference graph, adding missing severity/urgency/impact configuration and author/settings dependencies. Review found that standard document template provenance is text, not a universal custom-template UUID FK; the graph now preserves that historical provenance. The first added document fixture omitted required user_id; after fixing the fixture, actual collected customer/task/foreign-author/configuration records survived encrypted sealing, authenticated reopening and graph revalidation in the final focused PostgreSQL case (9.48s). Broken author, service, policy and nested approver references are rejected. Five parent sources passed syntax checks; whitespace checks passed. No build, broad type check, live migration/provider call or browser run. Complete outer manifest/context/blob/vault binding, fresh whole-package delivery authorization, remaining remote/inline files and data coverage, public export UI/action, isolated destination admission and atomic full restore remain open alongside the other plan requirements.


Implementation checkpoint — complete collected-package coordinator and trusted restore record adapter (2026-09-08): parallel agents supplied a 113-table restore transformation, bounded remote Teams byte capture and an export coordinator; the parent bound these through a strict outer manifest and actual database integration. The restore adapter declares native scalar/composite/alias identities, rewrites conditional associations/thread roots/work references/known nested approvers, keeps users inactive and automation paused, preserves terminal history, and returns safe operational insertion defaults. Four global catalog mappings refer only to existing destination definitions and are explicitly excluded from insertion. Sixteen focused adapter/identity-engine tests passed in 1.90s. Actual collected customer/task/history records also passed the adapter in PostgreSQL. No destination records or authentication were created.

The remote collector uses retained creation receipts and the actual current customer Teams profile; OAuth/Graph redirects are disabled, response time/size are bounded, stored arbitrary URLs are never fetched, and staging is private with byte/hash verification. Local document/block-backed artifacts stay in their native sections. Three remote cases plus the actual graph/restore case passed in 11.77s, covering provider/session rotation, locator absence, truncated/oversized content, cleanup and no-provider final checks. Only fake provider responses were used.

The outer manifest binds full record components, package/source/capture context, all declared physical blobs, document/artifact/conversation relationships and native vault metadata. It checks the trusted record graph rather than obeying archive-supplied reference or restore-policy instructions. Component hashes detect mixing; authenticated archive framing supplies integrity. The encrypted vault's exact decrypted credential membership remains checked by its restore primitive. Seventeen focused manifest tests passed in 2.02s, including native tenant credential associations, missing/extra files, wrong bindings, context/checksum splices, local/remote artifact content, legacy documents, shared file declarations and conversation audience/history. Cross-module review caught rejection of valid native tenant credential associations and a quadratic transcript-block scan; the validator now accepts only the source workspace association and indexes block-bearing documents once. Credential associations remain limited to native supported operational entity types.

The coordinator collects every implemented section under one MVCC cutoff, seals outside database admission, removes plaintext staging, then provides a single-use encrypted-artifact consume handle. Before the trusted local-file callback, one fresh repeatable-read transaction rechecks all source/permission/file/vault metadata and retains locks; core/work source queries now use share locks too. Source change or session revocation prevents delivery, and provider failure or abandoned-handle disposal cleans all files. Three actual coordinator database tests passed in 12.63s. A separate actual shared/customer-private attachment package round trip passed in 10.53s, including surviving replies beneath a deleted root and excluding MSP-private/pending files. Its first fixture incorrectly supplied extra expected-context keys to the strict archive reader; the corrected exact context passed. Across the wave, eight distinct PostgreSQL cases and 33 focused unit cases passed. Parent syntax and whitespace checks passed. No build, broad type check, live migration/provider call or browser run.

Remaining: public export UI/actions and download ownership/lifetime, disk admission, full isolated destination admission/atomic database+file+vault restore, authored inline-reference remapping, remaining record/file coverage and all previously recorded plan gaps. The internal consume callback must finish local encrypted-file use before resolving; a future HTTP stream must define ownership explicitly and avoid holding database locks for a long network download. These implemented collectors and adapters do not yet constitute the plan's complete customer backup/restore feature.


Implementation checkpoint — streamed customer export and isolated native restore adapters (2026-09-08): added a UI-only flagged customer backup page, entry links and tracked-admin screen action, with 11 new labels in all ten locales. Native form POST streams the download without constructing a browser-sized Blob. Password fields remain uncontrolled to avoid reflecting secret values into UI automation metadata. The EE route enforces same-origin bounded requests, actual tracked customer authority and final source admission; the CE alias reports unavailability. No feature flag was added to route or backend checks. Middleware now allows the three actual co-managed browser download route shapes to perform their own session admission instead of requiring an unrelated API key. The encrypted descriptor is acquired under current authority, then streamed only after transaction commit and private-path unlink; EOF/cancel/error closes it. Real PostgreSQL confirms source locks are released before response consumption and checks wrong origin, malformed/oversized input and revoked sessions. This route case passed in 9.74s.

Parallel work supplied native vault and file restore adapters. After subagent usage limits stopped remaining threads, the parent completed and tested their unfinished work locally. Vault restoration allocates fresh credential/grant/association IDs, validates native client/ACL/entity ownership and re-encrypts under destination secret management; six tests passed in 2.84s, including use after changing the original AES key and mocked destination Transit. The file adapter binds native/legacy documents, conversation files and native/remote meeting artifacts to fresh native IDs and unique provider paths, preserves audience/attribution metadata, checks size/hash before and during upload, and cleans only attempt-owned objects on failure. Six file cases plus three download and three UI tests passed together in 2.88s. The initial file test passed an overly broad expected context to the strict archive reader; it was corrected. The UI test initially matched a required label without its visual asterisk; matching the accessible label correctly resolved the fixture. Thirteen middleware checks and five existing departure UI checks passed in 1.05s.

The parent completed the native database writer using the actual destination schema, fixed virtual handoff history by converting it to inert ticket activity, and taught insertion to recompute and verify projected generated columns rather than inserting them. Global catalogs match existing destination definitions by exact semantics. Required inactive native defaults and nullable-FK cycle repair preserve records without recreating live trust, credentials for login, connections or dispatch. A Knex-generated migration adds the distinct portable_restore_pending_activation suspension reason; cancellation recovery cannot clear it and downgrade refuses while restored tenants await activation. The source schema clone first rejected explicit generated phone normalization and the previously unsupported suspension reason, providing the evidence for these changes. An expanded fixture initially omitted comments.is_resolution; the required value was added. Final actual restore passed in 9.88s with customer/MSP handoff history, canonical roots, generated phone values, document bytes and usable secrets. It proves whole-transaction rollback, savepoint rollback even when a caller catches a mid-insert constraint failure, duplicate destination rejection and suspension recovery isolation. Eight syntax checks, migration syntax and whitespace passed. This wave ran two distinct PostgreSQL cases and 36 focused unit/UI checks; no production build, broad type check, live migration/provider request or browser smoke run.

Remaining: bounded export/restore disk admission, abort propagation during preparation, public isolated restore upload/admission and durable orchestration, destination entitlement/admin activation and worker review, authored inline URL/identity rewriting, remaining record/file coverage and all other previously recorded plan gaps. The tested native insertion engine is not a complete authorized public restore flow. Goal remains the entire original plan.


Implementation checkpoint — transfer limits and installation restore admission (2026-09-08): export preparation now shares request cancellation, a 30-minute maximum deadline, cumulative temporary-write admission and a 1 GiB free-space floor across native/remote staging, credential provider waits, archive encryption and extraction. The default write budget is 3 TiB plus framing headroom; it counts physical copies. Writes serialize in-process and check filesystem headroom before bounded chunks. This is not a filesystem quota against unrelated processes. Four resource tests, seven archive/download regressions, and six focused PostgreSQL export/remote cases passed during implementation. The latter six completed in 13.72s. Already-running KDF work remains bounded rather than interruptible.

Restore is exposed as an installation-side Node command, where the actual database role must own the destination database or be a superuser. This resolves the earlier inferred public-restore-UI gap without granting ordinary tenant administrators arbitrary provisioning authority. The original backup/isolated-restore requirement did not require browser-based destination administration. A hidden prompt or exact-byte protected stdin supplies the archive passphrase; it is never a command argument. Authenticated inspection lists eligible source workspace administrators. The coordinator privately captures encrypted input, validates the whole archive, resolves destination catalogs, remaps records/files/vault, and atomically inserts a new suspended tenant. The caller retains an explicit destination UUID for retries. An immutable receipt binds archive hash, package/source and both administrator identities; matching retries return it, conflicting destinations fail, and concurrent insertion rechecks it under the destination lock. The receipt also prevents fallback to installation licensing or AI credentials. It is registered for tenant distribution and deletion, and downgrade refuses retained ownership receipts.

The actual PostgreSQL native-restore case passed in 9.94s with the new receipt and tenant-license assertions. The operator case passed in 13.80s with an actual spawned tsx CLI inspection, a real restricted PostgreSQL role, provider failure after bytes were stored, retry without provider work, conflicting-archive rejection and staging cleanup. The CLI integration initially exposed a missing EE storage source alias; adding the existing source mapping fixed module loading. Its final cleanup assertion now accounts for tsx's own IPC/cache directory. This was source-mode validation, not a build. Public UI-only release gating remains unchanged.

Remaining: restored tenant entitlement/admin activation, durable upload-attempt reconciliation after process crash, cancellation of providers lacking upload abort support, authored inline URL/identity rewriting, remaining record/file coverage and all previously recorded plan gaps. A restored tenant is deliberately suspended until activation is implemented; this command and its focused tests do not complete the full backup/restore or overall plan.


Implementation checkpoint — self-hosted restored workspace activation (2026-09-08): added installation-only activation using a Pro license signed for the restored tenant and a new administrator password that meets the existing application policy. Admission requires the original pending restore, all imported users still inactive and the selected administrator's disabled restore password marker. A new Knex-generated immutable activation receipt binds the retained operation UUID, administrator and paid entitlement. The transaction stages the tenant license, applies additive PSA setup, grants the selected administrator the native PSA Admin role, enables only that user, records activation and clears only the portable-restore suspension. Exact retries do not reset passwords or clear a later cancellation suspension; conflicting operations fail. No source authentication bindings, installation license or MSP capacity are reused.

The operator CLI now accepts --activate with a bounded regular license file and a hidden new-password prompt (or exact-byte protected stdin). The activation module loads in the documented Node source-mode configuration. The shared four-step PSA setup sequence was extracted as initializeIndependentPsa and reused by the two existing paid upgrade adapters. PostgreSQL initially caught the standard opportunity-workflow seed creating active definitions; activation now pauses those new definitions before committing, alongside imported workflows. Maintenance, scheduled comments, meeting dispatch and automatic appointment approval retain their restore pauses.

The real restore/operator integration case now also checks tenant-bound signature rejection, complete rollback after a forced activation-receipt constraint failure (including license, seeded roles and password), usable destination password, exactly one active imported user, unchanged installation license, and retry after a later suspension. Its focused pass completed in 14.62s. The final combined run added the existing hosted and self-hosted paid-upgrade cases: all three passed in 14.81s. Migration syntax, source module loading and whitespace checks passed; no build or broad type check. No actual external license, Stripe request, email or live database migration was performed.

Remaining: hosted restored-workspace activation, durable upload-attempt reconciliation, non-cancellable provider handling, authored inline-reference rewriting, full worker activation review, remaining backup/archive content coverage and every previously recorded broader-plan gap. Self-hosted restore/activation is now connected; the entire plan remains the goal.


Implementation checkpoint — hosted restored workspace activation (2026-09-08): the installation activation command now also supports an already provisioned paid hosted PSA subscription owned by the destination tenant. It retains the native customer/subscription/item/price candidate, reads Stripe outside database locks, uses the existing strict paid-entitlement validator, and rechecks the local billing fingerprint before the shared atomic administrator/PSA activation transaction. The tenant-license and hosted adapters share that completion engine. The existing hosted-upgrade Stripe reader was moved beside the shared entitlement validator and reused without changing purchase behavior. No subscription is created or transferred by activation; destination hosted billing setup remains a prerequisite and a dedicated restore setup flow remains open.

The real restore/operator test now covers both activation modes, unpaid invoice rejection, a local subscription change during provider reads (with a 100ms lock timeout proving the read did not hold that row lock), successful hosted activation, no installation-license row, preserved subscription and retry without another provider read. This case and the existing hosted upgrade regression both passed in 15.27s. The actual Node activation module probe exposed a missing co-managed package source alias in the EE config; adding the existing source mapping fixed loading of both adapters. These were mocked Stripe reads only; no live provider, purchase, migration or email was performed. The earlier self-hosted milestone dossier write timed out but a read verified that it persisted, so no duplicate was written.

Remaining: dedicated hosted restore billing setup, durable upload-attempt reconciliation, non-cancellable provider handling, authored inline-reference rewriting, full worker activation review, remaining backup/archive content coverage and all broader-plan gaps. Both restoration activation adapters are connected to the operator command; the full plan remains in progress.


Implementation checkpoint — native storage compatibility and uncertain restore commits (2026-09-08): cleanup review found that the file adapter treated the local provider's default */* allowance as an exact MIME string and rejected ordinary files. It now follows native exact/category/all MIME semantics and treats an explicit empty list as denying all types. Seven file-adapter tests passed in 2.34s, including actual LocalStorageProvider upload/read/delete with default, category and exact allowances plus disallowed types.

The same review found that a lost database COMMIT acknowledgement could make the error handler delete files already referenced by the restored tenant. After a transaction error, cleanup now waits for the destination advisory lock in a fresh transaction and checks native file references. Referenced files are retained. If that database check itself cannot resolve the outcome, objects are retained for later reconciliation. An actual PostgreSQL test commits the restore, simulates a lost acknowledgement, proves the native objects survive and proves an exact retry returns the receipt without uploading again. The expanded operator case (including both activation modes) passed in 15.58s. This protects uncertain outcomes but does not implement the still-open durable upload journal or crash sweeper.


Implementation checkpoint — durable restore-upload recovery (2026-09-08): before provider writes, restore now records all allocated attempt file IDs, destination/package/archive identity and a credential-free storage-location digest. A Knex-generated migration adds a fixed 35-minute lease, immutable attempt identity and one-way uploading-to-committed/abandoned transitions. The native provider base exposes a stable location identity: local absolute path (also fixed at construction for actual I/O) or S3 region/bucket/endpoint; rotating S3 credentials does not change location identity. The final native insertion and committed journal state share one transaction. The coordinator settles uncertain outcomes under the destination lock, preserving committed/referenced objects or retaining them when the database cannot resolve the outcome.

Installation cleanup claims expired/abandoned attempts and fences commitment before provider deletion, performs provider I/O outside database locks, protects native object-path references even under different file IDs, and skips another storage location. Failures retry after one minute, interrupted claims after ten minutes, and successful tombstones are revisited daily for late provider writes. Live/referenced entries are rescheduled so they cannot keep occupying a due batch. Tombstones deliberately survive tenant deletion and also cover never-created destinations. The CLI exposes bounded single-destination and explicit installation-wide cleanup commands, requiring database-owner authority and no archive passphrase; provider cleanup failures produce a nonzero exit. This command is not yet wired to automatic installation scheduling.

A native external_files trigger retains the attempt row when any writer publishes a new or changed restore-object reference. It rejects abandoned-attempt and foreign-tenant references, closing the interval between cleanup admission and provider deletion. Unjournaled older paths remain compatible and cannot be selected by the new journal sweep. The first real trigger test caught Knex rewriting a question-mark regex quantifier inside raw SQL into a bind marker; using the equivalent {0,1} quantifier fixed the actual installed function. Native reference queries use a new tenant/path index.

Portable staging now stops awaiting cancelled provider uploads, destroys their input streams and removes a successful result delivered late. Durable tombstones cover partial or late writes after process exit. Thirteen focused unit tests passed in 2.41s, including actual local-provider transport, storage identity under credential rotation (no S3 request), and delayed provider completion after cancellation. The final two PostgreSQL cases passed in 16.93s: native restore compatibility and the complete operator/activation/recovery journey. They cover active/expired attempts, storage mismatch, failed deletion, second late writes, native copied references, post-abandon reference rejection, installation-wide cleanup without a tenant row, fenced commit rejection and the previous lost-acknowledgement path. Node module loading, seven source syntax checks, migration syntax and whitespace passed. No build, broad type check, live migration, live S3/Stripe request or email was performed.

Remaining: automatic installation recovery scheduling, cleanup of local archive/staging directories after process crashes, dedicated hosted restore billing setup, authored inline-reference rewriting, full worker activation review, remaining backup/archive content coverage and all broader-plan gaps. Durable provider upload recovery is now connected to the operator command; the complete original plan remains the goal.


Implementation checkpoint — scheduled installation restore recovery (2026-09-08): moved the pure database/storage recovery engine and installation authority into the shared co-managed package, retaining the existing EE command imports as compatibility exports. Added a system maintenance handler that reads the journal directly, so failed destination creation, suspension or later tenant deletion cannot hide work from an active-tenant selector. It defers missing rolling schema without creating storage and reports failed sweeps to the job runner. The application registers one global PG Boss schedule every 15 minutes; EE instead uses the existing Temporal maintenance workflow at that cadence. Handler registry, legacy scheduler registration and startup scheduling are all connected. No release flag, product tier or edition check gates the recovery engine.

Thirty maintenance/handler unit checks passed in 2.55s, four PG Boss edition-boundary checks in 375ms and four Temporal schedule checks in 658ms. The Temporal suite initially tried its default Docker setup and stopped before tests; a temporary config disabled only infrastructure setup for these fully mocked schedule tests, then was removed. No container was started. The actual scheduled handler was also exercised against the disposable PostgreSQL journal, proving recovery for a destination without a tenant row while preserving referenced objects; the expanded operator journey passed in 15.73s. These were Node source-mode and focused PostgreSQL checks, not builds or live Temporal/provider operations.

Remaining: cleanup of local archive/staging directories after process crashes, dedicated hosted restore billing setup, authored inline-reference rewriting, full worker activation review, remaining backup/archive content coverage and all broader-plan gaps. Scheduled provider recovery is connected on both runners; the complete original plan remains the goal.


Implementation checkpoint — recover local portable staging (2026-09-08): archive sealing/extraction, native blob staging, remote meeting capture and installation input capture now use one private-directory lease. A synced 0600 marker inside each 0700 directory binds its name, UID, device/inode and fixed two-hour expiry before customer bytes can be written. A local application-startup sweep runs every minute with a resumable 1,000-entry cursor, preserving live leases, unrelated files, symlink targets and directories with invalid/copied/unmarked ownership evidence. Each application process owns its timer, including development reload protection; no remote worker, database authority, edition, product or release-flag gate is involved. The existing completion cleanup remains immediate.

Prepared encrypted downloads reject expired staging before database admission and before/after consumption. Public transfer assertions also compare the absolute deadline, preventing a delayed event-loop timer from extending the 30-minute operation limit. Recovery requires an application process on the same temporary filesystem; unmarked files from earlier versions remain outside automatic deletion authority, and permanently retired replica storage requires normal infrastructure lifecycle cleanup.

Eighteen focused unit checks passed: 16 archive/download/transfer/filesystem checks in 4.81s and two startup/timer checks in 406ms. The final six filesystem/timer checks passed again in 447ms after cursor error recovery was added. Eight existing PostgreSQL export/restore journeys passed in 23.92s, including actual CLI inspection, both activation modes and provider recovery. A new prepared-expiry check initially used the fixture's untracked actor and correctly failed authorization; switching to the existing tracked-principal fixture fixed the test. That case plus the valid browser-download journey passed in 10.71s, proving expired download rejection before any database transaction and immediate staging disposal. Nine source syntax checks and whitespace passed. No build, broad type check, live migration, live provider request or email was performed.

Remaining: dedicated hosted restore billing setup, authored inline-reference rewriting, full worker activation review, remaining backup/archive content coverage, S3 transport review and all broader-plan gaps. Local and provider crash recovery are now connected to their respective runtimes; the complete original plan remains the goal.



Implementation checkpoint — S3 portable restore transport (2026-09-08): native S3 uploads no longer replay consumed Readables, and HEAD receipt retries cannot repeat a successful PUT. Buffers remain replayable. Capability admission now reports the configured MIME allowance and the lower of the configured maximum and existing 5 GiB single-PUT ceiling, rejecting invalid configured maxima. This closes a restore-policy bypass where S3 previously exposed only its transport ceiling.

An actual loopback request through the installed SDK initially failed with an undefined x-amz-decoded-content-length header. StorageUploadOptions now carries optional exact content_length; both portable restore and the native uploadStream service forward their already-known source size. S3 supplies ContentLength and rejects invalid/mismatched declared buffer lengths. The restore adapter retains its independent source checksum, byte-count, complete-consumption and provider-receipt checks. No archive buffering or new multipart dependency was introduced.

The final 16 focused storage checks passed, including actual Node HTTP/SDK loopback transport, non-replayable failure, independently retried receipts, replayable buffers, policy limits, native streaming size forwarding and mismatched-receipt rejection. Ten portable file-adapter checks passed in 2.27s; the added loopback journey decodes the SDK's checksum chunk framing, compares all eight restored files byte-for-byte and confirms cleanup preserves an unrelated object. No external S3 request, live credentials, live migration, build or broad type check was used. Live endpoint/IAM/bucket policies, large multipart transport and the broader original-plan gaps remain open.



Implementation checkpoint — restore embedded native file/document links (2026-09-08): the file adapter now uses its verified destination mappings to repair native media/link routes inside declared document, comment, project-task and KB-template content. Tiptap/ProseMirror and BlockNote retain structure and serialized-vs-object representation. Markdown resource/reference destinations and raw HTML media/link attributes are patched at parser-reported source offsets, preserving quoting, whitespace, escaping, captions, titles and literal code. The parser packages were already installed; micromark 4.0.2 and parse5 7.3.0 are now explicit co-managed dependencies. No package download or full build was needed.

Unknown or absolute external URLs, ordinary UUID text and unrelated workflow/configuration objects remain unchanged. Ambiguous legacy view/download identities reject rather than choosing between different source file/document records. Only allocated imported identities can rewrite a native reference; this transformation neither fetches URLs nor broadens document authorization. Absolute links tied to the old installation, unmapped/encoded legacy links and additional content/file coverage remain open under the full original plan.

Ten focused rich-text/Markdown/HTML checks passed in 1.22s; ten file-adapter regressions passed in 2.40s, including the existing actual S3 loopback fixture. Two PostgreSQL journeys passed in 17.37s after adding parser support: an encrypted archive restores native BlockNote comments, Tiptap document content, Markdown and HTML with destination IDs while preserving source data, and the real Node operator/activation/recovery path still loads and runs. The standalone markup module passed a focused no-emit typecheck. No broad typecheck, build, live migration or external provider request was performed. The entire original implementation plan remains active.


Implementation checkpoint — task conversation files across collaboration, retention and departure (2026-09-08): the shared attachment engine now accepts qualified project-task parents. Migration 74 enforces exactly one ticket/task parent on live and retained files, protects retained task identity and rejects accidental use of ticket draft/disclosure operations for tasks. Posted task comments expose the existing upload/download/remove UI inside the release-v1-6-feature boundary; actions and routes remain independent of that flag. Current task/project scope, canonical audience, author-only mutation, body redaction, session and lifecycle admission stay in force. Shared and private stores remain separate. Surviving reply files remain readable when their root is a tombstone.

The MSP archive captures independent task file bytes with immutable task identity, continuing to serve locally authorized archive reads after live access ends or a source upload is removed. Portable customer export includes only ready own-store task files and preserves canonical audience and qualified saved authors. The closed authenticated manifest accepts an optional taskAttachments field while preserving older ticket-only archives. Native restore remaps task/thread/comment/file/document identities, creates task document associations and preserves historical attribution; it creates no source trust or live conversation attachment rows.

Thirty focused attachment/archive PostgreSQL checks passed in 21.48s and 43 UI/action checks passed in 1.55s. Four additional portable PostgreSQL checks passed in 12.40s, including actual encrypted customer export after termination and native task-file restore with byte comparisons and MSP-private exclusion. Thirty-three manifest/file-adapter unit checks passed in 2.76s. The first unit run encountered sandbox loopback binding restrictions in its existing S3 simulator; rerunning with loopback permission passed. Source syntax, migration syntax and whitespace checks passed. No build, broad typecheck, live migration or external provider call was used.

Remaining: task draft/disclosure and requester-portal file flows, task notification/event coverage, broader project/archive artifacts, dedicated hosted restore billing setup, remaining portable content coverage, full worker activation review and the complete original T01–T22 coherence audit. This checkpoint completes the posted task-file path, not the entire original implementation plan.


Implementation checkpoint — direct RMM product admission (2026-09-08): the requirement audit found that a seeded co-managed administrator could call Tactical RMM and generic RMM settings/automation actions through system_settings permission despite their hidden UI. All 28 actions now require the existing `rmm` product capability before settings, secret, provider or rule operations. Bulk and single-agent Tactical synchronization enforce the same capability independently for background callers. The webhook authenticates its secret, then rejects excluded products with a generic 403 before ingestion. These are ordinary product rules; no release flag is consulted.

A real migrated PostgreSQL case invokes every action as the seeded co-managed Admin, verifies both background engines and the authenticated webhook reject, checks that direct operations never opened the secret provider or created integration records, and verifies the PSA engine still reaches its normal unconfigured-integration result. It passed in 9.24s. The 37 existing Tactical unit checks passed in 3.73s after their isolated PSA fixtures explicitly allowed the new product boundary. Six source syntax checks and whitespace passed. The audit's separate Teams/telephony action, provisioning recovery/policy, delegated administration and other implementation gaps remain active.
