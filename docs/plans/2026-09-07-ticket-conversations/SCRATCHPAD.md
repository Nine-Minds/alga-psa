# Ticket conversations — working notes

## Design status and placement

- Product scope was discussed and accepted incrementally before planning. The user selected option C (persistent conversation navigator) and explicitly requested the ALGA plan in the workflow-expected location. That authorization covers producing the synchronized PRD and checklists; no new design interview is required.
- The card's Draft Implementation verification explicitly discovers a design plan under `docs/plans/`. Therefore this folder intentionally overrides the alga-plan skill's default `ee/docs/plans/` location, following the user's location instruction. All four artifacts live together; no duplicate PRD.
- The work is stacked on `feature/co-managed-it`; inherited base at planning is `a475996666b6cc303407ecf9d2b50bcfabcb5f39`. Parent plan: [Co-Managed IT](../2026-09-06-co-managed-it-plan.md). The parent remains a dependency, not evidence that every requirement in its PRD is shipped.
- The card explicitly requires `release-v1-6-feature` around new UI only. Backend, APIs, routes and authorization must not depend on this release flag.
- Individually restricted technician conversations are follow-up scope. Organization-private audiences remain essential in this release.

## Prototype evidence

- Three local standalone HTML options are under `/tmp/alga-ticket-conversations-prototypes/`; C is `03-conversation-navigator.html`, and `index.html` compares all three.
- Checked all three in headless Chromium at desktop and mobile sizes: per-author draft persistence, reviewed selective share, audience filtering, scoped AI context, explicit regeneration, default/opt-in/cutoff reply policy, creation and sending. No JavaScript errors. Screenshots and `verification.txt` reside alongside the HTML.
- These are simulated design artifacts, not application or server-security tests. All fixture data is embedded in each HTML. The persona switch is an access demonstration, not a client-portal redesign. Synthesis responses are canned; the late-reply follow-up is an activity event.
- `/tmp` artifacts are disposable. The PRD captures the chosen layout and interaction contract in text so implementation does not depend on their continued availability.

## Working decisions

- Ordinary tickets open on requester conversation; explicit deep links open their destination. Persist drafts per qualified author and conversation, not per ticket globally.
- Drafts are private to their author as the proposed default adopted for planning; collaborative drafts and delegation are not introduced.
- Sending uses a stable explicitly authorized mailbox: ticket-owning workspace for requester; originating organization for vendor. Shared IT visibility does not grant sending authority.
- Named conversations are containers above existing comment reply roots. Never map every `comment_threads` root to a sidebar conversation.
- Vendor recipient additions from accepted correlated replies do not grant application access. No automatic forwarding of ticket history.
- Share and synthesis create explicit editable copies; private source provenance stays protected. Attachments are selected, never copied en masse by default.
- Vendor/AI activity does not fulfill requester-response obligations. Board side-reply opt-in defaults off and reuses the existing policy when on.

## Existing code discoveries

- `packages/tickets/src/components/ticket/TicketDetails.tsx` supports grid and entry preferences; grid uses `bento/TicketBentoLayout`. Preserve entry and embedded-drawer compatibility.
- `packages/tickets/src/components/ticket/TicketConversation.tsx` owns existing reply rendering, comment-order preference and reactions; named navigation must preserve these capabilities.
- `packages/co-managed/src/ticketConversation.ts`, `privateTicketConversation.ts`, `sharedWork.ts`, `sharedWorkIdentity.ts`, and `conversationPolicy.ts` already establish qualified identity, authorized reads and separate private storage.
- `packages/co-managed/src/conversationDrafts.ts` is an immutable, operation-keyed publication reservation protocol for staged attachment messages. Its current request requires at least one file. It is NOT an editable text autosave model. Extend or adapt deliberately; do not overwrite immutable reservations on each keystroke or route ordinary local tickets through a co-managed license check.
- `packages/co-managed/src/conversationAttachments.ts`, disclosure modules, inbound reply admission, notification recipients and event outbox modules are reuse points. Compare their actual APIs before changing schema.
- `shared/services/email/processInboundEmailInApp.ts` reads `inbound_reply_reopen_enabled`, `inbound_reply_reopen_cutoff_hours`, `inbound_reply_reopen_status_id`, and `inbound_reply_ai_ack_suppression_enabled`. Default enabled is false, normalized cutoff 168 hours. Explicit valid open status wins, otherwise board default open status. Cutoff handling occurs before automated/ack suppression. Within-window internal technician replies retain existing reopen behavior; existing automated-message/rate protections remain.
- `packages/tickets/src/components/settings/BoardsSettings.tsx` and `actions/board-actions/boardActions.ts` surface/persist these settings. `ticketBundleUtils.ts` also has `reopen_on_child_reply`; side traffic must not be misclassified as a requester/child reply.
- AI integration candidates: `ee/server/src/services/chatCompletionsService.ts`, `chatStreamService.ts`, and `lib/chat-actions/chatActions.tsx`; use current provider/entitlement policy, with server-side audience filtering before inference.

## Commands and verification

- Plan validation: `python3 /home/robert/.codex/skills/alga-plan/scripts/validate_plan.py docs/plans/2026-09-07-ticket-conversations`.
- Confirm feature/test IDs are unique, every mapped feature exists, PRD section references resolve, and all checklist entries begin false.
- Parent comparison is against the stacked parent branch, not an unqualified whole `main` diff that would attribute parent changes to this card.
- Two pre-existing setup changes are outside this plan: root lockfile SDK normalization and migration CLI executable mode. Preserve them.
- Implementation must append actual schema/API choices and test evidence here. Prototype validation does not mark product features or tests implemented.

## Planning completion

- Estimated a modular workflow plan of roughly 100 features before generating the checklist; final scope is 102 atomic feature entries and 35 representative tests. All entries are false. Tests map to the specific behaviors they exercise, with migrated PostgreSQL happy/guard paths and actual browser/email journeys.
- The main alga-plan skill's current Pareto test guidance takes precedence over its older format reference suggesting more tests than features.
- Validated required JSON fields, unique stable IDs, PRD heading references, complete feature-to-test traceability, and unimplemented status. No application implementation/test completion is claimed.
- Persisted planning scope includes all accepted capabilities: vendor email, optional AI, selective sharing, full-source synthesis, parent audience/ownership integration, option C, and reuse of existing board policy. No individual restriction or autonomous-send work added.

## Implementation checkpoint — named containers and editor drafts

- Added migrations `20260908013607_create_named_ticket_conversations.cjs` and `20260908015652_create_ticket_conversation_editor_drafts.cjs` through the repository Knex scaffolder. Named containers are separate from reply roots, use qualified ticket/store references, default audience slots, revisions, independent Open/Done and mailbox identity fields. Existing consistent native roots are grouped by audience; private ticket roots stay in their home store. Inconsistent root/reply flags remain unassigned for guarded handling rather than being broadened. Replay preserves existing mappings; rollback refuses retained conversation/draft records.
- Added `shared/lib/tickets/namedConversations.ts` (transactional storage engine), `packages/co-managed/src/namedTicketConversations.ts` (current home session + native/shared ticket authorization), and `server/src/lib/actions/namedTicketConversationActions.ts` (unflagged authenticated commands). Native PSA operations use native RBAC/bundle policy with no co-managed entitlement prerequisite. Private names never enter the other organization's store/list.
- Added `shared/lib/tickets/conversationEditorDrafts.ts` and private text/rich-content draft commands. Each draft is stored in the author's home tenant, scoped to qualified author and destination. Revision/operation hashes serialize edits and retries. Discard retains a revision tombstone so a delayed autosave cannot resurrect text. Publication reservation tables are not reused or mutated by autosave. Attachment/provenance columns are reserved for the later authorized sharing/upload bridge; no browser API accepts arbitrary attachment/provenance values yet.
- Registered tables in tenant metadata and tenant-deletion ordering. No live application database migration or real outbound delivery was performed.
- Marked only implemented backend checklist items F001/F002/F003/F007/F008/F010/F030. Remaining feature and product test entries stay open; the six database cases are partial evidence toward the planned representative journeys, not completion of the whole feature.

### Validation and environment

- The rebased checkout lacked its local `node_modules/@alga-psa/co-managed` workspace link and inherited package builds. Restored the link and built licensing/co-managed with their tsup configs. The db package uses `npm run build` (TypeScript and tsc-alias), not tsup; its build passes.
- Run focused DB checks from `server`: `NODE_OPTIONS=--max-old-space-size=12288 node ../node_modules/vitest/vitest.mjs run src/test/integration/coManagedBootstrap.integration.test.ts -t 'named ticket conversation'`. They clone schema only into a randomly named disposable database, use real session/role/relationship fixtures, then drop that database. All six new cases pass: qualified defaults/private storage/idempotent creation, native no-license use, permission/state guards, migration replay/root mapping, author/destination draft isolation, and concurrent/stale/revoked draft operations.
- The complete inherited DB run initially passed 883/885 cases. Investigated both failures: factory event mocks retained call history between fixtures, and a nested `mockRestore()` reset the enclosing `createTenantKnex` test mock rather than restoring its previous implementation. Repaired test isolation; the six new checks and both prior failures pass together (8 passed). A complete rerun is recorded below when finished.
- Full server TypeScript check initially exposed five inherited diagnostics in `nativeTimePeriod.ts`, `nativeTimePeriodSettings.ts`, and `nativeTimeSheetList.ts`: empty-array inference and nullable calendar dates. Added explicit return-element array types and a fail-fast invalid-date guard. The foundation server typecheck passed after these fixes; the final draft/action check is recorded below.
- Logs: `/tmp/ticket-conversations-drafts-and-regressions.log`, `/tmp/ticket-conversations-checkpoint-full-pg.log`, `/tmp/ticket-conversations-drafts-final-tsc.log`, and `/tmp/ticket-conversations-db-build.log`.

### Next implementation work (goal remains active)

- Wire legacy and named message publication to container/root association, and add authorized named-message reads without filtering after pagination. Native/co-managed/portal projections must exclude inappropriate side content even when the release UI flag is off.
- Implement option C in actual ticket grid, entry and embedded-drawer surfaces with draft switching and deep links, then the minimal requester portal selector. The current backend commands do not yet realize these UI flows.
- Complete file staging/publication integration, sender/envelope authority, inbound correlation and protected review, notifications/read cursors, board side-reply opt-in, selective sharing/disclosure/provenance, and actual optional AI/full-source synthesis.
- When wiring private legacy defaults, handle repeated relationships for the same owning organization and qualified ticket deliberately: current default-slot uniqueness groups by store/ticket/audience, while private row admission also binds relationship identity. Do not accidentally reuse a previous relationship's private default or disclose its history on reactivation.
- Finish the plan's actual application browser/email/AI evidence and remaining migrated-DB guard cases before marking tests or the overall goal complete. Prototype checks are not application evidence.
- Final server check after the editor-draft actions passes with `NODE_OPTIONS=--max-old-space-size=12288 node ../node_modules/typescript/bin/tsc --noEmit --pretty false` (empty `/tmp/ticket-conversations-drafts-final-tsc.log`).
- The next whole DB run passed 886/887; a separate inherited KB import test exposed the same factory-mock call-history issue under a different shuffle order. Added `beforeEach(vi.clearAllMocks)` to isolate call histories across this suite without changing mock implementations or weakening assertions. Final complete-suite log: `/tmp/ticket-conversations-checkpoint-final-pg.log`.
- Actual UI anchor confirmed: `TicketBentoLayout.tsx` builds `leftRail` and renders it at the bottom grid (around line 906), then `BentoTimelineTile` in the center. Add navigator/content slots or a cohesive controller here, preserving existing grid/entry/drawer behavior; do not merely render the standalone prototype inside the app. Existing co-managed UI is `server/src/components/co-managed/CoManagedTicketConversation.tsx`.
- Final complete migrated PostgreSQL suite: **887/887 passed**, including six new conversation/draft cases (`/tmp/ticket-conversations-checkpoint-final-pg.log`, 156 seconds). Final server typecheck is clean. Database and co-managed package builds were refreshed after the final source changes. The inherited duplicate `get` warning in the Next test stub is non-fatal and outside these changes.
- This turn made concrete implementation progress; the full goal is intentionally still active. No UI/email/AI completion is claimed by this backend checkpoint.

## Implementation checkpoint — conversation reads and root association

- Added `getNamedTicketConversationMessages` and its authenticated server action. The shared published-history query now also serves native named conversations under the native authority wrapper. Named root/store/audience selection happens before pagination; exact microsecond cursors, qualified authors, deleted-body tombstones and private disclosure exclusions are preserved. Parent references whose audience is outside the selected conversation are omitted.
- The existing co-managed aggregate reader now includes only unmapped legacy roots or audience defaults. Newly created side conversations are not flattened into that legacy screen. Other native/portal read adapters still need the corresponding explicit projection before the UI rollout is complete.
- Added migration `20260908022249_scope_ticket_conversation_defaults_to_relationship.cjs` via the Knex scaffolder. Canonical defaults retain ticket-wide uniqueness; home-private defaults are unique within their retained relationship. Migration replay fills previously unassigned retained roots and keeps existing identities. Downgrade refuses to collapse multiple retained relationship defaults. The original initialization migration uses untargeted conflict-ignore so replay remains valid with either index generation.
- `Comment.insert` now assigns native roots to their existing audience default in the publishing transaction; a trusted collaboration context may supply an already authorized named destination. Existing replies cannot be reassigned to a sibling, and a rejected destination rolls back the inserted comment. Private ticket commands likewise assign roots inside the home store and current relationship. Project-task commands retain their separate model.
- Added representative database cases for named pagination among busy siblings, publication-state and audience guards, exact parent references, native reads without co-managed licensing, home-private access/revocation/disclosure, migration replay and replacement-relationship defaults. These exercise real commands and migrated queries. A first private-disclosure fixture incorrectly assigned an unknown FK; replaced it with the actual preview/transfer command, with no attachment transport expected.
- Focused validation after root association: **16 passed** (new named cases plus inherited audience, attribution, pagination, revocation and redaction cases), `/tmp/ticket-conversations-root-association-pg.log`. Broader inherited regression and final typecheck results follow below.
- This is partial implementation evidence toward the plan's complete journeys. Checklist entries that also require UI, email, AI or lifecycle integration remain open; no additional whole feature or representative end-to-end test is claimed complete solely from these backend cases.
- The first full inherited run passed **889/890**. Its failure used a raw `collaboration_audience` update as a stand-in for disclosure, leaving the new root association in the old private container. Updated that test to exercise the real preview/disclosure command and assert the destination mapping. Both actual disclosure commands now place the transferred root in the destination audience default within their existing authorized transaction; ordinary reply routing still rejects reassignment. Cross-store transfer remains a real copy with its existing attachment/receipt protocol. Focused disclosure/native/private routing checks pass **14/14**, `/tmp/ticket-conversations-disclosure-association-pg.log`.
- Next required integration anchors include `optimizedTicketActions.ts`: its screen data query reads `comments` directly around line 508, and optimized comment creation inserts roots/comments directly around lines 3312/3325, with bundle mirrors around 3402/3415. Those paths bypass `Comment.insert` and still need default association, side-event classification and legacy projection; the model hook alone does not cover the actual ticket grid.
- UI wiring note: ticket components live in `packages/tickets`, which already depends on `@alga-psa/co-managed`. The current new action wrappers live under `server/src/lib/actions`; move/reuse the action boundary through the ticket package or an explicit host adapter rather than importing the server application into a package. `server/src/lib/co-managed/browserActor.ts` uses only auth/session helpers and the co-managed error type, so its verified-session logic can be shared deliberately. Preserve its API-override rejection.
- The final server typecheck after disclosure/default association passes with an empty `/tmp/ticket-conversations-reader-complete-tsc.log`.
- Final complete migrated PostgreSQL regression run: **890/890 passed**, `/tmp/ticket-conversations-reader-complete-full-pg.log` (156 seconds). Final server typecheck is clean. No live application database migration, outbound email delivery or product UI smoke is claimed by this checkpoint.

## Implementation checkpoint — internal composer and retained reply drafts

- Moved named-conversation authenticated actions and the verified browser actor helper into the ticket package, preserving server re-exports. Ticket UI now imports its package action boundary; no package-to-server application dependency. Moved the existing co-managed rich document renderer and text helpers into the same shared ticket UI surface, with server compatibility exports.
- Added the option C navigator and named internal panel to `TicketDetails` and `TicketBentoLayout`, including entry and drawer slots. Requester stays pinned above supporting names/audience labels; the navigator sits above contact details. New controls use the approved UI-only `release-v1-6-feature` flag. Internal creation, named history, Open/Done, private rich drafts, serialized autosave and navigation flush now use real server actions.
- Added migration `20260908024119_create_ticket_conversation_publications.cjs`, immutable internal Post receipts and the production ticket adapter. A Post consumes the current author's exact draft revision, publishes to its selected native/shared/private store, retains canonical internal events where applicable, and clears the draft atomically. Retry returns the original receipt without consuming a newer draft. The API rejects email transport and attachment-bearing drafts until their separate reviewed publication bridge is implemented.
- Native optimized/simple/shared-model root insertion paths now associate roots with their existing audience defaults. The consolidated ticket data query and native comment model legacy reader exclude additional named side containers rather than flattening them into legacy history. Other portal/search/activity adapters still need the final projection audit.
- Added migration `20260908030840_retain_ticket_conversation_draft_reply_target.cjs` through the Knex scaffolder. Reply targets are retained alongside author-private draft text and revisions, qualified through the destination store/conversation. Migration replay is idempotent and downgrade refuses to discard retained targets. Selecting a different target or choosing a new message preserves body edits.
- Draft-save and publication admission both verify the selected root and parent still belong to the authorized conversation, have the expected audience, and are published/not deleted. Publication derives its parent from the persisted draft; conflicting explicit parent intent is rejected. Deletion/disclosure cannot turn an interrupted reply into a new root or a different conversation.
- The composer restores a saved reply target after remount and previews the parent when present in the authorized page. Refreshing a changed `messageVersion` updates history without reloading an edited draft; stale older-page responses cannot overwrite a newer page. An uncertain Post retains its operation identity even if publication succeeded but the subsequent draft refresh failed.
- Marked F017/F018/F020 as implemented in code. Broader navigation, persistence, publication and compatibility entries remain open because their requester/email/file/AI or actual-browser requirements are not yet fulfilled. No complete product test entry is claimed from these component/backend checks.

### Evidence and remaining integration

- Focused migrated PostgreSQL conversation suite passed **12/12**, `/tmp/ticket-conversations-reply-pg.log`. Extended the representative cases to cover native no-license internal publication and legacy query exclusion, transactional rollback on durable event failure, restored shared/private reply parents, exact retry after a newer draft, and a deleted reply target at publication. Full regression results are recorded below after completion.
- Actual hook/composer component suite passed **7/7**, `/tmp/ticket-conversations-reply-final-ui.log`: flag behavior, pinned selection, coalesced save before navigation, unknown save/Post retries, author change/revocation, reply-target restore/detach without text loss, incoming-message refresh without draft loss, and successful Post followed by failed draft refresh. Tests mock the action/provider boundary and rich editor rendering; they are not a running-application browser smoke.
- The server TypeScript check passed before the final small reply-preview/retry adjustment; final check log is `/tmp/ticket-conversations-reply-final-tsc.log`. Co-managed package build passes and its runtime exports were refreshed, `/tmp/ticket-conversations-reply-co-build.log`.
- The default Requester selection temporarily uses the existing ticket panel/composer. Complete strict requester projection and reviewed Send, co-managed foreign-screen integration, minimal portal navigation, All activity, selected files, edits/reactions/order preferences, unread/follow state and exact-message deep links remain required. Do not treat the internal-only UI as realization of the full selected C design.
- Email discovery: `TenantEmailService.sendEmail` intentionally ignores `providerId` as a transport selector and resolves tenant outbound settings with a possible system fallback. Existing requester routing lives in `packages/jobs/src/lib/handlers/coManagedRequesterEmailRouting.ts` and transport in `coManagedCommentEmailTransport.ts`. The vendor implementation must bind and revalidate the reviewed organizational sender/envelope rather than assume passing `providerId` guarantees exact From/provider identity. Existing requester delivery documents lost-SMTP-ack retries as potentially duplicate external sends; vendor uncertain-delivery handling must preserve its explicit operation outcome.
- Live application migrations, real browser/email/AI runs and the remaining plan capabilities are still outstanding. The goal remains active.
- Final full migrated PostgreSQL regression: **893/893 passed**, `/tmp/ticket-conversations-reply-full-pg.log` (156.8 seconds), including the expanded native/rollback/deleted-parent checks. Final server TypeScript check passes with an empty `/tmp/ticket-conversations-reply-final-tsc.log`; co-managed build and plan validation pass. The inherited duplicate-member warning in the Next test stub remains non-fatal.

## Implementation checkpoint — reviewed email transport and mailbox authority

- Added `packages/email/src/reviewedEmail.ts` and `TenantEmailService.prepareReviewedEmail`. Review resolves the actual provider, From and Reply-To without delivery, workflow fanout or legacy ticket-thread writes. The resulting consistency hashes cover the resolved sender/settings snapshot, explicit recipients, rendered subject/body, exact selected attachment bytes and conversation RFC headers. These hashes are consistency checks, not mailbox authorization.
- Reviewed sends require caller-owned recovery, freeze addresses/headers/file bytes before awaits, reject a changed sender or payload before transport, and preserve the conversation's Message-ID/References rather than merging it into ticket-wide requester threading. Unknown provider outcomes are explicitly `unknown` and not automatically retryable; pre-transport rejection is `not_attempted`. Ordinary email callers retain their existing behavior.
- Microsoft Graph now exposes its fixed actual mailbox through an optional provider sender-resolution method. Reviews display that identity rather than trusting a caller-supplied From that Graph would replace. A system fallback, if selected, must be reviewed as its actual platform sender; the reviewed path preserves the conversation's explicit intake Reply-To.
- Added migration `20260908033011_create_ticket_conversation_sender_grants.cjs` through the Knex scaffolder, tenant metadata and deletion ordering. Grants are owned by the mailbox tenant, scoped to the qualified conversation/ticket/relationship and qualified grantee. Migration replay preserves them and downgrade refuses retained grants.
- Added `packages/co-managed/src/conversationMailboxes.ts` and authenticated ticket-package action adapters for listing/selecting a mailbox and granting/revoking a sender. Requester mailbox ownership is the ticket owner's; vendor ownership is the creating organization before first selection and remains the selected owner thereafter. Responsibility/assignment changes do not rehome it. Selected mailbox changes and sender-grant changes advance conversation revision.
- Current ticket update/read authority is necessary for mailbox commands. Same-organization staff retain native ticket-send semantics without co-managed licensing. Cross-organization sending additionally requires a per-conversation grant issued by the mailbox owner's current co-management policy administrator. Use of a grant still requires current ticket access, audience visibility, active session/lifecycle and an active connected mailbox; it cannot grant application access or foreign private visibility.
- `withNamedTicketConversation` is the retained-authority extension point for additional conversation operations. `withNamedConversationMailbox` admits a callback only with current resource, grant and selected mailbox authority. This is transport admission plumbing; the named external Send/publication job has not yet been connected to it.

### Verification and next delivery work

- Migrated PostgreSQL named-conversation checks pass **14/14**, `/tmp/ticket-conversations-mailbox-final-pg.log`, including native mailbox use without a co-managed entitlement, shared vendor ownership through actual escalation/hand-back, requester owner selection, explicit foreign grants, stale revisions, revoked/disabled senders and migration retention. The first hand-back fixture assumed an escalation existed; corrected it to execute the real escalation and use the stored work revision. No product code was weakened for that fixture.
- Reviewed email/provider regression checks pass **67/67**, `/tmp/ticket-conversations-reviewed-email-regression.log`. Additional sender-cache, fallback, caller-retry and legacy-threading compatibility checks pass **33/33**, `/tmp/ticket-conversations-reviewed-email-compatibility.log` (four review cases overlap). Final focused review checks pass **4/4**, `/tmp/ticket-conversations-reviewed-email-final-unit.log`, including mutation of caller-owned recipients/file buffers after the send begins. Provider/network boundaries are mocked; no external email was sent.
- Final server typecheck passes with an empty `/tmp/ticket-conversations-mailbox-final-tsc.log`. Database, email and co-managed package builds pass (`/tmp/ticket-conversations-mailbox-db-build.log`, `/tmp/ticket-conversations-reviewed-email-build.log`, `/tmp/ticket-conversations-mailbox-co-build.log`). No complete planned feature/test entry is newly claimed from this partial foundation.
- Next: persist author-private email recipients and subject, bind sender selection in creation/composition, reserve the immutable reviewed Send with its qualified publication/correlation records, and connect actual delivery/recovery to current authority. A fixed Microsoft sender must match the mailbox grant being exercised; the intake mailbox alone cannot grant use of a different outbound mailbox. Keep all qualified references in the origin's store when the ticket belongs to the other organization.
- Durable delivery needs appropriate current-author authorization after an accepted Send; do not fabricate a browser session or treat an expired browser credential as permanent queued-send authority. Provider `unknown` must remain recoverable without a blind second send. The existing co-managed notification transports document possible repeated SMTP delivery and are not a substitute for this explicit-send receipt contract.
- Complete inbound admission/recipient updates, attachments, protected review, notifications/reopen policy, sharing, AI and actual application/browser/email validation remain required. This checkpoint does not realize vendor email end to end; the goal remains active.

## Implementation checkpoint — reviewed vendor Send and composer

- Added `20260908034701_create_ticket_conversation_email_operations.cjs` through the Knex scaffold. Email envelopes are retained in author-private editor drafts and immutable publication receipts. Author-home operations hold private review/payload snapshots; confirmed correlation routes live in the selected mailbox's store. Qualified tenant metadata and deletion order include both new tables, and downgrade refuses retained data.
- `packages/co-managed/src/conversationEmailOperations.ts` separates preparation, confirmation and delivery. Preparation publishes nothing. Confirmation rechecks current ticket/audience/mailbox authority and exact sender/draft/conversation revisions, consumes the draft, and atomically publishes the selected vendor message, envelope and inbound correlation route. Repeated confirmation cannot consume a later draft. Incomplete recipient text is preserved while editing; review normalizes recipients, excludes routing addresses and deduplicates To/CC.
- Delivery commits an attempt marker before transport, then reacquires current authority for that attempt. A lost provider response or a database rollback after provider acceptance cannot cause a blind second send. Pending accepted sends can resume under a current authenticated author session; a five-minute unresolved attempt is displayed as unknown. No fabricated session or stale browser credential is used as queued authority. An autonomous durable worker and richer blocked/unknown recovery remain outstanding.
- Production adapter `packages/tickets/src/lib/namedConversationEmail.ts` renders only the selected rich-text draft, supplies a qualified reply token and stable conversation RFC headers, and invokes the reviewed email transport. A fixed Microsoft From must match the authorized selected mailbox. The Send publication adapter currently admits vendor conversations only; requester Send still needs canonical requester effects and legacy notification deduplication before it can use this path.
- The selected navigator now offers internal or vendor email creation. Vendor composition retains To, CC, subject and body through the same serial draft writer; mailbox selection flushes and rebases the draft. Review freezes editing and displays the actual resolved sender, recipients, subject and sandboxed body. It can return to the private draft before confirmation. An uncertain confirmation exposes a status check for the same operation instead of a second Send.
- The author's latest accepted send is restored after remount under current destination read access. History exposes only the published envelope and narrow delivery status to authorized destination readers; it does not project another author's private review, payload or reply token. Deleted messages omit the envelope. Incoming-envelope projection and next-reply recipient proposals remain to be connected.
- Existing Requester composition is still the legacy panel. Files, foreign co-managed screen/portal integration, All activity, unread/follow behavior, inbound routing and review, reopen policy, sharing, synthesis and explicit AI participation are still required. The vendor UI is implementation progress, not an actual running-app/email smoke or full feature realization.

### Verification

- Before the final composer/history additions, the full migrated PostgreSQL regression passed **899/899**, `/tmp/ticket-conversations-send-full-pg.log` (161.35 seconds). The four additional Send cases cover shared/private destinations, exact retries, later drafts, stale review, unknown outcomes and successful provider transport followed by forced database rollback. Rendering/provider boundaries are mocked; no external delivery is claimed.
- The real rich-text renderer plus reviewed transport and navigator/composer tests passed **15/15**, `/tmp/ticket-conversations-email-composer-unit.log`. Three new component journeys cover envelope restoration, review/cancel/confirm, uncertain-send checks and remount, and mailbox revision changes. The component tests mock actions and editor rendering; the separate renderer test uses the actual selected-content renderer.
- Initial author-private recovery checks passed in the focused migrated PostgreSQL suite **18/18**, `/tmp/ticket-conversations-email-composer-pg.log`. Final history projection, composer revisions, localization and package/typecheck verification are recorded below when complete.
- Marked F037/F038/F043/F059 implemented: mailbox ownership remains stable across real responsibility changes; foreign sending requires an explicit grant independently of read/post; recipients are envelope data and confer no application/follower access; conversation Open/Done commands do not change ticket status. Broader Send, files, recovery and inbound entries remain open.
- Final focused migrated PostgreSQL checks pass **18/18**, `/tmp/ticket-conversations-email-composer-final-pg.log`, including private latest-send recovery and shared published-envelope visibility without private review access. Final component/renderer/transport checks pass **15/15**, `/tmp/ticket-conversations-email-composer-final-unit.log`. Final server typecheck passes with an empty `/tmp/ticket-conversations-email-composer-final-tsc.log`. Database, email and co-managed builds passed; the final co-managed build is `/tmp/ticket-conversations-email-composer-co-build.log`.
- Full database regression after published-envelope projection passes **899/899**, `/tmp/ticket-conversations-email-composer-full-pg.log` (161.22 seconds). No test-list journey is marked complete from these partial implementation checks.
- Added an explicit interim ingress reservation for `tc1:` body markers (including malformed/case variants/competing quoted tokens) and conversation RFC reply references. Until the dedicated vendor writer is connected, these messages return the existing protected `quarantined` result before any legacy requester lookup/write. Threading regression passes **8/8**, `/tmp/ticket-conversations-inbound-reservation-unit.log`. This is preservation while implementation continues, not completed inbound admission or reviewer UX.
- Live HTTP check initially returned 500 because Next could not resolve the stacked co-managed package in instrumentation. Registered its source aliases/transpilation beside the existing workspace packages and matching CommonJS source exports, consistent with licensing. `require.resolve` finds the workspace source and the live sign-in page now returns **HTTP 200**. That proves compilation of this entry path, not ticket-screen behavior. The IDE/card read timed out; no actual browser pane or external email smoke has yet been completed.
- Final checkpoint checks pass after the ingress reservation and draft-flush adjustment: **18/18** composer/threading tests in `/tmp/ticket-conversations-send-checkpoint-final-unit.log`; server TypeScript exits successfully with an empty `/tmp/ticket-conversations-send-checkpoint-final-tsc.log`. The second IDE connection attempt also timed out. Live sign-in compilation is verified, but browser interaction remains unverified. Next implementation work is dedicated qualified vendor inbound admission; replace the interim protected reservation only when the named destination writer is available.

## Implementation checkpoint — qualified inbound vendor replies

- Added `20260908042702_create_ticket_conversation_inbound_receipts.cjs` through the Knex scaffolder. The mailbox store retains digest-qualified intake receipts and sender authentication evidence; the conversation content store retains the published inbound envelope and proposed next recipients. Private comments support explicit external authors without inventing staff users. Existing staff identity constraints remain enforced, and downgrade refuses retained external replies.
- Added `inboundNamedConversationEmail.ts` and a composition interface in the shared inbox engine. Both server and standalone email-service workers register it. Admission intersects all recognized body tokens and conversation RFC references with the receiving tenant/provider; unknown, malformed, competing or mismatched evidence is quarantined before legacy requester matching. Validly correlated new vendor colleagues are accepted using receiving-MTA sender authentication and current organizational conversation authority.
- Inbound reception uses the capability created by an accepted Send and current mailbox/relationship/collaboration scope. It does not borrow the sending technician's browser session or create an account for the vendor. Current operational lifecycle pauses still defer through the existing inbox engine. A changed mailbox, revoked collaboration or ended relationship cannot redirect a reply into another destination.
- Canonical customer-store replies use `TicketModel.createComment` with the admitted parent and internal audience; private replies stay in their origin store with explicit external attribution. The inbox transaction commits the message, qualified receipts, effect dedupe and terminal outcome together. Conversation Open/Done and message version change independently of the ticket; generic requester response, SLA, ticket activity and outbound email paths do not run here. Authorized follower fanout remains outstanding.
- Native mailbox owners with accepted conversation routes now require durable ingress even when the installation rollout setting is off, matching the existing retained co-managed intake requirement. This supplies durable MIME retention and the qualified writer without a co-managed entitlement.
- Named history displays accepted envelope evidence and external attribution. Reply defaults derive from the latest accepted incoming/outgoing envelope and remove routing addresses/duplicates; reading defaults never overwrites an existing author draft. New-correspondent highlighting and applying updated suggestions during an already-open draft remain outstanding.
- Qualified vendor artifacts currently stay in the protected artifact path and defer pending the named attachment adapter. The original MIME remains staged with the mailbox and is never made a generic ticket file. File publication/downloads, protected review UI/re-admission, side reopen board policy/follow-ups and follower notifications remain required before inbound work is considered complete.

### Evidence so far

- The new migrated database writer journeys pass **3/3**, `/tmp/ticket-conversations-inbound-pg-3.log`, covering Shared IT/private stores and current sender/mailbox/resource guards. The first fixtures used an obsolete attempts column and omitted the required ingress/source staging relationship; corrected them to the actual inbox schema without relaxing product constraints.
- Expanded named-conversation checks pass **21/21** against the real migrated schema, `/tmp/ticket-conversations-inbound-core-pg.log`. The new happy paths execute the actual fenced inbox processor and its native/qualified writer, verify terminal replay and one effect set, preserve the ticket row, and check external envelope/recipient projections. MIME fetch/parse transport boundaries are mocked; this is not a provider mailbox smoke.
- Composer/threading checks pass **18/18**, `/tmp/ticket-conversations-inbound-ui.log`; co-managed and database package builds pass. Further tokenless-reference, automated-reply, full regression and final TypeScript results are recorded below after completion.
- Marked F040/F042/F045/F046/F050 implemented for accepted envelope defaults/history, qualified reply correlation, new vendor correspondents and sanitized body/evidence retention. Attachment admission, protected-review resolution and live provider journeys remain separate incomplete requirements.
- Audited T001 against the actual concurrent default, native no-license, and repeated migrated-root integration cases. Marked T001 implemented: these tests execute real schema/queries, retain multiple roots and parent associations, reject incompatible linkage and preserve one requester default. Other full planned journeys remain unclaimed where files, UI or live-provider evidence is missing.
- Full migrated PostgreSQL regression passes **902/902**, `/tmp/ticket-conversations-inbound-full-pg.log` (161.17 seconds). Final focused conversation checks pass **21/21**, `/tmp/ticket-conversations-inbound-final-pg.log`, including header-only vendor replies, automated reply suppression, and the next reviewed Send's RFC In-Reply-To pointing at the accepted vendor message while preserving the conversation's outgoing reference.
- The incoming RFC identity comes from the digest-qualified inbox's RFC message ID, independently of the provider's message ID. Reply defaults and threading use the same deterministically ordered accepted email history; immutable sent envelopes are not rewritten by later arrivals.
- Final composer/threading checks pass **19/19**, `/tmp/ticket-conversations-inbound-final-ui.log`, including accepted recipient defaults and preserving a manually edited envelope through refresh/reload. The server typecheck passed before the final small RFC/redaction refinements; final verification is recorded below. The standalone email-service build and co-managed package build pass (`/tmp/ticket-conversations-inbound-service-build.log`, `/tmp/ticket-conversations-inbound-co-final-build.log`).
- Final server typecheck passes with an empty `/tmp/ticket-conversations-inbound-final-check-tsc.log`; final co-managed build passes in `/tmp/ticket-conversations-inbound-final-co-build.log`. Plan validation and `git diff --check` pass. The feature goal remains active.
- Remaining admission work includes protected-review destination selection/re-admission and recognizing ambiguous vendor mail with no conversation token/RFC reference (including subject-only replies) before any requester fallback. Only qualified marker/header admission is complete in this checkpoint. Do not broaden that claim from the passed writer tests.

### 2026-09-08 — Correspondent reservations and accepted-message reply chains

- Added `20260908045632_track_named_conversation_correspondents.cjs` with the Knex scaffolder. The mailbox-owned correspondent index is populated only by confirmed Send recipients and authenticated, accepted inbound authors. It excludes routing mailboxes, grants no application access, and never admits an email by sender address. The migration backfills retained qualified publications/receipts, is replay-safe, and refuses a downgrade that discards reservations; tenant metadata/deletion ordering includes the new table.
- Named admission now probes durable incoming messages before native matching. A known correspondent without named correlation is quarantined in the receiving inbox, with staged MIME retained and no ticket/comment/artifact/notification effects. Explicit cm1/cm2 artifacts still use their existing guarded admission unless conflicting named evidence exists. Unknown correspondents with no named evidence remain on ordinary intake. Reservation scope is the exact receiving organization and provider, not an email-address-wide association across organizations.
- Accepted vendor RFC Message-IDs now participate in correlation, so a colleague can reply to an accepted vendor message after the original Alga token/reference is stripped. All recognized evidence must still agree on the qualified conversation; ambiguous reused RFC identities, missing routes, invalid sender authentication and revoked destination authority remain quarantined. The nearest accepted reference retains its actual reply parent, subject to current publication/thread/audience checks.
- Artifact routing now checks the immutable named intake receipt as well as MIME markers. Header-only replies without a recognizable Alga token cannot fall into generic portal-visible attachment processing. Named artifacts still defer pending the dedicated file admission implementation; this is not completion of file support.
- The shared pipeline without a named writer also reserves known correspondents and accepted vendor RFC references. Native mailbox policy already requires durable intake once accepted conversation routes exist.
- Real migrated DB checks cover uncorrelated vendor mail through the actual fenced inbox core with/without the named writer, terminal replay, no requester effects, receipt-based artifact protection, reply nesting, sender-auth guards, mailbox/organization isolation, migration replay/backfill and retained-route FK enforcement. Provider MIME fetch/parse remains mocked at the transport boundary.
- Verification: full existing PostgreSQL suite **905/905** passed in `/tmp/ticket-conversations-correspondents-full-pg.log`; composer/legacy threading **19/19** in `/tmp/ticket-conversations-correspondents-unit.log`. Database, co-managed and standalone email-service builds passed. Final focused checks after the reply-parent adjustment are recorded below.
- Protected review remains unfinished, so no full ambiguity/review checklist entry is marked complete. Inspection found durable terminal quarantine (`inbound_email_inbox.status = skipped`, `outcome_reason = quarantined:conversation_reply_requires_admission`) and retained staged MIME, but no incoming review UI. Outbound email-log views are not an appropriate substitute. `inboundEmailIngressStagingWorker.buildEnvelope` retains From/To/CC/subject/reference metadata; full source must be fetched through its digest-verified staging API. Email provider configuration already uses `ticket_settings:update` for mailbox operational controls. Review must deliberately choose a currently permitted destination, preserve sender authentication and existing inbox dedupe/fences, and must not expose one private candidate's body merely because a reviewer can access another candidate conversation. Do not invent a new token or borrow a technician identity to force re-admission.
- Final focused PostgreSQL checks pass **24/24** after reply-parent and RFC-ambiguity guards, `/tmp/ticket-conversations-correspondents-final-pg.log`. Server TypeScript exits successfully with empty `/tmp/ticket-conversations-correspondents-check-tsc.log`; final co-managed build and plan/diff validation pass. No live browser/provider verification is claimed.

### 2026-09-08 — Protected review and explicit resolution

- Implemented **Email Settings → Held replies**, behind the existing UI-only `release-v1-6-feature`. The paginated list identifies the receiving mailbox, sender and subject. Opening a row loads digest-verified staged MIME, shows the extracted message/envelope/file names and offers an explicit currently writable vendor destination. No destination is preselected; reviewing alone publishes nothing. An uncertain response retains the exact resolution operation for retry. Refresh and tracked-session changes clear the prior review state.
- Held source is an administration surface owned by the receiving mailbox organization, guarded by a current internal home session and `ticket_settings:update`, matching existing mailbox operational controls. A foreign send grant or visibility of one candidate conversation does not authorize viewing held source or private candidate metadata. Every offered destination independently passes current ticket/audience/mailbox admission, and source fetch is followed by a fresh authorization check. The PRD now records this concrete extension of the existing terminal quarantine path.
- Added `inboundNamedConversationReview.ts` and authenticated integration actions for list/read/resolve. Source authentication comes from the original MIME; the browser cannot change the message or claim a sender. Incomplete authentication/unusable text remains held. Destination selection cannot switch the receiving mailbox, turn the reply into Requester, or bypass current conversation/ticket/relationship authority. Receipt replay also requires current reviewer and destination read authority.
- Added `resolveQuarantinedNamedReply` to the durable inbox engine: resume only the exact named-reply quarantine with unchanged source digest, use the original inbox and a fresh fenced claim within the owning transaction, run the canonical commit phase, insert normal effect/artifact manifests, and terminalize once. Failure rolls the transaction back to held status. Explicit review is classified as `manual_review`, not pretended header/token evidence. The named writer still enforces source, sender, current mailbox/route/audience and publication checks.
- Added migration `20260908051006_retain_named_reply_review_resolutions.cjs` through the Knex scaffold, with mailbox-home operation/inbox uniqueness, qualified destination references, request/source hashes, retained author identity, tenant metadata/deletion ordering and a non-lossy downgrade guard. The resolution receipt, admitted message, inbound receipt and original inbox outcome commit together. This does not create a synthetic token, external Send, requester lifecycle event, or duplicate ticket.
- Pareto DB scenarios cover preview-without-publication, real shared/private resolution via the inbox engine, stable retry, migration replay/retention, revoked administration permission, foreign reviewers, changed source digest, unverified sender, changed mailbox, unavailable destinations, and no requester effects. Component scenarios cover explicit destination selection, uncertain-response retry with identical intent, and disabled unverified-source publication. Existing email configuration resync tests now provide the newly used feature-flag context.
- Verification: **27/27** focused migrated PostgreSQL tests in `/tmp/ticket-conversations-review-pg.log`; **908/908** full co-managed PostgreSQL suite in `/tmp/ticket-conversations-review-full-pg.log`; **29/29** review/composer/threading/email-settings component checks in `/tmp/ticket-conversations-review-check-unit.log`. Server TypeScript passes with empty `/tmp/ticket-conversations-review-final-tsc.log`; database, co-managed and standalone email-service builds pass. Plan validation and diff checks pass.
- F047 is now code-complete; T016 remains incomplete because these tests mock provider MIME fetch/parse and do not constitute the planned email-infrastructure smoke. Named files still have their protected deferred artifact path, pending actual file publication/download support. No other feature/test entry is advanced by this checkpoint.
- Browser availability was checked using the Browser skill and its documented runtime discovery. Setup succeeded but selection returned “No browser is available”; documented browser listing returned an empty list. No live browser, live provider, or end-to-end feature completion is claimed. Reuse the existing runtime binding if a browser becomes available; there is no selected browser/tab to reuse. This does not block the remaining implementation work.

### 2026-09-08 — Received conversation files and authorized downloads

- Extended the existing protected `co_management_conversation_attachments` store/transfer engine instead of creating a parallel file store. Migration `20260908052731_extend_conversation_files_to_native_vendor_replies.cjs`, created through Knex, permits an absent co-managed relationship for native tickets and an explicitly external author with no fabricated staff ID. The identity constraint requires exactly a staff user or external email author. Existing human request fingerprints and qualified object paths remain compatible; rollback refuses retained native/external files.
- `transferAuthorizedCoManagedAttachment` now accepts a retained external intake actor and a native resource, while native transfers require the actor, ticket and content store to belong to the same organization. Existing co-managed interactive and worker adapters keep their original authority. The shared reservation, immutable byte/hash/path checks, pending/ready state, retry behavior and cleanup remain in use.
- Added `processNamedConversationReplyArtifact` and wired it through both actual inbox worker composition roots. The adapter checks the accepted mailbox-home receipt and its unchanged envelope, matching inbox/effects, current selected mailbox/relationship/conversation, published message/root and a live fenced artifact claim before transfer. No old technician session is borrowed for an external correspondent. File-size/type policy uses the destination content store. Disabled/revoked authority defers the artifact instead of exhausting retries or falling back to generic files.
- Attachments and embedded images materialize only in their conversation's content store. Completion and the fenced artifact receipt commit together; the conversation message version changes so the panel refreshes after a file becomes ready. Exact worker replay yields one file set. An uncertain upload remains unreadable in pending state, reuses the same object identity and can resume once mailbox authority returns. Original MIME is now marked complete as private staged audit evidence, with no conversation file, generic document, or generic external-file row.
- Added named-conversation file projection and authenticated `/api/tickets/conversation-attachments/[attachmentId]` downloads. The service checks current session/ticket/conversation/root/message/audience and attachment field redactions, then delegates byte retrieval and digest verification to the existing protected reader. Files display as qualified download links in the actual conversation component; storage paths never enter the page data. Deleted/unavailable roots suppress their files without breaking the existing history projection. Downloads use the existing non-executable, no-store response headers.
- Representative real-DB cases cover ordinary native tickets with no entitlement, customer-owned Shared IT, MSP-private vendor files, exact replay, original MIME separation, cross-organization read boundaries, byte tampering, deleted message denial, migration retention, an unknown upload result, disabled mailbox deferral and recovery. MIME fetch/parse and object storage are mocked transport boundaries; the inbox, attachment engine, metadata and authorization use the real migrated schema.
- Focused PostgreSQL checks pass **31/31** in `/tmp/ticket-conversations-files-fixed-pg.log`; composer/review/threading checks pass **22/22** in `/tmp/ticket-conversations-files-ui.log`, including a qualified download-link assertion. Server TypeScript passes with empty `/tmp/ticket-conversations-files-final-tsc.log`; co-managed and standalone email-service builds pass. Full-suite results after the retry-recovery assertion are recorded below.
- This completes the received-file path, not all conversation file work. Author-private draft uploads, explicit selection/removal, Send preview/transport files, atomic outbound content/file publication and selective-share file copies remain required. `conversationEditorDrafts` has a reserved manifest but the current Send prepare still rejects a nonempty manifest. The next extension must keep staged files author-private, bind published file visibility to the accepted operation/message, and preserve the same verified bytes through review, publication and transport. Do not fake comment/thread/user IDs to fit the earlier attachment contract.
- Broader F049/T017 remain incomplete until the planned allowed notification effects are integrated; current evidence proves inbox-message/file dedupe, not future follower fanout. No live browser/provider smoke is claimed.
- Full migrated PostgreSQL suite passes **912/912**, `/tmp/ticket-conversations-files-full-pg.log`, including existing human attachment/disclosure/cleanup behavior and the new recovery assertion. All test/build processes are complete; plan validation and diff checks pass.


## Private editable file storage — implementation checkpoint (2026-09-08)

- Implemented true editor-file bindings in the existing protected attachment table. Files belong to the authenticated author's home and the qualified destination, with no comment/thread until publication. The Knex-generated `20260908054601_retain_named_editor_file_bindings.cjs` migration enforces editor-versus-message binding and refuses rollback with retained editor files.
- Applied leverage as a bounded revision: common immutable byte validation, reservation, upload, and completion now live in `protectedConversationFileTransfer.ts`; published/inbound adapters retain their domain authority and historical request fingerprints. The editor adapter authorizes current session, ticket, lifecycle, destination and attachment fields in each transaction. Exact upload retries reuse bytes/path; uncertain acknowledgments leave pending, unreadable uploads. Author-private downloads verify the retained SHA and size.
- Draft requests accept explicit file references; metadata/digests come from admitted ready uploads. Existing text autosaves preserve selected files when the field is omitted. Selection/removal participates in draft CAS and operation replay; discard clears the manifest. Files cannot be borrowed from another author, ticket, or destination. Limits are twenty selections and 25 MiB aggregate, additionally subject to existing provider policy when the host adapter is connected.
- Extended existing seven-day cleanup to unselected ready editor uploads. Selected files survive cleanup. Candidate filtering excludes selected files before the maintenance limit, and selection is checked again under the file lock, so old active drafts neither lose files nor starve orphan cleanup.
- Focused migrated PostgreSQL checks initially pass **35/35** including three native/shared/private file journeys and one guard case. The full suite exposed a changed error category for corrupted storage paths (915 passed, one failed); restored the existing forbidden result and verified that case plus named conversations **36/36**, `/tmp/ticket-conversations-private-editor-fixed-pg.log`. Final cleanup-limit regression and final verification are recorded below.
- This is a backend checkpoint. Upload controls/host storage wiring, reviewed outbound bytes, content-owner file copies with atomic message visibility, and selective-share copies remain unfinished. No file feature/checklist is marked complete solely from these service tests. The active goal remains the complete feature, including actual app/email/AI verification.

- Next publication integration constraints: keep editor files in the author home until explicit Post/Send, then copy verified selected bytes to content-owner storage. A reserved publication operation may qualify future comment/root IDs, but visibility must require its actual accepted `ticket_conversation_publications` receipt; a ready object or coincidentally matching comment UUID alone is insufficient. Final publication must verify draft/conversation revisions and the complete selected manifest before atomically publishing content/files and consuming the draft. Copies need durable pending reservations for unknown upload acknowledgments and cleanup for never-accepted publication operations.
- Send transport already hashes real Buffer attachments, while `ConversationEmailPayload` is retained as JSON. Prefer qualified retained file descriptors with digest-verified byte hydration at preparation/recheck/delivery over embedding base64 files in database payloads. If accepted delivery uses author-home staging, cleanup must retain those exact files until the durable send no longer needs them; alternatively bind delivery to the independent, verified published copies after confirmation. This remains design guidance, not completed code.

- Final verification passes: **916/916** migrated PostgreSQL checks, `/tmp/ticket-conversations-private-editor-final-full-pg.log`; **22/22** component/threading checks, `/tmp/ticket-conversations-private-editor-ui.log`; server TypeScript exits successfully with empty `/tmp/ticket-conversations-private-editor-check-tsc.log`; co-managed package and standalone email-service builds pass. Plan validation and diff whitespace checks pass. Storage transport is mocked in the new database scenarios; no live browser or provider verification is claimed.


## Published files and reviewed attachment composition — implementation checkpoint (2026-09-08)

- Added `namedConversationPublicationFiles.ts`: explicit Post/Send stages independent digest-verified copies into the destination content store. Copy IDs derive from the immutable publication operation and qualified source file. Source editor bytes stay in the author's home. Each reservation/transport pass checks the current draft/conversation, author/session, lifecycle, audience and file selection. The final canonical publication checks every ready copy and consumes the draft in the same transaction as the message and publication receipt.
- `20260908060651_retain_named_file_publication_operations.cjs` adds the retained operation binding. Existing protected file readers now require the accepted named publication receipt for these copies; merely uploading a ready object or naming a future comment does not publish it. The normal seven-day cleanup handles unaccepted pending/ready copies, rechecking publication under the file lock. Accepted copies survive source-editor cleanup.
- Named email payloads retain qualified file descriptors and digests rather than database-embedded file bytes. The real ticket email adapter loads and verifies bytes before prepare, recheck and send; the existing email engine fingerprints actual Buffers. Confirmation changes transport file references to the verified published copies and rechecks that their bytes produce the same human-reviewed message hash. Delivery reacquires current source/file authority; removed files or redacted/deleted message sources cannot be used by a resumed pending send. The actor-private operation/payload is not exposed to conversation readers.
- Wired upload selection, file links, removal, stable retry/cancel and Send preview files into the actual named composer. Its serial draft writer now carries selected references through text/envelope edits and reload. Pending uploads lock navigation/composition until resolved, with the same upload identity reused after uncertainty. Downloads use the author-private editor endpoint with current tracked-session and named destination admission. Existing multipart size limits, tenant storage validation and complete provider acknowledgment apply. The storage helper and upload-limit parser moved to the ticket package with server compatibility exports; the tickets package now explicitly depends on storage.
- Added representative migrated-database journeys for native, Shared IT and organization-private publication: uncertain copy acknowledgment, hidden ready copies, canonical rollback where the host owns publication, concurrent exact retry and source cleanup with independent published bytes. The email scenario uses the actual ticket renderer/transport adapter and real reviewed-byte hashing with mocked provider I/O; it rejects changed bytes, preserves the draft, switches to published copies, denies removed files and sends once after source cleanup. Component coverage adds unknown-upload retry, draft/file reload and removal, plus file names/sizes in Send review.
- Verification: full migrated PostgreSQL suite **920/920**, `/tmp/ticket-conversations-file-send-full-pg.log`; component/threading suite **23/23**, `/tmp/ticket-conversations-file-send-final-ui.log`; server typecheck and co-managed/standalone email-service builds pass. Final concurrent-retry refinement and typecheck evidence are recorded below. Provider/storage I/O is mocked; no live browser/mailbox verification is claimed.
- Requester remains the legacy host panel, so the broad draft/publication/review checklist items remain open until requester/co-managed/portal composition is integrated. Selective-share file copies are also still required. This checkpoint does not reduce the full feature goal. Next priority: bring the Requester and existing conversation surfaces onto the named foundation while preserving canonical requester delivery, response/SLA behavior and the current ticket layout.

- Final refinement passes **39/39** named-conversation database checks including concurrent exact publication retries, `/tmp/ticket-conversations-file-send-concurrent-pg.log`. Server TypeScript exits successfully with empty `/tmp/ticket-conversations-file-send-check-tsc.log`; the final co-managed build and plan/diff validation pass. All verification processes are complete.


### 2026-09-08 — Co-managed navigator and minimal requester portal selector

- The co-managed full ticket screen now composes the same Option C navigator with qualified ticket/home actor scope. Its Requester panel uses the canonical existing co-managed comment, attachment, and notification flow, restricted by the named Requester read before projection. Ordinary embedded co-managed controls retain their existing layout. A current legacy Requester edit blocks switching with a clear finish/cancel hint; this is an interim compatibility bridge, not evidence that unified Requester autosave/reviewed Send is complete.
- Added an owner-scoped portal projection after existing client/board/ticket admission. It exposes only requester conversation names/IDs/state, defaults to Requester, and holds the selected audience through the read/publication transaction. Explicit private/unknown/invalid IDs fail closed. Legacy unassociated public roots remain in default Requester under the existing root/audience guards; explicitly named siblings never flatten into it.
- Portal posting now binds its root to the selected requester conversation and increments/reopens that conversation while preserving canonical requester response state, bundle processing, and existing events. Edit/delete refreshes retain the selected destination.
- The existing portal ticket screen has a minimal selector under the UI-only release flag. Unfinished edits block switching; pending work freezes the composer; failed loads retain the current conversation; ticket changes discard prior requests. An account/session change removes prefetched ticket bodies and documents and asks the user to reopen under their current identity. Marked F102 implemented in code. T034 remains pending because it explicitly requires actual application/browser validation including the side-policy UI.
- Verification: 13/13 real migrated PostgreSQL portal tests passed in `/tmp/ticket-conversations-co-portal-final-pg.log` (921 total suite cases, 908 unrelated cases skipped). The representative new case exercises actual portal reads/posts, default/additional separation, root binding, Open/Done and requester response effects, plus private/invalid destination rejection without writes. The disposable schema-only clone was dropped after the run; the development database was not mutated.
- Component/action verification: 77/77 passed in `/tmp/ticket-conversations-co-portal-final-ui.log`; the subsequent portal account-change case brings the distinct set to 78, with final portal 4/4 in `/tmp/ticket-conversations-portal-session-ui.log`. The tests render actual screen/controllers but stub rich editors, UI primitives and remote actions; they are not live browser evidence. Existing action mocks were adjusted for the new projection and the parent's visibility helper. Server TypeScript passed with an empty `/tmp/ticket-conversations-co-portal-final-tsc.log`.
- Remaining next integration: unified requester drafts/reviewed Send must preserve requester identity/correlation, response-state/SLA/bundle effects, and canonical event fanout without duplicate email. All activity, subscriptions/read cursors, side reopen policy, sharing/AI synthesis and live end-to-end checks remain within the full plan.


### 2026-09-08 — Canonical requester publication foundation

- Moved the canonical co-managed comment writer into `packages/tickets/src/lib/applyTicketConversationComment.ts`. The existing server command and named/native publication adapter now call it. Admission and idempotent receipts remain in their existing command layer; the writer preserves canonical comments, current response-state permission/setting, qualified actor activity, stable event identities, and after-commit publication. Named side traffic still changes neither requester response state nor customer-visible ticket activity.
- The named publication context carries its effective response-state field permission from the owning admission. A representative DB case applies a requester message under real named-ticket admission, verifies denied response-state writes roll back the comment and effects, and verifies permitted writes update requester state/activity and classify public message events correctly. This is writer-level coverage, not proof that the new requester email command is enabled.
- Named reviewed email declares ownership of external delivery using the existing `suppressContactNotifications` event contract. Native and retained requester/contact/watch-list notification paths can therefore avoid duplicating the reviewed envelope; this leaves internal notification authorization/preferences intact. Legacy requester comments retain their normal notification behavior. Requester guards in email preparation/publication/files and inbound named routing are deliberately still present until the complete request/response path is integrated.
- Evidence: 921/921 PostgreSQL regression cases passed after extraction in `/tmp/ticket-conversations-writer-full-pg.log`; the final response-state permission/suppression changes and new representative test passed 44/44 focused cases in `/tmp/ticket-conversations-writer-final-named-pg.log` (922 total cases, 878 outside this targeted set). The disposable schema clone was dropped. No live email provider or browser claim follows from these tests.
- Action/notification/renderer checks: 24 cases passed in `/tmp/ticket-conversations-writer-unit.log`; its one renderer fixture needed the now-required explicit `files: []`, and the corrected case passed in `/tmp/ticket-conversations-writer-rendering.log`, giving 25 distinct passing checks. Server TypeScript passed with empty `/tmp/ticket-conversations-writer-check-tsc.log`; co-managed package build passed in `/tmp/ticket-conversations-writer-co-build.log`. All process handles closed. No additional feature/test checklist item is marked complete because unified Requester composition and the broader plan requirements remain outstanding.
- Next routing discovery: `cm1` requester tokens bind one current contact/location and source comment; `tc1` named routes currently terminate in the vendor writer, bypassing canonical requester reopen/follow-up behavior. Reusing a vendor writer by simply removing requester guards is incorrect. The existing `handleThreadedReply`/`handleNewTicket` callbacks in `processInboundEmailInApp.ts` already preserve requester lifecycle and guarded follow-ups; named requester admission must feed a currently admitted identity/destination through that path and retain the actual reply/follow-up conversation and file bindings. Avoid manufacturing internal user identity from an email address, adding a legacy fallback for rejected named evidence, or losing explicit reviewed To/CC semantics.


### 2026-09-08 — Named requester inbound engine callback and follow-up binding

- Extended the trusted `NamedConversationReplyAdmission` port with the canonical requester writer. `processInboundEmailInApp` supplies it after preparing its parser/policy helpers, while retaining named rejection before legacy dedupe/token/thread matching. A runtime audience guard prevents this callback from applying requester lifecycle to side traffic. Vendor admission keeps its own writer.
- The admitted reply may prepare a named container on an already-authorized cutoff follow-up before its first comment is created. `createCommentFromEmail` and `TicketModel.createComment` carry an explicit conversation ID through the existing qualified root-binding helper. They validate same-ticket/audience association and reject conflicting parent/container binding atomically; no post-publication root reassignment or duplicate ticket writer is introduced.
- Added four representative real-inbox/DB cases: canonical requester reply, cutoff follow-up with a named container, revoked authority with no legacy fallback, and foreign-container rejection rolling back the newly created ticket/comment/effects. They reuse a real cm1 credential to exercise the engine's trusted admission port. This is deliberately not evidence of finished tc1 requester route discovery, sender admission, or UI Send. The relevant 15-case DB subset passes in `/tmp/ticket-conversations-requester-followup-pg.log`.
- Final targeted unit tests: 32/32 in `/tmp/ticket-conversations-requester-engine-final-unit.log`. Server TypeScript passes with empty `/tmp/ticket-conversations-requester-followup-tsc.log`; the email-service build passes in `/tmp/ticket-conversations-requester-engine-email-build.log`.
- Remaining domain integration: distinguish current requester/contact and authorized internal-technician senders without inventing login identities; qualify tc1 source/mailbox and original reviewed envelope; retain the actual accepted reply/follow-up destination and attachment lineage. A follow-up receipt may point to a different ticket/container than the originating Send route, so routing and artifact readers must validate that retained relationship instead of assuming source and destination are identical. Correlation across quoted ancestor messages must preserve ambiguity rejection; unrelated or sibling conversations cannot be silently joined. Outbound requester guards remain until these pieces and the unified composer are integrated.
- Full regression: **926/926** migrated PostgreSQL cases passed in `/tmp/ticket-conversations-requester-engine-full-pg.log` (169.25s), including legacy requester/technician intake and named vendor flows. The disposable database was dropped and all process handles are closed. No broader feature/test checklist entry is newly marked complete; actual named requester admission and full application delivery remain pending.


## Implementation checkpoint — reviewed requester mail and retained follow-ups

- Enabled Requester in the named reviewed Send and prepared-file publication commands. Requester publication now passes `is_internal: false` to the shared canonical writer; its response-state/activity/event behavior and external-delivery suppression therefore use the existing requester rules. Internal Post still requires internal transport.
- Added `packages/co-managed/src/namedRequesterReplyIdentity.ts`. Accepted reviewed To/CC provides email correspondence authority, never a portal account or follower grant. Current active contact/client/board visibility and additional email aliases determine contact attribution. Unknown explicitly addressed correspondents remain external. Technician attribution requires the existing stronger aligned MTA authentication plus current owner identity, read/update permissions and field visibility; cutoff follow-up creation also checks create authority. No staff session is fabricated from an email address.
- Connected real tc1/RFC route admission to the canonical requester engine callback. Mailbox, source publication/root, audience, durable source and sender checks precede the writer. Requester automation remains with the existing engine so cutoff handling still precedes automated/ack suppression. Lifecycle expiry is rechecked after the writer. A worker missing the canonical writer retries instead of misclassifying valid mail as an unauthorized reply.
- Requester intake is owner-scoped. A departed relationship does not veto an otherwise operational paid owner's requester mail; an inactive co-managed workspace still observes its existing lifecycle pause. Vendor/private receiving continues to retain the applicable relationship and originating mailbox scope.
- A cutoff follow-up initializes the equivalent named requester container before the first root. It retains the name/default slot and mailbox without inventing a human creator. Its `creation_hash` binds the originating accepted Send, inbox, destination and source parent. Receipt/envelope rows point at the actual follow-up message, with the original route operation retained separately.
- Accepted Send envelopes also retain their audience: later audience changes cannot reinterpret vendor mail as Requester or requester mail as internal traffic. Requester remains excluded from internal Post and its file preparation path, preventing a bypass of reviewed Send.
- RFC correlation validates that older references are proven ancestors of the nearest destination. Continued replies to a follow-up and later human Sends work with retained original references; sibling destinations and altered lineage quarantine instead of falling back to Requester or guessing a ticket.
- Received attachments use the same verified follow-up destination and recheck current requester sender authority. Current named-ticket readers can read owner-stored requester files even when a file has no relationship ID; only this already-authorized requester read omits the relationship filter. Private and Shared IT file qualification remains unchanged. Tests verify both owner/MSP access, denial after MSP revocation, no upload after requester deactivation, and independent owner access.
- Real domain tests now exercise reviewed requester Send through actual durable inbound processing (not a substituted cm1 credential): contact and alias attribution, explicit external recipients, strong/weak/revoked technician identity, board/contact/client guards, native and departed-owner behavior, ordinary/header-only replies, cutoff/automated-cutoff follow-ups, receipt failure rollback/retry, ancestry conflicts, received files and current reader revocation. The reviewed-byte transport test also runs for native Requester with actual rendering, hash validation, publication copies, staging cleanup and exact retries; only object storage and the provider transport are controlled boundaries.
- Verification: all 944 cases passed in `/tmp/ticket-conversations-requester-domain-full-final-pg.log`. After the requester file-read correction, 29 targeted requester/received-file cases passed in `/tmp/ticket-conversations-requester-final-pg.log`; the two final reviewed-byte tests passed in `/tmp/ticket-conversations-requester-outbound-files-pg.log`. There are now 949 cases total; this is not a claim of a subsequent full 949-case run. Fifteen UI/renderer unit cases passed in `/tmp/ticket-conversations-requester-domain-unit.log`. Final verification after the audience and Post guards: 69/69 named-conversation cases passed in `/tmp/ticket-conversations-requester-verified-pg.log`; server typecheck and co-managed build passed (`/tmp/ticket-conversations-requester-verified-tsc.log`, `/tmp/ticket-conversations-requester-verified-build.log`). Each PostgreSQL run used and dropped its own disposable database.
- F031 (atomic prepared publication), F051 (legacy requester/reply compatibility) and T011 (prepared-file atomicity/retry coverage) are now implemented. Default Requester still needs the unified private-draft/reviewed composer in the actual ticket shell; this checkpoint does not mark those UX features or browser journeys complete.
- Remaining full scope includes that Requester UI integration and portal file delivery, All activity/deep links, read cursors/followers/allowed notification delivery, side-conversation reopen opt-in and follow-ups, disclosure/share/synthesis/AI, ownership/export integration and actual application/email/browser verification. The full plan remains the goal; no board step was advanced.


### 2026-09-08 — Co-managed Requester composer and owner file display

- Replaced the co-managed default Requester creation/reply bridge with the shared private-draft and reviewed Send composer. The existing qualified history retains edit/delete actions; opening an edit first flushes the private draft, active history edits disable composition and switching, and email review locks prevent concurrent history edits. Sending refreshes the selected history. This does not yet integrate the native grid/entry composers.
- Empty Requester composition now derives editable recipient/subject defaults from the currently visible active primary contact or active default client location, after current client/contact/board and field-visibility guards. Existing accepted conversation envelopes take precedence. Prefilling an untouched composer creates no draft; editing/switching/reloading restores the author's private envelope without replacing it with defaults.
- The Requester history renders published email envelopes and reads files through named conversation authority. This includes owner-stored inbound files without a relationship ID. Downloads use the existing named-file endpoint. Existing upload/removal controls remain, with removal hints restricted to files admitted by their current management command; receipt-owned files do not acquire removal authority merely from being readable. The attachment screen periodically rechecks access and clears file names after denial.
- Added a reusable named message attachment read using the same qualified message/root/audience context as download. Real PostgreSQL coverage verifies both currently authorized organizations can list/read received Requester files, MSP revocation rejects subsequent access, and the owner's access remains independent.
- Corrected three new test fixtures after the first run: history edits submit through Send, native ticket references omit the co-managed relationship, and inactive location behavior is tested without violating the schema's single-default-location constraint. No production guard was weakened to fit those fixtures.
- Verification: 71/71 component/action tests pass in `/tmp/ticket-conversations-requester-composer-attachments-ui.log`; eight focused migrated PostgreSQL tests pass in `/tmp/ticket-conversations-requester-composer-attachments-pg.log` (955 total cases, 947 outside this subset). The suite creates and drops a disposable schema clone. Server TypeScript passes with empty `/tmp/ticket-conversations-requester-composer-attachments-tsc.log`; the co-managed build passes in `/tmp/ticket-conversations-requester-composer-attachments-build.log`. The final email-envelope history check passes 29/29 in `/tmp/ticket-conversations-requester-history-envelope-ui.log`. All verification processes are complete. Actual browser/provider validation remains pending.
- No full Requester UX feature is marked complete from this co-managed-only integration. Remaining full scope still includes native grid/entry/drawer composition with resolution/scheduling/history actions, additional Requester creation, portal files, All activity, deep links, subscriptions/notification fanout, side reply policy/follow-ups, sharing/disclosure, synthesis/AI, ownership/export integration, and live application/email/browser verification.


### 2026-09-08 — Additional Requester conversation creation

- The navigator now exposes New conversation to users who can write only Requester, and the creation dialog includes every currently writable audience. Private remains the initial choice when allowed. Choosing Requester selects and locks email transport; creation does not publish, save a message draft, or send mail, and never replaces the pinned default Requester. Additional Requester conversations open the shared reviewed email panel.
- Creation retains the exact operation on an uncertain response. The dialog is keyed to the qualified session/resource and drops late responses after unmount, preventing a prior session's completion from navigating the new session.
- Verification: 19/19 named-conversation component tests pass in `/tmp/ticket-conversations-additional-requester-session-ui.log`, including mixed/requester-only creation authority, email-only transport, uncertain creation retry, no publication effects and late-session response rejection. The existing domain creation and default-slot rules are unchanged. No additional feature/checklist item is claimed complete.
- Native integration investigation: `TicketConversation.tsx` and `bento/BentoTimelineTile.tsx` have distinct history/composer presentations, including resolution/close-status, suppression, scheduled publishing, reactions, reply panels and clipboard file lifecycles. Preserve these when attaching the named Requester composer; simply replacing either history with the generic named panel loses required behavior. The canonical collaboration writer currently rejects resolution/scheduling metadata, so supporting those publication options requires an intentional command design rather than passing unsupported flags. Legacy comment reads already apply `legacyTicketConversationSql`; do not undo that named-side isolation while adding selected Requester history.


### 2026-09-08 — Portal files follow the selected Requester conversation

- Reused the existing client-portal attachment action, protected endpoint and composition provider. Portal ticket reads now project the already-authorized selected conversation ID on each visible comment. Attachment list/download requests carry that ID, and the component remounts on a conversation change so an earlier response cannot restore filenames in the new selection.
- Portal file admission now selects and locks the Requester container through `readPortalTicketConversations` after current tracked-session, portal role, contact/client and board checks. It then verifies that the published requester message/root belongs to that container. A missing conversation ID supports only default Requester (including its unassociated legacy roots); additional Requester files require their explicit destination. Sibling, inaccessible, invalid and private-container references fail before storage access. Store ownership still comes exclusively from the verified requester session.
- The existing root-tombstone semantics, retained customer ownership after relationship departure, per-file content hashing and non-executable download responses remain. Named-conversation denials return the same opaque 404 as existing attachment denials. UI release gating remains in the provider; backend authorization does not depend on the release flag.
- Verification: 26/26 component/action/endpoint checks pass in `/tmp/ticket-conversations-portal-named-files-ui.log`; another 4/4 portal selection checks pass in `/tmp/ticket-conversations-portal-files-selection-ui.log`. Sixteen real migrated PostgreSQL checks pass in `/tmp/ticket-conversations-portal-named-files-pg.log` (956 total suite cases, 940 outside this subset), including additional/default isolation, private-container rejection before storage, current requester access and retained customer file reads. The suite created and dropped a disposable database. Server TypeScript passes with empty `/tmp/ticket-conversations-portal-named-files-tsc.log`. All verification handles are complete. These tests do not substitute for live application/browser/provider validation.
- The previous assumption that portal files required a new endpoint was incorrect: the existing provider/endpoint already transported the published file representation; the missing element was selected-container binding. No duplicate endpoint or separate file store was introduced.
- Full goal remains active. Native default Requester composition still needs resolution/scheduling-compatible publication and integration in grid/entry/drawer histories; All activity/reply navigation, subscriptions, side reply policy, disclosure/share, synthesis/AI and actual end-to-end validation remain required.


### 2026-09-08 — All activity and source composer reply links

- Added All activity to the common navigator and its URL selection state. It displays authorized messages chronologically with their named conversation/audience, existing email envelopes/files and resolution markers. It has no broadcast composer. Each Reply navigates to its source conversation with a qualified thread/comment target.
- Extracted the existing authorized container-list and selected-message readers locally so both single-conversation and aggregate reads use the same current ticket/organization admission. All activity holds that admission and each selected audience within one transaction. It merges bounded source pages using the exact microsecond timestamp/store/comment ordering, then emits a global cursor. A busy sibling cannot conceal messages or introduce duplicates across pages. No private store outside the current actor's authority enters the read.
- Added resolution projection to the existing history query, including the explicit false value for private-store rows in its union and field-redaction handling. Body, author, deleted-message, file and email filtering remains with the existing reader. This is a bounded application-driven reuse of the existing engine, not a second ticket permission system.
- Shared reply-link consumption checks a current authorized parent before changing the named private draft, retains its content, saves the reply relationship, then removes the consumed link parameters. A rejected parent leaves the saved draft unchanged. Late/unmounted callbacks do not apply a reply. The native entry and bento histories admit links from their already-authorized published Requester comments and open their existing inline reply/dock; open native edits/composition defer the link. Native unified private drafting/reviewed Send remains a separate outstanding requirement.
- All activity retains the number of loaded pages during periodic refresh, rechecks their contents, discards stale in-flight results, clears bodies on denied reads and provides a retry action. Default Requester labels and all added controls use localized strings.
- Focused verification: 23 named UI cases pass in `/tmp/ticket-conversations-all-activity-refresh-ui.log`; 55 named/co-managed/portal UI cases passed before the final refresh case in `/tmp/ticket-conversations-all-activity-final-ui.log`. Twenty-three native reply/order/composer checks pass in `/tmp/ticket-conversations-all-activity-native-final-ui.log`, including actual entry and bento component reply-link opening (editors/actions remain controlled test boundaries). TypeScript passes with empty `/tmp/ticket-conversations-all-activity-final-tsc.log`; the co-managed build passes in `/tmp/ticket-conversations-all-activity-build.log`.
- The two expanded migrated-PostgreSQL history scenarios pass in `/tmp/ticket-conversations-all-activity-final-pg.log`: 57 mixed messages paginate exactly with microsecond ties, scheduled messages are excluded, resolution markers remain, reply targets reject sibling containers and foreign tickets, and owner readers cannot enumerate the provider-private source. An initially malformed test reference was corrected to exercise the actual sibling guard. The broader regression is being checked separately below.
- Marked F026 implemented in code. Its actual-application All activity test remains unimplemented because browser verification with preserved drafts is still required. The full scope remains active, including native Requester composition with resolution/scheduling semantics, subscriptions/notifications, side reply policy, sharing/disclosure, synthesis/AI, ownership/export and live end-to-end verification.

- Full regression passes **956/956** in `/tmp/ticket-conversations-all-activity-full-pg.log` (173.11s); its disposable database was dropped and every verification handle is complete. The final default-Requester activity label uses the same localized text as the navigator.
- Next native integration evidence: `packages/tickets/src/actions/comment-actions/commentActions.ts` retains scheduled-comment admission, after-commit job registration and rescheduling, while `optimizedTicketActions.ts` carries the existing resolution/close and scheduling flow used by the ticket shell. The named writer adapter in `packages/tickets/src/lib/postNamedTicketConversation.ts` currently calls `applyTicketConversationComment` without those publication options. Preserve the canonical scheduling/close rules when extending the command and its reviewed-email receipt. Legacy native Requester draft preservation while leaving the panel is still outstanding; source-link support alone does not complete that requirement or the actual-browser All activity test.

## Implementation checkpoint — native Requester resolution intent and bundle effects

- Added `20260908091259_retain_named_requester_publication_options.cjs` through the Knex scaffold. Author-private drafts, immutable email operations and publication receipts retain an exact structured resolution option separately from the message body. The migration is replayable and refuses to discard retained options on downgrade. Existing options are preserved when an older autosaver omits the field; explicit null/discard clears them.
- Resolution annotation is currently admitted for native owner Requester email conversations only, under current update/read authority and resolution field visibility. Shared/private conversations reject it. Preparation and confirmation bind the option to the exact draft revision and recheck current permission. The canonical writer records the resolution marker without changing ticket status. This does not yet implement close-with-resolution or scheduled named publication.
- The shared composer reads server publication capabilities, privately autosaves/restores the resolution switch, and includes its effect in the locked human email review. Publication clears the consumed option. Resolution field hiding includes both unqualified and `comments.*` aliases.
- Extracted `packages/tickets/src/lib/ticketBundleCommentEffects.ts` from the optimized native comment action and reused it after native default Requester publication. Published default Requester messages retain child-to-master reopening and `sync_updates` mirrors, resolution attribution, default child container mapping and mirror deduplication. Child container message versions advance with a new mirror. Additional Requester, vendor and internal containers do not gain these effects. Bundle copies retain the existing history-only behavior; they do not add an outgoing envelope or email recipients.
- Named publication admits every affected bundle ticket using the author's current real home session, read/update scope and field masks before changing it. Failure rolls the source comment, route, mirrors and draft consumption back together. The legacy optimized action retains its existing tenant-wide comment admission; shared/foreign bundle propagation is not newly enabled.
- The native default Requester UI still uses its established composer. Its replacement must preserve close rules/overrides, notification suppression, scheduling, clipboard file lifecycle, reactions/history editing, reply drawer and grid/entry layout. The resolution and bundle foundations are progress toward that integration, not completion of native draft switching or the full plan.

### Verification

- Recovered completed handles from the prior implementation turn: 24 composer tests and four migrated PostgreSQL resolution cases passed. The four DB cases cover retained/restored options, stale review after option changes, shared/private denial, canonical marker and unchanged ticket status, exact retry, and migration replay/downgrade protection.
- Four additional migrated PostgreSQL bundle cases pass (`/tmp/ticket-conversations-native-bundle-pg.log`): default Requester mirrors and history, child reply reopen, current target scope revoked after review with atomic rollback, and additional Requester exclusion from mirror/reopen. Provider transport is mocked; no external delivery is claimed.
- Focused component and legacy notification checks pass 47/47 (`/tmp/ticket-conversations-native-bundle-final-unit.log`). The first run exposed a new component test clicking before mailbox loading completed and old notification-only fixtures passing fake non-UUID roots into the actual container binder. Fixed the test readiness wait and isolated the binder/bundle boundaries in that notification suite; the actual storage and bundle behavior is covered by the migrated DB tests above.
- The first TypeScript pass caught the legacy DTO's optional comment ID at the extracted call. The writer now passes the already validated generated ID. Final typecheck and full database regression results will be recorded after their running processes complete. Co-managed package build passes.
- No complete feature/test checklist item is newly claimed from this compatibility foundation. Sharing, followers/unread, board side-reply behavior, whole-source synthesis, optional AI and actual application/browser/email verification remain in the original scope.
- Final full migrated PostgreSQL regression passes **964/964**, `/tmp/ticket-conversations-resolution-bundle-full-pg.log` (175.51 seconds). Final server TypeScript exits successfully with an empty `/tmp/ticket-conversations-resolution-bundle-final-tsc.log`; co-managed build passes. The adjusted tenant-scoping contract passes **8/8** from the server test root (`/tmp/ticket-conversations-bundle-contract-final.log`). All test processes for this checkpoint are complete.

## Implementation checkpoint — reviewed native Requester close-with-resolution

- Added `20260908094308_retain_named_requester_close_intent.cjs` through the Knex scaffold. The retained publication option can now contain an explicit closed-status ID and optional human override reason. Shape checks reject extra keys and malformed status/reason data. Migration replay is supported; downgrade refuses retained close intent.
- Current native Requester admission checks the owning ticket's board, a currently closed ticket status, bundle-child restrictions, status/board field visibility, and `ticket:close_override` for override intent. These checks run at private save, preparation and final publication. Current capability reads supply only applicable closed statuses and the existing override permission. Email body/rendering never receives this business-state intent.
- `closeNamedRequesterTicket.ts` invokes the existing `updateTicketInTransaction` after the resolution comment exists in the same publication transaction. Existing close rules, SLA/response-state effects, attribution, audit and permissioned overrides remain authoritative. Close-event contact delivery is suppressed because the exact reviewed Send owns external delivery. Every bundle target is separately admitted before native status propagation; a blocked target rolls back the source and child effects together, including additional Requester closes.
- The shared composer privately retains/restores the selected closed status and explicit override reason, shows them in review, and clears them after accepted publication. The actual Send adapter returns narrow close-rule identifiers after current access is rechecked; the review shows unmet requirements without hidden field values/counts and allows returning to the intact private draft. No comment, status change, consumed draft, route or external delivery is committed when close rules reject the command.
- The ticket closes when the reviewed Send is accepted, rather than waiting for provider delivery confirmation. Unknown delivery still uses the same retained send operation; changing ticket state does not authorize another external send.
- Native default Requester integration still needs supported scheduled-comment behavior, private draft preservation across grid/entry/drawer, and existing file/history/reaction/editor controls. This checkpoint does not switch the native default composer or claim the broader compatibility checklist complete.

### Verification

- Five migrated PostgreSQL close journeys plus the eight affected resolution/bundle cases pass **13/13** (`/tmp/ticket-conversations-close-intent-final-pg.log`, 12.30 seconds). Coverage includes the actual Send adapter's blocked-close result, exact retry without duplicate close events, canonical state and resolution marker, override audit/reason, invalidated closed-status configuration, inaccessible bundle-child rollback, migration replay and downgrade refusal. Provider boundaries are mocked.
- Shared composer tests pass **25/25** (`/tmp/ticket-conversations-close-intent-ui.log`), including restored close intent, visible closed-status review, blocked-send recovery, a separately reviewed permitted override and final draft clearing.
- The initial TypeScript check and co-managed build pass. Final rendering/notification and TypeScript results will be appended when their current processes finish. No new whole-plan feature/test entry is marked complete from these partial compatibility steps.
- Final rendering and legacy notification checks pass **16/16** (`/tmp/ticket-conversations-close-intent-rendering.log`). The native ticket host now explicitly refreshes its canonical ticket/comment snapshot after an accepted named publication, including when live updates are disabled. Composer acceptance callbacks are awaited; blocked closes do not fire them. Final shared-composer checks pass **25/25** (`/tmp/ticket-conversations-close-intent-final-ui.log`), and native host/live-update checks pass **8/8** (`/tmp/ticket-conversations-close-host-final-ui.log`). The existing T037 conflict case needed its initial editor dirty-state report to settle before injecting a competing event; it passes with that explicit readiness wait. Final server TypeScript passes with an empty `/tmp/ticket-conversations-close-host-final-tsc.log`. All checkpoint processes have completed.
- Next: integrate supported native scheduled replies with the author-private draft/reviewed publication path and existing worker policy. Do not close a ticket, publish a reply, move response state, notify followers or reopen a bundle when merely scheduling. A future worker needs durable current authority for an accepted schedule, not a fabricated browser session or an assumption that the original interactive session remains valid. Then attach the shared composer to the native default Requester history in grid/entry/drawer while preserving the existing editor, files, reactions and reply-drawer workflows. No full application/browser or provider-mailbox smoke is claimed yet.

### 2026-09-08 — Deferred native reviewed email delivery

- Confirmed native Send can now resume through the existing maintenance recovery job after the originating browser session expires or is revoked. The worker accepts only a qualified retained operation; it derives the original author from that operation and locks the current active home identity, native ticket read/update policy, conversation audience/revision and selected mailbox. It never constructs a substitute session or grants a foreign author native authority. Co-managed deferred delivery still needs its relationship-aware counterpart.
- Extracted native resource admission and the two-phase reviewed-delivery engine as a bounded layering change: the former browser-only engine prevented a deferred author from using the same checks. Authentication/retained-intent admission stays in separate callers; current resource/mailbox/file rules and the durable attempt marker are shared. Worker locks follow browser identity → ticket → conversation → operation order.
- The retained human publication and mailbox route must both exist. An unconfirmed review cannot become background Send. Source visibility is checked even for messages without files; deleted source/root content and current envelope masks prevent transport. Transport still sends the immutable reviewed payload and verifies sender/message revisions.
- Migration `20260908101532_retain_named_email_recovery_due.cjs` was generated through Knex. Its retry timestamp spaces failed scans without changing publication intent or replacing the delivery attempt identity. Bounded recovery uses row locking/skip-locked selection; only native pending operations are discovered. Sending/unknown outcomes are never blindly retried, including a database rollback after provider acceptance.
- Scheduling discovery: existing scheduled comments publish through `publish-scheduled-comment`, with startup/maintenance reconciliation. Named scheduled replies must retain the reviewed envelope and bypass generic contact-email delivery. Existing scheduling does not close tickets at acceptance or due time; do not introduce scheduled-close semantics. The current named composer does not yet expose scheduling, and no scheduling feature is marked complete by this delivery work.
- Verification: the full migrated PostgreSQL regression passed 979/979 (`/tmp/ticket-conversations-deferred-full-pg.log`, 182.16s). After the final envelope-mask guard, the focused recovery suite passed 11/11 (`/tmp/ticket-conversations-deferred-final-pg.log`); total declared database cases are now 980. These journeys exercise the actual registered maintenance handler, revocation/role/mailbox/source guards, unconfirmed-review rejection, unknown outcomes, post-provider rollback and retry spacing with mocked provider transport.
- Maintenance fanout and reviewed renderer checks passed 23/23 (`/tmp/ticket-conversations-deferred-unit.log`). Server TypeScript passed with an empty diagnostic log (`/tmp/ticket-conversations-deferred-verified-tsc.log`); the co-managed package build passed. No browser/provider smoke or full scheduling completion is claimed. Existing feature/test checklist statuses remain unchanged because the related end-to-end requirements still have unfinished scope.

### 2026-09-08 — Reviewed Requester schedule acceptance and due-time publication

- Added native Requester scheduling to retained publication options, alongside an optional resolution marker. Future UTC instants and IANA zones are validated under current ticket/session/field authority. Close plus schedule is rejected: existing scheduled-comment behavior does not close the ticket at acceptance or publication. Co-managed/foreign scheduled authorship has not been added by this native adapter.
- Migration `20260908102605_retain_named_requester_schedule_intent.cjs` was generated through Knex. It expands the options and email-operation states (`scheduled`, `canceled`) and retains a canonical scheduled-content hash. An earlier close-intent migration replay now recognizes the later schema instead of narrowing its constraint; schedule downgrade refuses retained schedule intent/state.
- Explicit confirmation atomically consumes the author's draft, retains its reviewed envelope/files/route and inserts a canonical scheduled comment. It leaves published history, reply count, conversation message version, ticket status/response state and notifications unchanged. Published-file/source admission keeps withheld content unavailable to requester readers. Resolution is retained as an annotation.
- The actual `publish-scheduled-comment` worker recognizes named schedules before generic contact notification dispatch. At due time it rechecks the retained original author's current native authority, canonical source/hash, parent/root, mailbox, reviewed envelope and selected files. Publication, reply/version counters, response state, activity and retained events commit together; the existing reviewed delivery engine then owns the exact external envelope. No second generic contact email is emitted.
- Scheduled publication uses durable qualified event records even for an ordinary PSA workspace, preserving recovery after event transport failure. It does not require co-managed licensing. Due-time activity keeps the existing scheduled-publication event/system attribution; communication events retain the human author. As with the existing scheduled worker, scheduling does not introduce additional bundle mirroring or ticket-closing effects.
- Existing reschedule/cancel actions now admit named destinations with the actual browser session and current read/update/field policy. Rescheduling changes canonical timing without replacing the original reviewed envelope or author; cancellation atomically retains canceled email state. Due time on the canonical comment is authoritative after a reschedule; publication options retain the initially accepted review. Generic model/API edits must cancel the schedule before altering its message; the due-time hash also rejects out-of-band content changes.
- Extracted the existing queue registration helper for both legacy scheduling commands and named acceptance. Initial queue failure preserves the accepted schedule for maintenance; stale enqueue/reschedule/cancel races retain the canonical row's authority. Native named due rows join maintenance discovery without admitting unrelated legacy native schedules to co-managed policy.
- Still outstanding: expose scheduled composition and the complete scheduled-reply list/reschedule/cancel UI in the named composer, including private draft restoration and the existing time-zone picker conventions. The native default Requester screen still uses its legacy composer. No whole scheduling/UI feature or browser gate is marked complete by this backend checkpoint.
- Verification: the first full run exposed older close-migration replay narrowing the options CHECK; fixed that replay and reran. The full migrated PostgreSQL suite then passed 991/991 (`/tmp/ticket-conversations-schedule-verified-full-pg.log`, 182.21s). The final focused scheduled journeys passed 12/12 (`/tmp/ticket-conversations-schedule-verified-pg.log`), including the additional initial queue-failure/maintenance recovery case; the declared suite now has 992 cases. Earlier targeted existing scheduling/action regressions passed 21/21, and the combined close/schedule migration regression passed 16/16.
- Current named-composer, maintenance fanout and email-rendering checks passed 48/48 (`/tmp/ticket-conversations-schedule-ui.log`). Co-managed build passed (`/tmp/ticket-conversations-schedule-co-build.log`); server TypeScript had no diagnostics (`/tmp/ticket-conversations-schedule-verified-tsc.log`). Transport and queue providers are mocked in these database tests; current author/resource/audience queries, canonical writes, registered worker, reschedule/cancel actions, maintenance recovery and event outbox are real. Browser/UI scheduling verification remains outstanding.
- Final diff review caught a compatibility detail: only reviewed named schedules defer reply-count increments. Legacy scheduled replies retain their existing insert-time counter behavior because their generic due worker does not increment it. Added a real-query compatibility assertion to the representative scheduled journey.
- Final compatibility run passed 12/12 (`/tmp/ticket-conversations-schedule-final-compat-pg.log`, 13.70s), including legacy counter behavior. The final pre-checkpoint TypeScript run was also clean (`/tmp/ticket-conversations-schedule-final-verified-tsc.log`). All provider/worker processes used here were test processes against disposable databases; no live outbound email or deployment occurred.
- Preserved one more existing scheduler policy distinction: a hidden response-state field blocks due-time publication only when that publication would change the value. An already-awaiting-client ticket can publish a scheduled reply without modifying its hidden response state. Immediate Send keeps its prior guard. Representative real-query cases cover both unchanged and changed values; no broader permission is lent to the deferred author.
- The final policy/compatibility run passed 22/22 (`/tmp/ticket-conversations-schedule-verified-policy-pg.log`, 15.47s): all 14 named scheduling cases plus existing scheduling field-policy and immediate close-intent cases. Total declared database cases are 994. TypeScript completed without diagnostics (`/tmp/ticket-conversations-schedule-complete-backend-tsc.log`). No additional feature checklist completion is claimed while the named scheduling UI remains unfinished.

## Implementation checkpoint — named Requester scheduling controls

- Added scheduling to the shared named composer: timing remains in the author's private draft, restores after remount, and uses the existing strict wall-time/time-zone conversion. Invalid or past times prevent review; invalid edits also prevent navigation from silently discarding them. Enabling scheduling clears immediate close intent while retaining the resolution marker. The email review displays the publication time and requires explicit **Schedule email** confirmation.
- Added a current-authority, conversation-qualified scheduled-message reader and authenticated action. It projects accepted withheld canonical messages with bounded due-time/ID pagination, selected file names, and the reviewed subject/To/CC. Private editor drafts, immutable operation payloads and routing tokens are not queried or returned. Current body/schedule/envelope/file/resolution masks apply; current session and ticket authority remain mandatory. Unrelated conversation rows are excluded.
- Added `NamedScheduledReplies` with refresh, pagination, confirmed cancellation, and rescheduling through the existing authorized scheduled-comment actions. A list refresh does not reload the composer draft. Late responses after unmount/identity change are discarded. The server requires a narrow `{storeTenant, conversationId}` reference; the UI explicitly projects it rather than passing the full conversation DTO.
- Added three representative component journeys for restored private timing and invalid-time blocking, reviewed schedule acceptance, paginated reschedule/cancel without draft replacement, and late responses after an author change. Extended the real migrated PostgreSQL scheduling suite to verify the exact canonical projection, cursor/destination isolation, envelope masking and revoked-session denial alongside publication/recovery/close compatibility.
- Verification: **28/28** component tests pass in `/tmp/ticket-conversations-scheduled-list-final-ui.log`; **15/15** migrated PostgreSQL scheduling cases pass in `/tmp/ticket-conversations-scheduled-list-pg.log` (13.29 seconds). Co-managed build passes in `/tmp/ticket-conversations-scheduled-list-co-build.log`; final server TypeScript check passes with an empty `/tmp/ticket-conversations-scheduled-list-final-tsc.log`. The declared database suite now contains 995 tests; this checkpoint ran the focused 15, not the full suite.
- Browser discovery was rechecked using the available browser runtime: no browser is connected (`browsers.list()` is empty). The local application endpoint also did not respond. No actual browser/provider smoke is claimed. These observations do not block continued implementation.
- Native default Requester still uses the legacy composer. Next integration must retain its history editing, deletion, reactions, ordering, scheduled-history behavior and embedded layouts while adopting the named private composer. Full notification/follower/reopen, sharing/synthesis/AI and actual application/email verification remain open. No whole planned journey or broad compatibility feature is marked complete from this checkpoint alone.

## Implementation checkpoint — native default Requester composition

- `TicketDetails` now supplies a named default Requester panel in both grid and entry/embedded layouts. `NativeRequesterConversation` keeps the existing `BentoTimelineTile` or `TicketConversation` history renderer and composes through `NamedConversationComposer`. The native snapshot already excludes non-default named containers; the Requester renderer additionally excludes the separate internal default history. Existing history ordering, thread structure, reactions, edits/deletes, resolution markers, bundled-child context and scheduled rows remain in their existing renderers.
- History Reply routes the qualified comment/thread into the private named draft. The old inline/drawer/main composers and their submit shortcuts are disabled while this composition contract is present. Beginning a history edit flushes the private draft; an active edit blocks conversation switching and disables the new composer. The native compose shortcut focuses the private editor; submit opens review and cannot bypass the explicit Send confirmation. Requester panels are keyed by current session/home author/ticket/conversation, including the co-managed wrapper.
- The native wrapper memoizes its ticket reference so a background conversation refresh does not reload and replace an in-progress draft. Existing scheduled rows keep their history controls without duplicating the named scheduled list. Those controls can flush the private draft and refresh history after cancellation/rescheduling instead of reloading the page. Failed changes show recoverable feedback. Named cancellation also refreshes the latest-send banner independently of the draft.
- Added `getNamedConversationMessageDetails` and its authenticated action for batches of at most 25 qualified comment/thread references. It reuses the existing current-source/root proof plus the published-email and file projections. Deleted, mismatched or sibling-conversation messages are omitted; revoked ticket/session authority rejects the read. The response includes only comment ID, authorized envelope and published files, without a second body/author stream or private operation payload. The two existing decorators now express their actual minimal input fields rather than requiring a fabricated full comment.
- Extracted `ConversationMessageDetails` for the default and named history renderers. Published files use the same qualified download route and show beside their original message. Native details revalidate on history changes and periodic/focus refresh; stale responses after a session change cannot restore old details. Metadata failure leaves the privately saved composer intact and offers Retry. The grid history retains its streaming Suspense boundary.
- Leverage decision: ACT / bounded-now for reusing the existing message/root proof and envelope/file projections. Two history presentations need identical qualified details; duplicating audience/redaction/source admission would create correctness risk. The revision changes only the decorators' required input types; their existing admission/projection behavior remains intact.
- Marked F028/F029 implemented now that the default native Requester also uses the author/destination-private text/file draft pipeline. Actual-application checklist journeys remain open; component tests are not browser evidence.

### Verification

- **69/69** UI tests pass in `/tmp/ticket-conversations-native-requester-final-ui.log`: native default private drafts, refresh/identity boundaries, reviewed sending, metadata/download links, both history reply integrations, existing ordering/reply behavior, copy/metadata/reactions, and guarded scheduled cancellation. The final status-label regression is recorded separately below.
- **3/3** real migrated PostgreSQL cases pass in `/tmp/ticket-conversations-native-requester-details-pg.log` (10.13 seconds). They cover the native default Requester review/confirmation/delivery and canonical legacy history, revoked-session rejection, qualified envelope details, sibling/mismatched/deleted exclusions, real inbound files and current customer/MSP access revocation. The declared inherited database suite now contains 997 tests; this checkpoint ran the focused three, not the entire suite.
- Server TypeScript passes with an empty `/tmp/ticket-conversations-native-requester-final-tsc.log`; co-managed build passes in `/tmp/ticket-conversations-native-requester-co-build.log`. No browser or external-provider delivery is claimed.
- Remaining feature work includes full follower/unread notifications and side-reply reopen policy, selective sharing/protected lineage, synthesis and explicit AI participation, broader historical-control integration in side conversations, plus actual application/browser/email verification. The goal remains active.
- Final focused status/guard verification passes **38/38** in `/tmp/ticket-conversations-native-requester-status-ui.log`, including the canceled-send banner updating while the active private draft remains unchanged. All test/typecheck process handles from this checkpoint are terminal.


## Implementation checkpoint — qualified following and unread state

- Added Knex-scaffolded migration `20260908115747_create_ticket_conversation_attention.cjs`: a separate `attention_version`, immutable per-message sequence records, and qualified per-viewer follow/read preferences. Both new tables use tenant-qualified keys, distribution metadata and tenant-deletion ordering. Migration replay preserves rows; rollback refuses retained events or preferences. Historical messages are not backfilled as new unread events.
- Do not reuse `message_version` for unread: attachment imports increment it. The new publication sequence advances once for an accepted published human/external message, with canonical qualified author attribution and source/root/audience proof. Exact retries deduplicate; scheduled Requester replies advance only at publication. System/AI and existing persisted automated/ACK decisions do not create attention. Named inbound keeps Done when the retained message has a suppressed attention decision. The side-specific plain-ACK classifier and board opt-in are still pending.
- Added `getNamedConversationAttention`, `updateNamedConversationPreference` and `listNamedTicketConversationOverview`. The overview holds current ticket/conversation authority while projecting counts. Private/deleted/disclosed roots cannot lend unread state; body masks and explicit attention-field masks withhold counters/cursors. Follow changes cannot name another user or grant access, change envelopes, or affect ticket business state. Read cursors use decimal bigint strings, reject future values and merge monotonically so a delayed acknowledgment cannot consume a later reply.
- Option C now displays unread counts and selected-conversation Follow/Following and Mark as read controls. These use the actual browser-authenticated action and displayed read cursor, preserve private composer edits, retain the original preference intent on retry, and discard late completions after identity/selection loss. Marking read is explicit; selecting another conversation or All activity never clears a sibling count. Controls remain within the existing UI flag boundary; backend commands are not flag-gated.
- Marked F053 implemented for the actual persisted follow/unfollow preference. F054 (follower/assignee delivery), broader unread/legacy writer coverage and the representative product tests remain open. Follow preferences are not yet consumed by a notification delivery worker; do not mistake the control or event ledger for completed fanout.

### Verification

- Initial focused PostgreSQL run passed 18/19; the failed fixture copied generated `comments.note_index`. Omitted that generated column and the focused suite passed **19/19**, `/tmp/ticket-conversations-attention-fixed-pg.log`.
- Full migrated PostgreSQL regression passed **1,000/1,000**, `/tmp/ticket-conversations-attention-full-pg.log` (181 seconds). This includes new monotonic/qualified cursor and follow guards plus shared/private vendor attribution, provider retry dedupe, attachment-import independence and scheduled publication attention checks. It also verifies current body masks remove unavailable conversation metadata and retained migration data blocks downgrade.
- Final explicit attention-field mask extension passed **2/2** migrated PostgreSQL cases, `/tmp/ticket-conversations-attention-masks-pg.log`; the suite now declares 1,001 cases. The full run preceded this additional parameterized mask case; it is not a claim of a second full 1,001-case run.
- Actual hook/composer component suite passed **33/33**, `/tmp/ticket-conversations-attention-ui.log`, including persisted follow/unfollow without draft reload, snapshot-only mark-read with an incoming reply/sibling count, retry intent and revoked/replaced identity cleanup. These are component tests, not a running application browser smoke.
- Server typecheck passed with an empty `/tmp/ticket-conversations-attention-final-tsc.log`. Co-managed package build passed, `/tmp/ticket-conversations-attention-final-co-build.log`. Plan validation and `git diff --check` pass.

### Next integration work

- Consume the attention ledger for deduplicated follower/current-assignee delivery, rechecking actual recipient identity, current follow/assignment, audience, source and preferences while delivering. Reuse the existing notification infrastructure; no fake session or actor home-tenant switch. Existing `co_management_ticket_references.assigned_to/assigned_team_id` holds MSP routing; `tickets` holds customer/native assignment. Candidate routing is not read authority.
- Canonical owner reads can reuse `withCoManagedCustomerCommentNotification`; foreign shared/private resource admission uses `withCoManagedNotificationRecipient`. Existing `storedCommentNotification` handles retained foreign shared comment receipts and current assignee checks, so it cannot simply be reused unchanged for followers or private sources. Inbox/email/realtime reads must understand any new receipt type and must never fall back to cached private titles or text.
- Extend attention publication to the legacy/default/portal/workflow/bundle writer paths. Native insertion anchors include `Comment.insert`, optimized ticket comments and shared `ticketModel` (all already attach roots to defaults). Do not derive unread from attachment or metadata edits or mark those unconnected paths complete.
- Side reopen policy, selective sharing, whole-conversation synthesis, explicit audience-safe AI, remaining history/layout/lifecycle coverage and real application/email/provider validation remain in scope. No full-feature completion or live product smoke is claimed by this checkpoint.

## Implementation checkpoint — conversation inbox and channel delivery

- Added `namedConversationNotifications.ts`: current qualified recipient admission for native/customer-owned and foreign shared/private sources, read-authorized follower/current-assignee checks, original qualified self-suppression, immutable message-event identity, current source/root proof, and fresh text/author projection. Background recipients use active home identity and current resource policy; they do not fabricate sessions. Interactive stored reads still use the actual tracked session and may retain a readable old notification after unfollowing.
- Reused the ticket history query as a single projection engine beneath the existing session-checked page reader and a new single-message reader for admitted notification recipients. The page wrapper retains its final session check. This bounded extraction avoids recreating private/canonical attribution and body redaction in notification code; it does not create a second permission engine.
- Added Knex-scaffolded migration `20260908122807_retain_named_conversation_notification_receipts.cjs`, recipient-owned qualified source receipts and per-event in-app fanout completion/retry state. The source references are soft, qualified historical references; no copied body/title is treated as authority. Registered tenant metadata and deletion order; replay preserves receipt data and downgrade refuses retained receipts/fanout.
- `recoverNamedConversationNotifications` consumes current side-conversation attention records, discovers followers and current native/customer/MSP assignees, and creates existing `ticket-comment-added` template notifications using existing subtype/category/user preferences. Notification rows, source receipts, existing in-app/Teams/push channel intents and fanout completion commit atomically. Concurrent retries cannot duplicate them. A failed queue insertion rolls back the receipt and inbox row and leaves the event retryable.
- The existing maintenance recovery selector includes stores with pending conversation attention; the existing recovery handler runs this fanout. Recipient stores are also discovered by the existing channel queue selector. Requester events keep the original ticket notification matrix. Named side posts suppress the old generic internal notification fanout so it does not duplicate the new conversation-specific inbox notices.
- Stored notification admission and presentation now support a version-3 conversation source, with exact qualified conversation/message links. Inbox filtering/counts and delayed channel delivery classify by the new receipt even when cached metadata is stripped. They reconstruct permitted text/title/link under current access, follow/assignment and notification preferences rather than falling back to cached private content. The queue continues to use the existing channel transport mechanisms.

### Verification

- Four migrated PostgreSQL scenarios passed for native/shared/private following and a replacement native assignee with no session. They cover canonical/private attribution, current assignment, existing preferences, inactive users, scoped grants, exact links, current inbox projection, stripped metadata, fanout retry, transactional queue failure and pending-channel suppression after unfollow/revocation.
- Initial fixture corrections were test-only: the conversation is named `Carrier exchange`, notification template rows must be seeded explicitly in the schema-copy fixture, and the named fixture has board-scope read authority in addition to an explicit work grant. The revocation guard now removes both applicable grants and first proves the ordinary named ticket reader is denied.
- Full migrated PostgreSQL regression passed **1,004/1,005**, `/tmp/ticket-conversations-notifications-full-pg.log` (189 seconds). The single failure was the scheduled recovery test's exact result expectation missing the newly returned `namedNotifications` counters. Updated that expectation. The final focused run passes **5/5**, `/tmp/ticket-conversations-notifications-final-pg.log` (12 seconds), including the corrected real job handler, concurrent fanout replay and migration-retention guards. No production code changed after the full run; no second full-suite run is claimed.
- Component + maintenance unit run passed **55/55**, `/tmp/ticket-conversations-notifications-unit.log`. Added a pending-attention-only tenant discovery case; after providing its active-tenant fixture, the final maintenance suite passes **23/23**, `/tmp/ticket-conversations-notifications-final2-unit.log`.
- Server typecheck passes with empty `/tmp/ticket-conversations-notifications-final-tsc.log`; co-managed package build passes in `/tmp/ticket-conversations-notifications-co-build.log`. Plan validation and `git diff --check` pass. These tests capture channel delivery callbacks; they do not claim an actual application/browser, push/Teams provider or email-provider smoke.

### Remaining delivery work

- F054 and T018 remain open: follower email alerts are not yet wired to this conversation source. Preserve independent email and internal-notification preferences; an in-app opt-out must not inadvertently suppress an enabled email preference. Reuse the existing admitted email transport/queue mechanisms and current recipient checks. Internal alerts must not borrow a requester reply token or the reviewed external vendor envelope as notification authority.
- `co_management_email_deliveries` already centralizes admitted email attempts, retry completion and stable transport identities for ticket/task sources. Its existing source schema/admission distinguishes foreign ticket and task messages; extend it deliberately for native/private conversation sources or extract the common delivery engine, rather than fabricating a relationship to fit it. Any migration must preserve parent migration replay with retained newer records.
- Named side in-app notifications are now real backend behavior. Legacy/default/portal/workflow/bundle attention writers still need full coverage; requester notification links and the rest of the event/email matrix require final alignment. Side reopen policy, sharing, full-source synthesis, explicit AI, remaining UI/history/lifecycle behavior and actual product/provider validation remain in scope. The full goal remains active.

## Implementation checkpoint — independent conversation email alerts

- Extended the existing `co_management_email_deliveries` queue with a qualified `ticket_conversation` source, rather than creating another transport queue. The Knex-scaffolded migration `20260908125648_retain_named_conversation_email_notifications.cjs` adds source store/conversation/attention sequence and an independent email fanout cursor. Native owner alerts need no fabricated co-managed relationship. Resource checks preserve legacy ticket/task ownership constraints and reject incomplete/private foreign source identities. Earlier qualification-migration replay uses the same schema-aware check installer; retained email work prevents destructive rollback.
- Extracted the existing conversation fanout loop into the co-managed domain layer, shared by inbox and email adapters. This is a bounded layering change: both consumers need identical qualified event/recipient admission, transactional completion and retry behavior, while channel preferences/completion must remain independent. Inbox opt-out or failure cannot complete/suppress an email obligation. Requester notifications still use their existing matrix pending the remaining alignment work.
- Email delivery rereads current following/assignment, user identity/address, email preferences, source/audience and field permissions under the existing delivery locks. Only qualified source identifiers are retained in the queue. Repeated fanout deduplicates the same identity; transport retries retain the existing stable RFC Message-ID and attempt policy without claiming exactly-once SMTP.
- Internal alerts use the existing home-organization notification transport and an exact conversation/store/message link shared with inbox rendering. They do not borrow requester reply tokens or the vendor sender's mailbox authority. Sending a vendor message still requires the reviewed vendor-email operation. Rendering uses the current conversation title and authorized message, not cached private bodies or a full-history email.
- Added real migrated-DB cases for native/shared/private fanout, independent inbox/email preferences, concurrent replay, fresh address/title on retry, unfollow/revocation after queueing, schema replay/guards, and actual job recovery after a queue insert failure with reassignment. Added transport assertions for exact navigation and absence of borrowed reply capability.
- Initial focused PostgreSQL run: **11 passed**, `/tmp/ticket-conversations-email-alerts-focused-pg.log`. Transport/maintenance tests: **79 passed**, `/tmp/ticket-conversations-email-alerts-unit.log`. Co-managed build and initial server typecheck passed. Full database regression and final typecheck results follow below.
- F054/T018 remain open: Requester followers/exact links and other legacy/default/portal/workflow/bundle publishers still need the complete recipient/attention matrix. This checkpoint does not establish the remaining reopen, selective sharing, synthesis, AI, lifecycle or actual browser/provider journeys.
- Next recipient-matrix integration anchors: `internalNotificationSubscriber.ts::handleTicketCommentAdded` resolves current payload, mentions (with a notified-user set), primary/additional ticket agents via `ticket_resources`, and contact portal notices. `ticketEmailSubscriber.ts::handleTicketCommentAdded` first delegates co-managed customer/requester consumers, then uses author-address exclusion, address deduplication, primary contact and watcher semantics. Preserve these roles when adding Requester followers and exact links; replacing all of them with side-conversation assignees/followers would narrow existing behavior.
- Full migrated PostgreSQL regression: **1011/1012 passed** in 189 seconds (`/tmp/ticket-conversations-email-alerts-full-pg.log`). The one failure was the existing task-migration rollback test's exact error wording, now expanded to include retained conversation emails. Updated that expectation without weakening the retained-data guard. Final affected migration plus all new email cases: **8/8 passed**, `/tmp/ticket-conversations-email-alerts-final-pg.log`. No functional production changes followed the full run; only formatting and the corrected test expectation changed.
- Final server TypeScript check is clean (`/tmp/ticket-conversations-email-alerts-final-tsc.log`, empty), co-managed package build passed, plan validation and `git diff --check` passed. No actual provider send or application/browser smoke is claimed. All test/build process handles are terminal. The full feature goal remains active.

## Implementation checkpoint — Requester follower and destination alignment

- The existing co-managed Requester recipient engine now adds currently following technicians alongside the existing assignment/team candidates, including when no routing assignee exists. The existing owner-technician email queue likewise adds Requester followers alongside primary/additional/team recipients. Discovery is candidate-only; delivery rechecks current ticket/source access and follow state. A follower who is also assigned is still one candidate with one existing per-event/channel receipt. Unfollowing does not withdraw independently valid assignment eligibility.
- Added `requesterConversationFollowers.ts`: follower discovery joins the exact published comment/root, canonical named conversation and qualified actor-tenant preferences; its current-delivery check consumes the already admitted message target. A Requester follow cannot subscribe to a sibling Shared IT/private conversation or grant application read/write authority. Existing source self-suppression and email settings remain in their existing readers/workers. Customer email reply tokens retain the existing current read AND update admission on reply; following itself grants neither permission.
- The shared locked comment notification reader now returns an optional `conversationTarget` after proving its named container belongs to the same owner/ticket and matches the root/message audience. It locks the named container before thread/source rows and rejects a changed association or masked named metadata. Legacy unmapped roots keep their existing behavior. Existing queued/stored notifications recompute this destination under current authority; no cached source title or browser-provided source identity is trusted.
- Added the pure shared `ticketConversationMessageLink` helper and reused it across named/legacy co-managed inbox presentation and internal/requester email rendering. It preserves existing portal host, tenant query and fragment while appending the qualified conversation/store/message. The portal ticket page now passes the requested conversation into its existing authorized action, rejects inconsistent store/query scope, and keys the returned conversation projection so a route transition does not keep prior conversation state.
- Focused migrated PostgreSQL: **12 passed** (`/tmp/ticket-conversations-requester-notifications-focused-pg.log`), including existing side-email regressions and new follower/same-source guards. Email transport and portal route tests: **62 passed** (`/tmp/ticket-conversations-requester-notifications-unit.log`). Co-managed package build and initial typecheck passed; final typecheck/full database results follow below.
- Scope still outstanding in F054: native generic subscriber follower delivery and owner in-app followers must join their existing mention/additional-agent/contact/watcher matrix without duplicate or unqualified cached notices. Also still needed: remaining legacy/default/portal/workflow/bundle attention writers, automatic read-on-open behavior and actual message focus/deep-link browser validation. Existing checklists remain open for these complete journeys; this checkpoint does not claim full notification or product completion.
- Full migrated PostgreSQL regression: **1015/1015 passed** in 185 seconds (`/tmp/ticket-conversations-requester-notifications-full-pg.log`). Final server typecheck is clean (`/tmp/ticket-conversations-requester-notifications-final-tsc.log`, empty).
- Review found one additional compatibility edge after that full run: the existing notification reader permits a published reply beneath a deleted root tombstone. Removed the new follower-discovery query's extra root-deleted exclusion, retaining publication/audience/source checks and the selected comment's deletion guard. Added a real persisted reply/tombstone regression; final follower cases plus existing source-visibility case: **4/4 passed**, `/tmp/ticket-conversations-requester-notifications-final-pg.log`. The localized predicate change is the only functional change after the full run. Refreshed co-managed build passed; plan validation and diff checks passed.
- No actual outbound provider or browser validation is claimed. All test/build handles are terminal. The full goal and incomplete checklist entries remain active; next implementation must complete native/owner in-app follower integration and remaining attention publishers rather than treating these qualified queue paths as the entire notification matrix.

## Implementation checkpoint — native Requester internal email recipients

- Native ticket comment email now uses the existing durable owner-technician queue for assigned users, additional resources, current Requester followers and eligible internal watch-list addresses. Native delivery retains its primary/additional assignment policy; co-managed owner delivery retains team membership. The external contact/bundle/watcher subscriber remains the existing external delivery path. Queue retry does not replay that external path.
- Migration `20260908133439_retain_native_ticket_email_recipient_policy.cjs` adds retained native policy and monotonic hashed external-address exclusions to `co_management_customer_email_deliveries`. The migration was generated through Knex; replay preserves records, downgrade refuses retained native recipients. PostgreSQL DDL cannot bind the regex as a parameter, and Knex interprets a literal question mark as a binding: use the equivalent fixed `{0,1}` regex quantifier.
- Candidate discovery does not grant authority. Queue preparation and actual transport both recheck native ownership/RBAC, published source and audience, identity, assignment/follow/watch-list eligibility, notification preferences, close suppression and current address. Watch-list eligibility is limited to human internal updates in the default Requester conversation; additional Requester conversations use their own followers.
- Existing external/author address exclusions are normalized and hashed; replay can only add exclusions. Native delivery serializes a qualified comment/address through an advisory transaction lock and checks committed delivery/token receipts, preventing duplicate successful sends when two internal users share an address. SMTP acceptance followed by a lost database commit retains the existing stable Message-ID/token retry limitation; no exactly-once SMTP claim.
- Native internal alert replies now use the current owner `cm2` source/address token path. Actual native reply writing is tested with no co-managed entitlement, preserves parent/thread/default Requester association, and rejects the same credential after user deactivation. Token possession does not grant a session or override current write authority.
- Presentation limitation to revisit during actual email smoke: native internal alerts now use the existing authorized owner alert renderer (current message text, author, title and exact conversation link). External reviewed/native email framing and selected file delivery are unchanged. The internal renderer does not reproduce the former native rich ticket-details/CID-image template. Do not claim presentation parity or completed email UX from recipient tests.
- Focused migrated PostgreSQL recipient journeys: **8 passed**, `/tmp/ticket-conversations-native-recipient-focused-pg.log`. Focused watcher/contact/inline-image/transport unit checks: **66 passed**, `/tmp/ticket-conversations-native-recipient-unit.log`. Server typecheck is clean (`/tmp/ticket-conversations-native-recipient-final-tsc.log`), co-managed package build passed (`/tmp/ticket-conversations-native-recipient-build.log`). Whole-suite result follows once the live process completes.
- F054/T018 remain open: native/owner in-app follower delivery still uses the direct legacy subscriber and needs protected receipts/current reads. `internalNotificationSubscriber.ts` handles native mentions, primary/additional recipients and portal contacts at `handleTicketCommentAdded`; foreign co-managed follower notices already use `persistCommentNotifications.ts`. Preserve mention precedence and portal recipients when replacing the native internal loop. Existing generic cached inbox rows have no qualified source receipt; merely appending follower IDs would leave current-access checks incomplete.
- F052 remains open: `ConversationAttentionControls` currently provides explicit Mark as read. Automatic acknowledgment must occur only after the selected history successfully renders, using a bounded displayed attention snapshot; navigator selection alone or a later refreshed count must not acknowledge unseen messages. Named side panel and native Requester history have separate loading paths; All activity must not mark every conversation read.
- Final complete migrated PostgreSQL regression: **1019/1019 passed**, 189.50 seconds, `/tmp/ticket-conversations-native-recipient-full-pg.log`. This includes the added native reply/token round trip and deactivation guard. No real provider delivery or browser smoke is claimed. All process handles for this checkpoint are terminal; the full implementation goal remains active.

## Implementation checkpoint — acknowledge the opened conversation

- Implemented F052: side history, native default Requester history and co-managed default Requester history automatically acknowledge only their selected, successfully loaded messages. All activity and navigator rows do not acknowledge messages. Native history waits for current source-detail admission; no read action runs merely because its composer or navigation loaded.
- Added `acknowledgeNamedConversationMessages` and its authenticated action. The UI supplies bounded batches of displayed comment/thread identities and only the qualified conversation reference. The server rechecks current home session, ticket/audience/body and attention masks, exact source/root association, then derives the cursor from matching attention events. It never fetches a newer navigator cursor and marks it read. Later unseen replies stay unread, and concurrent acknowledgment remains monotonic without changing following.
- Extracted the existing preference write into a shared transaction helper used by manual follow/read and automatic acknowledgment. Unchanged retries avoid rewriting preference timestamps; retained lifecycle and actor qualification checks remain. No migration or new storage required.
- `ConversationReadAcknowledgment` waits for a visible, focused browser tab, keeps failure retryable, sends only its captured message list, and cancels remaining batches/completion callbacks after unmount or session/destination changes. It does not flush or reload private drafts. Successful cursor changes refresh navigator counts; exact message focus/deep-link pagination remains F021/F022 work.
- A first fixture used a thread ID absent from the inbound result; now it reads that identity from actual authorized history. Strict reference validation also caught passing a whole conversation row: both the UI action call and tests now use only store/conversation IDs. UI assertions verify the exact reference shape to prevent recurrence.
- Migrated PostgreSQL: **7 focused read/guard cases passed**; then **16 read/follow/notification regression journeys passed**, `/tmp/ticket-conversations-read-state-final-pg.log`. The cases cover native, Shared IT and private storage, displayed/later messages, concurrent acknowledgment, untouched sibling/default preferences, wrong thread/destination, private access, revoked session and masked source/attention fields.
- Actual React component tests: **68 passed**, `/tmp/ticket-conversations-read-state-ui.log`, including side selection/loading versus All activity, native history source admission, co-managed Requester loading, visible/focused tabs, retry and stale session batches. These are component tests with mocked server actions, not browser/provider smoke. T006/T018/T020 remain open for their broader journeys.
- New read-failure text is localized in every supported locale, including pseudo-locales. `scripts/validate-translations.cjs` still reports **1813 existing missing-key errors**, largely inherited co-managed and previously added conversation keys; none names the new `namedConversations.readFailed` key. The broader feature localization gap still needs resolving before claiming complete product validation. Log: `/tmp/ticket-conversations-read-state-translations.log`.
- Co-managed package build passed (`/tmp/ticket-conversations-read-state-build.log`); final server typecheck result is `/tmp/ticket-conversations-read-state-final-tsc.log`. This checkpoint does not complete the full ticket-conversations goal.
- Final tightened UI reference-shape checks: **6/6 passed**, `/tmp/ticket-conversations-read-state-final-ui.log`; full prior component suite remains 68/68. Final server typecheck is clean. Plan validation passes; only F052 is newly completed. All process handles from this checkpoint are terminal.


## Implementation checkpoint — exact message destinations

- Named history now accepts an optional exact message target. It resolves the source through the current authorized conversation projection, then returns the target and preceding history with the same precise timestamp/store/comment cursor. It does not scan every newer page. Cursor plus target is rejected. Native, canonical shared/requester and home-private stores use the existing authority path and current attachment/email projections.
- Missing, invalid, sibling, scheduled and deleted targets produce the same message-unavailable result inside an independently authorized conversation. The permitted latest history remains available and the draft remains mounted. Loss of conversation authority still denies the entire read. Published replies below a deleted root remain linkable according to the existing body projection; file admission keeps its stricter source rules.
- Native Requester, co-managed Requester, named side panels and portal history consume the qualified URL target. The exact rendered message receives focus/highlight; native entry/grid thread trees reveal previously collapsed older replies and select their visible history filter. All activity and a URL for another store/conversation do not apply focus to the old visible selection while a draft flush is pending. Deleted tombstones are not focused.
- Show latest messages removes only the message target and preserves conversation/query state and draft edits. Unavailable-target fallback history does not automatically acknowledge the latest unread messages. Normal displayed-message acknowledgments remain unchanged.
- Added message-unavailable/latest labels to all ten locale files. The translation validator still reports the existing 1813 missing-key errors; neither new key contributes to that backlog.
- F021 is implemented in code. F022 and the broader T006 journey remain open: portal selector/back-forward synchronization and actual running-application navigation/browser validation still need completion. These focused component tests are not a browser smoke or evidence that the complete feature is realized.

### Verification

- Focused UI suites: 82 passed in `/tmp/ticket-conversations-focus-ui-v3.log`, covering the new exact-message selection, draft preservation, opaque missing target, co-managed Requester, portal boundaries, and actual recursive collapsed-thread rendering/focus. After adding native loaded-history focus coverage, its two focused tests passed in `/tmp/ticket-conversations-focus-native-ui.log`.
- Migrated PostgreSQL: 11 message/read-cursor/current-notification regressions passed in `/tmp/ticket-conversations-focus-regression-pg.log`. The expanded exact-message case additionally verifies a surviving published reply below a deleted root; it passed in `/tmp/ticket-conversations-focus-tombstone-pg.log`. The new pagination case uses 60 actual persisted messages with tied microsecond timestamps, an older target beyond the first page, complete continuation without skips/duplicates, native ownership, opaque missing/sibling/scheduled/deleted targets and current access revocation. Existing private-store disclosure/revocation coverage now exercises exact targets too.
- Co-managed build passed (`/tmp/ticket-conversations-focus-build.log`); server TypeScript passed (`/tmp/ticket-conversations-focus-final-tsc.log`). The first validation failures were a duplicate import, optional portal tenant typing, an unknown union-property guard, missing test router/DOM mocks and a synchronous-throw assertion; these were corrected and the affected suites passed.
- No test processes remain live at this checkpoint. The next work includes the remaining navigation/browser journey, native/owner in-app followers, legacy attention hooks, side-reply board policy, selective sharing, AI participation/full-source synthesis, lifecycle/export selection and actual email/UI validation. The full implementation goal remains active.

## Implementation checkpoint — portal URL navigation and actual browser validation

- The portal selector now uses a serialized navigation controller with the existing mutation lock. Full-page selections update the qualified conversation URL through Next's native history integration; embedded dialogs keep the surrounding page URL intact. Browser back/forward is deferred while an edit contains unsent work. Finishing or canceling the edit applies the pending destination; newer navigation supersedes old in-flight reads.
- The controller validates the returned ticket, tenant and selected conversation before rendering it. Unknown/denied destinations leave the current history intact with one opaque error, Retry and Return to current conversation. Initial denied links likewise offer Open Requester conversation while retaining permitted portal URL context. The server page uses a stable ticket key so same-ticket navigation does not unconditionally remount the editor.
- New pending-navigation and recovery copy is present in all ten locale files. The existing translation backlog remains 1813 missing-key errors, without new missing keys from this slice.
- Actual browser compilation exposed an application defect missed by isolated component tests: the notification action barrel exported synchronous notification hooks, pulling current-delivery authorization, databases and inbound-email dependencies into the portal's browser bundle. Removed that runtime export (the type remains exported) and changed server event-bus registration to import the explicit hook module. Existing post-commit delivery and hook tests pass; the live portal compiles successfully afterward.
- F022 is implemented. T006/T007/T008 remain open for their full native/co-managed persona, layout, draft, attention and permission-loss journeys; the successful portal journey below does not establish those broader cases or full feature realization.

### Actual application evidence and reusable local environment

- The card's ordinary server answered on port 3952, but its database did not yet contain named-conversation schema. Created a separate random `ticket_conversations_browser_*` database using schema-only copy plus public reference catalogs, then applied the same 65 migrations exercised by the existing PostgreSQL integration harness. No tenant data was copied. All synthetic users, sessions, permissions and ticket history belong to that isolated database.
- The initial attempt used the dev PgBouncer endpoint, which cannot route newly created database names. Resumed the verified-empty database through direct PostgreSQL on port 5472. Setup/seed scripts are `/tmp/ticket-conversations-browser-db-setup.cjs` and `/tmp/ticket-conversations-browser-seed.cjs`; the seed uses current thread audience/required comment fields and ticket timestamps. They intentionally refuse an unsafe database name or unreviewed replay into existing data.
- Local runtime state, including fixture references, is kept outside the plan in `/tmp/ticket-conversations-browser-state.json`. `/tmp/ticket-conversations-browser-server.cjs` starts a separate server on port 3962 using that database, the UI-only release flag override, an ephemeral credential-encryption key, and an independent build directory. Authentication uses a signed synthetic requester session with a real session row and current portal permissions; no application auth or action handler was mocked. No email/AI provider was invoked.
- The initial direct Next/Turbopack launch failed on existing `.js`-to-TypeScript aliases. The isolated server uses webpack, which supports the repository's configured extension aliases. Its subsequent browser-only database import failure was the actual notification boundary defect fixed above. Initial compilation also exposed a missing test credential-encryption setting and a missing synthetic ticket timestamp; those were corrected in the test environment.
- Headed Chromium against the actual application passed all eight checks in `/tmp/ticket-conversations-browser-navigation-result.json`: qualified selector URL and preserved unrelated query, unsent reply retained on Back, cancellation followed by the pending destination, Forward, opaque private destination, Return to current, narrow-width selector, and initial denied-link recovery. No browser errors were recorded. This traversed real server actions and persisted authorized requester histories; the third private conversation's name/body remained absent.
- Browser runner: `/tmp/ticket-conversations-browser-navigation.cjs`; log: `/tmp/ticket-conversations-browser-navigation.log`. Screenshots: `/tmp/ticket-conversations-browser-pending-draft.png` and `/tmp/ticket-conversations-browser-narrow.png`. The earlier hydrated default screen is `/tmp/ticket-conversations-browser-page.png`.
- The isolated server was deliberately stopped after the successful journey. Its build cache was moved to the existing gitignored `server/ticket-conversations.next/` convention; the runtime state file now points there. The database and fixture remain available for subsequent native/co-managed and email/AI verification. A state file is not evidence of a live server: start or verify the actual process before using it again. The ordinary worktree server was not stopped or rewired.

### Focused automated verification

- Navigation regressions: 52 passed in `/tmp/ticket-conversations-portal-navigation-regression-ui.log`. After the final recovery-copy and export-boundary changes, 21 affected portal/route/post-commit/hook tests passed in `/tmp/ticket-conversations-portal-boundary-final-ui.log`.
- Notification package build passed in `/tmp/ticket-conversations-notification-boundary-build.log`. The preceding navigation server typecheck passed in `/tmp/ticket-conversations-portal-navigation-final-tsc.log`; the final typecheck after the notification import adjustment is recorded below when its process terminates.
- No database query implementation changed in this slice. The live browser journey supplies additional real-query and denied-audience evidence; this does not replace the plan's broader migrated-DB and lifecycle tests.
- Final server TypeScript check passed with an empty `/tmp/ticket-conversations-portal-boundary-final-tsc.log`. All validation and isolated-server process handles are terminal at this checkpoint. This goal turn made concrete implementation and real-browser progress; no blocker or full-goal completion is claimed.

## Implementation checkpoint — selective-share text preparation

- Added `packages/tickets/src/lib/prepareNamedConversationShare.ts`, a server-side preparation command using current named-ticket/source authority and the existing private editor CAS. It accepts qualified source/message references and destination revisions, never caller-supplied source content/provenance. Source thread/root/message locks retain the projected version until the private copy commits.
- Reuses the ticket rich-text plain-text extractor, preserving visible text/line breaks while omitting embedded file/image blocks and hidden hyperlink targets. Optional quoting produces an editable quote block. No source title, sender identity, recipient envelope or historical files are appended. Preparation sends/posts nothing and changes no ticket state or published history.
- Existing non-null destination content requires explicit replacement plus the exact current revision. Competing autosaves have one winner. An uncertain retry returns the current edited draft without copying subsequent source edits over it; changed intent under the same operation conflicts. Replacement resets files, email, parent, resolution, close and schedule intent for subsequent deliberate review.
- Protected qualified source references, source revision/timestamp and a content snapshot digest are retained in the existing author-home `provenance` field. The ordinary draft DTO never exposes this field, and browser autosave cannot supply it. Human edits retain it; discard/consumption clears it so a later unrelated draft cannot inherit old lineage.
- Current published-body admission also permits surviving replies below deleted root tombstones. Sharing text preserves that behavior. Selected files must continue using the independent stricter root/file gate; this checkpoint does not relax it.
- Validation: seven focused tests passed against the existing isolated migrated PostgreSQL harness (`/tmp/ticket-conversations-share-regression-pg.log`): native/co-managed private snapshots, concurrent duplicate preparation, no publication/ticket effects, private metadata/draft ownership, edited-draft retry, explicit replacement versus autosave, forged provenance rejection, wrong-ticket/sibling/deleted/revoked source guards, surviving replies and scheduled-source denial, plus existing draft and private-publication regressions. The first run exposed a test assertion expecting a Promise from a synchronous input validator; corrected the assertion, with no command behavior change. Final server TypeScript check passed (`/tmp/ticket-conversations-share-final-tsc.log`).
- This is a backend preparation checkpoint, not completion of sharing. No new UI/server-action entry is exposed yet. Continue with exact selected-file staging, immutable published-copy lineage and current-reader source-link projection, then source-message actions and existing/new destination selection in the actual UI. The private draft provenance is not yet transferred to published lineage; wire that before exposing the share action. F068–F076 and representative sharing journeys remain incomplete. Full AI/synthesis, notification/lifecycle integration and actual email/UI validation remain in scope.

## Implementation checkpoint — selected sharing files and published lineage

- Extended preparation with explicit source attachment references (default empty), deterministic author-home copy IDs, existing protected-file reservation/transfer and final private draft CAS. Every transfer phase retains current source, ticket, destination, author and draft revision authority. Verify exact source-byte fingerprints before transport and at final selection; preserve an existing draft on failed or uncertain upload. Completed retries return the current edited draft. Unselected file names, images and hidden link targets do not become copied body content.
- Added `namedConversationShareFiles.ts` and `namedConversationShares.ts` in the co-managed domain. Native preparation now acquires update authority at transaction entry, retaining the ticket write lock before conversation/source locks; it does not upgrade a prior read lock after inspecting drafts. This also applies current update permission on preparation retries.
- Migration `20260908152720_retain_ticket_conversation_share_lineage.cjs` was scaffolded through Knex. `ticket_conversation_shares` retains immutable qualified source versions and selected-file mappings in the preparing author's home store. A narrow `share_operation_id` on the destination publication qualifies its private receipt lookup. No source title/body or private prompt is stored in destination metadata. Added tenant table metadata, distribution, safe rollback checks, test harness migration registration and tenant-deletion ordering. References are soft and qualified: source/author-store deletion cannot cascade into an already published destination copy.
- Reviewed Send/Post validates source access and retained lineage before comment/event effects, records the final selected editor-file IDs transactionally, then consumes the draft. Human edits and explicit file deselection remain valid. Published text/files remain independent of later source edits, deletion or original/staging object removal. The final files still pass the existing exact publication-copy gate.
- Added a technician-only source-link projection through authenticated server actions. It first admits the published destination, uses the accepted publication's author/operation to locate private lineage, and independently admits the current source before returning a name/link. An unavailable/private/deleted source yields no link. Ordinary message DTOs and external payloads do not include this lineage. The UI source-link affordance and direct portal verification remain outstanding.
- Scheduled copies recheck source access and selected source-file fingerprints when becoming due, before publication or external transport. Already published copies retain their independent destination authority. A lost source leaves the schedule withheld; it does not leak a side effect before the guard.
- Corrected the earlier share test's native variant: a co-managed customer ticket with an omitted relationship is still admitted through its inferred relationship, so that variant did not prove ordinary-MSP behavior. Both sharing and existing email fixtures now reuse an actual sponsor-owned native ticket fixture with co-managed entitlement removed. The corrected native text, file, source-guard and lineage cases pass.
- Validation: 20 focused real migrated-PostgreSQL tests passed (`/tmp/ticket-conversations-share-final-pg.log`), including source guards before any event effects, exact selected bytes, unknown upload recovery, removed-file final deselection, independent published copies, protected source links, scheduled-source loss/recovery, existing file publication and existing requester scheduling. Blob storage and outbound transport use explicitly injected in-memory/test adapters; these are not real-provider email/browser smoke results. Database package build, final co-managed package build (`/tmp/ticket-conversations-share-release-build.log`) and server TypeScript (`/tmp/ticket-conversations-share-action-final-tsc.log`) passed. No running process handle remains from these checks.
- F070 (editable text/quote preparation), F072 (private until deliberate publication), and F075 (independent published copies without source access changes) are implemented through the authenticated domain/actions and tested publication path. F068/F069/F071/F073/F074/F076 and the complete sharing test journeys remain open for the actual share UI, destination creation/selection, authorized source-link rendering, portal/email presentation, and synthesis draft replacement. All prior whole-feature scope remains active.

## Implementation checkpoint — sharing review UI and authorized source links

- Added a shared message action and review dialog across named side history, All activity, native Requester history and co-managed Requester history. Fresh source/destination reads default to Requester, offer existing/new permitted destinations, leave files unchecked and optionally quote the selected text. A readable source can be copied into a writable destination without requiring source write permission.
- New email destinations collect explicit To/CC and retain them in the private draft. Preparation validates those recipients, includes them in immutable retry intent, and rejects email envelopes for internal destinations. Sending-mailbox selection and the actual email review stay in the destination composer; preparation does not send/post.
- The host flushes the source before opening the modal and pauses the editor while it is open. Existing draft content requires explicit replacement or Open existing draft. Unknown preparation/creation retries reuse their operation; conflicts require reloading the current draft and a fresh deliberate replacement. Late destination reads and old-identity completions cannot replace current selection/content.
- Real browser testing exposed a navigation defect absent from the initial mocked-router cases: after creating a destination, the new editor's pending load could prevent the source-to-destination handoff. The host now selects the prepared destination immediately and retains that choice while the URL catches up. Added a regression with delayed private-draft loading and delayed URL propagation. Same-conversation preparation remounts the editor from the resulting saved draft.
- Named history, All activity and native detail projections now decorate published copies with `sharedFrom` only when the current technician can independently read the exact source. Accepted publication receipts qualify private-store lookup; candidate publications are fetched per page. Masked lineage, forbidden/deleted sources and non-technician identities omit the property altogether. This supersedes the preceding checkpoint's statement that ordinary message DTOs never carry an authorized link. Protected receipts and source snapshots remain private.
- Source links preserve ticket URL context and flush current edits before navigating to the qualified original message. Link IDs include destination-message identity, including multiple copies from the same source. Co-managed Requester rendering uses the same affordance; email and portal do not receive this decoration through their readers.
- Added 25 sharing strings in all 10 locales, with matching key sets and interpolation placeholders. Repository-wide translation validation continues to report its existing 1,813 errors; no new sharing-key gap was found.
- Focused validation: **44 UI tests passed** (`/tmp/ticket-conversations-share-navigation-ui.log`); **16 real migrated-PostgreSQL cases passed** (`/tmp/ticket-conversations-share-links-pg.log`). These cover recipient/private-draft guards, published source-link projection across native/co-managed/All activity/details, hidden source metadata, source deletion, exact selected-file publication, schedule source guards and immutable snapshots. Co-managed package build passed (`/tmp/ticket-conversations-share-links-build.log`). Final typecheck and browser outcome follow below.
- F068/F069/F071/F073 are implemented. F074 retains its outstanding direct portal/email presentation evidence, F076 still includes synthesis draft replacement, and complete sharing/synthesis test journeys remain open. Full AI, reopen-policy, notification/surface/lifecycle completion and final provider-backed smoke remain in the plan.
- Final server TypeScript check passed with an empty `/tmp/ticket-conversations-share-complete-tsc.log`. Headed Chromium against the isolated, migrated native fixture passed six real sharing checks (`/tmp/ticket-conversations-browser-sharing-result.json`, `/tmp/ticket-conversations-browser-sharing-fixed.log`): Requester default, private new-destination copy, edited explicit Post, authorized exact-source navigation with draft flush, Open existing draft preservation, and same-conversation explicit replacement. No browser errors were recorded. Screenshots: `/tmp/ticket-conversations-browser-share-dialog.png` and `/tmp/ticket-conversations-browser-share-published.png`.
- Browser validation uses a synthetic internal user with an actual signed session and database RBAC. Server actions, rich editor, private drafts and publication are real; no browser action/provider mocks were used. This journey posts an internal copy and does not prove provider email delivery or AI. Its first run needed longer explicit waits for the initial dev build; its second exposed the real handoff bug documented above; the corrected journey passed. The ordinary card service and live application database were not changed.
- Additional headed browser checks passed for the native Requester Share action, dark theme and a 390px sharing-dialog fit (`/tmp/ticket-conversations-browser-sharing-layout-result.json`). Visually inspected `/tmp/ticket-conversations-browser-share-dark.png` and `/tmp/ticket-conversations-browser-share-narrow.png`: dialog text/controls fit and theme tokens adapt. This proves the modal layout only; the surrounding technician shell still needs the plan's separate complete narrow-layout work. Browser scripts close their own contexts after each run.

## Implementation checkpoint — complete-source AI and private synthesis drafts

- Added `shared/lib/tickets/conversationAi.ts` as a provider/context contract without UI or ticket-application dependencies. `packages/tickets/src/lib/conversationAiContext.ts` traverses all authorized published pages, restores reply order, strips rich-document URLs/embedded file references, and projects only permitted message text, author labels and file metadata. Unpublished, deleted and masked content does not enter inference. Admission bounds cover scanned rows and serialized input; oversized complete sources fail explicitly rather than returning a prefix.
- Ordinary internal AI source selection uses the destination-audience intersection: Shared IT excludes either organization's private context; private AI admits only the caller's own organization plus readable requester/shared conversations. The deliberate whole-source synthesis exception stages an outward result privately. Requester-directed inference receives no protected source conversation title. Ordinary internal AI invocation/history UI remains outstanding.
- Added a server-only `@product/chat/conversation-inference` edition seam. EE reuses the author's current signed session, existing PSA product policy, AI activation setting/add-on, and gateway/OpenRouter/Vertex resolver. CE returns unavailable. The adapter exposes no tools, mention expansion or business-state mutations, accepts only complete plain-text responses, preserves provider context-limit errors and existing credit notifications, and bounds each provider call below the retained operation lease. No new AI licensing policy was introduced.
- Migration `20260908165830_retain_ticket_conversation_ai_runs.cjs` retains author-home generation intent, hashes, source manifests, attempts, lease, terminal result and publication linkage. It registers tenant ownership/deletion ordering and refuses destructive downgrade after retained runs. Qualified soft references avoid cross-organization cascades.
- `prepareNamedConversationSynthesis.ts` reserves intent and releases every database transaction before inference. An attempt token and draft revision check discard expired/cancelled/stale completions. Exact retries coalesce active runs and preserve newer human edits after success. Completed retries and private-draft status recheck current source changes. Generation never publishes, emails or replaces an existing draft without explicit confirmation.
- Reviewed publication retains synthesis lineage privately and rechecks each admitted source; scheduled email delivery repeats source admission. Published copies follow their destination independently. Normal history and public draft DTOs do not expose generation prompts/manifests/operation IDs.
- Authenticated `conversationAiActions.ts` wires production capability, preparation, cancellation, source selection and private status. The ticket UI reuses the existing destination/create/draft-review dialog for synthesis, with whole-conversation selection, explicit new requester recipients, cancellation, stable retry intent and explicit regeneration. Per-author source-change notices preserve editor content. New controls remain inside the host's approved UI release flag and identity boundary.
- Component regression found an actual sibling React key collision between the new controls and existing conversation panel; gave the controls a distinct key so opening a modal cannot remount the source unexpectedly. The standard textarea's reflection ID may be absent outside its provider; the synthesis prompt also supplies an explicit accessible name.
- Verification: **10 migrated PostgreSQL cases passed** in `/tmp/ticket-conversations-synthesis-actions-pg.log`; **53 component/provider tests passed** in `/tmp/ticket-conversations-synthesis-ui-final-unit.log`; server typecheck and DB package build passed. Translation validation remains at the pre-existing **1,813 errors**, with 19 synthesis strings added to all ten locale files. The focused tests cover full-page context, source/audience rejection, no inference-held locks, generation retries/cancellation, edited-draft CAS, publication source loss, private lineage, UI replacement, explicit new recipients, unavailable AI and honest context failure.
- Headed browser verification is being run against the isolated fixture app and a loopback synthetic provider. This exercises real application authentication, permissions, actions, database, provider adapter and editor; generated prose is controlled test output. Do not claim a real external model response or full AI participant/history completion from this checkpoint.
- Real browser cancellation exposed a transport defect missed by mocked component actions: Next's client action queue serializes server actions, so a long generation action prevented Cancel from reaching the server until generation had already replaced the draft. Confirmed both in the installed Next action-queue implementation and in the isolated database's completed regeneration row. Moved only the long inference request to `/api/tickets/conversation-synthesis`, reusing the same authenticated command and adding a same-origin JSON boundary. Cancellation/status now remain independently callable while inference runs. Added route cases for trusted command forwarding, cross-origin/invalid requests, controlled context errors and opaque auth failures. Browser verification must pass this corrected path before the synthesis checklist is marked complete.

### Synthesis browser findings and completed evidence

- The session-authenticated HTTP route also needed the API middleware's narrow allowlist. Its original URL-origin check rejected valid requests because Next normalizes the internal request URL; it now uses the existing deployment-aware public-origin resolver, including its forwarded-host trust rules. Live probes prove same-origin invalid input reaches the handler (400) while an anonymous same-origin command remains denied (403). Added exact matching for the new route and existing conversation attachment/editor-file browser routes, whose handlers already enforce their own qualified sessions; adjacent API paths remain protected.
- Cancellation now retains author-private intent even if it arrives before generation reservation. Closing after an uncertain response waits for a cancellation acknowledgment. Known cancelled requests cannot be revived; expired running leases become explicitly retryable in status instead of leaving the browser polling indefinitely. These cases pass against the real migrated database.
- Final headed application journey passes **six checks**: complete-conversation selection defaults to Requester; a new requester-directed destination retains explicit To/CC and an unpublished author-private result through the real route/provider adapter; a fresh page load preserves edited text and reports source changes; cancellation during a delayed provider response preserves the original draft; provider context failure reports no partial summary and preserves edits; only explicit human Post publishes the reviewed internal synthesis. `/tmp/ticket-conversations-browser-synthesis-result.json` and `/tmp/ticket-conversations-browser-synthesis-complete.log`; no browser JavaScript errors.
- Supplemental headed checks pass for light/dark synthesis dialogs, 390px dialog fit, and sharing a message into a new requester-directed private draft with explicit To/CC. `/tmp/ticket-conversations-browser-synthesis-layout-result.json`. Inspected light, dark, narrow and published-state screenshots. The surrounding ticket shell is still not fully responsive; this proves the dialog, not completion of the broader layout requirement.
- Provider output in these browser checks is **synthetic** from a loopback gateway-compatible endpoint. Authentication, tenant/role/AI settings/add-on checks, production HTTP/server command/provider adapter, PostgreSQL, source admission, draft composition and reviewed publication are real. Rollout flags are enabled only in the isolated fixture. This is not evidence of an external model's response quality.
- The dev server hit its 24 GiB compilation heap while hot-reloading the large application. Restarted only the isolated app with 40 GiB and ran the successful browser checks against stable code. Do not infer an app failure from the older test runs' URL timing or 60-second screen-load timeouts; persisted drafts were inspected and the fresh-page journey subsequently passed. The normal card server was not restarted.
- Focused final evidence: **51 ticket component cases**, **4 inference adapter cases**, **4 HTTP route cases**, **13 middleware cases** pass; **11 migrated PostgreSQL AI-context/synthesis cases** pass, including a final **6-case** synthesis rerun for expired-lease recovery (`/tmp/ticket-conversations-synthesis-lease-pg.log`). Latest server typecheck is `/tmp/ticket-conversations-synthesis-complete-tsc.log`. DB and co-managed package builds passed earlier in this checkpoint; translation baseline remains 1,813 existing errors.
- Marked **F076–F086** implemented and **T026/T027/T029** verified. The full T028 checklist remains conservative pending its explicit pre-generation decline/accept browser sequence; the delayed-completion, cancellation, context-failure and edit-preservation portions now have actual-browser evidence. Optional internal AI participation, attributed stored prompts/replies and its source picker (**F087 onward**) remain implementation work, along with the previously recorded notification, policy, lifecycle and layout gaps. No full-feature completion is claimed.
- Final publication review found that source-file metadata needs the same current grant as source text, even though synthesis never downloads or copies file bytes. Private generation manifests now retain admitted attachment IDs, include them in source fingerprints, and recheck them at immediate/scheduled publication. IDs remain excluded from model input and public history. Native and co-managed database cases prove removal blocks publication without consuming the draft, explicit regeneration restores publication from current context, and no storage download/upload occurs.
- Final guard verification: **13/13 selected migrated PostgreSQL cases passed**, `/tmp/ticket-conversations-synthesis-file-guard-pg.log` (1,052 total suite cases). Co-managed build passed, `/tmp/ticket-conversations-synthesis-file-guard-build.log`; final server typecheck passed with empty `/tmp/ticket-conversations-synthesis-final-guard-tsc.log`. The isolated browser app and loopback provider were stopped after their successful journeys. No external model quality or full-feature completion is claimed.

## Implementation checkpoint — internal AI participation and source picker

- Committed the verified synthesis slice as `3de382f885`; the unrelated SDK lockfile normalization and migration CLI mode remain outside the task commits.
- Added `invokeNamedConversationAi.ts`, authenticated actions and `/api/tickets/conversation-ai`. Explicit Ask AI reserves an author-home `conversation_reply` run, releases all database transactions for inference, rechecks the complete audience-filtered source snapshot, then atomically publishes the qualified human prompt and attributed AI reply in the selected internal conversation. Failed/cancelled/stale attempts publish neither. It does not consume or overwrite the manual private draft.
- Reuses the existing provider/entitlement adapter with no tools or business-state actions. Sources are exactly the explicitly selected qualified conversations, including an intentionally empty source list. Email/requester destinations are rejected; Shared IT cannot accept private sources even for a personally authorized caller. Input is snapshot before asynchronous work. Completed retries return retained message references without re-publishing; active retries coalesce. Cancellation-before-start and expired-attempt recovery preserve operation identity and discard old completions.
- Added migration `20260908191034_retain_ticket_conversation_ai_participation.cjs` via Knex scaffolding. It adds the explicit native `ai` author enum value, admits private `actor_kind = ai` with no login identity, and extends retained-run completion constraints for published prompt/reply pairs. Downgrade refuses retained AI participation; PostgreSQL's unused additive enum label remains rather than destructively rebuilding the type. Synthesis status/cancel binding now includes its run kind, preventing cross-kind operation use.
- `conversationAiPublication.ts` writes the pair into the existing comment/private-comment stores, retaining normal roots, parent relationships and qualified human authors. AI has no user/contact/collaboration login reference. Native, Shared IT, private and All activity use the same existing authorized reader, which now projects author kind `ai`. Explicit private-thread disclosure preserves AI attribution and does not manufacture a human actor reference. Publication advances conversation history only: no email, follower attention event, ticket state/SLA change or business workflow event.
- Added Ask AI beside the synthesis action for authorized internal conversations with available AI. The existing host flushes and pauses the ordinary composer before opening the identity-keyed modal. The dialog displays the admitted source list, lets the user narrow it, explains prompt/reply publication, preserves uncertain retry intent, awaits cancellation acknowledgment before closing and clears source/prompt details after access rejection. Long inference uses the independent HTTP path; cancellation/status remain separately callable. Thirteen user-facing keys are present in all ten locales.
- Focused evidence: **16 migrated PostgreSQL tests passed**, `/tmp/ticket-conversations-ai-participation-final-pg.log` (eight participation journeys plus eight synthesis regressions). They prove native/shared/private attributed publication, manual-draft preservation, no business/delivery effects, source narrowing, forbidden Shared IT context, known/uncertain retry identities, early/late cancellation, expired lease retry, source/audience/session revocation, explicit disclosure of AI authors, migration replay and retained downgrade guards. Earlier fixture assertions were corrected to use the existing message-events table, decode rich text, and pass only qualified reference keys.
- **77 component/HTTP/middleware cases passed**, `/tmp/ticket-conversations-ai-dialog-complete-unit.log`: 56 ticket component cases, eight HTTP cases across both AI endpoints and 13 middleware cases. Server typecheck passed, `/tmp/ticket-conversations-ai-participation-ui-tsc.log`; co-managed package build passed. Translation validator remains at the pre-existing 1,813 errors. Focused UI tests use mocked authenticated action/HTTP boundaries and rich-editor rendering, not a live browser.
- Remaining for this checkpoint: run the actual headed application through native/private and mixed Shared IT Ask AI, source narrowing, unavailable capability and cancellation. Include the updated 68th migration in the disposable browser database, refresh the provider capture script to accept the conversation task, and verify persisted history plus manual-draft restoration. Do not mark T030–T032 or F087–F094 complete from the component-only UI evidence; both-organization model-input and live-browser coverage remain to finish. Broader notification, reopen policy, lifecycle and responsive layout work is still outstanding; the full goal remains active.
