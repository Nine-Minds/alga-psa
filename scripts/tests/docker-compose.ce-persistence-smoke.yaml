services:
  postgres:
    ports: !reset []

  server:
    command: !reset []
    depends_on: !reset {}
    entrypoint: ["/bin/sh", "-c", "while :; do sleep 3600; done"]
    ports: !reset []
    secrets: !reset []
    volumes: !override
      - files_data:/data/files
