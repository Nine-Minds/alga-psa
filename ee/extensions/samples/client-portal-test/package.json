{
  "name": "@alga-samples/client-portal-test",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "build:js": "npx tsup src/handler.ts --format esm --out-dir dist/js --clean",
    "build:component": "npx jco componentize dist/js/handler.js --wit ./wit/extension-runner.wit --world-name runner --disable all --out dist/main.wasm",
    "build": "npm run clean && npm run build:js && npm run build:component"
  },
  "dependencies": {
    "@alga/extension-runtime": "^0.1.0"
  },
  "devDependencies": {
    "@bytecodealliance/componentize-js": "^0.19.3",
    "tsup": "^7.2.0",
    "typescript": "^5.4.0"
  }
}
