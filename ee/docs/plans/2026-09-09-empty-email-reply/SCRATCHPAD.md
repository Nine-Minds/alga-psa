Production diagnostics reported explicit-boundary/html-boundary followed by original-text fallback. Current processor checks sanitized text but constructs comment from sanitized HTML when available. Existing token-only test mocks the parser and misses full notification fallback. Scope authorized by request to fix empty-reply fallback.
Regression reproduced before fix: real parser restored full notification text and processor returned replied, creating a comment. Two parser empty-body expectations also failed. Fix restricts original-text fallback when a conversation token plus explicit text boundary or removed token artifacts establishes Alga reply structure. No processor or database changes.
Validation: shared vitest config ran lib/email/__tests__ plus all three processInboundEmailInApp suites: 63 passed. Original replyParser.test.ts is excluded by shared config, so a temporary standalone config ran its 6 tests: all passed. git diff --check passed. Changes are local, not deployed.
