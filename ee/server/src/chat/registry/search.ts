// Re-export shim. The ranked registry search now lives in the shared CE package
// @alga-psa/agent-tooling. Kept so existing `./search` imports keep resolving.
export * from '@alga-psa/agent-tooling/registry/search';
