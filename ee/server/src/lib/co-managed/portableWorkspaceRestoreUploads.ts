// Compatibility entry for the installation restore command. Recovery is shared
// so maintenance can run independently of the current product edition.
export * from '../../../../../packages/co-managed/src/portableRestoreUploads';
