{
  "name": "@alga-psa/db",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/db/src",
  "projectType": "library",
  "tags": [
    "scope:db",
    "type:horizontal"
  ],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{projectRoot}/dist"
      ],
      "options": {
        "comment": "Run the full package build (tsc -b + tsc-alias), not bare tsc. @alga-psa/db is type:module, so the dist needs explicit .js import extensions for raw Node ESM consumers (e.g. the knex migration runner); tsc -b alone emits extensionless imports that only a bundler can resolve.",
        "command": "npm run build",
        "cwd": "packages/db"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ],
      "options": {
        "lintFilePatterns": [
          "packages/db/**/*.{ts,tsx}"
        ]
      }
    }
  }
}
