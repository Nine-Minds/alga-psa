import { mkdtempSync, rmSync } from 'node:fs'; import { tmpdir } from 'node:os'; import { join } from 'node:path'; import { describe, expect, it } from 'vitest'; import { AmpPackageBuilder, AmpSqliteReader, validateAmpPackage } from '../src/index.js';
describe('AMP package contract', () => { it('generates independently and is readable through the server reader seam', () => { const dir=mkdtempSync(join(tmpdir(),'amp-')); const file=join(dir,'sample.amp'); new AmpPackageBuilder(file).write({format_version:'1.0.0',package_id:'sample',created_at:'2026-08-25T00:00:00Z',producer_name:'independent-test',producer_version:'1',source_system:'fixture'}, {organizations:[{package_record_id:'org-1',source_record_id:'1',external_identifier_namespace:'fixture',name:'Example'}]}); expect(validateAmpPackage(file)).toMatchObject({valid:true}); const reader=new AmpSqliteReader(file); expect(reader.rows('organizations')).toHaveLength(1); reader.close(); rmSync(dir,{recursive:true}); }); });
