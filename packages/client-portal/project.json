{
  "name": "@alga-psa/client-portal",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/client-portal/src",
  "projectType": "library",
  "tags": [
    "scope:client-portal",
    "type:vertical"
  ],
  "targets": {
    "build": {
      "executor": "nx:noop"
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ],
      "options": {
        "lintFilePatterns": [
          "packages/client-portal/**/*.{ts,tsx}"
        ]
      }
    }
  }
}
