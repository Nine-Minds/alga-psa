{
  "name": "@alga-psa/agent-tooling",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/agent-tooling/src",
  "projectType": "library",
  "tags": [
    "scope:agent-tooling",
    "type:horizontal"
  ],
  "targets": {
    "build": {
      "executor": "nx:run-script",
      "options": {
        "script": "build"
      },
      "dependsOn": [
        "^build"
      ]
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ],
      "options": {
        "lintFilePatterns": [
          "packages/agent-tooling/**/*.{ts,tsx}"
        ]
      }
    }
  }
}
