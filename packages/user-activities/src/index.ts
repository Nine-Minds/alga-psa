// @alga-psa/user-activities — horizontal "user activities" dashboard feature.
// UI surface (client components). Server actions live under the `/actions` subpath and
// must be imported explicitly to avoid pulling "use server" modules into client bundles.
export * from './components';
