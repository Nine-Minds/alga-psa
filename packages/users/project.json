{
  "name": "@alga-psa/users",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/users/src",
  "projectType": "library",
  "tags": [
    "scope:users",
    "type:vertical"
  ],
  "targets": {
    "build": {
      "executor": "nx:noop"
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ],
      "options": {
        "lintFilePatterns": [
          "packages/users/**/*.{ts,tsx}"
        ]
      }
    }
  }
}
