'use client';

/**
 * CE stub for the asset detail credentials section
 * (ee/server/src/components/credentials/AssetCredentialsSection.tsx, resolved
 * via the edition-swapped `@enterprise` alias). Render nothing in CE.
 */

interface AssetCredentialsSectionProps {
  assetId: string;
  clientId: string;
}

export function AssetCredentialsSection(_props: AssetCredentialsSectionProps) {
  return null;
}

export default AssetCredentialsSection;
