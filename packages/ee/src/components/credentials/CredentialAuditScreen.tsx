'use client';

/**
 * CE stub for the vault-wide audit log screen
 * (ee/server/src/components/credentials/CredentialAuditScreen.tsx, resolved
 * via the edition-swapped `@enterprise` alias). The credentials vault is an
 * EE-only feature; render nothing in Community Edition.
 */

export function CredentialAuditScreen() {
  return null;
}

export default CredentialAuditScreen;
