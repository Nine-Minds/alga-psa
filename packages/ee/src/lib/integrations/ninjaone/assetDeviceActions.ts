/**
 * Community Edition stub. NinjaOne device actions (refresh, reboot, script,
 * remote control) are Enterprise-only; the asset RMM actions resolve no
 * adapter for NinjaOne assets in CE (which cannot be configured there anyway).
 *
 * Intentionally untyped, matching the sibling stubs: importing the interface
 * from @alga-psa/integrations would create an ee-stubs -> integrations cycle.
 */

export const ninjaOneAssetDeviceActions = undefined;
