{
  "name": "@alga-psa/reporting",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/reporting/src",
  "projectType": "library",
  "tags": [
    "scope:reporting",
    "type:horizontal"
  ],
  "targets": {
    "build": {
      "executor": "nx:noop"
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ],
      "options": {
        "lintFilePatterns": [
          "packages/reporting/**/*.{ts,tsx}"
        ]
      }
    }
  }
}
