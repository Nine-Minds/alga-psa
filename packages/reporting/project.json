{
  "name": "@alga-psa/reporting",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/reporting/src",
  "projectType": "library",
  "tags": ["scope:reporting", "type:horizontal"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "tsup",
        "cwd": "{projectRoot}"
      },
      "inputs": ["production", "^production"],
      "dependsOn": ["@alga-psa/core:build", "@alga-psa/db:build", "@alga-psa/types:build", "@alga-psa/validation:build"]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit",
        "cwd": "{projectRoot}"
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["{workspaceRoot}/coverage/packages/reporting"],
      "options": {
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["packages/reporting/**/*.{ts,tsx}"]
      }
    }
  }
}
