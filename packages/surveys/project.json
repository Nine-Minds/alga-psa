{
  "name": "@alga-psa/surveys",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/surveys/src",
  "projectType": "library",
  "tags": ["scope:surveys", "type:vertical"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "tsup",
        "cwd": "{projectRoot}"
      },
      "inputs": ["production", "^production"],
      "dependsOn": ["@alga-psa/core:build", "@alga-psa/db:build", "@alga-psa/types:build", "@alga-psa/validation:build"]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit",
        "cwd": "{projectRoot}"
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["{workspaceRoot}/coverage/packages/surveys"],
      "options": {
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["packages/surveys/**/*.{ts,tsx}"]
      }
    }
  }
}
