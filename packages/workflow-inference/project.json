{
  "name": "@alga-psa/workflow-inference",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/workflow-inference/src",
  "projectType": "library",
  "tags": [
    "scope:workflow-inference",
    "type:horizontal"
  ],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{projectRoot}/dist"
      ],
      "options": {
        "command": "npm run build",
        "cwd": "packages/workflow-inference"
      }
    }
  }
}
